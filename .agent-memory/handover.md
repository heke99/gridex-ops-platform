# Handover

Updated: 2026-09-04

Branch `claude/gridex-ops-production-hardening-lrknxa`, seven commits, pushed.
One forward migration; no runtime or application code touched.

Delivered:

1. `npm run db:parity` and `db:parity:selftest` — canonical/live schema parity
   in both directions, plus the gate that guards the engine itself.
2. `db:schema:snapshot` / `db:schema:check` — canonical `schema.sql` and a
   schema-wide fingerprint.
3. Dockerless clean replay via `GRIDEX_REPLAY_DB_URL`.
4. `20260904120000_canonical_tenant_invariant_convergence.sql` — makes a system
   rebuilt from this repository pass the tenant isolation gate, 21 breaches to 0.
5. The tenant gate, the parity self-test and the schema snapshot wired into the
   `clean-migration-replay` job.
6. A production dependency audit gate that separates a vulnerability from an
   unreachable registry.
7. `db:types:gen` generating from the local shadow rather than a linked project.

Pick up here, in order:

- `ops-hardening.yml` runs on `pull_request` and `push: main` only. Pushing this
  branch triggers NO CI run, so nothing on it has been verified by CI yet. A
  pull request is the only way to get these gates to run. Ask the user before
  opening one.
- Once CI runs, take `rem002-schema-snapshot/` from the green
  `clean-migration-replay` job and commit it as `supabase/schema.sql` and
  `supabase/schema.fingerprint.json`. The verification step is already written
  and guarded on those files existing, so committing them turns the gate on.
- Do NOT commit a baseline generated by the dockerless harness. It is not
  byte-equivalent to the Supabase stack; see current-task.md Stage 10.
- Do NOT change `EXPECTED_FINGERPRINT` in the replay script. CI's clean replay
  is green on main, so that constant is correct.
- Never edit `gridex-aud-003-migration-provenance-regression.cjs` to make a
  replay change pass; see Stage 9.
- Production parity stays blocked on the production Supabase project.

Read `.agent-memory/current-task.md` first. It carries the findings register,
every design decision, the experiments that were run and the ones that were
disproved.
