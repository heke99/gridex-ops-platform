# Independent Batch A incoming runtime review

Date: 2026-09-20. Reviewer: `remaining_d_incoming_review`, independent of the runtime author.
Candidate: `74871ea63e118bce3dfb8c41d33a15b7ec443b7f`.
Runtime: `2f8ac76cf5b0c72bff9fa08ee80d9af6bf8a63c5`.
Whole-branch base: `39aeb94fc1a33f24269a4887a661851792feb1d5`.

## Verdicts

| Review | Verdict | Boundary |
|---|---|---|
| TASK/SPEC | APPROVE | Nine selected incoming cells, including bounded supplied323, actual typed consumers and existing operational holds. |
| QUALITY | APPROVE | Durable oracles, preserved failed/setup receipts, final runtime binding and required local gates verified by independent receipt inspection. |
| TENANT-boundary | APPROVE | Existing authenticated/scoped action and source-message ownership boundaries preserved; no schema, RLS, grant, role or loader expansion. |
| WHOLE-BRANCH | APPROVE | Completed bounded Batch A against the stated base, including source qualification and evidence packaging. No full-cell/masterplan or merge certification. |

No confirmed blocking or nonblocking runtime finding. No remediation requested. Approval does not replace root's exact published-tree CI, guarded merge, or actual-main73 + OPS acceptance.

## Scope and method

Read the task brief before the author report, root dispatch constraints, approved source review, complete remaining-D source qualification, runtime audit and evidence ledger. Reviewed the 129-file package; its diff body independently matches `git diff --unified=10 39aeb94f..74871ea6` exactly. Read the changed implementation/tests and the actual unchanged callers/callees governing source ownership, diagnostic projection, text readiness, canonical persistence, final rendering, prior-flow authority and tenant attribution.

Inspected repository AGENTS, active memory and relevant decisions/domain context. Applied code-review, bounded source-to-code tracing, differential review, verification-before-completion and direct false-positive checks. Inspected Supabase guidance for local persistence/authorization review; no platform feature or live schema work. The explicit one-reviewer/no-subdelegation instruction overrides the source compliance skill's usual fan-out. Using-superpowers explicitly exempts dispatched workers. No broad repository/security/static-analysis campaign, UI/Next refactor, SQL tuning, deployment or skill creation is triggered by this finite review.

No runtime, test, source, memory or prior receipt edits; no commits, live database/provider/send calls, role activation or subdelegation. Only this requested report is written. No test suite was rerun: concrete inspection did not leave a doubtful counterexample warranting a new execution probe. Test results below are inspected author receipts, not reviewer executions.

## Independent original-source check

Read original PDF pages21,74,119,123 directly, including the complete new323 source clauses. Fresh SHA256 matches `83c2f1d2915851d2e670731f6ab404ef06c9b9def282afbafdfa0eda836a6e95`.

P119 separates presence/requiredness from supplied-content checking and prohibits negative rejection for false-condition extra information. P123 explicitly lists B71–B76 for323. P21/P74 limit selected reporting scope and request correspondence. The implementation checks supplied323 only for qualified own Z14 S17/S18, ignores exact Z96 before its content, makes no missing/private inference from local U, and does not fabricate request comparison authority. It rejects actual C8891131/3055 misuse and actual composite/element overflow while retaining unused optional components. P74's anomalous A02 example does not override the explicit valid-code list, as already adjudicated in source review.

UNKNOWN alone remains unselected323, consistent with root's exact ruling. Ambiguous placement/pair/process ownership remains internal. New323 is not generalized to Z13. Existing321 date-format authority is reused, with explicit physical line attribution.

## Actual consumer tracing

| Chain or invariant | Inspected result |
|---|---|
| Shared assessment | `prodatIncomingSelectedAck.ts` invokes the existing reporting, meter-change, death and GAS320 owners. It projects source diagnostics, checks own-reference ambiguity, and explicitly holds aggregate multi-UNH delivery while retaining independently scoped known errors. |
| Nine-cell predicates | Z14:321/323; Z10:254/242; Z05/Z06/Z09:310; Z04/Z06:320. Existing own scope, true/false/U and source-false-before-content behavior stay in their owners. Z09E remains intrinsically required310. |
| Canonical policy/runtime | `canonicalPolicyFieldValidator.ts` consumes the finite assessment and avoids duplicate old inbound selected-owner calls. `runtimeDecision.ts` projects ready fields into the negative response plan and carries internal disposition separately. Syntax remains a preceding gate. |
| Canonical persist/reload | `inboundProcessing.ts` stores the response plan and disposition on the source row, reloads that plan for ACK creation, filters internal-review errors through `isQualifiedProdatApplicationError`, and prevents positive/business processing under internal review. Existing diagnostic persistence/warning events and qualified negative reporting remain intentional. |
| Direct engine/wrappers | `decisionEngine.ts` carries selected typed errors and returns manual review for internal disposition. `prodatAperak.ts` blocks selected internal uncertainty instead of inventing legacy105, and the legacy error projection consumes the shared source-owned errors. |
| Registry derive/resolve | `selectedProdatAckRegistryIssues` asserts readiness before mutable registry or persistence access. Derivation retains known selected errors on independent permission/register/energy holds. The resolver uses the owned ERC, field, complete ready text and references instead of mutable rule code/text. |
| Backend manual decision | `resolveBackendAperakDecision` assesses fields before TGT/prior context and before permission events/returns. Prior-context holds carry known selected fields. Ready selected reporting errors route through registry resolution before the permission event and negative return, preserving backend rule metadata for detail attachment. |
| Three manual actions | `createAckDraftAction`, `createAndSendAckAction`, and `createAndSendRecommendedAckAction` reach the backend assessment before draft construction; recommended ACK additionally guards before related-ACK/TGT reads. Existing source access checks precede assessment. |
| System-test manual consumer | `resolveSystemTestAckDecision` resolves typed errors before positive heuristics. `createAndSendSystemTestAckAction` guards selected readiness before active-run lookup, and preserves platform-admin and source-company/run-company checks. |
| Final P-APERAK | `ack.ts` forwards typed metadata to `aperakEngine.ts`; its normalization independently recomposes/qualifies owned text, emits ERC and FTX field code, escapes the complete selected text/references, and does not replace own-absent LI from cached source references. |

The three new durable files exercise all nine cells, all three service alphabets, actual extracted backend/action functions, actual system-test manual draft, actual final renderer, canonical persistence/reload, hostile mapping, repeated physical references, absent own LI, independent source true/false/U facts, ambiguous own references and mixed ready/internal cases. The GAS canonical persistence cases correctly assert the existing `ediel_guide_resolution_missing:PRODAT:2026-09-20:E2SE6B` hold. Historical typed manual/system draft rendering is separately exercised; there is no claim of operational GAS canonical persistence or send support.

## Physical identity and tenant boundary

| Boundary | Evidence and conclusion |
|---|---|
| Two persistence keys | Selected `ruleKey` includes field, ERC, physical LIN and deterministic finding index. `insertValidationIssue` includes this key with source `message_id`; `insertResolvedDetail` includes it with `source_message_id`. Both actual conflict targets include `rule_key`; repeated equal business refs therefore remain distinct. Multi-message delivery holds before these writes. |
| Optional own references | Typed occurrence/ownReferences remain attached from source owner through resolver and renderer. Missing own LI remains null; ambiguous duplicates add internal disposition. Final rendering suppresses whole-message fallback when typed occurrence exists. |
| Register boundary | Existing `permissionRegistryRegisterFailures` remains unchanged and limited to Z14/Z15/Z18 omitted-C829 derivative258 cases. New323 uses physical Z14 LIN ownership under the explicit root ruling. Populated C829/genuine register holds remain; there is no Z04/Z06/Z10 exemption. |
| Manual actor/source | Existing write/send guards and `requireScopedEdielMessageForAction` enforce actor/company access before resolver calls. Selected records carry that exact source row ID. ACK drafts inherit source ownership and the canonical kernel requires company and source rule-pack identity. |
| System tests | Platform-admin entry remains explicit. Source company is required; requested run company is compared with it, and run/related-ACK lookups use the source company. |
| Prior-flow authority | The unchanged loader filters tenant/company, environment, direction, family, transport actors and bounded historical time. The unchanged pure owner revalidates source/candidate scope, own references and business chronology; issued-context fingerprint/hash protection remains. Selected readiness now precedes that lookup. |
| Schema/service role | No schema, migration, grant, RLS, credential, source loader or authorization changes in the branch. Existing two-table definitions and conflict keys inspected. New pure assessment has no tenant lookup. Required local tenant integrity/shutdown/ratchet receipts remain successful; ratchet2400 against unchanged2402. |

This is approval of the changed path and its existing tenant boundary, not a fresh live-RLS or whole-database security certification. No cross-tenant counterexample was found.

## Evidence verification

Independently recomputed and compared:

* 19/19 runtime/test ledger SHA256 hashes.
* 78/78 archived runtime evidence hashes and all15 gate-log hashes.
* 1695/1695 runtime/test filesystem hashes against the manifest and actual final-runtime Git blobs; zero mismatches.
* 558/558 pre-existing audit files against their ledger and actual `263790d2` Git bytes; zero mismatches.
* All18 test-attempt receipts' total/pass/fail fields against the ledger, including failed attempts and setup/fixture corrections; all match.
* Final full receipt contains311 actual test-result files. Runtime/test diff from `2f8ac76c` to candidate is empty.

| Saved verification | Verified receipt outcome |
|---|---|
| Initial durable TDD | 34 cases:1 PASS/33 RED, retained. |
| Focused final | 286/286, including117 new cases across3 files. |
| First full | 4912/4914; both composition regressions retained and disclosed. |
| Post-fix focused | 194/194. |
| Final full | 4914/4914 in311 files, local Node24.19.0. |
| Packaged quality | 45/45. |
| Existing eight strip-only scripts | 818/818. |
| Required gates | All15 recorded exit0: full, quality, strip-only, three typechecks, lint, integrity, tenant integrity/shutdown/ratchet, large-files, performance, route readiness and implementation diff check. |

The run02 HEAD label is `10342f71`, not the final runtime commit. `final-runtime-tree-binding-01.json` expressly reconstructs the start implementation from that HEAD plus the two already-edited runtime files, lists exactly those changes, and records final filesystem/Git hashes. It does not claim a full start-time filesystem measurement. The two final hashes match, every other runtime/test blob is bound as unchanged, and original receipts remain untouched. This supports the stated local result with the disclosed provenance limitation; it is not a reason to rewrite receipts or automatically repeat the local full suite.

Root subsequently reports published `eb904a11`/tree`c918c95c` equals candidate, and exact Node22.23.2 coverage job106094364464 confirms4914/4914 in311; three non-OPS workflows are successful and OPS is ongoing. This is root-provided live-CI status, not independently retrieved CI evidence in this review. Final CI/merge/main adjudication stays with root.

Only the three individually authorized old category-hold assertions were semantically changed. Their new expectations assert literal typed errors and mocked persistence. The prior-flow test modification supplies the newly invoked pure dependency and leaves old assertions intact. No baseline, threshold, frozen source, workflow, shared codec or shared grouping change was found.

## Findings, rejected concerns and limits

Confirmed findings: none. No unresolved likely runtime finding is used to withhold approval.

The initial appearance of a review-package mismatch was only its documented commit/stat preamble and ten-line diff context; its actual diff exactly matches the candidate. The final local run's stale HEAD label is fully disclosed above. The historical GAS canonical hold is required behavior, not a failure of the authorized historical typed draft path. Known field errors accompanying internal holds are preserved evidence, not permission to perform positive/business processing. UNKNOWN-alone323 and the narrow omitted-C829 permission boundary follow explicit root rulings, not additional reviewer waivers.

No broad test rerun, new runtime probe, live database, provider transport, exact-CI retrieval or actual-main execution was performed by this reviewer. Saved mocked consumer tests establish the bounded protocol/control behavior; they do not establish production actor rights or authoritative persisted positive source population.

Counts remain98/110 numeric +10/10 parents. Batch B outgoing240, positive persisted producers, live/formal market capability, F4 GAS routing, broader state/ACK and quote-bearing legacy-list residuals remain separate. PR310 stays paused. This report approves Batch A only and makes no full F3/masterplan/cell-acceptance claim.
