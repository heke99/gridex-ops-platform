name: Agent apply checkout audit

on:
  push:
    branches:
      - audit/website-contract-e2e-20260817

permissions:
  contents: read

jobs:
  apply:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary patch applier already completed successfully.'
