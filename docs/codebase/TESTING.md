# Testing

Primary commands are `npm test`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run db:migrations:check` and the focused Ediel regression scripts in `package.json`. Hosted CI also performs clean migration reconstruction. Production release requires the declared Node 22 runtime and exact merged SHA.
