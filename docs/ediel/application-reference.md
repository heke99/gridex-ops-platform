# Application Reference

Application Reference is selected from message family, business process, roles, route capability and environment. Route configuration declares and validates an expected value; it cannot override the canonical policy. PRODAT DDQ and DGI flows are separated. ACK messages correlate to the source message's exact context.
