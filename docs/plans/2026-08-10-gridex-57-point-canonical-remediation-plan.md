# Gridex OPS canonical remediation — implementation plan

1. Restore connected-dev migrations and register exact checksums.
2. Protect the canonical boundaries with an executable regression.
3. Consolidate request access, invitation ordering and lifecycle transitions.
4. Add the leased provisioning worker and secured cron.
5. Add repair SLA, fail-closed reconciliation, performance budgets and release receipts.
6. Reconcile the legacy application into an explicit repair workflow.
7. Regenerate database types and run migration, type, test, lint and build gates.
8. Require hosted CI and preview evidence, then merge and verify the production SHA.
