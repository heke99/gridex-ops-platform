// Public compatibility facade. The implementation lives inside the canonical
// PRODAT engine boundary so operational callers cannot become rule owners.
export * from '@/lib/ediel/prodat/compatAdapter'
