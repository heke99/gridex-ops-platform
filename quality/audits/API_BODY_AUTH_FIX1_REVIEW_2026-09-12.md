**T10A-R1 — plural invalid-target rejection contract: ADDRESSED.** `lib/partner-api/core.ts:750` now checks only the missing/empty target, then passes every nonempty normalized target reaching this validation through the real public-target policy at line 752. HTTP, malformed URLs and HTTPS-private targets therefore reach the preserved 422 `webhook_target_not_public` response at lines 762–778, with only `error` and `request_id` in the body. The policy still requires HTTPS and public addresses (`lib/integrations/publicWebhookTransport.ts:83–141`); removing the lexical HTTPS guard does not bypass it. Idempotency and subscription creation remain after policy validation at `core.ts:784–802`.

**T10A-R1 — covering evidence: CHECKED.** `__tests__/api-body-auth-contracts.test.ts:334–361` defines six singular/plural HTTP, malformed and HTTPS-private cases asserting the exact error/message/request ID, complete header map, one authentication RPC, zero create RPCs and zero idempotency writes. The corresponding actual-source assertions are `quality/audits/proofs/api-body-auth-regression.mjs:227–260`. The report identifies their RED contract mismatch and subsequent GREEN source run. I did not repeat that run solely to confirm the report.

**T10A-R1 — missing-target compatibility: PASS, focused reviewer check.** The retained plural guard preserves `webhook_https_required`; the singular guard remains `webhook_target_required` (`lib/partner-api/simple.ts:1032–1033`). An in-memory use of the current source harness setup, stopped before its test loop, exercised omitted, null, empty and whitespace targets on each actual catch-all route. All eight returned 422 with the existing route-specific code and envelope keys, exactly one auth RPC, zero create calls and zero idempotency-table effects. Command: `node --experimental-strip-types --input-type=module` with stdin; exit 0. Output: `PASS 8 focused actual-dispatch missing-target compatibility controls: omitted/null/empty/whitespace across singular/plural; auth=1, create=0, idempotency=0`. This resolved the missing-target doubt not covered by the author's listed rejection cases. The VM/runtime limitation below applies.

**T10A-R2 — substituted policy/dispatch and Ediel identity: ADDRESSED.** The source harness now loads actual idempotency-key, readiness, IP, tenant-context, route-cost, secret-hash and payload-safety implementations (`quality/audits/proofs/api-body-auth-regression.mjs:112–145`), uses native `node:net.isIP` at lines 6 and 141, and loads canonical dispatch at lines 170–180. Actual business/simple/canonical/core selectors establish the same reachable route selection: singular uses simple; plural falls through canonical to core. No source replacement of the reviewer-named policies remains on these paths. The real auth implementation passes actual required scopes and route cost into `authenticate_integration_request_v1` and constructs tenant context from its returned company/client (`lib/integrations/apiAuth.ts:343–433`). The harness's RPC fixture asserts cost 10, required webhook scope and absent trusted client IP at lines 81–91. Native rate-counter and tenant/RLS execution remain outside this proof.

**T10A-R2 — Ediel covering evidence: CHECKED.** The permanent test removes the actor-helper mock, configures synthetic UUID `00000000-0000-4000-8000-000000000001`, and substitutes only profile/auth-user/membership database I/O for actor resolution (`__tests__/api-body-auth-contracts.test.ts:70–71,127–159,303`). The real helper validates that UUID, checks the profile, then checks auth identity and active membership (`lib/ediel/automationActor.ts:12–79`). Both worker routes still invoke that real helper. Generic controls assert the real actor with `companyId: null`, including the blocked submitted override; the company positive control asserts the actor/company/environment and audit payload (`__tests__/api-body-auth-contracts.test.ts:571–578,601–626`). The source harness likewise loads the real actor helper at lines 290–292 and checks profile/auth/membership calls and company audit effects at lines 460–465. All-fallback-secret-absent cases and explicit oversize/no-worker assertions now cover all four routes. These are controlled I/O fixtures, not proof of actual profile rows, company membership, database authority or worker behavior.

**T10A-R3 — missing Resend noncanonical UTF-8 fixture: ADDRESSED at the authored-test/source-review level; installed-SDK execution remains pending.** `noncanonicalResendRawAtBytes` contains indentation, spaces around separators, a final newline and real `Å🙂` text, and sizes ASCII metadata padding by encoded byte length (`__tests__/api-body-auth-contracts.test.ts:239–259`). `splitInsideUtf8` places the first stream boundary after the first byte of `Å` (lines 262–269); the stream consumes that boundary explicitly at lines 207–213. The two length-declaration cases at lines 483–498 sign the exact raw text, require 200 at 2,000,000 received bytes, and require 413 plus one cancellation at 2,000,001 bytes, with absent and false `Content-Length: 2`. Canonical JSON reserialization or incorrect split-byte decoding would change the signed payload and fail exact-cap acceptance.

**T10A-R3 — real verifier contract: CHECKED, NOT EXECUTED.** The fixture builds the Svix-style signature over `id.timestamp.raw` using the decoded signing secret (`__tests__/api-body-auth-contracts.test.ts:427–432`). Neither this test nor its configured setup mocks `resend` or `verifyResendWebhook`. The actual route sends bounded raw text directly to `verifyResendWebhook` (`app/api/webhooks/resend/route.ts:35–49`), whose implementation calls `new Resend(apiKey).webhooks.verify` with that payload and headers (`lib/email/resendWebhookEvents.ts:69–96`). The lockfile pins `resend@6.12.4`. The sent/bounced/clicked/received metadata controls remain at test lines 468–480; the large fixture contains metadata padding, not attachment content. The 2,000,000-byte limit remains an explicitly selected application cap, not a documented provider maximum or proof that every possible provider event fits.

**New breakage in the correction: NONE found.** Zero new Critical, Important or Minor defects confirmed in the three-path delta. The only production change is the one-line plural guard correction. The amended fixtures and loaded policies do not introduce a new production tenant, signature, scope, quota, idempotency or body-cap change.

**Out-of-scope observations: NONE.** Frozen initial code was read only where needed to resolve R1–R3 execution paths; no unrelated defect was reopened.

**Spec compliance verdict: APPROVED for this scoped correction review.** All three original defects have an adequate source/test correction. This verdict does not label Task 10a VERIFIED: required supported-runtime and actual installed-SDK acceptance remain outstanding.

**Code quality verdict: APPROVED for this scoped correction review.** All original Important findings are addressed at the reviewed implementation/test level, with no new Critical/Important breakage. No compiler, lint or SDK success or failure is inferred from reading source.

**Frozen boundary check: PASS.** HEAD remains base `8f8d33119542a0b3cfd489eeb89107f8e686db33`. Independently recomputed the three assigned SHA-256 values:

- `task-10a-fix1-report.md`: `9c9d775b426ec1e3fababbbdc0d25aef4d421e0953cb09cb2fbea1511a5025ab`.
- `task-10a-fix1-delta.diff`: `0a79756b5527b93f5b771e2ad6370f9a3846a61c406935c4b09d4b631eba449a`.
- `task-10a-fix1-review-package.diff`: `74c133e60724b2135419a759cbe13180fe0bb534232023a855347694fbd55d5c`.

**Owned-file hash check: PASS.** All 15 file/package SHA rows in the report match current bytes, including all 13 owned source/evidence files. A read-only Python reconstruction using `git show` base blobs applied the full package entirely in memory, and separately applied the frozen initial package plus correction. Both reconstruct the same 13 current files byte-for-byte. Delta paths are exactly `lib/partner-api/core.ts`, `__tests__/api-body-auth-contracts.test.ts` and `quality/audits/proofs/api-body-auth-regression.mjs`. The first reconstruction attempt stopped on the reviewer's handling of the existing no-final-newline patch marker; correcting that in-memory parser yielded exit 0. No repository patch application or source mutation occurred.

**Evidence accounting: CHECKED.** The author report includes the source RED symptom, the exact GREEN command, its `PASS 32 actual-source cases` output and exit status, and names the other executed checks. Reading the current source agrees with the expanded inventory of 40 authored permanent cases (13 partner, 13 signed-webhook, 14 worker). These remain author-reported source results and authored coverage, not independently rerun supported tests. Only the eight focused missing-target controls were executed by this reviewer; no broad suite was repeated.

**Unsupported acceptance limits: OPEN, root-owned.** Node 22 ESLint, script/test typechecks, permanent Vitest, full Vitest, Next build and actual installed `resend@6.12.4` execution are NOT_RUN locally. No dependencies were installed or retried. The source harness strips TypeScript/imports and evaluates VM contexts, supplies a minimal `NextResponse.json`, fixed test API-version/OpenAPI values, controlled database/RPC/DNS I/O and scheduling/terminal boundaries. Its Resend verifier sentinel is intentionally unreachable in the exercised missing-secret/oversize cases and supplies no signature acceptance. Node emitted the known `stripTypeScriptTypes` ExperimentalWarning in the reviewer probe. No actual Next server, DB rate counter, native RLS, provider delivery or maximal provider payload acceptance is claimed.

**Routing and scope check: COMPLETE.** Read Task 10a brief/prep, prior review, fix report/delta/full-package context, AGENTS, active memory pointers and relevant API/security/auth domain memory. Applied scoped re-review, code-review, differential-review, requirement-to-source comparison, direct false-positive checking and verification-before-completion guidance. Broad audit fan-outs, remediation, UI/performance, SQL/migration, installation and delivery operations were skipped because this is a frozen correction-only review with explicit no-agents/no-source-edits boundaries.

**Mutation check: ONLY THIS REVIEW FILE WRITTEN.** No DB/provider/external calls, worker execution, subagents, source edits, dependency/lockfile/workflow/migration/type changes, root memory/index/history/branch edits or publication. Existing unrelated dirty files were preserved.

**Fix round verdict: ALL THREE FINDINGS ADDRESSED, NO NEW CRITICAL/IMPORTANT BREAKAGE.** Ready for root-owned supported/hosted gates; no further author correction requested by this scoped review.
