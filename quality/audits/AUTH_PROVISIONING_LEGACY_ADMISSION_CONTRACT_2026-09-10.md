# Auth provisioning legacy admission and forward restoration contract — 2026-09-10

Status: **DESIGN COMPLETE; IMPLEMENTATION, SQL ACCEPTANCE AND SELECTION PENDING.**

## Decision and authority

The next coherent unit is **all eight immutable sources A/B/C/D/E/F/H/I plus one immediate forward reconstruction Q, executed through one fail-closed offline transaction envelope**. Keep the verified first43 unchanged. Admit an empty affected-business-data state, including the actual canonical empty replay with its existing role catalog. Preserve the entire preexisting role catalog exactly; allow only the source-defined missing catalog seeds, with no new role assignments or permission links. Reject populated affected-business-data states before executing the originals. Their historical behavior is characterized in reduced rollback fixtures, never an approved populated upgrade.

This is a precise zero-eligible-row boundary, not an exclusion or a substitute for source execution. Every one of the eight sources executes in full, in order. A unconditionally updates event rows; F unconditionally updates memberships and invitations; E/H/I infer access from incomplete legacy identities and role aliases; C accepts invitations without canonical verification. A general snapshot-and-reverse scheme would also need to reverse trigger-generated history and potentially external work. That is unnecessary and unsupported when empty replay is the next authorized boundary. Restricting admission resolves the source-backed safety problem without a product-permission question.

The role catalog is the one deliberately supported populated target: D/F overwrite existing names/flags, and F can reactivate inactive catalog entries. The envelope locks all related access tables, saves an exact role preimage, runs complete sources, and Q restores every existing role field before commit. No source-generated access row may survive or even be generated in the admitted lane. Missing catalog entries are a source-defined structural seed, not a grant. Existing role permissions and privilege checks remain unchanged.

ADR-006 (`.agent-memory/decisions.md`) remains authoritative: **do not replay or mass-mark this noncanonical historical chain live**. Eventual live convergence uses the existing `20260728170000_live_schema_code_canonical_sync.sql` repair point and its independent green post-apply/lint gates. Q is an offline replay reconstruction with a required envelope context, not a separately executable production migration. No production invocation, ledger entry, provider operation, credential generation, real-user read or outgoing message is part of this task.

Verified baseline: commit `194fd0cf2250f0bb64f199e72f45f32a3c3750e4`, tree `80b28b18d51da671b9a9754c3698181db04e2d9f`; OPS34482627601/auth102888925544 PASS complete fixed16. Foundation84/first43 and the original16 commands are frozen. Current594 =524 FULL_FILE_SELECTED +24 SUBSTITUTED +42 UNCLASSIFIED +4 EXPLICITLY_EXCLUDED;66 unresolved. Focused340 =281/21/34/4;55 unresolved. These are selection counts, not surviving-effect or production proof. Earlier counts and pending-G statements in reused reports are historical.

Activated skills: repository SDD worker contract, source-backed architecture comparison/brainstorming, writing-plans, Supabase and PostgreSQL security/locking guidance, code-security (SQL execution, secrets and infrastructure error paths), sharp-edges and verification-before-completion. Read AGENTS, current memory and relevant architecture/auth/database domain files, ADR-006 and known failures. `using-superpowers` exempts dispatched workers; controller owns review and status. No repeated repository inventory, new effects matrix, broad scanner audit, UI/performance work, deployment or hook installation is indicated. SQL/TDD and exact-head hosted acceptance activate in implementation. Explicit task scope controls plan/report paths and removes execution-choice questions. Supabase changelog markdown fetch was unsupported; this document adds no changing Supabase feature or assumed CLI behavior. CLI generation uses the already verified pinned setup below.

## Immutable inputs and complete source order

Reuse `AUTH_PROVISIONING_SOURCE_EFFECTS_2026-09-10.md` without redoing its1644-line/103-unit inventory. The eight bytes/hashes and every matching history-manifest record were checked again for this design. G's7 units/51 lines are already selected and outside this restoration scope.

| ID | File under `supabase/migrations/` | Lines | SHA256 |
| --- | --- | ---: | --- |
| A | `20260519_company_invite_temp_password_sync.sql` |415| `09ed878125a71c77c792e004fd1a38c4fa56a0b23e0bc3eafeb62be271c85dc9` |
| B | `20260520_direct_account_temporary_password_flow.sql` |98| `0fff8d88e6c89c4d4cf16ad1f760bf079b29b7d684b9e8d398efd2bbcead93ba` |
| C | `20260520_direct_temporary_password_auth_sync_fix.sql` |183| `f81c427325e8ecdb9380038ebad994def06004f1a67cd6b7718247e090b632db` |
| D | `20260527_debug_user_invites_role_flow.sql` |80| `3d47fa7e5307e3b4568e737a8ee54806e67049a41200b4581e25f1b479e2f107` |
| E | `20260527_fix_company_user_creation_schema_safe_backfill.sql` |222| `e6a91a085651cd6c3cd6eb4b75faf35f7f4a061f2046824367247bc4ff9edd22` |
| F | `20260527_fix_company_user_invite_runtime_columns.sql` |160| `bd83735afcfa2f4eeeda0bded43a447e0f060584c7fc28b2c6b6fd6c36d3560c` |
| H | `20260528_final_user_access_schema_safe_repair.sql` |296| `4968391d74a8ff813ce1f56a8b8d9ade682692d183988917e736d0f3c5857bd2` |
| I | `20260528_fix_user_roles_without_role_column_and_compact_users.sql` |190| `394a8eba24370f0158d52ddc84ea0baf938f3ef94a9d561ae1e679475974f3ca` |

Current order-file SHA256 is `00f1536428a802a6549c27605d93008d17660b0af80d0e12052a4c795c5b70a6`. The actual first43 is first41 plus G42/R43. `scripts/canonical-auth-provisioning-diagnostics-selftest.py:actual_prefix_sql` subsequently executes `20260908120000_preserve_gridex_user_has_role_key.sql`; this **additional downstream helper is not present at ordinal43**. The new actual-prefix lane must observe first43 before that helper. A second clearly named lane can apply the helper afterwards and reuse final RBAC/governance/journal checks.

Prospective foundation order after proof/review: preserve1–43; insert A44/B45/C46/D47/E48/F49/H50/I51/Q52; shift former44–84 by9 without other changes, total93. Q is the actual CLI-created file ending `_canonical_auth_provisioning_legacy_boundary.sql`; no timestamp is invented here. Sources at44–52 form an indivisible executor batch. Foundation scheduling must consume the complete group once and timestamp scheduling must exclude every already-selected member, including Q. Reject a partial/duplicate group before any source is submitted. A direct migration path must not execute these offline originals; Q independently fails without the same transaction context, and ADR-006 continues to forbid whole-history production push/replay. Keep P38 `bootstrap/20260527_company_memberships_role_key_foundation.sql` at38 with unchanged bytes/hash `46c5e05a35063f84547dcf6554bc378d9c90d62171cd38843383542c6fe602c5`; update its existing derived-bootstrap metadata to `preserveSourceReplay=true` only in the subsequent selection commit. This closes F's missing full-source accounting without breaking RBAC38–41 adjacency.

No extra whole invocation of the flexible-action normalizer or existing transaction-owning invitation/actor reconstructions is inserted inside this envelope. Q reconstructs their explicitly identified catalog guarantees below; it does not classify their fragments as whole-source execution. They retain their existing independent execution/selection identities. Sources A–I have no transaction control; Q must also contain no BEGIN/COMMIT. The existing R has its own transaction and remains outside, at43.

## Four different acceptance boundaries

| Boundary | Next execution and acceptance |
| --- | --- |
| Empty canonical replay | Complete managed-compatible bootstrap and exact first43 bytes, then the envelope with complete eight originals and Q; commit. Business targets start/end empty. Canonical role seeds may exist. No reduced table excerpts or manually supplied downstream helper in this lane. |
| Seeded compatibility proof | Synthetic preserved Auth/company identities, unrelated history and diagnostics, inactive/custom role catalog entries; business target tables still empty. Commit only under the same admission. Separately, explicitly reduced rollback fixtures exercise populated source behavior; their success is characterization only. |
| Dirty-state admission | Synthetic populated affected targets, incompatible catalog, extra triggers/rules, wrong indexes/FKs or conflicting identities must fail before A and leave rows/catalog unchanged. An observation report is not an admission token and cannot authorize later execution. |
| Eventual live forward convergence | No eight-source envelope. Derive reviewed DDL-only structural convergence from verified final replay at ADR-006's repair point; preserve live rows, reject unresolved mappings, and use canonical actor-authorized workflows for access changes. Full final-chain, ledger and runtime-binding gates remain separate. |

The complete-source characterization already proves completed→sent/unknown event normalization, flexible profile-action erasure, expired password-metadata acceptance and skipped inline actor REFERENCES. Reuse `AUTH_INVITATION_CHAIN_REVIEW_2026-09-07.md`, `INVITATION_REPLAY_EFFECTS_2026-09-06.md` and existing five-source/actor-FK tests. Do not claim the five-source reduced fixture was an actual first43 replay. `20260520_company_delete_backfill_and_admin_layout.sql` orphan DELETE branches remain outside A–I and unproved by the FK-constrained fixture; do not append this source as an expedient CHECK repair.

## Admission and transactional execution contract

Create `scripts/canonical-auth-provisioning-legacy-batch.py` and its source-controlled SQL admission/assertion support under `scripts/sql/`. They are executor infrastructure, not substitute migrations. The module exposes one strict function taking the reviewed absolute source paths/hashes and an owned isolated-database handle; no generic user SQL, arbitrary database URL or production fallback. The clean-replay foundation loop delegates exactly44–52 as a single batch. Fixture and real offline replay use the same executor implementation.

1. Verify all input bytes, exact path order/adjacency, Q hash, selected-source identity and absence of transaction/meta-command escape in new support/Q before connection. Reuse the immutable A–I no-transaction-control evidence; never split DO statements, filter sensitive lines, rewrite checksums or catch-and-continue around source errors. Require explicit disposable target ownership and the existing isolated-host/port/database-name allowlist. Clean-replay external mode additionally retains its explicit no-ledger-provenance boundary.
2. Open one connection through `psql -X --single-transaction -v ON_ERROR_STOP=1` with separate `-f` arguments for admission, each complete original, Q, then final assertions. Capture output privately; never print the argv/URL or source. Use READ COMMITTED, local lock_timeout10s and statement_timeout60s. Initial source parsing and all verification must finish before psql commits. Failure/connection death rolls back the entire batch, rather than leaving A/B committed before a later failure.
3. Lock the existing touched and dependent relations in a deterministic qualified-name order before reading admission state: Auth users and sessions when present; public companies, profiles, memberships, invitations, auth_email_events, roles, user_roles, permissions, role_permissions, user_permissions and user_permission_overrides; auth_provisioning_events; company_user_audit_journal and discovered enabled-trigger write targets. Use ACCESS EXCLUSIVE for directly altered/updated public targets, SHARE for read-only parents/dependent histories; reject unreviewed additional trigger/rule dependencies. This is an offline envelope, not a low-lock production procedure. Check catalog identity again after acquiring locks. READ COMMITTED reads after waiting see newly committed data; do not take a pre-lock REPEATABLE READ snapshot and then trust stale emptiness. Lock contenders that cannot finish must return55P03, never proceed without a lock.
4. Require the five business targets **user_profiles, company_memberships, company_invitations, auth_email_events, user_roles** to be empty; the role catalog is separately governed below. Count each exactly; admission requires each count=0. Do not delete, detach, temporarily hide, change tenant ownership, reset status, null metadata, or move rows to meet this condition. Profiles are included because A/B/C rewrite status/action, user_roles because H/I normalize NULL activity, and A/F's unconditional UPDATE prevents a weaker predicate from preserving triggers/timestamps on nonempty targets. Auth users may exist, but with no profile/membership/user-role edges E/H cannot create a profile; assert that candidate set separately equals0. Companies may exist but are not mutated. The condition is independent of tenant, actor, profile status or expiry.
5. Require existing canonical relation kinds/owners, UUID PK identities, types/defaults/nullability and exact named index/FK/CHECK contracts for the actual-prefix lane. Required targets must exist; A's fallback CREATE is not permission to create a weak no-FK substitute when an actual canonical table is missing. Extra compatible columns can be preserved only with an explicit reviewed catalog projection; reject unknown constraints/rules/triggers, generated columns touched by source, domain/type coercions, deferred identity checks and unclassified grants rather than silently altering them. All enabled triggers stay enabled. Allow only the exact prefix's reviewed transactional triggers and dependency set; reject any external/network/provider side effect in the envelope.
6. Save owner-only temporary role preimages keyed by roles.id, the set of preexisting columns, named CHECK definitions listed under Q, complete non-target row multisets and catalog/ACL/policy/trigger fingerprints. Use ON COMMIT DROP. Snapshot values stay in the owned database or private memory, never receipts/artifacts. No app-callable admission RPC, security-definer shortcut or persistent journal is added. Q must reject missing/mismatched context, transaction ID or stage; standalone Q without the envelope fails before mutation. The context is an operational assertion for the owner-run replay, not an authorization credential.
7. Execute A/B/C/D/E/F/H/I in order, with no session-role/RLS/trigger changes. Record safe per-file completion and exact affected-candidate assertions. Q immediately reconstructs the final boundary, then the assertion file proves exact allowed row/catalog deltas and marks the context completed. Only successful final assertions permit COMMIT. Never run this batch through the old per-file autocommit loop. Outside observers must see either the pre-batch catalog/rows or the final repaired state.

The schema spelling/count above is normative: the five empty business relations are listed explicitly. `roles` is the sixth mutated relation, with its own nonempty admission contract. `auth.users` and company/history/diagnostic rows are retained dependencies, not part of the emptiness requirement.

## Identities, row multisets and allowed final changes

Compare rows with bidirectional EXCEPT ALL over explicit fields or full to_jsonb row projections where column additions are accounted for; counts or hashes alone do not establish preservation. Catalog comparisons separately retain owners, RLS/FORCE RLS, grants including columns/inheritance, policies, enabled triggers, defaults and constraints. Synthetic IDs can be logged only as case labels; no row values are necessary.

| Relation/identity | Allowed before→after committed multiset; rejection cases |
| --- | --- |
| Profiles / id=Auth user UUID | Empty→empty. All status/action/password flags/times, active_company_id, email/name/phone, creation/update fields are therefore untouched by source DML. Any populated row, including NULL/unknown/inactive status, flexible action, expired metadata or orphan FK shape, is DIRTY_DATA and preserved on rejection. No profile generation from provider metadata. |
| Memberships / id, unique(company_id,user_id) | Empty→empty. No role/role_key/role_id, is_active/status, invited email/actors, joined/invited/accepted/disabled/removed times/reasons, metadata or audit ownership is synthesized. NULL IDs, duplicates, orphan Auth/company references, cross-tenant mappings and inactive rows all reject with population; native relationship cases run in reduced rollback lanes. Do not add a membership Auth-user FK merely because UUID. |
| Invitations / id plus company_id | Empty→empty. Preserve distinct UUID token, legacy invitation_token and accept_token_hash catalog; no token alias conversion/hash generation/temporary password/acceptance/login_ready mutation. Any pending password-issued row, including expired, any sending/delivery_uncertain row, conflicting email aliases, mismatched invited-user/company, NULL status or noncanonical role is rejected before C. |
| auth_email_events / id | Empty→empty in admission. Historical event_type/action/status/email/source, user/company/actor IDs, JSON metadata and timestamps are never normalized on populated data. Completed/unknown/NULL/extended events are characterization/rejection cases, never discardable history. |
| user_roles / id; active triple(company_id,user_id,role_id) | Empty→empty. No default admin assignment, NULL→active, inactive reactivation, missing-role recovery or latest-row choice. Orphan/NULL company, duplicate triples, multiple different roles per pair, equal-created-at ties, inactive profile and conflicting membership states reject. Existing global/platform roles reject too; no implicit exemption. |
| roles / id; key unique when non-NULL | For all preexisting IDs, exact preexisting field values survive, including key/name/description/scope/is_active/is_system_role/is_system when present/created_at/updated_at and extra admitted fields. Q must restore NULL and false distinctly. If is_system did not exist at entry, its added value for preexisting IDs is D's declared default false; no invented authority. Existing inactive, custom-name and explicitly non-company rows are preserved. Non-NULL duplicate keys, invalid PK/default or ambiguous key/name aliases for the fourteen seed keys reject. New IDs may exist only for missing exact seed keys listed below, with the complete source's final projection and no related permissions/assignments. |
| Auth / auth.users.id; auth.sessions identities | Exact full synthetic-row multiset including metadata/credential/session fields; no provider reads or operations. No password/ban/delete/session revocation/refresh operation is implied. Do not emit snapshots. E/H candidate query must remain empty. |
| Companies / id; all durable work/history identities | Exact full row multisets and attribution, including company_user_audit_journal, provisioning jobs/intents, audit/domain/outbox tables present at the boundary, role_permissions/permissions and permission overrides. No new durable work or role permission row is allowed. auth_provisioning_events history is preserved, including NULL/orphan/multi-company diagnostics. G/R view projection, invoker option, policy OID and effective client denial remain intact. |

Fourteen missing-role keys, exactly D/E/F's catalog set: company_admin, admin, operations_manager, operations_agent, customer_service_manager, customer_service_agent, sales_manager, pricing_manager, pricing_approver, finance_readonly, executive_readonly, compliance_manager, partner_manager, partner_api_user. New rows use source-generated UUIDs and timestamps; key/name equal key after F; description follows D's insert, scope company, source final flags true. H fills only NULL/blank values and introduces no extra key beyond this set. Verify full source-derived values, not just these summaries. D/F match `roles.key = seed.key` exactly, not lower(name), name fallback or aliases. Preflight computes missing keys by that same equality, requires at most one existing row per key, and rejects NULL/empty-key rows whose name aliases a seed key or other key/name ambiguity; never choose a matching row by timestamp. New-role count must equal the preflight missing-key cardinality and repeat must add0. No new role_permissions links means no new permission grant; user_roles remains empty. For preexisting roles, an UPDATE timestamp trigger that prevents exact preimage restoration requires rejection or a separately reviewed restoration mechanism; never disable it. The scoped first43 source trace finds only tenant-operational guard creation in selected31/37 and no roles update trigger; verify that actual catalog in the new fixture. This is a concrete gate to prove against the actual prefix, not permission to weaken timestamps or add an allowlist by name alone.

## Q: exact immediate forward reconstruction and later winners

Q has one atomic DO/body sequence with no transaction control; it is executed inside the batch's outer transaction. All dynamic identifiers are quoted with PostgreSQL identifier-safe formatting. CHECK definitions come only from validated catalog/source contracts, not row text or caller input. Q never performs generic inverse business-data UPDATE, DELETE, auth mutation or privilege grant.

| Superseded effect | Immediate Q behavior and authoritative winner |
| --- | --- |
| D/E/F/H catalog seed overwrites | Restore every preexisting roles row's preimage, including trigger-touched fields; retain only the exactly missing source seeds. Assert permissions and assignments unchanged. Later fill-NULL seeds are not an inverse of F activation; this restoration must happen before commit. |
| B flexible action replaced by C's eleven-value CHECK | Recreate `user_profiles_last_auth_email_action_check` as NULL or length<=120 and `^[a-z0-9_:.]+$`, exact `20260520_user_profiles_auth_action_constraint_hardfix.sql` selected6 contract. Do not run its lossy normalization DML against populated rows. Original source6 remains independently selected. |
| A/B/D/F narrow invitation-status CHECK | Replace `company_invitations_status_check` with exact ten-state canonical delivery vocabulary pending/sending/sent/delivery_uncertain/accepted/revoked/expired/invitation_revoked/invited/failed, fully validated; source `20260810193450_canonical_access_provisioning_runtime_v1.sql:60–68`. A later wider CHECK cannot recover earlier row states; empty admission is required first. |
| A/C event-type/action/status CHECK churn | Restore the exact pre-batch validated definitions of `auth_email_events_event_type_check`, `auth_email_events_action_check`, `auth_email_events_status_check`, including absence when no such CHECK existed. Actual first43 authority is selected template7 plus complete selected predecessors; snapshot only an explicitly validated actual-prefix or reduced-fixture contract. Preserve all other CHECKs. No union of guessed event vocabularies and no log-row normalization. The separate cleanup source is not introduced. |
| A/B/E/F/H membership CHECKs and profile status | Final membership role/status checks retain complete H's source vocabularies, with semantic assertions against existing canonical governance states and source matrix; profile status retains the canonical five-state contract from selected governance/B. Additional existing restrictions remain, never silently dropped. Validate NULL/default and relevant inactive/revoked cases. New structural defaults are catalog effects, not permission to infer current access from NULL. |
| B skipped inline REFERENCES on existing columns | Validate/add exact `user_profiles.active_company_id`→companies(id) SET NULL FK, using selected8 source; membership disabled_by/removed_by→auth.users(id) SET NULL, validated/nondeferrable/NO ACTION update/SIMPLE, from `20260907121951_canonical_membership_actor_fk_reconstruction.sql`. Validate existing definitions exactly and reject mismatches. No guessed membership.user_id FK. |
| Invitation actor/token omissions | Validate/add invited_by and invited_user_id→auth.users(id) SET NULL FKs with exact catalog semantics from `20260906081839_canonical_company_invitation_runtime_reconstruction.sql`; validate existing token/hash indexes and UUID token prerequisite selected33. No token column/value regeneration. Preserve company RESTRICT and actor nullability. The existing reconstruction's name-only guards are not sufficient dirty-shape proof. |
| A/E/F/H indexes and missing columns | Retain complete added columns with source type/default/nullability, except preexisting shape must have passed validation. Verify each named index's keys/order/predicate/uniqueness/validity. Invitation company/status index retains the earlier canonical two-key winner from selected28 (`20260909120100_canonical_invitation_status_index_reconstruction.sql`) despite A's same-name three-key request; assert the exact original named definition. Preserve lower(email) index's verified existing definition and token/hash uniqueness. H's active user-role triple index does not establish one role per pair. |
| Functions/triggers/ACLs | A–I create/replace no stored function, trigger, policy or ACL. Q creates no callable API. Preserve inherited function identity/body/search_path/EXECUTE, all enabled triggers and relation/column privileges. No extra default relation grants: required targets already exist. Preserve G/R effective denial and restrictive policy OID. Any unexpected new effective client access or external trigger side effect aborts. |

Existing reconstruction hashes remain: invitation `d30a89a4fa793cddf3cf4560e1bb40bb2831505e911bd630c1409117e7f877f8`; membership actor `530dec49b180ba741340572e8117f7c47ac8ef60c6112a2b88a8bb195e90d27b`; token prerequisite `51f0b8df14704c4eb7d9cd6bccf59855ad0cbf7f5a0016bbfb10ea87078c25b2`; R `018d81e763e6134ddb3d886ef7e219d6247584a9dad14b871e2ef98014db6333`. Q must not loosen these guards.

Canonical callers remain `lib/auth/companyInvitationFlow.ts` (create durable intent, leased provider delivery, hash/verified-user token acceptance) and `lib/auth/companyUserAccess.ts` (same-company active membership and matching active role). Exact acceptance authority is `20260802203000_canonical_runtime_consistency_hardening.sql:190–385`, service_role-only DEFINER/public,auth,pg_temp JSONB RPC with invitation/company/pair locking, actor=user binding and fresh acceptance expiry/identity/profile/company/assignable-role checks. Its prior-accepted/idempotent branches precede some later active checks; this design does not invent stronger behavior. Create authority is `20260810193450_canonical_access_provisioning_runtime_v1.sql`, with search_path hotfix `20260810221500_canonical_invitation_delivery_hotfix.sql`. A–I do not replace these functions.

The later `guard_tenant_invitation_acceptance` trigger checks accepted transitions against company state, not all fresh canonical acceptance rules. Delivery enqueue from `20260810224500_canonical_review_remediation_v1.sql:563–585` is AFTER INSERT for pending idempotent intent; A/C/F UPDATE does not invoke that INSERT-only path. Preserve final session/RLS restrictions and durable company attribution. No automatic acceptance, login readiness, session revocation or delivery follows from this offline reconstruction. Full later June/August policy/session/function composition requires its own complete final-chain proof; current first43 and extra-helper lanes cannot certify it.

## Complete103-unit discharge register

This table references, rather than duplicates, the established complete-unit and DO-child matrix. Each listed ID is covered exactly once; child effects inherit its row's boundary. E=empty canonical execution; S=seeded admitted compatibility; D=dirty rejection; L=eventual live DDL-only convergence. D applies to every group through admission; populated historical source behavior is reduced rollback characterization only.

| Exact source units | Count | Next boundary and retained dependency |
| --- | ---: | --- |
| A01–A04 |4| E/S/D/L: extension/profile shape and named status drop; canonical existing targets required; five-state restoration/catalog assertion. |
| A05–A08 |4| E/S/D: zero profile rows; no NULL/unknown activation, status vocabulary checked. L preserves populated values. |
| A09–A12 |4| E/S/D/L: full profile index/membership columns and CHECK drop; exact identity/FK/index admission. |
| A13–A22 |10| E/S/D: zero memberships blocks every status/role rewrite; final H CHECKs, no alias grant. |
| A23–A27 |5| E/S/D/L: exact pair uniqueness/invitation fields/status drop; token and company/actor dependencies retained. |
| A28–A36 |9| E/S/D: zero invitations blocks state/role loss; Q canonical expanded delivery winner. |
| A37–A43 |7| E/S/D/L: token/email/status indexes and event shape/CHECK drops; existing event relation/catalog required. |
| A44–A59 |16| E/S/D: zero events blocks normalization/history loss; Q exact preimage CHECKs. |
| A60–A62 |3| E/S/D/L: exact event indexes/user-role columns; no access normalization or grants. |
| B01–B04 |4| E/S/D/L: all four complete statements; zero profile/action rows; Q flexible action, exact profile/membership/invitation actor FKs and delivery CHECK. |
| C01–C10 |10| E/S/D/L: profile/event columns and CHECK effects; all normalization candidates empty; Q restores known canonical checks. |
| C11 |1| E/S/D: eligible invitation count0 including after A/B; dirty issued-password rows rejected before A. L only verified canonical acceptance workflow. |
| D01–D02 |2| E/S/D/L: role preimage restoration/missing seeds plus full membership/invitation CHECKs; exact scope/flags preserved. |
| E01–E04 |4| E/S/D/L: complete shape/normalization and catalog seed DOs; roles restore; membership/user-role emptiness. |
| E05–E07 |3| E/S/D: all profile/membership/role insert candidates0; inactive/orphan/default-admin/tie cases rejected. |
| E08 |1| E/S/D: commit-delivered notification only after Q/assertions; rollback delivers none. |
| F01–F03 |3| E/S/D/L: every DO child executes; empty memberships/invitations; role preimage restoration; no alias guess/reactivation survives. |
| H01–H03 |3| E/S/D/L: full role/access shape, index/CHECK and seed effects; exact catalog/role preservation. |
| H04–H07 |4| E/S/D: profile, membership and role insert/update candidates0; no nondeterministic/multi-role reconciliation. |
| H08 |1| E/S/D: notification only on successful complete commit. |
| I01–I02 |2| E/S/D/L: full extension and schema/normalization DO; NULL activation prevented by empty targets. |
| I03–I04 |2| E/S/D: both INSERT/UPDATE branches execute with0 candidates; no duplicate-pair insertion or implicit company_admin. |
| I05 |1| E/S/D: final source notification is not commit or readiness; Q/assertions still required. |
| Total |103| A62+B4+C11+D2+E8+F3+H8+I5;1644 immutable lines. |

## Sensitive failure and cleanup-log contract

Current hosted service cleanup prints SQL STATEMENT lines for expected database errors. This is an observed logging path, **not evidence of an observed secret leak**. Future sensitive fixtures must protect both client receipts and raw server/container cleanup output before submitting source SQL; filtering a Python exception alone is insufficient.

Use an explicitly owned disposable PostgreSQL17 container for the new sensitive fixture lane, started with logging collector enabled and a private owned container-local0700 log directory and0600 log files (`log_file_mode=0600`) so SQL/error content never reaches Docker stdout/stderr during normal query failures. Configure `log_statement=none`, `log_min_error_statement=panic`, `log_error_verbosity=terse`, `log_parameter_max_length=0`, `log_parameter_max_length_on_error=0`, and disable duration/sampling/auto-explain/audit statement emission in that owned container. Verify settings before any sensitive source is sent. PostgreSQL documents that failing statements are separately logged by default and that PANIC disables ordinary error-statement logging; collector/file isolation also covers unexpected DETAIL/CONTEXT/FATAL/PANIC paths rather than relying on masking. See [PostgreSQL17 logging](https://www.postgresql.org/docs/17/runtime-config-logging.html). Do not change production logging or the original16 command semantics.

The dedicated runner must not unconditionally print `docker logs`, log files, psql stderr, SQL text or provider payloads during teardown. Capture raw client/server streams privately, scan synthetic sentinels without emitting matching lines, produce allowlisted diagnostics, and destroy the owned container/private volumes after verification. Cleanup runs on success/failure/timeout/cancellation, and must not delete unrelated containers. Configure service initialization/startup so no source is executed before logging settings are verified. If using the existing GitHub service instead, implement equivalent collector isolation before sources execute and prove the runner's automatic service teardown cannot print captured content; a Python-only patch is not equivalent.

Client receipts permit case ID, source alias/path/hash/line-count, stage, exit code, five-character SQLSTATE, and static error category. Native error text can itself contain row data, so do not emit the primary message merely because CONTEXT/STATEMENT was stripped. Use category mapping such as DIRTY_DATA, CATALOG_MISMATCH, ENVELOPE_REQUIRED, PRESERVATION_FAILED, UNEXPECTED_TRIGGER, LOCK_TIMEOUT. Always preserve the actual SQLSTATE; never convert an unexpected native error to PASS. Synthetic tests must deliberately raise expected and unexpected errors containing a noncredential sentinel in primary/DETAIL/CONTEXT/statement/parameter positions and prove it absent from public Python, workflow and teardown receipts. No raw log artifact uploads. Paths/line references identify sensitive original statements without copying literals.

## Concrete implementation, review and acceptance tasks

1. **Preparation, if Q filename is needed.** Use a separate hosted no-database job with `supabase/setup-cli@v1`, pinned CLI2.101.0; run `supabase --version`, `supabase migration new --help`, then `supabase migration new canonical_auth_provisioning_legacy_boundary`. Upload only the exact new zero-byte skeleton with actions/upload-artifact@v4. Record run/job/head/artifact ID, ZIP SHA256, sole basename, size and member SHA256; retrieve exact bytes through existing authorized artifact tools. Remove preparation steps before authoring/publishing Q so no duplicate timestamp is generated. This reuses Task2's verified setup/artifact protocol (artifact10151184576 for diagnostics), not its old filename or empty content. Do not retry absent local PG/CLI or blocked probes, invent a timestamp or mutate a database to obtain a name.
2. **Implement unselected batch and test harness.** New Q, `scripts/canonical-auth-provisioning-legacy-batch.py`, `scripts/canonical-auth-provisioning-legacy-selftest.py` and reviewed support under scripts/sql; appropriate current history-manifest Q registration only. Reuse existing bootstrap, prefix source loaders, multiset helpers and fixed16 tests. New selftest exposes `--selection-only` for hashes/order/context/static invariants; no SQL claim from that mode. Keep source selection/foundation unchanged initially. Independent implementation review must assess transaction envelope, exact role restoration, CHECK/FK/index repairs and safe cleanup before hosted execution.
3. **Hosted isolated proof.** Add a standalone new-lane job using the owned-container logging design and synthetic-only target. After the initial unchanged fixed16 prerequisite receipt, the two isolated jobs may run independently and concurrently; acceptance and source selection still require both jobs to PASS on the same current reviewed head. Existing16 take about8min under10min timeout; give the new isolated job a separately justified20min budget and deterministic lock wait bounds, rather than removing tests or silently exceeding the old budget. Emit timings per case/lane. Do not restart the effects inventory. Execute all cases below and retain exact-head safe receipts. A failure advances diagnosis only; no selection follows until repaired/reviewed execution passes.
4. **Selection/executor integration after proof.** Add exact A–I/Q foundation membership and order, P38 preserveSourceReplay metadata and source/hash envelope manifest. Update `scripts/gridex-aud-003-clean-replay.sh` to group exactly44–52 through the same executor, skipping none or double-running none; retain existing timestamp skip logic. Extend `scripts/gridex-aud-003-clean-replay-selftest.py`, `scripts/gridex-replay-input-accounting-selftest.py`, `scripts/canonical-auth-membership-group-selftest.py` and source-specific selection tests for exact adjacency and rollback enforcement. Preserve historical prefix30/31/32/33/38 and existing diagnostics16 boundaries. Append the new selftest as command17 to `scripts/canonical-auth-membership-group.py`; execute unchanged16 in original order, then17 with its owned-container service. Increase the combined auth job timeout to25min with recorded prior8min plus bounded new-lane budget, or retain two explicit jobs whose union is verified17; do not claim fixed17 if only the original16 ran.
5. **Exact reviewed-head acceptance.** Independently review the selection/integration diff and publish the complete coherent batch. Require exact commit/tree and all17 commands PASS, source hashes and execution counts, actual first43→batch/Q proof, and unchanged earlier lanes. Keep quality/build/Ediel gates. Generated-types/full-source-completeness red gates stay open; no artifacts/types refresh from this partial replay and no phase/readiness closure. Root then continues final-chain restoration and ADR-006 convergence continuously.

Prospective accounting for exactly one new selected Q plus all eight selections:595 =533 FULL_FILE_SELECTED +23 SUBSTITUTED +35 UNCLASSIFIED +4 EXPLICITLY_EXCLUDED;58 unresolved. Focused341 =290/20/27/4;47 unresolved. Delta: +1 input, +9 selected, −1 substituted, −7 unclassified, exclusions unchanged; foundation84→93. If implementation establishes that an additional genuine prerequisite migration is required, obtain/review its exact source/hash/count delta before selection; do not relabel this design's expected595 as observed. This design commit changes no counts.

### Required SQL and infrastructure cases

| Lane | Exact assertion |
| --- | --- |
| Actual empty prefix | Bootstrap + first43 complete bytes, no fabricated later helper; role catalog baseline saved before batch; complete A–I/Q commit; exact allowed rows/catalog and G/R denial. Repeat the entire batch and assert0 new role keys, stable old role rows and stable catalog definitions (CHECK OIDs may change where sources explicitly recreate them). |
| Additional downstream helper | From the independently identified post-Q boundary, execute full `20260908120000_preserve_gridex_user_has_role_key.sql`, then existing final RBAC/governance/journal checks. Do not label its guarantees as ordinal43 guarantees. |
| Seeded admitted | Two companies, Auth-only users/session rows, retained diagnostics and unrelated journal/work rows; role catalog custom names/inactive/NULL metadata/non-company scope; all five targets empty. Existing role preimages exact; new missing keys only; no permissions/access/session/durable intent change. Reject role-key ambiguity rather than choose latest. |
| Dirty business data | Individually seed each of five targets and a mixed case: NULL/inactive/unknown statuses, revoked membership, expired password-issued pending invitation, sending/delivery_uncertain, flexible action, completed/unknown event, orphan/null IDs and aliases, conflicting tenant or multi-role/tie identities. Admission returns static category before A; exact rows/catalog remain. Do not normalize rows to make admission pass. |
| Reduced populated characterization | Full source bytes under rollback, clearly distinct from actual-prefix lane. Reuse A/B/C losses already executed; add D/E/F/H/I inactive-role/name overwrite, NULL normalization, E/I default-admin and H conflicting-role candidates, I duplicate-pair23505 and tie/alias scenarios. Prove unsafe candidates are rejected by admitted executor. Never commit the unsafe characterization or use it as acceptance authority. |
| Dirty catalog/native | Missing target42P01, missing required column42703, bad type42804, wrong named index/FK/CHECK, invalid23503 actor, duplicate23505 key/triple/pair,23514 CHECK and23502 NOT NULL. Separate admission catalog rejection from native full-source rollback fixtures. Do not require preflight to intentionally run a known-dangerous source to obtain its native error. |
| Atomic failure/repeat | Inject synthetic errors after A, after C, after F and inside/following Q; full pre-batch rows/catalog preserved including role flags/history. Kill connection mid-batch; no partial commit. Q alone/missing context/wrong order/stale preflight fail. No notification delivered on rollback; schema reload only after successful commit. |
| Real contention/stale observation | Hold a target relation lock in session1; session2 envelope returns55P03 and leaves no changes. In another test commit a business row while session2 waits; after lock acquisition its fresh preflight rejects the new nonempty state. Two complete batch contenders serialize, retry adds no duplicate roles; trigger/catalog contention must be a real observed wait, not injected errors. |
| Privilege/session/trigger | Public/anon/authenticated/authenticator and inherited-grant variants retain exact baseline denial at G/R; service count preserved. No new roles/grants, external trigger invocation or Auth/session mutation. Enabled unexpected trigger/rule causes rejection. Existing selected guard and journal definitions remain exact; full later-runtime session/durable gates are explicitly downstream. |
| Logs and cleanup | Expected native and unexpected sentinel errors through client/server/cleanup paths; safe SQLSTATE retained, raw source/error/credential/provider content absent from public output/artifacts; failure cleanup destroys only owned disposable resources. |

## Self-check and limitations

The document's discharge table is mechanically checked against the reused matrix:103 distinct existing units, no duplicates or omissions, and total1644 source lines. Source hashes and matching manifests, referenced filenames, actual first43/extra-helper distinction, current accounting and doc diff whitespace are checked. No SQL was run for this design; no local PG/CLI availability is inferred. The central role-trigger restoration and actual-prefix shape conditions are implementation acceptance gates, not already executed assertions. Failure at either requires a precise source-backed correction and independent review; it does not authorize general populated replay or file-by-file fallback.

The full system integrity contract `SYSTEM_DATA_INTEGRITY_ACCEPTANCE_2026-09-09.md` remains open for complete tenant ownership, PK/FK/index/retention and final-chain proof. Offline source selection never proves live schema parity, runtime database binding, effective final RLS/session behavior or a successful production release.
