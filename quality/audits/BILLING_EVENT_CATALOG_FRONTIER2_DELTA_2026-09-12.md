# Task 12 frontier 2: current-observed fixture admission

2026-09-12. **BLOCKED for the unchanged full normal + Partner API + portfolio fixture profile.** Frontier 2 supplies the actual publication-revision helper/sink, price-plan parent, and portfolio permission assembly support. The actual compatibility validator exposes one missing application helper; a required relation default also lacks its complete extension-function projection. The next finite input is exactly two function signatures and no additional relation roots. This is metadata admission, with no SQL/native behavior executed or claimed.

## Evidence and exact scope

| Input | Bytes | SHA256 |
| --- | ---: | --- |
| quality/audits/BILLING_EVENT_CATALOG_FRONTIER2_2026-09-12.json | 233827 | ce4b1be582c68a390da2cd559e82b37bcb8eff957b7d480958fccedae4f79dfb |
| Actual embedded SELECT | retained in receipt | 5b198661402ae8c479cef35fdec01684f55ee9e9585daedc432196774a1fc83d |
| Earlier billing receipt | 5163485 | 63a84e0aa7e98503cd95c58e0a36b963e54b77dc273a4f3863ba5dd52ca9aa2b |
| Earlier billing delta report | 19974 | 5dd61d0ef5696ee1ecf41b4afedf348858aae114bb5598e17b8d3acf61f32786 |

Frontier 2 observed at **2026-09-12T14:08:31.676848+00:00**, earlier billing at **13:54:42.623376+00:00**. Reported project remains gridex-ops-dev/piidsfebjqjmnepdpnas, PostgreSQL 17.6, ledger count 279/tail 20260904222450. Runtime binding remains UNPROVED. There are three full relations, eleven functions (four named new helpers, one direct custom trigger function and six PostgreSQL RI functions), 200 direct dependencies, seven outgoing FKs, three parent-key projections, twelve index-detail projections and no sequence roots. All named function/relation missing assertions are empty; each of the four explicitly named function names has one observed overload. Those assertions do not cover subsequently discovered helper calls.

All four actual helper bodies and all new relation columns/checks/FKs/indexes/policies/triggers/ACLs were read. Companion pins identify exact JSON selectors, UTF-8 byte spans, raw slice hashes and separately labeled canonical hashes. No prior file was changed. Classification rows for only the named relations and ledger count/tail are authorized governance-metadata exceptions to system catalogs; no business, permission-assignment, ledger-statement, secret or sequence-current-value reads are represented.

The seven overlapping function objects (catalog revision trigger and six RI functions) exactly equal their earlier billing projections. Roles, memberships, default ACLs, namespaces and ledger also exactly equal the earlier billing projections. Auth users and companies parent-key projections exactly equal prior objects. The portfolios parent projection is a narrower id-only projection: selected column/index objects are exact matches; the prior receipt additionally selected company_id/composite-key metadata. This is a projection difference, not evidence of object drift. No complete previously retained relation was requeried; none is asserted fresh or simultaneous with frontier 2. Session context here is postgres/current_user and postgres/session_user, server_version_num 170006, and the exact retained search_path string. It does not retroactively populate the missing session_context field in the earlier executed 77d query.

## Actual relation and setup closure

| Relation / OID | Required shape and constraints | Trigger/effect and access implications |
| --- | --- | --- |
| public.contract_publication_revisions / 139868 | company_id uuid, channel text, revision bigint default 0, revision_token uuid default gen_random_uuid(), updated_at timestamptz default now(); all NOT NULL. PK(company_id,channel); channel CHECK website/api/internal/phone/partner; company FK CASCADE | No custom triggers/rules. RLS on, FORCE off, postgres owner. Actual table ACL includes anon/authenticated/service full table privileges; preserve it with the observed restrictive policies, including anon false guard. This is not authorization to grant the same ACL elsewhere |
| public.portfolio_settlement_permission_grants / 138273 | 13 columns; required id, user_id, permission, company_id, granted_by, granted_at, valid_from, reason, created_at. Optional portfolio_id, expires_at, revoked_at, revoked_by. id default extensions.gen_random_uuid(); timestamp defaults now(). Five FKs: company, portfolio, user, granting and revoking auth users, all ON DELETE RESTRICT. Eight checks/keys/FKs total; seven indexes | No custom triggers/rules. RLS on, FORCE off, postgres owner. ACL owner/service full, authenticated SELECT only. Active unique key(user_id,permission,company_id,COALESCE(portfolio_id,zero_uuid)) WHERE revoked_at IS NULL. Permission CHECK admits read/create/import/calculate/review/approve/lock/correct; expiry must be NULL or later than valid_from. Empty support relation is sufficient for assembly in the existing event profile; this does not drop policies or assert authenticated authorization |
| public.price_plans / 58021 | 13 columns. Required id/company_id/name/pricing_model/status/created_at/updated_at/plan_code/customer_type; defaults UUID, spot, draft, now(), now(), both as applicable. Optional description/created_by/updated_by/contract_product_id. Company FK CASCADE, id PK, company/id UNIQUE; unique company/plan_code index; pricing_model CHECK spot/fixed/portfolio/mixed/manual_override | One enabled custom AFTER INSERT OR DELETE OR UPDATE trigger trg_gridex_catalog_rev_price_plan / 345174 → revision trigger 345166. RLS on, FORCE off. Owner/authenticated/service full actual table ACL; anon no table access. Required parent INSERT has real revision/event/delivery effects |

No FK exists on the observed price_plans.contract_product_id or created_by/updated_by fields. Their nullable columns do not independently admit or require unrelated product/actor setup branches. The existing price_plan_versions id and same-company composite FKs now have a complete actual parent definition, not merely key metadata. Preserve name and company-unique plan_code in a valid synthetic parent.

The grants table's auth.users, companies and portfolios parent definitions/keys are already observed and pinned. All seven new relation-policy function references have exact retained wave-1 bodies: auth.role()16543, auth.uid()16542, gridex_can_read_company(uuid)41369, gridex_can_write_company(uuid)41370, gridex_is_current_session_allowed()41365, gridex_user_company_ids()40963, gridex_user_is_platform_admin()41367. The eighth unselected direct reference, extensions.gen_random_uuid()16460, is addressed below. Catalog dependencies alone do not enumerate PL/pgSQL/string-bodied SQL call graphs.

Parent-side internal RI triggers on price_plans include inbound relations beyond this profile. They fire on parent DELETE/key UPDATE, not the required parent INSERT. The declared fixture adds the parent/version graph and exercises the already specified event transaction; it does not delete these masterdata parents or change their keys. Preserve the observed registrations as evidence and the existing fixture FK boundary; do not claim a full database clone or silently add DELETE tests. No additional inbound child setup is required to execute the specified parent INSERT.

## Publication helper and reached revision sinks

Actual `public.gridex_bump_contract_publication_revision(uuid,text,text,text)` /139886 returns jsonb, is volatile PL/pgSQL SECURITY DEFINER postgres, search_path public,pg_temp, with owner/service EXECUTE only. It requires company_id and an accepted channel and rejects a missing company.external_tenant_reference. It UPSERTs the actual revision relation, incrementing revision on conflict and replacing revision_token/time; it inserts `contracts.publication.changed` into the already observed domain_events using the partial nonnull idempotency-key conflict target. When an event was inserted it selects active same-company webhook_subscriptions matching `*` or that event type and inserts actual webhook_deliveries, with subscription max_attempts/endpoint and the exact webhook idempotency-key conflict handling. **It does not insert event_outbox.** No further application helper call appears in this body.

The exact current revision trigger345166 invokes this helper for website and api for each reached price_plans, price_plan_versions, portfolios and portfolio_monthly_settlements row, using the real company_id and table.operation reason. All required INSERT effects remain enabled. Synthetic company external_tenant_reference must be present. Preserve real revision rows, domain events, and matching direct deliveries in setup baselines and rollback accounting; filtering only the later invoice event cannot be mistaken for total database silence. Conditional branches for unrelated table names in the generic trigger do not fire for these four roots and add no fixture relations. Domain events/subscriptions/deliveries and their exact trigger support were already observed/pinned; frontier 2 introduces no new sink relation beyond contract_publication_revisions.

## Portfolio policy assembly

Actual `gridex_portfolio_actor_has_permission(uuid,text,uuid,uuid)`138335 is STABLE SQL SECURITY DEFINER postgres, search_path public,auth,pg_temp, owner/authenticated/service EXECUTE. It first requires service-role context or actor=auth.uid(), then either the exact superadmin helper or a matching active, in-time, unrevoked company/optional-portfolio grant. The delegated branch excludes `portfolio_settlement.manage_access`.

Actual `gridex_portfolio_actor_is_superadmin(uuid)`138334 is STABLE SQL SECURITY DEFINER postgres with the same path but **owner/service EXECUTE only**. It requires a nonnull actor and service context or actor=auth.uid(), then checks active admin_users or active user_roles LEFT JOIN roles for the exact aliases `super_admin` and `platform_superadmin`. These three support relations are fully observed in wave 1 and pinned here; auth functions are retained as above. The permission helper's definer context can call its nested helper without granting authenticated direct EXECUTE. The grants table's PUBLIC SELECT policy directly invokes the restricted superadmin helper: retain this actual ACL/policy combination and do not turn metadata admission into an authenticated-positive claim. No permission assignment rows are required or observed. The application-helper and relation assembly closure for this policy graph is complete; the grants UUID default metadata is the remaining separate built-in/extension projection gap.

## Compatibility validator: exact carried edge

The dual portal FK remains mandatory: customer_invoices.partner_export_id references partner_exports(id), while company_id/partner_export_id also references invoice_export_items(company_id,id). A real compatible partner_exports row with the same alias UUID is still required. No NULL alias, dropped FK, disabled trigger or alternate reduced graph is admitted.

Actual `gridex_validate_partner_export_payload(text,jsonb)`38704 is volatile, non-strict PL/pgSQL **SECURITY INVOKER**, search_path public,auth,extensions. It has actual PUBLIC/owner/authenticated/service EXECUTE. Its ten unconditional calls are to **public.gridex_jsonb_text(p_payload,text[])** for customer.id/full_name/email/phone/personal_number/org_number, site.id, metering_point.id, contract.id and contract.status. That exact application function has no complete object projection in frontier 2, the earlier billing receipt or either retained RLS function wave. Missing-reference output does not list this string-bodied call, so an empty catalog missing list is insufficient.

The validator requires extracted customer id/name, at least email or phone, at least personal or organization number, and for billing_underlay/meter_values requires site/metering-point/contract ids. For those kinds it raises when extracted contract status is NOT IN signed/active/terminated. SQL NULL in that predicate does not itself evaluate TRUE; do not rewrite this as a stronger null-rejection rule. A provisional synthetic payload should provide explicit nonblank identifiers/name/contact/organization and explicit active/signed status, but **valid-payload admission remains BLOCKED** until the actual extractor body/ACL/strictness are known. No valid invocation was performed.

## Exact next finite metadata input

| Function signature | Reached reason | Required output |
| --- | --- | --- |
| public.gridex_jsonb_text(jsonb,text[]) | Ten unconditional actual validator calls, including mandatory compatibility-parent INSERT | Complete current function definition and identity/result, owner/language/security/config/strictness/volatility/parallel/leakproof, ACL/effective EXECUTE and extension membership; exact missing assertion plus same schema/name overload identities |
| extensions.gen_random_uuid() | Observed grants.id default, direct pg_depend OID16460; only its name/OID is present in prior receipts | Same full function projection, direct extension membership/version/schema and implementation identity. Do not replace with pg_catalog.gen_random_uuid(), assume ACL from schema usage, or handwrite a wrapper |

**No new relation root is requested.** The extension function is already named by retained default dependencies, but naming is not full observed owner/ACL/implementation metadata. Existing pgcrypto1.3 digest equivalence in the RLS fixture contract does not independently pin this function's current ACL. This gap can be closed with its exact projection; no extension installation or native execution is authorized by this assessment.

For these two signatures only: one read-only catalog SELECT using the exact prior full function projection, direct pg_depend edges (no recursive traversal), type/language/schema/owner identity and ACL metadata, explicit missing signatures and overload identities, extension membership/version/schema if applicable, role/schema access context. Retained role/default-ACL and ledger count/tail/version context may be repeated under the existing governance authorization. No classification rows are necessary without new relation roots. No customer/assignment rows, custom calls/RPC, ledger statement contents, DDL/DML, counter values, broad name discovery, or prior-object bulk freshness requery. If the actual extractor body reveals another application dependency, carry that exact next edge rather than predict closure. A built-in or extension body is reconstructed through the owned native substrate, never an invented SQL implementation.

## Preserved limits

The **absent current monthly-price sync trigger** remains the explicit earlier13:54 observation: price_plan_versions had only immutable-lock and catalog-revision custom triggers. Frontier 2 did not requery that relation. Do not reinstall historical sync registration, invoke the unused helper, or introduce portfolio_monthly_prices based on helper existence. The historical source-selection conflicts remain independently BLOCKED and are not cured by current metadata.

The future transaction migration identity remains `20260912052509_invoice_provider_event_atomic_apply.sql`. The contract/addendum's event fence → discovery → underlay shared mutex/row → optional run → item revalidation → portal order and state/numeric/credit/cancellation semantics are unchanged. This report neither implements nor verifies them. Full observed-fixture author admission remains BLOCKED on the two exact projections above; no fixture/SQL/native/production behavior, successful seed, test, or grant change is claimed.

Companion: `task-12-frontier2-delta-pins.json`, 30522 bytes, SHA256 `41583eb3111fcfdc17e2ef09b50e24363495c0e92bfca1e24c0810301159d731`.
