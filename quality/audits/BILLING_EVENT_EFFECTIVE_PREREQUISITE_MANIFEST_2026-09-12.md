# Task 12 effective observed prerequisite manifest

**Frozen read-only authoring input; same finite READY scope as the accepted final admission.** The manifest resolves seven admitted receipts into exact effective object selectors. It is data and installation constraints, not SQL, a fixture implementation or an executed acceptance result.

`task-12-effective-observed-prerequisite-manifest.json`: 763671 bytes, SHA256 `90a0bb834206e2928208fe859e9350e97d1d5208f59115bc63cdcad7aa9e52c4`.

The effective inventory is **39 full-shape relations, five existing empty composite-row-type inputs, and nine additional empty referenced-key relations**: 53 distinct relation identities. Eleven key-projection entries include two already counted empty rowtype relations. It selects 105 function objects, retains 44 other function objects as explicit native-core or out-of-profile evidence, preserves 175 outgoing FK definitions, and selects 90 exact custom trigger bindings. Required function definitions and large metadata objects remain in their raw receipts; manifest selectors include UTF-8 byte spans, slice hashes and separately labeled canonical hashes.

The manifest makes the selection decisions explicit:

- Each effective full object has one selected receipt. All 66 overlapping full-table/function comparisons are exactly equal; older copies remain comparison evidence. Full current partner/export/portfolio/price-plan definitions supersede earlier key-only inputs. Empty parent keys select an actual projection containing every required referenced index.
- Roles, memberships, schemas, owners, table/column/function ACLs and default ACLs are pinned. Functions retain exact definer/invoker and search-path metadata. Actual pgcrypto1.3 supplies both digest overloads and UUID implementation; PUBLIC/grant-option differences stay visible. Customer sequence configuration comes from its TEXT receipt, never the rounded older maximum; settlement audit uses its actual identity/text configuration. Neither imports counter state.
- The exact portal aliases are **partner_export_id, invoice_export_item_id, canonical_export_item_id**. The real partner_exports compatibility row has the item UUID and satisfies the old FK alongside the company/item FK. Real Partner emissions, publication-revision setup writes, direct deliveries, portfolio audit and underlay projection remain required effects. The absent monthly-price synchronization trigger is not installed.
- Six ordered installation phases and acyclic function dependency levels specify substrate/roles/sequences/default helpers, table/rowtype/key creation, remaining helpers/constraints/policies/ACLs/triggers, deterministic state construction and prerequisite gate. Temporary staged assembly is not permission to test incomplete constraints or skip missing function-body validation.

Two concrete conditional assembly gaps are especially important for the fresh author. A full company-onboarding helper installation would require the unobserved `ediel_production_state` rowtype. An admin_users write would require the unobserved `admin_users_audit_events` sink. The accepted profile uses the preconstructed-company boundary and an empty admin support relation; it does not execute those operations. All actual registrations remain named and pinned. Required actor scope guards remain selected and must never be disabled to manufacture permissions. The manifest similarly records exact unadmitted numbering, commercial/signed-contract, verified-site/address-change and empty-parent DML branches. Entering one is a new assembly blocker, not an excuse to install a stub or suppress a trigger.

The preconstructed company, empty legal fallback and empty commercial rowtype omissions are explicitly enumerated existing projection boundaries. They are not a reduced normal/Partner/portfolio path, not full managed-schema equality and not approval to omit any reached billing/setup sink. All ordinary declared setup INSERTs must use their selected real triggers and complete rollback accounting. If native assembly exposes a reached missing edge despite these boundaries, stop with that edge; READY input status does not convert an assembly error into an accepted test result.

Verification was limited to local receipt/selector/hash resolution, exact overlaps, all selected trigger-function and declared rowtype inputs, FK parent coverage and acyclic body-reference installation levels. No new catalog query, SQL/native execution, install, fixture/runtime/test code or prior artifact modification occurred. Historical admission remains BLOCKED; runtime binding, complete replay and native behavior remain unestablished. Observations retain their separate timestamps.
