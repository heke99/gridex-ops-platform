# Task 12 final observed-input admission and authoring brief

2026-09-12. **READY TO AUTHOR the finite current-observed fixture input for the unchanged full normal + Partner API + portfolio profile.** Frontier 3 closes both exact outstanding metadata edges. No additional current catalog function or relation input is now required for this declared graph. This status admits the observed object inputs for bounded implementation; it does not claim an assembled fixture, successful seed, migration install, native behavior, historical replay, production readiness or runtime-to-project binding.

**Historical source admission remains BLOCKED.** The separate observed path was selected by root and does not select a coherent historical migration sequence. All earlier reports/pins are retained unchanged; their contemporaneous BLOCKED conclusions are superseded only for the two subsequently observed input gaps, not rewritten.

## Final evidence and comparison

| Evidence | Identity |
| --- | --- |
| Actual frontier 3 receipt | quality/audits/BILLING_EVENT_CATALOG_FRONTIER3_2026-09-12.json; 7525 bytes; SHA256 63c73ececcfe8ec8f4b36a37518190a02de785807d45c2a439fa2b7252b7722a |
| Actual executed SQL, also task-12-frontier3-query.sql | 3042 bytes; SHA256 7282abc1294347b433e958646c76d1d7a23e7d1d7406b0c14df1f0125cd94518; embedded SQL hashes identically |
| Observation | 2026-09-12T14:17:26.32801+00:00; reported gridex-ops-dev / piidsfebjqjmnepdpnas; PostgreSQL 17.6 |
| Ledger metadata | count279/tail20260904222450; exact equality with frontier 2 |
| Prior frozen frontier 2 conclusion | task-12-frontier2-delta.md, SHA256 c716cb95ac61896f168a3d7d4ee36316c76f246a5a6dd6eae5965c4f957b2001 |

The full frontier 3 receipt, exact two function definitions/ACLs and all three direct dependency entries were read. Both exact named signatures resolve and missing_functions is empty. This query did not project same-name alternative overloads; the exact argument signatures resolve to the functions needed by the observed call/default, so no unresolved overload choice remains. It did not repeat role attributes, schema/default ACLs or retained relations/functions; there is no claim those were reobserved. Database/server version and shared session context fields exactly equal frontier 2: postgres current/session user and the retained search_path string. server_version_num was not projected here.

Inputs remain separately timestamped: earlier RLS waves09:41/09:58, billing13:54, frontier2 14:08, frontier3 14:17. Prior equality comparisons remain bounded to their stated object projections. They are not one simultaneous snapshot and provide no live target install gate. Ledger count/tail is the authorized governance-metadata exception; this receipt has no customer/permission-assignment rows, ledger statement contents, sequence counters or application invocation.

## Actual final helpers

| Function | Complete relevant metadata | Closure conclusion |
| --- | --- | --- |
| public.gridex_jsonb_text(jsonb,text[]) /38684 | Returns text; SQL IMMUTABLE, non-strict, SECURITY INVOKER, parallel unsafe, not leakproof; postgres owner; search_path public,auth,extensions. Exact body: `select jsonb_extract_path_text(payload, variadic path)`. ACL `{=X/postgres,postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}`; anon/authenticated/postgres/service effective EXECUTE true; no extension membership | Only a PostgreSQL core JSON extraction operation is called; no additional custom function/relation or effectful sink. It neither trims nor normalizes the extracted text. The exact extractor and validator bodies together admit an explicit nonnull textual payload shape for the mandatory compatibility parent |
| extensions.gen_random_uuid() /16460 | Returns uuid; C, volatile, non-strict, SECURITY INVOKER, PARALLEL SAFE, not leakproof, no function config; postgres owner. Actual implementation `$libdir/pgcrypto`, symbol `pg_random_uuid`; extension pgcrypto1.3. ACL `{=X/postgres,postgres=X*/postgres,dashboard_user=X/postgres}`; anon/authenticated/postgres/service effective EXECUTE true | Complete required default-function projection now available. Preserve PUBLIC execute, postgres grant option, dashboard_user grant and extensions schema access from retained metadata; use the real extension/native substrate, not a replacement wrapper or substitute default |

The three direct dependencies are extractor→public namespace2200 and UUID function→extensions namespace16392 plus pgcrypto extension16447. Extension schema usage and role attributes were observed/pinned previously; this observation adds the exact implementation/membership and function ACL. Core PostgreSQL functions/types and actual pgcrypto substrate are platform prerequisites, not an invitation to recursively audit all built-ins or copy C function DDL as a standalone substitute for an installed extension. Native assembly must verify substrate equivalence and preserve these ACLs.

For a synthetic billing_underlay compatibility payload, provide JSON objects with explicit textual customer.id/full_name/email/org_number, site.id, metering_point.id, contract.id/status (status active or signed), consistently naming the canonical synthetic graph. Additional phone/personal_number may be supplied but are not required when email/org_number are present. The actual extractor returns text at each path; the actual validator checks the alternatives and required paths described in frontier 2. This is static valid-shape admission, not a claim that an INSERT was executed. Retain the validator's actual NULL/NOT IN semantics and do not add a stronger payload policy during this concurrency fix.

## Concrete sole-author brief

1. **Freeze the finite observed prerequisite manifest before authoring.** Use the raw-object selectors/hash chains in task-12-source-admission-pins.json (observed entries only for this path), task-12-observed-catalog-delta-pins.json, task-12-frontier2-delta-pins.json, and this final pin file. Preserve complete observed table columns/defaults/checks/indexes/FKs, exact function bodies/owner/config/ACL, trigger registrations/enabled state, policy definitions and reached support. Source slices remain navigation/history evidence, not silently selected replacements for current observations. Do not apply the whole permission candidate or broaden grants to make assembly pass. Existing platform/company/customer/actor preconstructed-parent and FK boundaries remain as declared; this is not a full database clone.
2. **Assemble the entire declared normal + Partner + portfolio input.** Include the genuine compatible partner_exports parent for each portal alias UUID because both the old single-column partner FK and company/export-item FK exist. Preserve all three portal export aliases, company/customer/contract/underlay identity, current status constraints and parent prerequisites. Retain the actual payload validator/extractor, status/operational/audit/timestamp guards and their reached sinks. Use actual price_plans/version and portfolio/settlement/locked-pricing evidence; no fabricated minimal relation or disabled trigger.
3. **Preserve real setup effects and actual privilege contexts.** Inserting price_plans, versions, portfolios and settlements invokes publication revision website/api bumps and actual domain events plus matching direct webhook deliveries. Require synthetic external_tenant_reference and baseline these writes. Retain contract_publication_revisions and portfolio settlement audit/identity configuration with exact textual bigint configuration from prior receipts. Keep portfolio permission support relations/helpers/policies even though this event profile is not an authenticated permission-management positive. The restricted nested superadmin helper and owner-only Partner emitter do not need broader direct grants; existing definer contexts invoke them. A normal invoice and a true Partner customer with nonempty references and applicable subscription exercise distinct real effects. No outbound send is part of fixture setup/apply.
4. **Author the already designed transaction without redesign.** The genuine future migration is `20260912052509_invoice_provider_event_atomic_apply.sql`. Add the contract receipt columns/check and service-only `gridex_apply_invoice_provider_event_v1(uuid,uuid,uuid)`, using persisted identity/state and locked canonical financials. Fail installation for missing/incompatible prerequisite shapes or unexpected effectful dependencies; never create replacement billing tables or skip installation. Execute the complete new migration unchanged in the future owned fixture, not selected statements. Keep the existing claim semantics and every exact replay/token/lost-claim/legacy/review outcome from the contract.
5. **Use the integration addendum order.** Event row lock/fence → candidate discovery without item lock → shared company/underlay mutex and underlay row → optional pricing run → item lock/reload/exact identity and parent-binding revalidation → portal → actual trigger/domain/outbox/completion effects. If discovery changed, abort/retry the whole transaction without acquiring a replacement parent after the item. Dispatch omits only the event prefix. No lock spans provider HTTP, and no graph holder may subsequently acquire an event row. Preserve the actual Partner AFTER-trigger ordering before underlay projection; all effects roll back together.
6. **Keep the original state/numeric/credit/cancellation semantics.** The complete transaction contract is authoritative for state rank/alias resolver, JavaScript number/ToInt32 parity, amount tolerance/currency fallback, timestamps and finance normalization; this brief does not replace it. Preserve sent cancellation, disputed no-portal projection, immutable prices/GUID/evidence, equal-rank acceptance and stale-event no-op. Use explicit review for incomplete/mismatched identity and unknown stored/incoming states. Preserve portal identities/references/snapshots/metadata and verify reused domain/outbox identities rather than swallowing uniqueness errors. Record only the specified updated-item public-number traceability correction and explicit currency/period fallback decisions.
7. **Prove the full finite contract with the owned PG17 substrate after implementation.** Required cases remain paid/overdue both orders; same-token concurrency and exact receipt replay; reclaim/lost/null/wrong-company fences; response loss/retry/review-new-claim/legacy behavior; all rank pairs and aliases/numerics; identity/reference/conflict failures; failure injection after each phase with complete item/portal/underlay/domain/outbox/direct-delivery/completion rollback; normal/true Partner positives; complete portfolio/pricing evidence and negative guards; ACL/prerequisite/outbox insertion checks; neighbor/canary isolation. Combined dispatch/event lock barriers and candidate mapping change cases from the addendum are mandatory for coordinated lifecycle claims. Use observable waits and bounded timeouts, not sleep-based race evidence. The new complete migration, real helpers/triggers/ACLs and separately owned private execution/cleanup boundaries must be used. No such execution has occurred in this preparation.
8. **Runtime replacement remains separately reviewed implementation.** Replace processor financial/projection/domain writes with the persisted company/event/token RPC and typed result translation. Unknown commit outcome retries the same token to recover the stored receipt; ambiguous failures must not blindly mark failed/reapply. Any postcommit low-latency fanout cannot alter completed billing status. Preserve independent current implementation authorship; this preparation does not authorize another implementation author or claim current runtime changes already conform.

## Topology and acceptance limits carried forward

The earlier billing observation explicitly had **no monthly-price synchronization trigger** on price_plan_versions: its custom triggers were immutable-lock and catalog-revision only. Frontier 2/3 did not requery that table. Do not reinstall the historical sync registration or infer a reached portfolio_monthly_prices sink from an unused helper. This preserves the actual observed graph and does not reduce the portfolio profile.

The publication helper writes revision/domain/direct-delivery sinks, not event_outbox. Provider apply must still write each contract-required webhook_fanout_v1 outbox job with attempts0/max_attempts12 and verify persistence despite triggers, while preserving the actual outbox indexes/defaults. Partner invoice triggers can additionally write distinct domain/direct deliveries; all are included in failure snapshots. Portfolio settlement audit effects and real underlay invoice projection remain in scope.

**Finite next catalog frontier: empty.** A future prerequisite fingerprint mismatch or unexpected reached helper discovered during exact fixture assembly is a new concrete blocker, not permission to suppress it. Native substrate/fixture/install/concurrency/runtime gates, historical admission, complete migration replay, target runtime binding and production correctness remain unestablished. Arbitrary direct invoice/status writers remain outside the coordinated transaction guarantee. The input-ready finding applies only to this separately labeled finite observed profile; it is not Task12 completion.

Data-only final pins: `task-12-observed-input-admission-pins.json`, 4523 bytes, SHA256 `cce30617942a0f86de7afbbc3141c11d2ceea0611806615958aaba0fc9d56e45`.
