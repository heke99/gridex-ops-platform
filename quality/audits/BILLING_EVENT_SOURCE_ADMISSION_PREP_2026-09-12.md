# Task 12 — finite invoice-provider-event source admission preparation

Date: 2026-09-12. **BLOCKED for fixture author admission.** The event transaction and finite native cases are already designed; this report changes neither. It resolves source locations and existing observed metadata, identifies the remaining reached edges, and proposes one finite read-only catalog collection. No migration, fixture, application, test, workflow, plan, root memory, index or Git state was changed. No SQL, PostgreSQL/native harness, database, provider, network or external service was executed. Only this report and its data-only companion were written.

Skill routing: read AGENTS.md, active memory/checkpoint and database/tenant domain memory; searched decisions and known failures; inspected available local skills. Applied Supabase source/ACL discipline, Postgres prerequisite/locking guidance and verification-before-completion. using-superpowers explicitly exempts dispatched subagents. Full baseline audit, implementation/TDD, UI/performance, live diagnostics, branch delivery, planning changes and delegation are outside this bounded source-only assignment; no such workflow was started. Parent instructions explicitly prohibit memory/Git mutations here.

Published source base: `4d4fe075198ad61b9df85aa456e40965668549dc`. Future genuine CLI identity: **`20260912052509_invoice_provider_event_atomic_apply.sql`**, from the retained scaffold receipt. Neither permission07 nor billing08 is this migration. The companion pins the receipt and requirement bytes. The implementation author remains the parent's sole designated author; this preparation is not a second implementation.

## Declared finite profile and decision

`BILL03_EVENT_APPLY_NORMAL_PARTNER_PORTFOLIO_SOURCE_PREP_V1` covers the contract's claim/apply paths with two companies, independent ordinary/Partner API customers, one ordinary invoice and one portfolio invoice with complete synthetic locked evidence, associated runs/items/underlays and real portal triggers. Include matching active wildcard and specific-event subscriptions for the Partner positive control, inactive/nonmatching subscriptions, same-company neighbor and foreign-company/environment targets, plus the separate canary database. Graph parents are deterministic fixture state, not accepted pricing, settlement or customer-onboarding commands.

The apply statements touch provider-event claim/completion, item provider/reconciliation fields and optional status, the same portal graph, private/public domain events and non-null `webhook_fanout_v1` outbox destinations. The real portal projection updates **underlay invoice_readiness_status, invoice_export_run_id, invoice_export_locked_at and updated_at**, not underlay status/readiness_status. It reaches underlay tenant/company/update-time/audit triggers, and the resulting audit insert reaches audit normalization. Partner portal writes additionally reach the private emitter, domain_events and webhook_deliveries. These are required sinks, including during rollback checks.

Parent rows remain read-only during apply. Setup is separately accounted state construction: no accepted customer/contract onboarding, no pricing/settlement RPC, no auto-numbering and no commercial quote graph. Supply explicit parent references/numbers; no maintenance/skip GUC or trigger disablement. If normal setup INSERTs reach an effectful support trigger, retain its full body, registrations, ACL and sink instead of suppressing it. In particular settlement INSERT reaches portfolio_settlement_audit_log. A price-plan-version seed with snapshot_json lacking a portfolio_monthly_prices array takes the real synchronizer's early return; monthly-price materialization is outside this declared profile, not replaced with a stub. Any later choice supplying that array adds a reached dependency and must stop/reopen admission.

| Admission | Status | Reason |
|---|---|---|
| Contract, migration identity, existing runtime semantics | Pinned | Full contract read, including integration addendum; runtime/resolver source checked |
| Name-directed historical definition navigation | Prepared, not selected | 537 byte-slice pins over 118 unchanged base source files; mixed/conditional blocks and predecessor authority remain explicit |
| Reusable observed metadata | Pinned, partial | Existing six catalog receipts supply exact portal/underlay/audit/domain/delivery metadata and much parent support |
| Complete historical fixture | BLOCKED | No reconciled effect/order/ACL authority for the full reached graph; source conflicts below cannot be erased by selecting latest text |
| Complete observed-state fixture | BLOCKED | Core export/event/outbox/pricing and Partner-emitter metadata missing; compatibility/portfolio support gaps below |
| Task 12 implementation/native acceptance | Not performed | Source preparation is neither author acceptance nor runtime proof |

## Pin format and authority boundaries

Companion: `task-12-source-admission-pins.json`; SHA-256 `7a948c727a68f970c4173c650e45203d6dc2a8a3af9ac119b653213396dffe39`; 537 source slices, 118 parent files. Every parent file hash was compared to its exact published-base Git blob with read-only `git show`; all matched. Every slice uses zero-based UTF-8 byte offsets, start inclusive/end exclusive, and SHA-256. Leading comments/whitespace may belong to a statement slice; line numbers are navigation only. S0537 is an entire small bootstrap source. The source inventory is a bounded name-directed search, not a replay manifest: relation references also find incoming definitions; statement heads are navigation and may be truncated. No statement is approved for execution merely because it appears in this JSON. Dynamic/conditional blocks retain their whole parent span rather than inventing extracted effects.

Observed entries use exact raw-file hash, JSON pointer, raw byte span/hash, object name/OID and an independently labeled canonical JSON hash. The selected table object includes **all** observed columns/defaults/generated expressions, constraints, indexes, trigger registrations/internal FK evidence, rules, policies, ownership, RLS/FORCE, schema/table/column ACL and effective access. Function objects include full definition, identity arguments/result, owner, language, security context, config/search_path, volatility, strictness, extension and ACL/effective EXECUTE. Raw receipt hashes, not local OIDs alone, identify observations. The six observations are from different times on the same reported development project/ledger; they are not simultaneous and runtime binding remains UNPROVED.

Keep these authorities separate: (a) latest literal repository definition or subsequent amendment; (b) historical selected preconditions/effects; (c) separately observed development metadata; (d) future installed native fixture; (e) real runtime target gate. The observed RLS fixture's READY status excluded export, portfolio and Partner branches and does not transfer here. Never import the whole permission candidate or use its grants/rewrites to close these gaps.

## Resolved source definition anchors

The IDs below dereference exact parent SHA-256, byte spans and slice SHA-256 in the companion. All table columns/checks/defaults in the cited CREATE/ALTER statements remain in those pinned bytes; this table is a reading order, not rewritten DDL.

| Required object/effect | Exact source pins and latest known shape |
|---|---|
| invoice_provider_events base/claim | S0136 original DDL; July12 S0239–S0247 claim columns/status/attempt checks/claim index and full claim function; S0248/S0249 revoke/grant; S0252 company/item FK. Verify IDs by companion target/function selector, not ranges alone |
| Event idempotency/environment | S0524/S0276 July12 drop/create partial company/provider/environment/hash index; S0533/S0404 July28 drop/create same-named nonpartial index. S0279 sets environment NOT NULL; S0402/S0403 later replace CHECK with nullable-compatible expression but do not drop NOT NULL |
| invoice_export_items/runs | S0130/S0132 full original run/item DDL. July2 retry block expands item status; July12 adds reconciliation/provider request/provider idempotency/provider invoice identities and company FKs; July27 S0370/S0371 adds canonical contract/meter/period/quantity/currency/bridge and run idempotency; S0397 adds run payload_hash |
| Item invariants | S0217 complete sent/credited guard; S0218 conditional registration; July9 advisor search_path amendment is separately pinned. S0495 locked-pricing guard and S0496–S0500 revokes/registration; S0385 chain helper and S0389/S0390 item binding |
| Portal original/latest identity | S0006 original DDL, S0066 repeated foundation. S0374 unique (company_id,invoice_export_item_id) WHERE invoice_export_item_id IS NOT NULL. S0384 company/item FK. August5 S0534/S0535 drop former reference indexes and S0453 conditional named UNIQUE(company_id,partner_invoice_reference). S0463–S0468 public-reference column, NOT NULL, company/reference unique index and full helper/registration |
| domain_events | S0091 and S0148 repeated full DDL; global partial idempotency key S0092/S0149. Retain dotted event-type check, version/defaults, company/customer/auth parents; August1 tenant-key/check/FK blocks S0411–S0413 require selected effect accounting |
| event_outbox | S0097 and S0154 have same source table shape. May31 defines both expression uniqueness S0100 and non-null named uniqueness S0101; June9 repeated foundation supplies only the latter S0157 and does not DROP the expression index. Choose actual admitted shape, never infer absence from the second foundation |
| Outbox destination/ACL | S0436 actual webhook_fanout_v1 due index; S0437/S0438 revoke anon/authenticated and grant service. S0491 full retirement helper, S0492/S0493 registration, S0494 revoke. Only internal/customer-onboarding-orchestrator is suppressed; webhook/fanout must survive and return a durable row |
| Portfolio portal evidence | July18 S0358 initial body and S0360 registration; **latest full body S0433**, August3, includes fixed_share_percent/fixed_price/fixed_energy fields. Preserve registration UPDATE OF list and body separately: a later body does not silently expand registration |
| Partner effect | S0486 full private invoice trigger, S0487/S0488 exact AFTER registration; **S0484 full private emitter**, S0485 revoke. Emits resource event, then direct deliveries with subscription company/status/event-array filtering and unique idempotency; no HTTP |
| Underlay projection | **S0506 latest full helper**, S0507/S0508 revoke/grant, S0509/S0510 registration. Preserve exact same-company/customer match and 23514 missing-underlay behavior |
| Reached underlay/audit helpers | S0310 energy normalizer; S0303 exact-ref guard; S0366 immutable configuration helper; S0440 price-area helper. S0393 full audit normalizer. S0522 latest literal critical-audit body is embedded in S0058 conditional parent block, registration S0059. Latest explicit same-company helper/wrapper source S0511/S0512 is September5, later than observed ledger; never relabel it current observation |
| Timestamp helper | S0537 `supabase/bootstrap/20260522_set_updated_at_timestamp_foundation.sql` is **derived from August7 pg_get_functiondef**, not original historical source; it matches separately observed helper 20523's shape. Historical pre-transform provenance remains distinct |
| Parent seed effects | S0523 locked-pricing guard; S0527 energy inheritance; S0525 locked-price-version guard; S0528/S0529 price-version monthly synchronizer/trigger helper; S0530/S0531 settlement immutability/audit; S0532 audit append-only helper. Exact bodies are preserved; their setup sinks/ACL remain admission work |
| Common mutex evidence | S0536 full existing pricing-persist body slice: pg_advisory_xact_lock(hashtextextended(company::text || ':' || underlay::text,0)), then same-company underlay FOR UPDATE. This is lock-source evidence only, not permission to install/replay pricing persistence |

The companion also pins contract enumerated migrations not individually repeated here, all matching structural statements for the finite roots, explicit drop-index statements, exact source-helper replacements, observed receipts, runtime processor/status mapper/domain writer, and platform bootstrap. Large mixed historical files were searched/inspected by exact relevant object or block; no full-file historical audit is claimed.

## Existing observed objects and reached closure

All rows below are exact separately observed development metadata; all six table ACLs are explicit owner/authenticated/service_role `arwdDxtm`, no explicit anon entry, owner postgres, RLS enabled/FORCE disabled. This does not mean authenticated can perform every command: policies remain attached and distinct from ACL. No explicit per-column ACL is present in these objects.

| Wave | Relation/OID | Columns / constraints / indexes / custom triggers |
|---|---|---|
| 1 | customer_invoices / 39121 | 60 / 25 / 24 / 5 |
| 1 | billing_underlays / 33648 | 63 / 31 / 29 / 9 |
| 1 | audit_logs / 17607 | 19 / 4 / 10 / 1 |
| 1 | webhook_deliveries / 47597 | 33 / 8 / 12 / 0 |
| 2 | domain_events / 47493 | 13 / 8 / 11 / 0 |
| 2 | webhook_subscriptions / 47560 | 29 / 10 / 12 / 0 |

Required portal custom registrations, all observed enabled O: customer_invoices_customer_chain_v1 → 179613; customer_invoices_portfolio_evidence_guard → 138463; customer_invoices_public_reference_trg → 341232; customer_invoices_partner_api_events_v2 → private 344931; customer_invoices_underlay_projection_v1 → 348033. Both applicable AFTER triggers are retained in their actual name order (Partner before projection). No custom delivery/domain/subscription trigger was observed; their full constraints still apply.

For the ordinary projection's underlay UPDATE, preserve operational guard 41311, company wrapper 67762 → assert_same_company 67738, timestamp 20523 and critical audit 42266 → audit_logs normalization 192996. Other observed underlay registrations remain in the fixture: chain179613, energy137339, exact-ref137289, immutable-configuration161091 and price-area337850; their UPDATE OF conditions are not artificially broadened or disabled. The projection does not name status, contract, pricing_snapshot, energy or configuration fields, so these conditional guards do not become proof of unrelated business invariants. Audit sink contents are part of transaction rollback/replay snapshots.

Reference helper341232 reaches extensions.digest(bytea,text), observed wave2 OID16449; digest(text,text)16448 is also pinned. Preserve pgcrypto implementation/ACL and schema usage. The Partner trigger344931 reaches **private.gridex_emit_partner_resource_event_v2(uuid,uuid,text,text,text,jsonb,text), absent from existing function receipts**. Source body S0484 reaches companies.external_tenant_reference, customers.customer_reference/customer_number, domain_events, extensions.digest, subscriptions and deliveries; all but emitter current metadata are already represented for the declared path. Nonempty tenant/customer references are required to exercise this branch; a false branch is not a Partner positive.

The existing receipts also provide full actor/company/customer/contract/site/meter/snapshot shapes and external referenced-key projections. Reuse the exact selectors, including integration_api_clients parent-key receipt. Empty/key-only projections are explicitly limited metadata, not full current tables. No blanket grant, handmade auth helper or permission-deny candidate is admitted.

## Exact remaining blockers

1. **Primary observed roots missing:** invoice_provider_events, invoice_export_items, invoice_export_runs and event_outbox have no full table metadata in the six receipts. Their actual columns/nullability, checks, conflict-index inference shapes, tenant FKs, trigger/rule registrations and ACL cannot be inferred from generated types or neighboring FK projections. pricing_runs is also only parent-key support, not a full shape/body/ACL observation. This blocks both ordinary and Partner/portfolio apply fixture admission.
2. **Partner emitter missing:** exact source body/revoke is available, but current function signature/owner/config/ACL/body and actual dependency edges are not in existing observations. Do not silently combine historical emitter bytes with current portal metadata and call it current-catalog parity.
3. **Actual compatibility parent:** observed customer_invoices.partner_export_id has BOTH customer_invoices_partner_export_id_fkey → partner_exports(id) and customer_invoices_company_export_item_fkey → invoice_export_items(company_id,id). Runtime writes the export-item ID into all three aliases. A positive observed-state fixture therefore needs an actual compatible partner_exports row with the same UUID, or an explicitly reviewed authority correction; it cannot drop the former FK, omit the parent or make the alias NULL. Source S0006/S0275 and observed 39121 make this a concrete assembly edge, not an executed failure claim. Full partner_exports current shape is missing.
4. **Portfolio parent/setup closure:** portfolios, portfolio_monthly_settlements and price_plan_versions are reached by non-null FKs; existing key projections do not establish full seed shape/checks/triggers/ACL. Actual settlement INSERT reaches gridex_audit_portfolio_settlement_write → portfolio_settlement_audit_log, whose metadata is missing. Include that sink. Price-version synchronizer body/ACL must be retained even for the declared no-array early return; an array would additionally reach portfolio_monthly_prices and is outside this profile. Direct pricing/settlement calculation, line/evidence writes and ledgers are not needed for this event-only seed-state profile.
5. **Historical environment nullability conflict:** July12 S0279 requires environment NOT NULL; July28 S0403 allows NULL in its CHECK but contains no DROP NOT NULL. The requirement's historical missing-identity/native cases cannot be assembled by assuming July28 removed the column constraint. A selected historical baseline must account for the actual predecessor/effects. A fresh observed column definition can characterize current behavior only. Missing company/item/GUID cases remain distinct and must not be substituted for an unavailable NULL-environment case while claiming all required cases.
6. **Historical generic effects and derived helper authority:** conditional trigger/ACL loops (including S0058/S0059 audit, S0061 search paths, August1 S0411–S0413 and later policy normalization) depend on predecessor catalog/selection. Latest September5 wrapper source is not the current receipt's older helper. S0537 timestamp bootstrap is explicitly observed-derived. May31/June9 outbox index coexistence and the portal dual FK must be resolved in chosen historical source composition. These cannot be repaired by a broad public-schema grant, using current ACL as historical defaults, choosing the oldest DDL, or selecting the numerically latest function without amendments/preconditions.
7. **Coordinated lifecycle remains separate:** source addendum requires a common underlay mutex/row order, but Task15's persist/lock/reserve/finalize changes are not implemented here. The combined native event/dispatch gate requires the actual coordinated implementation, not a mock dispatch SQL writer or a claim that provider-event serialization covers arbitrary existing writers.

These are material admission blockers. This report intentionally does not declare READY by reducing the Partner/portfolio/compatibility profile or removing reached sinks.

## One finite next read-only catalog allowlist

No query was executed. If the parent proceeds with an observed-state fixture, collect one coherent system-catalog-only SELECT under the existing read-only process, limited to these **10 public relations**: invoice_provider_events, invoice_export_items, invoice_export_runs, event_outbox, pricing_runs, partner_exports, portfolios, portfolio_monthly_settlements, price_plan_versions, portfolio_settlement_audit_log.

For each, return absence assertions plus complete columns/types/defaults/generated expressions, all constraints with exact definitions/validation/deferral/parent keys, indexes including expressions/predicates/validity, custom and internal trigger registrations/enabled state, rules, owner/RLS/FORCE, schema/table/column ACL and effective owner/anon/authenticated/service access. Return every **direct FK parent** as exact referenced key/type/index metadata only; do not recursively collect every incoming child. Return trigger-bound function definitions/metadata for these ten relations and the following **13 explicit signatures** (fail on missing names, do not call them):

- public.gridex_claim_invoice_provider_events(uuid,text[],integer,uuid,integer)
- public.gridex_protect_sent_invoice_export_items()
- private.gridex_require_locked_pricing_run_for_invoice_export()
- public.gridex_suppress_retired_onboarding_outbox_v1()
- private.gridex_emit_partner_resource_event_v2(uuid,uuid,text,text,text,jsonb,text)
- public.gridex_protect_locked_pricing_runs()
- public.gridex_inherit_pricing_run_energy_flow()
- public.gridex_reject_locked_price_version_mutation()
- public.gridex_sync_portfolio_monthly_prices_from_version_trigger()
- public.gridex_sync_portfolio_monthly_prices_for_version(uuid)
- public.gridex_guard_portfolio_settlement_immutability()
- public.gridex_audit_portfolio_settlement_write()
- public.gridex_portfolio_audit_immutable()

For a coherent reuse check, the same collection may additionally return **fingerprints or the identical metadata projection only** for these 13 retained relations: public.customer_invoices, public.billing_underlays, public.audit_logs, public.webhook_deliveries, public.domain_events, public.webhook_subscriptions, public.companies, public.customers, public.customer_contracts, public.customer_sites, public.metering_points, public.contract_price_snapshots, auth.users. The corresponding 17 retained helper identities are private.gridex_partner_invoice_event_v2(); public.ensure_customer_invoice_reference_v1(); public.gridex_assert_company_operational_for_write(); public.gridex_audit_critical_row_change(); public.gridex_billing_underlays_company_guard(); public.gridex_enforce_billing_underlay_price_area(); public.gridex_enforce_customer_contract_chain_v1(); public.gridex_guard_billing_underlay_exact_refs(); public.gridex_guard_portfolio_invoice_evidence(); public.gridex_normalize_audit_context_v1(); public.gridex_normalize_billing_energy_flow(); public.gridex_reject_billing_configuration_snapshot_mutation(); public.gridex_sync_underlay_invoice_projection_v1(); public.set_updated_at_timestamp(); public.gridex_assert_same_company(uuid,uuid,text,text); extensions.digest(text,text); extensions.digest(bytea,text). Compare to exact retained selectors before mixing observations. This is a finite freshness check of reached inputs, not the whole former RLS profile.

Required catalog field families are pg_class (OID/name/namespace/kind/owner/relrowsecurity/relforcerowsecurity/relacl/options), pg_namespace (owner/ACL), pg_attribute (attnum/name/type/typmod/collation/not-null/identity/generated/default via pg_attrdef/column ACL), pg_constraint (name/type/columns/parent/action/validation/deferral plus pg_get_constraintdef), pg_index and index pg_class (key/include order, unique/primary/valid/ready/live, expressions/predicate and pg_get_indexdef), pg_trigger (name/internal/enabled/event/update columns/function/constraint and pg_get_triggerdef), pg_rewrite rules, pg_policy (command/roles/permissive/using/check), pg_proc (identity arguments/result/language/owner/security-definer/config/strict/volatility/parallel/leakproof/ACL plus pg_get_functiondef and extension membership), role attributes/memberships/default ACLs and effective privileges for the explicitly named roles. Preserve raw object identity and independently labeled canonical hashes; catalog OIDs, counters and any bigint configuration must be serialized as **text** where exactness matters. If a reached default/type directly depends on a sequence, return pg_sequence configuration start/increment/min/max/cache/cycle and ownership/dependency as text; **never read last_value/is_called/sequence current values**. No sequence is added to the business-data allowlist.

Include direct pg_depend edges for returned root constraints/defaults/indexes/rules/triggers/functions and direct function-call references, preserving text for PL/pgSQL/dynamic references absent from pg_depend. Return role/schema/default-ACL context and timestamp/project/server/ledger identity needed to compare the retained receipts; these are metadata only. No business table rows, sequence values, custom RPC calls, provider data, broad recursive catalog dump or mutations. Explicitly report unexpected effectful triggers/helpers/reached parents for another bounded disposition; do not auto-expand to the entire public schema. Before composing old/new observations, verify retained object fingerprints have not changed or refresh only the changed allowlisted dependencies. This would advance **observed** admission, not historical selection or production/runtime binding.

## Unchanged implementation invariants and handoff

The integration addendum supersedes the earlier abbreviated event→item→portal wording: **event fence → unlocked candidate discovery → common company/underlay advisory mutex and underlay row → optional pricing-run row → item lock/reload/revalidation → portal → actual trigger/sink effects**. Dispatch has no event prefix. If discovery changes, abort/retry the whole transaction; never take the replacement parent after locking the item. Existing event claim uses SKIP LOCKED and a 15-minute stale processing rule; a null processing_started_at is not automatically reclaimed. Preserve same-token exact stored replay, replacement-token fencing and event-only lost-claim/review outcomes.

State ranks and resolver precedence remain exactly the contract: unknown -1; registered0, unpaid1, partially_paid2, overdue3, reminder_sent4, collection5, paid10, disputed11, cancelled12, credited13. Event-type alias first, then payload invoice_status/invoiceStatus/status; numeric bits test 1,4,8,16,64,2 with JavaScript ToInt32 semantics (65 is paid). Equal-rank distinct events remain accepted; lower-rank events have no financial/domain/outbox effects. Disputed leaves portal unchanged. Cancelled keeps an already-sent export status sent. Credited does not create a credit note or rewrite invoice totals. Amount tolerance remains >0.01, amount aliases use first successfully parsed value, optional currency is case-insensitive with existing SEK fallback. Preserve replay paid_at, identity/financial/portfolio snapshots, normalization and invalid timestamp handling. The updated invoice number in public payload is the separately declared narrow correction; currency and metadata.billing_month fallback decisions remain explicit. No resolver redesign occurred here.

Next concrete step: resolve the above finite observed input request **or** complete the exact historical source-effect admission; choose/label the resulting fixture authority, then independently review its extraction/trigger/ACL/sink manifest before the sole implementer authors the unchanged full forward migration and native harness. All contract native scenarios, including combined event/dispatch locks, rollback after each real sink, complete portfolio evidence and exact ACL/prerequisite rejection, remain required and unexecuted.

Verification for this preparation: parent source/base equality, source slice bounds/hashes, observed raw object selectors/spans/hashes and companion/report syntax were checked using local read-only file parsing/Git object reads. Only the two allowed output paths were written by this preparer; the shared worktree's existing application/memory changes belong to other active work. No native or SQL behavior is claimed.
