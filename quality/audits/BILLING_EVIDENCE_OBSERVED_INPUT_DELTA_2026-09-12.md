# Task 15 observed-input delta

**BLOCKED for Task15a and Task15b.** Task12's accepted finite observed-input manifest supplies substantial shared billing/portal/Partner/portfolio support, but does not admit Task15's economic-source/child/ledger/actual-RPC or dispatch attempt/legacy boundaries. The exact next frontier is **17 new relation definitions and eleven named functions for 15a (some shared with 15b), plus four relations and two functions for the full 15b/runtime path**. These are metadata inputs, not a query or implementation. Actual direct trigger/FK/policy/default dependencies discovered with those definitions must then be assessed; the initial list is not transitive closure.

The full Task15 brief and native evidence contract, including canonical evidence checks, all24 native scenario families and integration addendum, were read. Target source bodies and actual runtime call sites were used only to identify reached names and preserve semantics. No database/network/query, SQL/runtime/fixture/test authoring, install, or prior-artifact mutation occurred.

## Authority and reusable input

Reuse `quality/audits/BILLING_EVENT_EFFECTIVE_PREREQUISITE_MANIFEST_2026-09-12.json` unchanged: **763671 bytes, SHA256 90a0bb834206e2928208fe859e9350e97d1d5208f59115bc63cdcad7aa9e52c4**. Its seven receipt timestamps,53 relation identities,105 selected functions, exact ACL/schema/role/pgcrypto/sequence inputs and explicit parent/type boundaries remain separately labeled. Reuse is not a freshness requery or a simultaneous snapshot.

The historical/source path is independently **BLOCKED**: Task15 expressly requires complete source definitions and amendments, including core rename, July28 partner-reference patch/service revoke and latest September1 writer. The current-observed Task12 path does not discharge those source-history requirements. A future separately selected observed Task15 fixture can use actual metadata, but cannot label it canonical/full historical replay. Runtime binding remains UNPROVED. No native/fixture/install acceptance follows from either source inspection or this report.

Shared complete objects already admitted include underlays, pricing runs, canonical export runs/items/provider events, portal, snapshots/contracts/customer/site/meter/company/auth parents; domain/outbox/webhook sinks; real compatibility partner_exports; price plans/versions, portfolios/settlements and settlement audit; revision sinks and policy support. Their exact helper/trigger/ACL projections remain authoritative for observed-state reuse. The Task12 empty-key/company/actor/commercial-type boundaries remain limitations: Task15 cannot promote a previously empty parent to real business DML without its complete reached inputs.

One additional **already observed** full relation can be promoted without a query: `public.customer_invoice_lines`, wave1 `/catalog/tables/7`, OID39153. It has no custom triggers; its six FKs reference customer_invoices, contract_price_snapshots, price_plan_versions and itself, all now full observed inputs. Retain its whole column/default/check/index/RLS/ACL/policy object and actual FKs, not a made-up invoice-line shape. It was excluded from Task12's effective profile, so the companion explicitly pins this promotion. Other missing names below have no full table/function object in any of the seven admitted receipts.

## Exact additional relation frontier

| Relation | Phase | Reached reason |
| --- | --- | --- |
| public.billing_underlay_items | 15a | Complete interval item set; regeneration DELETE/reinsert; economic child INSERT/UPDATE/DELETE freeze and actual lineage trigger. |
| public.billing_underlay_events | 15a | Actual underlay writer inserts underlay_generated event; rollback sink. |
| public.metering_values | 15a | Raw current/replaced revision identity and actual ingestion return type/write graph. |
| public.normalized_metering_values | 15a | Required lineage/current eligibility/quantity source and replacement transitions; gate rowtype. |
| public.metering_value_sources | 15a | Actual idempotent and replacement ingestion provenance INSERT with tenant/FK guard. |
| public.ediel_messages | 15a | Required source-message identity and actual provenance FK/tenant guard parent; complete valid source setup. |
| public.customer_supply_periods | 15a | Exact supply-period/customer/meter/contract eligibility and segment evidence, not nullable dummy identity. |
| public.pricing_preview_lines | 15a | Persist INSERT and monetary sum/one-time-ledger inputs; direct sealed economic child mutations. |
| public.pricing_interval_evidence | 15a | Persist INSERT, full exact interval/selected-price evidence and child freeze; actual item RESTRICT FK must be observed. |
| public.contract_charge_ledger | 15a | First lock inserts once/event component charges; uniqueness/replay/rollback ledger. |
| public.price_period_locks | 15a+15b | First pricing lock upsert and compatibility period gating; dispatch month completion also writes it. |
| public.billing_period_locks | 15a+15b | Closed/reopened-period replay and first reservation gating; dispatch completion upsert. |
| public.billing_export_runs | 15a+15b | Actual compatibility writer rowtype/INSERT, wrapper existing-run/hash coherence and finish projection. |
| public.billing_export_run_items | 15a+15b | Compatibility writer ready/blocked INSERT and exact count, energy inheritance, finish sent/failure projection. |
| public.spot_price_intervals | 15a | Exact selected interval source ID/value/area/time/resolution under source consistency boundary; source value swap tests. |
| public.company_market_price_sources | 15a | Applicable source priority/resolution/area/portfolio policy used for real selected price evidence. |
| public.spot_price_monthly_summaries | 15a | Locked monthly variable-price settlement control and immutable selected source record; no quote-preview substitution. |
| public.invoice_export_attempts | 15b | Actual attempt result/request-hash/token-adjacent evidence sink must finalize atomically. |
| public.invoice_dead_letters | 15b | Existing terminal failure branch writes dead-letter evidence; cannot silently omit failure sink. |
| public.billing_provider_connections | 15b_runtime_adjacent | Current Capway configuration/environment/allowed status boundary; metadata only, no settings/secret_reference values. |
| public.automation_locks | 15b_runtime_adjacent | Actual surrounding acquire/release lock RPC sink; preserve lock expiration/reconciliation distinction, not evidence seal. |

The two rows marked15b_runtime_adjacent belong to the actual surrounding TS flow. They do not become calls inside the proposed native reserve/finish RPC merely because they are listed. Their exact inputs remain necessary for unchanged end-to-end runtime wiring and governance/provider-configuration controls. No credential/configuration values are requested. The attempt/dead-letter and legacy projection rows are actual financial dispatch outcome sinks, not optional logging that can be silently discarded.

For the price path, `spot_price_intervals`, the enabled company source policy and locked monthly summaries cover the contract's hourly/quarterly/monthly selected-evidence controls. Quote-preview `market_price_previews`, indicative daily fallback and `portfolio_price_estimates` are distinct source branches, not accepted substitutes for locked settlement evidence. This contract verifies authoritative frozen components/snapshot plus interval math; it does not reimplement the entire tariff/configuration-authoring engine. If the author expands execution to live component/rule/product/quote authoring, that newly reached branch must receive separate exact admission rather than using Task12's empty commercial rowtypes. The full portfolio locked-evidence control remains mandatory and uses the already observed plan/version/settlement graph.

## Exact additional named-function frontier

| Signature | Phase | Reached reason |
| --- | --- | --- |
| public.gridex_persist_pricing_run(uuid,uuid,jsonb,jsonb) | 15a | Success/failure direct RPC; child persist/supersession/source-readiness behavior and search_path/ACL unknown currently. |
| public.gridex_lock_pricing_run(uuid,uuid,uuid) | 15a | First lock ledger/period/seal integration and already-locked replay; actual body/ACL must be observed. |
| public.gridex_store_billing_underlay_batch(uuid,jsonb,uuid) | 15a | Batch mutex and per-command writer transaction; sorted shared-parent discipline. |
| public.gridex_store_billing_underlay(uuid,jsonb,jsonb,uuid) | 15a | Latest source September1 full writer supersedes prior patches; regeneration/item/event effects. |
| public.gridex_billing_underlay_item_gate_guard() | 15a | Existing source lineage guard must remain real; direct current registration/body/ACL needed. |
| public.gridex_create_invoice_export_graph_v1_core(jsonb,jsonb,jsonb) | 15a+15b | Actual amended owner-internal core, portal-before-legacy compatibility ordering and service revoke. |
| public.gridex_create_invoice_export_graph_v1(jsonb,jsonb,jsonb) | 15a+15b | Actual public wrapper/hash/idempotency/core/legacy/domain/outbox whole graph. |
| public.gridex_create_billing_export_run(jsonb,jsonb) | 15a+15b | Direct compatibility ready-item entrypoint plus blocked diagnostic behavior; actual grants retained. |
| public.gridex_unlock_pricing_runs_for_month(uuid,text,uuid,text) | 15a | Existing maintenance unlock writes run economics/history; cannot bypass reserved/sent seal. |
| public.gridex_ingest_metering_value_atomic(jsonb) | 15a | Actual current/replaced revision transitions and idempotent provenance; legitimate later ingestion remains allowed. |
| public.gridex_guard_metering_value_source_tenant_v1() | 15a | Actual provenance INSERT/UPDATE guard reaches raw/normalized/message identities. |
| public.gridex_acquire_automation_lock(text,uuid,uuid,integer,jsonb) | 15b_runtime_adjacent | Current app wrapper exact lock TTL/token scope; source performs global expired-lock cleanup. |
| public.gridex_release_automation_lock(text,uuid) | 15b_runtime_adjacent | Current app wrapper exact token-matched release; finally path not silently replaced. |

For each missing relation, the needed input is its complete current shape, owner/RLS/ACL/policies/indexes/checks/FKs and actual trigger registrations/enabled state, plus the actual direct custom trigger-function definitions and direct referenced-key metadata. For each named signature, the input is the full body/identity/return type/config/security context/ACL/effective EXECUTE, with exact missing-signature and same-name overload identities. Direct dependencies are finite, not recursive discovery. Any directly required sequence configuration must retain bigint values as text and exclude counters. Existing role/schema/default-ACL and authorized governance count/tail metadata remain separately timestamped context. This report authors no catalog SELECT and requests no business or assignment rows.

The proposed new assertion/seal/dispatch functions and `billing_pricing_evidence_seals` are **future migration outputs**, not missing historical/current prerequisite objects to search for or fabricate. Their genuine candidate remains `20260912052508_billing_underlay_evidence_guards.sql`. Task12's distinct event-apply identity remains52509. No source candidate or migration accounting was changed.

## Material reachability and coherence decisions

**Persist/lock/writer are newly exercised operations.** The reviewed source persist routine INSERTs preview lines/evidence and changes underlay pricing/source-readiness fields; first lock writes price-period and once/event charge ledgers. The latest writer deletes/reinserts underlay items and inserts billing_underlay_events. Task12's portal-only underlay UPDATE did not touch this economic child graph. Its input-ready finding cannot establish absence of child triggers or correctness of source-status overwrites. Preserve failure diagnostics/source issues, but require the actual current bodies before selecting exact pre-patch behavior. Current metadata must determine whether an interval-evidence item RESTRICT FK and energy-direction defaults/insert behavior match the source descriptions; source-price UUID text is not automatically a real source-price FK.

**Revision ingestion is an active positive path.** The September1 ingestion body replaces old raw/current normalized revisions, inserts replacements and adds provenance even on identical replay. The provenance guard reads metering_values, normalized_metering_values and ediel_messages. Supply-period/message/source rows are real required identities, not nullable placeholders. Their current triggers/sinks/policies are unobserved. Do not seal by globally forbidding source replacement or skip the actual ingestion positive. Old correction-case registration RPC is not a safe replacement-underlay generator and is not added as an invented prerequisite for the specified revision-ingestion interleavings.

**Graph core ordering is an unresolved current-body edge.** Source core creates item then portal with partner_export_id/invoice_export_item_id/canonical_export_item_id all equal to item UUID. Source wrapper invokes core before `gridex_create_billing_export_run`, whose compatibility rows are billing_export_runs/items, not partner_exports. Actual observed portal still has both partner_exports(id) and company/item FKs. Task12 could seed the compatible parent before an event apply; that seed does not prove Task15's actual graph RPC creates a self-consistent graph atomically. The current core/wrapper definitions must resolve this exact edge. If current bodies also omit the mandatory parent, report the actual assembly/behavior blocker and review a concrete correction; do not drop the FK, preseed unrelated rows and claim self-contained graph success, or equate billing_export_run_items with partner_exports.

The source core rename/partner-reference patch and owner-only service revoke remain distinct from the public wrapper's service grant. No regrant of core EXECUTE is allowed to make direct-RPC tests pass. Source wrapper's `auth.role() <> 'service_role'` is not null-safe; source context identifies an intended fix boundary, not observed current ACL proof. Its source outbox destination is `invoice.export_graph.created`, while provider-event apply uses `webhook_fanout_v1`; preserve exact independent destinations and test actual required persistence. Source metadata-excluding graph hashes cannot establish legal-evidence identity by themselves.

**All shared real triggers stay active.** True Partner customer references plus matching subscriptions exercise invoice resource-event/direct-delivery emissions. Price-plan/version/portfolio/settlement setup continues to bump publication revisions and emit actual domain/direct-delivery rows; settlement audit identity and immutable guards remain. Actual underlay invoice projection changes invoice_readiness_status/export lock fields, not source status/readiness; no guard may clear structured source errors or block legal projection merely because it is invoiced. Source-only monthly-price sync remains absent in the13:54 observation and is not reinstalled. New child/source/legacy triggers are unknown until their full current metadata is admitted; they cannot be presumed empty.

**15b must preserve both result and failure reachability.** Current dispatch reads approved item/portal/hash, pricing evidence and canonical graph; writes item/portal/run/legacy projections, attempts and terminal dead letters. Month completion writes billing/price-period locks. Tenant operational/outbound gates currently read already observed companies; no new governance-event sink is reached by the specific read-only `requireCompanyOperationalForWrites`/outbound check. Surrounding automation lock source deletes all expired locks before INSERT, not just one tenant's key: exact actual body matters for canary/neighbor scope. A TTL does not fence already running HTTP. Connection configuration and provider reconciliation remain runtime boundaries with synthetic metadata/mocked HTTP, never fetched secrets or claimed external exactly-once.

**Combined order and native gates are unchanged.** Task15 batch/operation mutex where applicable → sorted company/underlay mutex/rows → run rows → item reload/revalidation → portal; event apply has its event-row fence before discovery and that shared graph order. Neither path may acquire an event row after graph locks or hold locks across HTTP. The combined D-underlay/E-event wait barrier, reverse winner preserving paid, mutable candidate remap, post-portal/outbox failures and exact replay/rollback/Partner-delivery assertions remain mandatory alongside all24 evidence families. Child UPDATE tuple-before-parent hazards and source READ COMMITTED consistency require the specified enforced discipline, not assumed safety from parent-row locking.

## Frozen result

No reduced normal/Partner/portfolio/evidence profile is READY. Task15a first needs the17 relation/eleven function frontier; Task15b additionally needs its four relation/two surrounding function inputs and the completed15a seal integration. Overlapping objects and invoice-lines metadata can be reused exactly as pinned. Further current trigger/helper/FK edges are assessed only after this finite frontier is supplied; historical source admission, fixture assembly, actual native gates, runtime binding and production send safety remain independently open.

Companion data-only pins: `task-15-observed-input-delta-pins.json`, 19470 bytes, SHA256 `cfcf830e5178cbfb5108a9749ad77d390ee552c073a7cc95125242fecb766437`.
