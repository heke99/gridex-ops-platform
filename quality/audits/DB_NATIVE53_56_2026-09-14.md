# Native foundation53–56 CLI envelope — 2026-09-14

Status: IMPLEMENTED_NOT_VERIFIED. Native first52 remains the accepted preceding
boundary; neither through56 nor full replay is accepted by local tests.

## Scope and source authority

Activate Supabase guidance, source-first debugging, test-driven execution,
verification-before-completion and differential review for this bounded change.
No UI, application API, broad performance audit or deployment work is included.
No independent parallel reviewer or local Docker/Supabase execution is claimed.

Reuse canonical-user-rbac-repair-batch.py and its three unchanged SQL support
files. The four exact R2/E2/S2/W files are admitted only at foundation53–56.
One canonical execution unit is created AND applied by the official pinned
Supabase CLI. Do not invent four original applied-version rows or change any
historical input. All601 historical files and505 version groups are preserved.

The invoker DO body retains complete sources, original stage checks, role-row
identity restoration, policy/RLS preimages, private diagnostics and final full
scoped catalog/row/sequence assertions. Isolation is a read-committed precondition.
Original10s/60s deadlines are set before entering DO. No outer COMMIT occurs
before the CLI's own ledger insertion. No privilege elevation is introduced.

The same exact four provider-owned, unrelated read-only metadata identities
may use ACCESS SHARE only after native complete privilege-profile validation.
All other original table locks remain ACCESS EXCLUSIVE. Unknown deficits,
changed owners, missing/duplicate profile rows and source references fail closed.
This is the isolated db-only test adaptation, not equivalent production DML
serialization. The original lock denial remains a required negative control.

## Independent reference and execution controls

The expected final catalog is NOT the output of the four-source candidate. It
is constructed independently from source R2/S2 diagnostic declarations plus the
existing authority's owner-only diagnostic ACLs and invoker views. This executes
with ROLLBACK against this run's verified native52, with unchanged ledger,
catalog, table rows and sequence state checked immediately afterward.

The original source-bound index, helper function bodies and diagnostic absence
checks bind the native52 preimage. No source W or seed DML is executed by the DDL
oracle. The real group still performs the original independently derived seed
and exact role-row checks inside one transaction.

Mandatory genuine CLI probes before real apply:
-42501: original exact provider metadata lock denial, when applicable;
-P5653: failure after complete R2 before restoration;
-57014: original60s deadline after R2 via pg_sleep(61);
-P5656: failure after all four sources and marker DDL/DML;
-P5657: failure in the CLI's ledger INSERT, checking all table lock modes, the
 shared advisory mutex, completed same-session/transaction temp context,
 original local timeouts and marker presence.

Each probe must preserve the preceding actual CLI ledger and restore catalog,
rows AND sequence values. Every retained CLI input is revalidated by bytes,
0600 mode and device/inode before/after execution. Probe helpers are removed.
A real successful unit must match the complete ledger statements; no-op repeat
must change neither ledger nor schema/rows/sequences.

## Local validation and release boundary

New28 source/probe/control-flow tests,32 historical integration tests,22 prior
legacy tests,9 locking tests,15 lifecycle tests,8 ledger tests and14 bootstrap
tests PASS locally (128). These simulate CLI/database callbacks, not PostgreSQL
execution. Original601-file integrity and complete source accounting PASS.
The new tests were first observed failing for the missing53–56 module.

The ordinary OPS entry now invokes this unit only after verified52 and reports
success through56 only when its real controls pass. Incomplete full-chain guard
NATIVE_LATER_ENVELOPES_AND_FULL_ACCEPTANCE_REQUIRED remains in force. No type,
reference fingerprint or acceptance manifest is refreshed. Full remaining
foundation57–144,514 timestamp stages, schema equality, auth-email and mandatory
OPS/E2E gates remain open. Native53–56 execution is the next required evidence.
