# Native foundation44-52 CLI envelope — 2026-09-14

Status: IMPLEMENTED_NOT_VERIFIED. Native CI is required before this envelope
is accepted. First43 remains the preceding independently executed boundary.
This increment does not accept the full replay, schema/types, release or85/86.

## Scope and routing

Activate Supabase guidance, source-first systematic debugging, test-driven
execution controls, differential review and verification-before-completion.
This is a scoped migration-runner change, not a repository-wide security audit.
UI, application/API, deployment, performance and new skill installation are
outside the patch. No parallel reviewer or local native runtime is claimed.

## Design and source authority

Use the existing hash-locked A/B/C/D/E/F/H/I/Q executor authority and its full
admission, stage guards, source-derived DDL oracles and preservation assertions.
The nine original sources are byte-identical and ordered44 through52. One CLI
execution unit covers all nine; no original migration is marked applied and no
source filename or deployed history is rewritten. The actual ledger must contain
that complete execution body. It cannot claim nine independent commits.

The invoker DO body executes each complete original component without editing
its statements. Temporary context survives until the CLI's ledger insertion.
All original relation/advisory locks, dirty-data rejection, role/row restoration,
RLS/ACL checks, source CHECK authorities and FK semantics remain mandatory.
The executor's READ COMMITTED setting becomes an exact precondition because
changing isolation after a DO statement obtains a snapshot is not permitted.
It does not elevate the role or lower isolation. Native logging settings are
revalidated before reading any historical catalog/rows.

The catalog preimage is obtained from this run's verified native first43, not
from a live/hosted database or a copied final schema. Every previous CLI file,
program, source identity and ledger entry is revalidated before continuation.
Final deltas are checked using the independent immutable-source DDL oracles,
not by declaring a captured after-image to be the expected schema. This is
scoped preservation evidence, not full schema-reference equality.

## Required native probes

Before real application, mandatory CLI failures must leave the preceding
ledger, complete scoped catalog and public/auth/storage row snapshots unchanged:

- P5244: fail after whole A, before B or Q can restore it.
- P5252: complete all nine sources/assertions, add synthetic DDL/DML, then fail.
- P5253: fail in the CLI's own ledger INSERT while asserting the transaction
  mutex, all six ACCESS EXCLUSIVE locks, original10s/60s local timeouts, completed
  temp context and synthetic marker are still present in the same transaction.

Only after those probes and helper disposal pass may the real canonical unit
be applied. Exact SQL/ledger identity and a no-op CLI repeat are mandatory.
Raw inputs, catalog data and subprocess output remain in the existing private
owned workspace; only finite error codes, hashes and counts reach the receipt.

## Offline verification and release boundary

17 new envelope/transport/probe tests plus31 prefix/lifecycle integration,
9 lock-boundary,15 lifecycle,8 ledger and14 bootstrap controls pass locally.
These94 tests use simulated native calls and do not prove native SQL behavior.
The601 original files /505 version groups pass checksum verification; full
source accounting passes. First43 source/lock code and every historical
migration/support SQL file remain unchanged. CLI2.101.0 stays pinned.

The ordinary native entry continues through this envelope only after verified
first43. Full acceptance still stops at
NATIVE_LATER_ENVELOPES_AND_FULL_ACCEPTANCE_REQUIRED. A through52 result still
has zero timestamp inputs and must not unblock types or merge by itself.

No hosted database query, mutation/reset, type generation, schema-baseline
refresh, main merge or deployment is performed by this patch.
