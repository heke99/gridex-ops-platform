# Residual source transitions — 2026-09-13

Status: PARTIAL. No full database/release acceptance.

## Verified three-source increment

GitHub code `7a5b420d363770fd4d257c436402547b9dae29b8`, native run
34759568573, job 103729860099: PASS, including owned cleanup.
The eight constructor/negative tests passed. Native execution applied the actual
first77 foundation, then the following three sources, then the remaining original
foundation through144 and all513 selected timestamp stages and their prerequisites.

- May22 DB1 full original executes byte-identically after first77, before later
  incompatible view definitions. No historical SQL was edited.
- May21 intake completion retains every byte except one `ro.role_key` -> `ro.key`.
  All12 named tenant-read policies are created with RLS enabled; seven cases of
  the actual catalog policy predicate pass: tenant A, tenant B, revoked member,
  outsider, and each of the three authored global-admin role labels.
- May25 alignment retains surrounding source bytes but deliberately preserves
  the already-applied May26 four-column role function. Its exact authored body,
  arguments, SQL/stable/SECURITY DEFINER attributes, compiler validation and
  search_path are required. A pg_proc snapshot proves OID/owner/ACL and remaining
  definition unchanged. This is explicit supersession, not whole-file execution.

The original intake and role-return SQL fail with42703 and42P13 respectively;
failed transactions leave the catalog unchanged. A corrupted canonical-role
preimage is rejected with55000 and catalog rollback. Existing rulebook, inbound,
multitenant and six live-sync proof groups still pass through the continuation.
The seven predicate cases are NOT a full authenticated-caller RLS acceptance suite.

## New candidate under test

The next increment tests whole June1 environment-lock creation before June2 keyed
lock evolution, and a source-pinned May21 actor-view projection transition after
its company/actor-testing prerequisites. It preserves existing view columns and
appends runtime_rank; the May21 row ranking/filter is not silently rewritten.
New controls cover original rejection, altered preimages, two-tenant actor ranking,
lock seed idempotence/identity, duplicate rejection and retained user foreign keys.
Twelve source/constructor tests pass locally. Native results for this extended
candidate are not yet accepted by this document.

## Acceptance boundaries

These candidates are not yet wired into the ordinary canonical replay path or
input accounting. Seven dispositions remain blocking in its current accounting;
DB2 generic schema extraction versus historical single-company repair remains
unresolved. Complete clean replay, ledger provenance, generated schema and types
remain unverified. The migration manifest and generated type tail were not changed.
No production database, historical migration bytes, application/API code, paused
API patch, main branch or Vercel deployment was modified.

The first candidate run34759476202 stopped beforeSQL because its container name
was not admitted by the existing owned-input guard. Only the workflow label was
corrected; no ownership/provenance guard was weakened. A regression test now checks
that accepted workflow identity.
