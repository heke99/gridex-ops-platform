# Governance sync/import prerequisite effects — 2026-09-09

Status: bounded source evidence complete; separate evidence review required before implementation. No SQL executed by this task. Baseline `b9afbf68d4b5490f857fe5b2bd1424d3ab22217b`. Root reports the preceding reviewed full6D batch passed all11 fixed commands,15 governance lanes and61 identity cases on hosted PG17; this audit neither repeats nor supersedes those tests. Full6D2 remains unselected.

The smallest next independently testable implementation is **complete operations sync after existing full6D and before the membership role-key bootstrap**. It needs no new prerequisite table or attribution backfill at that actual boundary. Import-source restoration is a separate decision. In particular, importing an entire source merely to obtain its two useful tables would also replay tenant backfills, contract-version behavior, RBAC changes or policy changes documented below.

Accounting stays592 inputs:516 selected,26 substituted,46 unclassified,4 excluded; focused338:274/23/37/4. Foundation remains76 in this evidence-only commit. Full replay, artifacts/types, bidirectional ledger/catalog parity, runtime binding, customer lifecycle, intended retention and final journal authorization remain open under `SYSTEM_DATA_INTEGRITY_ACCEPTANCE_2026-09-09.md`.

## Routing and evidence authority

Read AGENTS, memory README/current state/checkpoint, relevant integrity/governance audits, decision/failure searches, actual selector/provenance, complete originals and direct definitions/consumers. Applied bounded source/spec comparison, direct false-positive refutation and verification-before-completion discipline, plus Supabase/Postgres schema, FK, RLS and statement-boundary guidance. The dispatched task explicitly restricts this to evidence: full quality-playbook/Council, repository mapping, vulnerability hunting, static scanners, new tests, TDD implementation, performance benchmarking, UI/Next.js, packages, hooks, infrastructure and production operations are not this task. No subordinate agents or new worktree were needed. Independent evidence review is root's next gate. Root owns memory, plan, prior-audit corrections and catalog receipt.

Aliases below always mean the complete immutable `supabase/migrations/` file, with one-based actual line citations:

| Alias | Original | Lines | SHA256 | Classification |
|---|---|---:|---|---|
| O | `20260519_operations_core_saas_sync.sql` |140|`e5863b15ec8c25794912b50c36eda6a370f3fb288800339a0bcfb16f2a3bb619`|UNCLASSIFIED|
| I | `20260519_customer_intake_contracts_tenant_hardening.sql` |272|`a448184e58e8777c41f8bdefb32e45a1365bd37fd9a8e316065da657e57e19f4`|SUBSTITUTED|
| F | `20260519_final_saas_hardening.sql` |634|`2037dbc535d18d7575820d7f40d2a8ef4848b67060161e6851a01eb15990105e`|SUBSTITUTED|
| D | `20260526_debug_step1_2f_customer_import_foundation.sql` |275|`b2e764f4533f0539af021669831e9077582b1a90a257cbb8564777f42971465a`|UNCLASSIFIED|

Total1321 lines. Actual O table/index/comment effects are105–140; actual I imports139–174, versions177–189; F tables208–235 and normalization239–260; D full table definitions6–48 and alignment51–87. Prior excerpt ranges are not authority over these bytes. All four originals lack a top-level BEGIN/COMMIT. In the real `scripts/gridex-aud-003-clean-replay.sh:338–345`, each `psql -X -v ON_ERROR_STOP=1 -f` statement commits separately. Each DO below is one transaction unit; each ordinary CREATE/ALTER/INDEX/COMMENT/function definition is its own unit. A source error stops following statements but does not roll back earlier committed statements. Function definitions are separate from later caller transactions.

## Complete statement/effect matrices

Every executable source unit is covered below. Lists of statements within a row are separate transactions unless explicitly labelled one DO. No source grants, trigger installation or view exists unless listed.

### O — complete operations sync

| Lines / unit | Guard and complete effect | Actual prerequisites / preservation |
|---|---|---|
|5–49, one DO|For19 relations listed below, table-existence guard; ADD company_id UUID IF NOT EXISTS; column-existence guard then `{table}_company_id_idx(company_id)` IF NOT EXISTS.|All19 actual-prefix tables already exist with company_id; no FK added and no DML. Existing columns and same-name indexes survive without definition convergence. Failure in any member rolls back the whole19-table DO.|
|52–102, one DO|Table plus all named-column existence guards; six nonunique indexes: `customers_company_customer_number_idx(company_id,customer_number)`, `customers_company_personal_number_idx(company_id,personal_number)`, `customers_company_org_number_idx(company_id,org_number)`, `customer_sites_company_customer_facility_idx(company_id,customer_id,facility_id)`, `metering_points_company_meter_point_idx(company_id,meter_point_id)`, `metering_points_company_ediel_reference_idx(company_id,ediel_reference)`.|All six column sets exist in core01. No normalization, uniqueness or company/customer matching is introduced. Existing same-name indexes retain their actual definitions.|
|105–127, one CREATE IF NOT EXISTS|Full19-column customer_sync_events, UUID PK/default, exact match-status check; detailed shape below.|gen_random_uuid available from managed/core foundation. Existing table causes the entire definition, including PK/check/defaults, to be skipped.|
|129–136, three CREATE INDEX IF NOT EXISTS|`customer_sync_events_company_status_idx(company_id,match_status,created_at DESC)`; `customer_sync_events_customer_idx(customer_id,created_at DESC)`; `customer_sync_events_source_idx(source_type,source_id,source_reference)`.|Unguarded column use after table CREATE. All nonunique B-tree, no predicate.|
|138–140, three COMMENT|Table linkage-history description; company_id ownership/population comment; matched_by keys comment.|Table/columns must actually exist. These overwrite comments; descriptions are not enforcement.|

The19 O relations are customers, customer_contacts, customer_addresses, customer_sites, metering_points, customer_contracts, customer_contract_events, contract_offers, powers_of_attorney, customer_authorization_documents, customer_operation_tasks, supplier_switch_requests, supplier_switch_events, grid_owner_data_requests, metering_values, billing_underlays, partner_exports, outbound_requests, audit_logs. O has **no DML, helper, view, RLS, policy, trigger, grant or FK**. Its journal is not secured solely by its name or comments.

### I — complete intake/contracts tenant hardening

| Lines / unit | Guard and complete effect | Actual prerequisites / preservation |
|---|---|---|
|4, CREATE EXTENSION IF NOT EXISTS|pgcrypto.|Extension privileges/managed surface.|
|7–51, one DO|18 O relations excluding customer_operation_tasks: table guard; ADD company_id UUID; `{table}_company_id_idx(company_id)`; if companies exists and exact relation-local `{table}_company_id_fkey` name absent, ADD validated FK to companies(id) ON DELETE SET NULL.|Existing same-name company FKs, including NO ACTION, survive. Differently named FKs do not suppress a new second FK. Referenced companies.id must have compatible UUID unique/PK identity; old orphans fail validation.|
|54–136, one DO|Nine null-owner backfills: contacts, addresses, sites, contracts, powers_of_attorney, switch requests from customers; metering points from sites; contract events from contracts; switch events from requests. Only child company_id NULL and parent company_id nonnull, joining exact parent ID.|Table guards do not guard join columns. Whole DO is atomic, but earlier FK DO remains committed. Existing nonnull mismatch is not repaired. No guessed attribution is permitted in a restoration fixture or forward repair; dirty would-change rows must be explicitly reported.|
|139–153, CREATE IF NOT EXISTS;155–156 index|Full import batches; company/created index.|companies/auth.users UUID PKs. See shape table.|
|158–168, CREATE IF NOT EXISTS;170–174 two indexes|Full import rows; batch/row_number and company/status indexes.|Import batch PK and company PK; no customer FK.|
|177–186, CREATE IF NOT EXISTS;188–189 index|Version table UUID PK, required company FK CASCADE, required offer FK CASCADE, required positive integer version_number, snapshot required object-default, nullable actor SET NULL, created_at required now; UNIQUE(offer_id,version_number). Index `contract_offer_versions_company_offer_idx(company_id,contract_offer_id,version_number DESC)`.|Existing early final-shaped version table skips all of this CREATE, including missing offer FK/unique/positive check. Same-name existing index remains ordered by created_at, not version_number.|
|192–201, one DO|Offer table guard; ADD version_number integer required default1, published_at, archived_at, last_price_change_at nullable timestamptz; index `contract_offers_company_status_idx(company_id,status,is_active)`.|Unlike early derived bootstrap, no is_active guard; core01 supplies is_active. ADD does not repair existing nullability/defaults.|
|204–225, one DO|Table guards only. Customers: `customers_company_status_idx(company_id,status)`, `customers_company_email_idx(company_id,lower(email))`, `customers_company_org_idx(company_id,org_number)`, `customers_company_personal_idx(company_id,personal_number)`; sites: `customer_sites_company_facility_idx(company_id,facility_id)`; metering: `metering_points_company_meter_point_idx(company_id,meter_point_id)`; contracts: `customer_contracts_company_customer_idx(company_id,customer_id,created_at DESC)`, `customer_contracts_company_offer_idx(company_id,contract_offer_id)`.|All named columns required if corresponding table exists; no predicates/uniqueness. Core01 supplies them. Shared names with O/F/later history do not guarantee equal definitions.|
|227–229, three ALTER|Enable RLS on batches, rows, versions.|Unguarded table references.|
|232–272, one DO|Per schema/table/name absence guard, create each `{table}_service_role_all` on batches/rows/versions, FOR ALL, role target defaults PUBLIC, USING and WITH CHECK `auth.role()='service_role'`.|No explicit GRANT; service-role predicate is not TO service_role. Existing named policy not replaced. Other permissive policies survive and compose. No authenticated read policy created here.|

I creates no helper/view/comment/trigger. Backfills may fire existing triggers; after6D, company_id updates on its17 targets cross the known operational-guard boundary. Replaying historical backfills is not approval to repair dirty attribution.

### F — complete final SaaS hardening

| Lines / unit | Guard and complete effect | Prerequisites / actual-prefix difference |
|---|---|---|
|5;10–26;28–43;45–63, four separate CREATEs|pgcrypto then IF NOT EXISTS companies, memberships, invitations.|Existing core01 tables cause all inline columns/checks/keys/FKs to be skipped. Full fresh shapes listed below are not actual-prefix shapes.|
|65–70, six indexes|Unique `company_invitations_token_key(token)`; nonunique `companies_status_idx(status)`, `company_memberships_user_status_idx(user_id,status)`, `company_memberships_company_status_idx(company_id,status)`, `company_invitations_company_status_idx(company_id,status)`, `company_invitations_email_idx(lower(email))`.|**Actual-prefix invitations.token is missing**: core01 has invitation_token TEXT; early auth alignment never adds token; SaaS only has token inside skipped CREATE. F fails at65 unless a reviewed prerequisite is added. Existing repaired three-key invitation company/status/created_at index must survive.|
|73–76, four ALTERs|SET DEFAULT gen_random_uuid on companies.id, memberships.id, invitations.id, invitations.token.|Does not backfill existing NULLs or add missing columns/PKs. Does not translate invitation_token.|
|81–118, one DO|For22 target names below, relation guard; ADD company_id UUID inline companies FK SET NULL IF NOT EXISTS; `{table}_company_id_idx(company_id)`.|Existing company_id skips FK addition entirely. access_logs, power_of_attorneys, meter_readings have no CREATE in actual31 prefix; guarded absence is reduced branch coverage, not proof of all22 effects.|
|121–203, one DO|Nine null-owner backfills: sites/contacts/addresses/contracts/switch requests/billing underlays from customers, metering from sites, switch events from requests, contract events from contracts.|Same actual ID/column requirements and no dirty attribution permission as I. I's powers-of-attorney backfill is absent here; F's billing-underlay backfill is absent in I.|
|208–221;223–235, two CREATEs|Competing batch and row shapes below.|First existing relation wins; neither CREATE aligns older fields/statuses.|
|239–260, one DO|If rows exists and batch_id exists without import_batch_id, rename batch_id to import_batch_id; if import_batch_id still absent ADD nullable UUID FK to batches ON DELETE CASCADE.|Rename preserves column identity/values/constraints; existing batch_id type/FK is not validated. When both names exist neither is merged. Missing added parent remains NULL, not guessed.|
|262–270, CREATE IF NOT EXISTS|Final-shaped versions, already supplied by bootstrap.|company_id nullable SET NULL; required offer UUID without FK; required version_number default1 without positive check/unique pair; actor SET NULL.|
|272–275, four indexes|Batch company/created; rows batch(import_batch_id only); `customer_import_rows_company_idx(company_id)`; versions company/offer/created_at DESC.|Batch-index shared name conflicts with I/D two-key definition. First named index survives.|
|278–294, one DO|Offer table guard; ADD company_id inline SET NULL, last_versioned_at timestamptz, version_note text; `contract_offers_company_status_idx` uses company_id,status,is_active if is_active exists, otherwise company_id,status.|company/status still required. Existing lifecycle bootstrap index persists.|
|299–377, one DO|Return if roles/permissions absent. Upsert company_admin on key, set localized name/description and is_system=true if that column exists. Upsert tenants.read/write/invite keys/name/descriptions. Return after these upserts if role_permissions absent. Resolve company_admin and super_admin IDs; insert up to6 role/permission pairs, ON CONFLICT DO NOTHING.|roles/permissions key must support ON CONFLICT, IDs/defaults must be suitable; role_permissions mandatory UUID pair/unique repairs must remain. Existing IDs survive; metadata/is_system updates and extra grants are actual DML, not read-only restoration. No super_admin role is created by this unit.|
|382–426, one function|`gridex_auth_has_role(text)` boolean STABLE PLpgSQL DEFINER, search_path public,auth. Null auth.uid or missing roles/user_roles returns false; dynamic ID join on role_id and r.key. If status exists use coalesce(status,'active')='active'; ELSE if is_active exists use coalesce(is_active,true).|Does not require both status and active when both exist; does not enforce global role scope or confirmed profile/session. No explicit ACL adjustment in F.|
|428–436;438–449;451–467, three functions|`gridex_user_is_super_admin()` uses above role 'super_admin'; `gridex_user_company_ids()` SETOF UUID uses member.user_id=auth.uid and status='active'; `gridex_user_can_manage_company(uuid)` uses super-admin OR same company active owner/admin membership. All SQL STABLE DEFINER, search_path public,auth.|CREATE OR REPLACE preserves signature/OID where compatible; overwrites body/security/config. Existing member is_active and lifecycle/session checks are omitted in these historical bodies.|
|470–524, one DO|19 table targets below: table guard, ENABLE RLS even before company_id check; companies/memberships/invitations skip generic policies. If other table has company_id, DROP/CREATE exact `_tenant_select`, `_tenant_insert`, `_tenant_update`, `_tenant_delete`, default TO PUBLIC permissive policies; all use super-admin OR company_id in company IDs, update both USING/CHECK.|Existing other policy names survive; relation presence alone does not mean company branch ran. No table grants.|
|527–568, twelve individual statements|Six DROP/CREATE pairs: companies_tenant_select (super-admin or own company ID); companies_super_admin_write FOR ALL (super-admin); memberships_tenant_select (super-admin OR self user OR own company); memberships_tenant_write FOR ALL (can-manage); invitations_tenant_select (super-admin OR own company); invitations_tenant_write FOR ALL (can-manage).|Each DROP and CREATE commits separately, unlike generic DO. No guarded table references; partial failure can leave a dropped named policy.|
|573–632, one function|`admin_customer_ids_by_latest_contract(text,text,text,integer,integer,uuid)` defaults NULL,NULL,'all',1,25,NULL; TABLE(customer_id UUID,total_count BIGINT), SQL STABLE default INVOKER. Customer text/status/company filters; DISTINCT ON latest contract by customer_id,created_at DESC; buckets all/none/pending_signature/signed/active/closed(terminated,cancelled,expired); page size minimum1 and nonnegative page offset; order customer ID DESC.|customers id/customer_number/full_name/company_name/email/phone/status/company_id and contracts customer_id/status/created_at are required unguarded. NULL company filter means all RLS-visible companies, not an explicit tenant check. No maximum page size. No selected later definition found; generic ACL/search_path hardening applies.|

F fresh companies additionally defines name required, slug unique, org_number, contact email/name/phone/website nullable, industry required default electricity_supplier, metadata required object, actor SET NULL, required timestamps now, status required default active/check active,inactive,suspended. Fresh memberships defines required company/user CASCADE, role required member/check owner,admin,member,viewer; status required active/check active,pending,suspended,revoked; nullable invited_email/invited_by(SET NULL), accepted_at/suspended_at; invited_at required now; metadata required object; unique(company_id,user_id). Fresh invitations defines required company CASCADE/email, nullable full_name/role_key, role required member/same role check, status required pending/check pending,accepted,revoked,expired; token required UUID/default random; nullable invited_by/invited_user_id SET NULL, expiry/accepted/revoked timestamps; metadata/timestamp required. None of these inline promises upgrades actual existing tables.

F company-column22 targets: access_logs, audit_logs, customers, customer_contacts, customer_addresses, customer_sites, metering_points, supplier_switch_requests, supplier_switch_events, outbound_requests, customer_contracts, customer_contract_events, contract_offers, billing_underlays, communication_routes, grid_owner_data_requests, customer_operation_tasks, customer_documents, power_of_attorneys, powers_of_attorney, metering_values, meter_readings. Policy19 targets: companies, company_memberships, company_invitations, customer_import_batches, customer_import_rows, contract_offer_versions, customers, customer_contacts, customer_addresses, customer_sites, metering_points, supplier_switch_requests, supplier_switch_events, outbound_requests, customer_contracts, customer_contract_events, contract_offers, billing_underlays, communication_routes. F creates no view/comment/trigger.

### D — complete debug import foundation

| Lines / unit | Guard and complete effect | Prerequisite / divergence |
|---|---|---|
|6–26;28–48, two CREATE IF NOT EXISTS|Full batch/row shapes below.|gen_random_uuid; UUID unique/PK companies, auth.users, customers and batches. No extension CREATE here.|
|51–68,18 individual ALTERs|ADD IF NOT EXISTS batches company_id UUID, source_kind/source_type/file_name TEXT; status required previewed; six integer counters required0; warnings/issues required array-default JSONB; metadata required object; created_by UUID; created_at/updated_at required now; imported_at timestamp.|No id ADD, PK repair, existing NOT NULL/default correction or inline FK in ADDs. All existing values untouched.|
|70–87,18 individual ALTERs|ADD rows import_batch_id/company_id UUID, row_number integer, status required pending, normalized/raw payload required object, customer_id UUID, error_message TEXT, warnings required array, issues required object, parser_confidence integer, reviewed_at timestamp, reviewed_by UUID, resolution TEXT, possible_existing_customer_id UUID, duplicate_match_payload required array, created_at/updated_at required now.|No id ADD/PK repair; missing parent/row_number stays nullable. Added possible_existing_customer_id gets no FK in later DO (fresh CREATE does).|
|90;91–93;95;96–109;111;112–114, six ALTERs|DROP then ADD named batch-status, row-status and parser-confidence checks; exact vocabularies below, all validated.|Each DROP commits separately before validating ADD. An invalid status/confidence can leave old check absent. Other differently named checks survive and can narrow allowed values.|
|117–154, one DO|If each constraint name absent anywhere in pg_constraint, ADD NOT VALID FKs: batch.company CASCADE; batch.created_by SET NULL; row.batch CASCADE; row.company CASCADE; row.customer SET NULL; row.reviewed_by SET NULL.|Name-only global lookup can be suppressed by another relation/schema. Does not check definition or validate existing rows. Existing SET NULL company FK survives CASCADE text. Missing possible_existing_customer_id FK is not added.|
|156–169, seven indexes|Exact list below, each IF NOT EXISTS separate statement.|All referenced columns mandatory for execution, regardless of their data nullability.|
|171–172, two ALTERs|Enable RLS on batches and rows.|No GRANT.|
|175–236, one DO|Guard schema/table/name. Each table gets service_role_all FOR ALL TO service_role true/true if absent; tenant_select TO authenticated using platform-admin OR can-read(company); tenant_insert TO authenticated check platform-admin OR can-write(company); tenant_update using platform-admin OR can-read, check platform-admin OR can-write.|Helpers gridex_user_is_platform_admin(), gridex_can_read_company(UUID), gridex_can_write_company(UUID), auth roles required; core01 supplies signatures. No DELETE policy created. Existing I/F/6D2 same-name policies preserved; other policies compose.|
|239–275, one CREATE OR REPLACE VIEW|gridex_debug_step1_2_schema_alignment_v exposes table_name,exists_in_db,rls_enabled,check_status.15 literal names: companies,memberships,user_roles,customers,sites,metering,contracts,import batches/rows,billing_export_runs/items,ediel_messages,ediel_inbound_cases,customer_portal_accounts/claims. Catalog to_regclass/pg_class only; missing_table, review_rls unless billing_export_runs, else ok.|Missing listed table is a report row, not a runtime dependency. Default owner view, no security_invoker or ACL adjustment here. Existing view must allow replacement column types/order. 'ok' says neither complete schema nor safe effective policy.|

D has no DML, comments, helper definitions or triggers. Its source-level status replacements are not permission to widen unrelated dirty checks in a forward repair.

## Exact journal and competing import shapes

All newly created IDs in these four sources are UUID PRIMARY KEY DEFAULT gen_random_uuid(), mandatory via PK. Existing tables skip that guarantee. Existing ID values, PK identity, FK actions and audit rows must survive replay; missing/ambiguous PK or incompatible UUID target is a prerequisite failure, never filled by guessing IDs.

O's19 columns: id; nullable UUID company_id/customer_id/site_id/metering_point_id/source_id/resolved_by; required TEXT source_type,event_type,title; nullable TEXT source_reference,description,resolution_note; required TEXT match_status default pending with check pending,matched,unresolved,ignored,resolved; nullable NUMERIC(5,2) match_confidence; required TEXT[] matched_by default empty array; required JSONB payload default object; created_at required timestamptz default now; resolved_at nullable timestamptz. It has **zero references**, no company ownership enforcement, no confidence0–100 check, no JSON structure check and no polymorphic source_type vocabulary. Customer/site/metering/company/source/actor agreement remains an explicit future ownership requirement.

| Import field/invariant | I fresh | F fresh | D fresh / D applied to older |
|---|---|---|---|
|Batch company|UUID required FK companies CASCADE|UUID nullable FK companies SET NULL|Fresh required CASCADE; older nullability/actions survive; missing ADD is nullable and later CASCADE NOT VALID unless name collision|
|Batch source|source_type required default manual; no source_kind|source_kind required default text; no source_type|Both TEXT nullable without default; older required/default remains|
|Batch status|required previewed; previewed/imported/partially_imported/failed|required completed; previewed/completed/failed|Fresh required previewed; check previewed/imported/partially_imported/completed/failed; old default remains|
|Batch counters|rows_total/rows_created/rows_failed required0 each CHECK>=0|total_rows/created_rows/failed_rows required0, no nonnegative checks|Both triples required0 when new; no new nonnegative or equality checks; I checks remain|
|Batch JSON|issues array default, metadata object default, both required|warnings array default required|warnings/issues array defaults, metadata object, required; no JSON type validation|
|Batch other fields|file_name nullable; created_by nullable auth SET NULL; created_at required now; imported_at nullable|file_name, actor,created_at as I; no imported_at|All I fields plus updated_at required now; no update trigger to advance it|
|Row parent and numbering|import_batch_id required UUID FK batches CASCADE; row_number required integer|Same|Both nullable fresh; ADD missing fields nullable; existing required stays required. None imposes positive numbering or UNIQUE(batch,row_number)|
|Row company|required UUID FK companies CASCADE|nullable UUID FK companies SET NULL|Fresh required CASCADE; old shape preserved as above|
|Row customer/possible match|customer_id nullable UUID no FK; no possible ID|Same|Fresh customer and possible_existing_customer_id nullable customer FKs SET NULL; older customer ADD gets NOT VALID SET NULL via DO, possible ADD gets no FK|
|Row status|required pending; pending/created/skipped/failed|required created; created/skipped/failed/duplicate_warning|Fresh required pending; check pending/ready_to_create/requires_review/duplicate_warning/missing_fields/created/rejected/failed/skipped/linked_existing_customer; old default remains|
|Row payload/issues|normalized_payload required object; issues required array|normalized_payload required object; warnings required array, error_message nullable|normalized/raw payload required object; warnings array, issues object, duplicate_match_payload array, all required; older defaults remain and JSON values not transformed|
|Row review/timestamps|created_at required now only|created_at required now plus error_message|error_message/resolution nullable text; parser_confidence nullable integer/check NULL or0–100; reviewed_at nullable time; reviewed_by nullable auth SET NULL; created_at/updated_at required now; no automatic timestamp trigger|

I batches13 columns/rows9; F batches11/rows10; D batches19/rows19. D following I/F creates a union of fields but not one canonical nullability/default/FK/index shape. Source ordering changes behavior: I→F keeps I checks/defaults and lacks F counters/warnings; F→I keeps F checks/defaults and lacks I counters/metadata. Adding D supplies fields and replaces only the three named checks, preserving first-created defaults, FK actions, required parent/numbering and same-name indexes. This is neither full shape agreement nor intended retention policy.

All three batch sources share `customer_import_batches_company_created_idx(company_id,created_at DESC)`. D also creates `customer_import_batches_company_status_created_idx(company_id,status,created_at DESC)`. I/D rows share `customer_import_rows_batch_idx(import_batch_id,row_number)` while F requests only(import_batch_id); I/D `customer_import_rows_company_status_idx(company_id,status)`; F adds `customer_import_rows_company_idx(company_id)`. D additionally creates `customer_import_rows_company_status_created_idx(company_id,status,created_at DESC)`, `customer_import_rows_company_batch_idx(company_id,import_batch_id,row_number)`, `customer_import_rows_customer_idx(company_id,customer_id)`. All are nonunique B-tree, unpredicated. PK backing indexes are separate. No cross-company composite FK is defined by these originals, and actor/possible-customer/source-reference indexes are not inferred from names.

## Ownership and identity obligations

| Reference | Actual enforcement in these originals | Required compatible identity / open agreement |
|---|---|---|
|sync.company/customer/site/metering|Nullable UUIDs, no FK|Logical owners/targets are companies.id, customers.id, customer_sites.id, metering_points.id respectively; all use core UUID PKs. Source does not enforce that site belongs to customer or meter belongs to site/company.|
|sync.source and resolution actor|source_type required TEXT plus nullable source_id UUID/source_reference TEXT; resolved_by nullable UUID, no FK|Polymorphic source has no declared parent table or key vocabulary. Actor attribution would need an explicit auth/users contract; do not guess source IDs or reviewer membership from these fields.|
|import batch.company/created_by|Single-column company FK with order-dependent action; nullable created_by auth.users SET NULL|Stable company/auth UUID PKs; auth identity alone does not establish actor authority in that company.|
|import row.batch/company|Separate batch UUID FK CASCADE and company UUID FK, with competing nullability/actions|Batch PK must be appropriate and stable; separate references do not prove row.company_id equals batch.company_id. No composite key or trigger in these four sources proves that invariant.|
|import row.customer/possible/review actor|I/F customer UUID has no FK; D fresh customer/possible IDs SET NULL; D reviewed_by auth SET NULL; older ADD behavior differs|Stable customer/auth UUID targets and explicit company agreement. Preserve references/actions/history; do not invent parent/candidate/actor data.|
|contract version.company/offer/actor|Actual early F-shaped nullable company SET NULL, required offer UUID with no FK, actor auth SET NULL; I inline stronger shape skipped|Preserve version UUID PK and existing values; no approved pair uniqueness/positive version/offer-parent/tenant-retention contract follows from replay skip.|

All timestamp columns in these four table definitions and alignment ADDs are `timestamptz`; references to timestamps in the matrices retain that exact type. Required/default JSONB means SQL nullability/default, not an object/array schema constraint.

## Actual prefix and derived prerequisites

Core01 is `01_db1_schema_repair_core_helpers_and_canonical_tables.sql`, core02 is `02_db1_operations_ediel_billing_dedupe_and_storage.sql`. Their actual ordered definitions, rather than F/I fresh CREATEs, establish the prefix:

| Required input | Actual source evidence and effect |
|---|---|
|companies identity|core01:339–359 UUID PK; existing created_by/paused_by UUID without inline auth FK. F CREATE skip cannot add its full company shape or change lifecycle check.|
|customer identity/search columns|core01:727–754 UUID PK, company UUID FK default NO ACTION; customer_number/personal_number/org_number/email/full_name/company_name/phone/status present. No I/F normalization is required to create their indexes/RPC.|
|customer ownership chains|core01 addresses755ff, contacts778ff, sites873–907, metering909–934: UUID PKs and customer/site references; company FKs do not prove same-company parent. Sites has facility_id; metering has meter_point_id/ediel_reference.|
|contracts/offers/events|core01 offers936ff has UUID PK,status,is_active; contracts has customer_id,contract_offer_id,status,created_at; events1048–1061 has customer_contract_id UUID (not inline FK). These names satisfy I/F index/backfill/RPC syntax, not complete parent ownership.|
|remaining O/I targets|core02 defines powers_of_attorney, authorization documents, operational tasks, switch requests/events (events156ff switch_request_id), grid requests, values, billing underlays, partner exports, outbound requests and audit_logs751ff. All19 O targets exist with company_id; all18 I targets exist.|
|role/permission references|core01 roles/permissions UUID PKs and unique key; current early uniqueness/identity reconstructions20260909120000/120200 remain before source seed. Final role-helper20260908120000 and invitation index20260909120100 remain unchanged. F inserts IDs, not guessed key-only references.|
|managed auth/functions|Managed Supabase bootstrap plus core foundation supplies auth.users UUID identity, auth.uid/auth.role, gen_random_uuid, authenticated/service_role, and core01 read/write/platform helper signatures for D. No live credential needed.|
|three missing6D2 targets|Static scan of all76 foundation files finds no CREATE for sync/import tables. Timestamp selected sources do not contain their literal names either; dynamic generic hardening can still affect them once created. Missing table guards in6D2 must not be counted as complete restoration.|
|F token blocker|core01:405ff has invitation_token TEXT; auth email alignment at entry7 adds no token; SaaS at entry30 defines token only inside skipped CREATE. F65 therefore has a missing-column prerequisite. Later selected `20260906081839_canonical_company_invitation_runtime_reconstruction.sql:18,45–46` adds required random-default UUID token/index, but is too late to satisfy F in the foundation. Moving that whole source early is not authorized by this audit.|

Retain these early bootstraps exactly; update replay-preservation provenance only in a separately approved implementation selecting the relevant whole source:

| Entry / bootstrap | Exact current shape and source provenance | Coexistence requirement |
|---|---|---|
|9 companies_primary_contact_email|Four nullable TEXT ADDs: primary_contact_email,primary_contact_name,phone,website; F16–19; SHA9a3be1644f22fb0ea8c3f08cf96d942a8fa645f4b3ae7038e78acaf0346c2c23.|Must remain even with full F: F companies CREATE skips existing core01 company and cannot add these columns. Selected tenant email dispatcher20260616205000:56,78 directly consumes contact email; later legal runtime20260717233000 also uses it.|
|11 contract_offer_versions|Exact F262–275 table/index subset; SHAdfc498aa35f02dcc1b4ab30481f8f52981a8987ccde911c06e4b52541d0c63e6.|Keep required early table and its existing PK/SET NULL company+actor actions, nullable company, no offer FK/unique pair, created_at-based index. Full F is an idempotent CREATE skip; full I must not pretend its incompatible inline version constraints were added.|
|12 contract_offers_lifecycle|I195–199 fields; SHAd4d1a4393ca71d8d70896fa9a21e1e4f897053e4a3c55724c833a4e4b378614d. Derived DO additionally guards is_active to use2- or3-key index.|Retain early metadata/defaults/index. Complete I can follow when is_active exists (true on core01); no replacing/removing index to obtain another name-equivalent shape. Full F adds separate last_versioned_at/version_note fields.|

Current additions metadata marks F/I substitutions without preserveSourceReplay; complete restoration requires explicit source selection plus preservation metadata for **both** F-derived bootstraps and I lifecycle bootstrap. O has no derived bootstrap to replace. Current sequence entries1–31 ends with SaaS30/full6D31; entries32–35 are role-key bootstrap and three6E sources. Keeping O after31 retains the reviewed6D prefix unchanged. This is an evidence-backed boundary proposal, not a selector change in this task.

## Named consumers, direct successors and remaining contracts

Immediate sync consumer `lib/tenant/controlTower.ts:113–115` counts pending/unresolved journal rows; no runtime writer was found by literal-name search in app/lib. O's comment about linkage must not be taken as an implemented matching/attribution pipeline. Preserve source_id polymorphism; do not fabricate a source table or actor identity.

Bulk import `app/admin/customers/actions.part-4.ts:29–90,283–305` uses service client after admin/company/operational guards; writes both source names, both counter triples, warnings, issues, metadata and created_by. Final state is completed or partially_imported. `actions.part-3.ts:280–315` writes row parent/company/number/status/customer/payload, object-shaped issues with missingFields/uncertainFields/duplicateWarnings/confidence, warnings and parser_confidence. Row numbering is input index+2 in bulk action, not a DB uniqueness/positive-number contract. `actionState.ts:126–133` requires ready_to_create,requires_review,duplicate_warning,missing_fields,created,rejected,failed: neither I nor F alone supports the current writer.

`actions.part-3.ts:561–615` recalculates both triples and statuses from row statuses; neither counters nor JSON structure are DB-derived. `loadImportRowForAction` at625ff rejects missing company. Create/link/reject actions load by ID and perform company-scoped access checks; create/link also check operational company. These separate calls use service-role access and are not one transaction with customer graph, import journal and audit. insertImportRow ignores returned insert error; bulk final batch update also ignores returned error. Record this as an audit-delivery/partial-failure requirement, not evidence of a production incident or exploitable path. `imports/page.tsx:134–174` uses authenticated server client, company-filtered batch query where scope exists, then rows by batch IDs: effective RLS and row/batch same-company agreement matter independently of mere table existence.

| Direct successor / selected status | Bounded implication |
|---|---|
|6D2 UNCLASSIFIED|Later candidate installs named generic policies/operational guards on sync/import, not PK/FK/row-parent/JSON agreement. Must follow all approved prerequisite shapes and existing6D, before later6E/final hardening. Retain known journal authorization boundary from predecessor audit.|
|20260521_batch_customer_intake_batch2_hardening/completion UNCLASSIFIED;20260522_batch4_multisite_duplicate_billing_hardening SUBSTITUTED|Source search finds additional column/status/review/duplicate changes, but they are **not direct selected complete successors**. Their existence cannot supply missing fields/checks in the actual chain. Do not add all these files as incidental dependencies.|
|20260526_debug_step1_2c_full_schema_code_alignment UNCLASSIFIED|An alternative debug view source is not selected; D view is not overwritten by that unselected name. No complete selected later definition of D's view found.|
|Selected6E family, performance20260612123000, lifecycle20260814162500|Rewrite company/platform/read/write helpers. Preserve order and final eligible identity/session/member-active/company lifecycle semantics. F's historical role helper still has status-else-active behavior unless separately corrected;6D2 replaces generic policy references, not every function.|
|Selected launch linter20260611190000:113–200|Iterates all public views to invoker/revoke PUBLIC+anon; fixes function search_path; listed F helpers and customer filter RPC undergo ACL/security changes. Per-object exceptions require final catalog assertions. No literal selected later F auth/super-admin/filter definition was found to erase body semantics.|
|Selected invitation runtime20260906081839|Adds token UUID and unique index later. Early F reconstruction must coexist without minting/replacing old delivery tokens; current provisioningWorker.ts:101,132 uses token and companyInvitationFlow.ts:287ff uses accept_token_hash. They are not interchangeable with core invitation_token TEXT.|
|Selected contract deletion graph20260726140000:385–388|Explicitly deletes version rows by company and offer. Existing nullable company/version-no-offer-FK shape is therefore an ownership/retention concern for this named consumer, not permission to convert all references to CASCADE. Full customer deletion remains separate.|

For each future journal/import access gate record relation ACLs, RLS enabled/forced state, owners, actual helper body/security/config/ACL, all permissive and restrictive policies (including unmatched names), caller role/service bypass and audit delivery. I's service policy can survive D unchanged; F/6D2 tenant policy names can cause D to skip its intended TO authenticated versions. O alone enables no journal RLS. No final access claim follows from these source comments. No adversarial workflow or production row query was performed.

## Findings, refutation and minimal implementation sequence

| Finding / confidence | Direct proof, impact and targeted treatment |
|---|---|
|HIGH reconstruction gap, confirmed static|76-file foundation lacks all three CREATEs. Refuted the alternative that an existing table name/extracted version bootstrap supplies them by scanning actual selected bytes. Restore coherent complete source(s), never three company_id-only stand-ins.|
|HIGH F prerequisite blocker, confirmed static|F65 requires token absent in actual first31 definition path. Refuted SaaS CREATE as repair because invitations already exists from core01. Add reviewed forward shape reconstruction before F; do not rewrite immutable F or move late complete source without its own effects review. No SQL failure was executed here.|
|HIGH import shape conflict, confirmed static|I/F writer fields and statuses diverge from immediate app; D fills union but retains first nullability/default/FK/index choices and does not repair IDs. Table-present is insufficient; establish exact accepted catalog and row preservation before source replay.|
|MEDIUM retained version/index divergence, confirmed static|Early F-shaped versions causes I CREATE/index skip. Refuted automatic convergence from IF NOT EXISTS. Preserve identities/actions; any stronger ownership/unique/version contract needs explicit forward preflight and approval.|
|MEDIUM D incomplete legacy alignment, confirmed static|No ADD id/PK; possible-existing-customer ADD lacks subsequent FK; global conname lookup; separate DROP/ADD validation units. Refuted 'additive/idempotent' as all-or-nothing/full-schema guarantee. Catalog-aware prerequisite validation, never permissive cleanup.|
|OPEN business/access requirement|Historical CASCADE vs SET NULL and nullable owner do not specify intended retention. Caller/service guards are evidence, not proof of complete RLS/audit recovery. Do not classify exploitability from source alone.|

**Step A — smallest next implementation, after independent evidence approval:** select checksum-pinned whole O once immediately after current entry31 full6D, before current entry32 membership role-key foundation. Retain all current sources/order/bootstraps and final role-helper/identity/index repairs. Build one complete actual31-prefix PG17 lane that verifies both O DO units, all19 targets/six search indexes, full19-column journal/PK/check/three journal indexes/three comments, absence of source-created FK/RLS/policies, row and object preservation, and second whole-source replay. No new forward shape reconstruction is statically necessary on that exact prefix. O creates its real journal without inventing references/IDs or changing customer values. Expected accounting after a future approved O selection would be592 total:517 selected/26 substituted/45 unclassified/4 excluded globally. The focused auth_membership_tenant group remains338 total:274 selected/23 substituted/37 unclassified/4 excluded: an exact input.path match confirms O is not a member of that group. Current task keeps all accounting unchanged. Operational authorization remains a later explicit gate; this step does not claim runtime readiness.

**Step B — import boundary, not a prerequisite to A:** choose and separately approve the exact retained import shape before selecting I/F/D. A coherent candidate, once policy and token contracts exist, is reviewed forward prerequisites → I → F → D →6D2, at the after6D/before6E boundary with O already present. I-first retains required company/parent/row_number and I company/parent CASCADE/check-counter semantics; D supplies consumer fields/statuses; early versions stays F-shaped. The competing F→I→D order retains nullable company SET NULL and F defaults/batch-only index. D-first permits nullable batch/row_number and loses I nonnegative checks, and is therefore not an equivalent substitute. This audit selects **none** of these alternatives. Exact source order is part of the eventual reviewed contract, not a chronological guess.

The choices needed for B are precise:

- Company/import-history deletion: I/D fresh CASCADE removes child history; F SET NULL retains history while removing direct owner attribution. Existing action must remain unchanged. Neither is intended policy merely because observed historically/live. A separately approved forward contract can require another action, but this evidence does not choose it.
- Batch-parent and row numbering: current app supplies a parent and number; I/F requires both, D allows NULL. Missing existing values cannot be inferred. Require existing stable PKs, compatible validated parent references and known values for a mandatory contract; report dirty states instead of fabricating parents/numbers. Do not invent uniqueness/positive numbering that no original enforces.
- Customer/possible/actor ownership: single-column references only establish existence, not same company; O has no FK. Establish desired same-company and nullable-actor behavior separately while preserving current actions/audit history. No broad customer-deletion audit is needed to start A.
- Forward F token reconstruction: for an empty replay, an explicit narrowly scoped source-defined UUID token prerequisite can precede F. For pre-existing rows, adding NOT NULL DEFAULT random would invent values; do not do that under this task. A nullable UUID column added without default/backfill, with F later setting the future-insert default, is a row-preserving candidate **only if approved as a partial compatibility shape**; it deliberately does not assert source-required NOT NULL or delivery-token convergence. Existing token type/values/duplicates/default/index must be validated and preserved, and later invitation runtime behavior tested. If mandatory tokens are required, stop on NULLs and resolve their authorized provenance separately. Never cast or copy invitation_token/accept_token_hash as guessed equivalents.
- I/F backfill and seed gates: preflight every would-change ownership row and every FK/constraint conflict. In a preserve-values lane, compatible rows make those ownership UPDATEs no-ops; a separate synthetic characterization may show exact historical DML but cannot approve dirty-data repair. F role/permission seed may update metadata/add grants; define expected seed effects explicitly and preserve IDs/mandatory references before selecting F.

Thus there is a concrete next source step now, with bounded further import decisions. No new migration, selector/provenance/manifest change, generated artifact or test was authored by this audit.

## Synthetic verification design — not executed

| Lane | Exact evidence required |
|---|---|
|COMPLETE actual O prefix|Managed Supabase surface + exact current1–31 sources, deterministic two-company references, then whole O and repeat.19/19 relation targets and6/6 search indexes; exact columns/types/default/nullability/PK/check/index keys/order/predicates/validity/comments. Preserve seeded IDs/JSON/values, unrelated attributes, retained FK actions/OIDs/index definitions,6D journal/guards/view and final repairs. Source has no new RLS/FK; assert this accurately.|
|COMPLETE prospective imports|Only after B approval: actual prefix + approved forward prerequisites + exact approved whole-source order. Verify all source effects, including role seeds/helpers, offer metadata/version retained shape, policies, RPC and debug view; no hand-built import-only stand-in substitutes for complete execution. F optional missing22-target branches are enumerated; a branch-complete supplemental lane requires those real prerequisites, not a false22/22 claim from actual-prefix19/22.|
|Repeat/dirty preservation|Synthetic pre-existing I-, F- and D-shaped tables; stable rows/PK/FK action/OID/default/index snapshots; batch_id-only/both-name cases; malformed missing ID/PK, wrong UUID type, same-name wrong index or foreign constraint name, nullable required fields, duplicate row numbers, existing JSON arrays/objects. Successful skips must be labelled mismatches, not convergence. Reject unsupported dirty inputs; never cleanup real rows.|
|Actual autocommit boundaries|Use real unwrapped psql ON_ERROR_STOP file semantics. O malformed existing journal missing later index/comment column: earlier DOs/index statements remain committed, failing statement rolls back. I dirty FK causes first DO rollback; dirty ownership join/check at later DO preserves first DO. F missing token fails65 before defaults/imports; later seed/RPC failure retains previous units. D invalid status/confidence demonstrates DROP committed before failed ADD, with finite timeout. Do not claim whole-file rollback from an enclosing test transaction.|
|Two-tenant reference contract|Synthetic A/B rows and same-company references; assert actual source FK existence/actions and explicitly record absent same-company enforcement. After separately approved ownership forward contract, sanctioned validation cases prove required parent/company/customer/site/metering/actor agreement. No live rows, destructive lifecycle operation or adversarial access probe.|
|REDUCED characterization|O optional relation/column absence; wrong existing journal PK/type/check; F absent optional access_logs/power_of_attorneys/meter_readings; D table-present missing PK and global constraint-name collision; debug view missing relation reports. These are reduced branch/error evidence, never complete restoration.|
|Selected downstream chain|Preserve complete6E and later actual hardening with their prerequisites; inspect composed ACL/RLS/helper/view behavior and invitation token repair interactions. Continue mandatory role-pair/helper/index checks. Do not append historical F/D after final hardening and call it safe.|
|Final gates|Full canonical replay, complete generated schema/types, ledger/provenance and bound production catalog parity, operational journal/import authorization, audit delivery/recovery and customer lifecycle remain separate. No phase closure from these source tests.|

## Verification receipt and limits

Fresh read-only checks and outcomes are appended before this audit-only commit; detailed command evidence is in ignored `.superpowers/sdd/TENANT_GOVERNANCE_SOURCE_RESTORATION_PLAN_2026-09-09/task-3-report.md`. SQL was not executed, no live rows were inspected, and root's separate catalog evidence is not used as intended-policy authority. Source findings above are deterministic static contract observations; execution outcomes remain to be established by the later approved synthetic lanes.

Fresh verification: `node scripts/check-migration-versions.cjs` PASS (592 files/496 version groups/checksums); accounting and focused-group CLIs exit1 as expected for unresolved inputs, with counts unchanged592:516/26/46/4 and338:274/23/37/4 and no contract errors. Independent Python byte comparison to baseline confirms140+272+634+275=1321 lines and all four SHA256 pins; extracted CREATE column counts are I13/9, F11/10, D19/19. `git diff --check` PASS; baseline-to-worktree diff for supabase/scripts/app/lib empty. All checks are static; SQL NOT EXECUTED.
