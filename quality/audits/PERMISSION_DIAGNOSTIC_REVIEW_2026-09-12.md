# Spec Compliance

- **PASS — source implementation is spec compliant; 0 confirmed findings.** The frozen Task11c change implements the actor-bound, service-only diagnostic and retires the parallel application resolver. This is source-review approval, not native/application acceptance.
- **Pending verification:** owned PG17 execution of all 129 candidate cases, supported Node22/Vitest 38 cases, lint, typechecks and build. Their absence is explicitly permitted by the brief's staged review/publication boundary; Task11c must remain IMPLEMENTED_NOT_VERIFIED until the controller obtains those receipts.

## Review identity and routing

- Base: `771e97a28205fd763cd08b99029ef63561fd9215`; uncommitted frozen author package only. Diff SHA256 independently checked: `953135492fc797b8baca4d21735ea2db0e4ab10aaf861baf9797ad73dfe8b0ab`. Report SHA256 independently checked: `b1eebcfe798bd5409c74e34e6e883baf3967dbe2cc8c82c6fdc17f68172327ad`.
- Reviewed AGENTS, task-reviewer prompt, active memory/checkpoint, RBAC/tenancy/database memory, decision/failure search, Task11c brief and exact canonical contract. Applied task-scoped code-review, differential-review, find-bugs, spec compliance reasoning, Supabase/Postgres privilege guidance and verification-before-completion. Read fp-check for potential findings; no suspicion survived to a confirmed finding. The controller's explicit no-agent/read-only boundaries override skill workflows requesting further agents, history operations or remediation.
- Broad repository audits, dependency/security scanner installation, performance measurement, design-system audit, planning, implementation/TDD, deployment and skill-authoring workflows were not triggered by this frozen, bounded review. `using-superpowers` explicitly exempts dispatched subagents. UI review is limited to the changed count source and scope wording, not a new whole-page accessibility audit.
- Changed hunks were read completely. Because package hunks cut the page/loader and native runner functions, the surrounding execution paths were read separately. Outside-diff checks addressed named risks below; no production action, SQL execution, dependency install, index/history operation, memory edit or sub-agent was performed.

## Strengths and requirement evidence

| Requirement / named risk | Evidence and result |
| --- | --- |
| Preserve Task11a algebra, writer and source identity | `scripts/test-canonical-permission-native-fixture.py:15–40` fixes the accepted prefix and original 102-case digest. Independent byte reconstruction of the current SQL prefix plus its original `commit` produced `ac1a2b63476e1eb509b622adbfbab924f8c143c65bac3a046a739df7ea9d4536`. Both new candidate copies match `73eb8a8d89b8468783d69f2829658daeb71d7deda667b5f54a9c5650cd6c47ad`; every one of the report's 12 owned file hashes matches. The manifest diff changes only `forward_candidate.sha256` (`scripts/sql/canonical-permission-native-sources.json:1046`). |
| SQL actor check precedes target existence | Candidate SQL `:800–841` declares STABLE/SECURITY DEFINER, fixed `public, auth, pg_temp`, rejects null IDs, invokes `canonical_actor_is_platform_admin` before `auth.users`, rejects missing/deleted targets with P0002 and has no exception-swallowing branch. |
| One canonical calculation; eligible company/platform meaning | Candidate `:825` calls only `public.gridex_get_user_permissions(p_target_user_id)` for permissions. Full unchanged shared/company/platform callees at candidate `:16–213` independently evaluate companies before union and preserve the separate canonical platform branch. The wrapper neither reads assignment tables nor recreates the algebra. |
| Owner-private internals and service-only diagnostic | Existing private sweep at candidate `:515–544` is preserved. New sweep at `:843–866` enumerates actual non-owner ACL grantees, including PUBLIC via default ACL expansion, revokes with CASCADE, and grants only service_role EXECUTE. Fixture `:485–504` checks both ACL entries and effective `has_function_privilege`, including owner execution of the internal resolver. |
| Real inherited privilege poison | Runner `:118–131` grants the custom parent to anon/authenticated/service_role WITH INHERIT TRUE, grants that parent execution, and asserts effective execution before recovery. This explicitly sets membership inheritance despite the API roles' NOINHERIT defaults. PostgreSQL17 documents this option and the separate SET option in [GRANT](https://www.postgresql.org/docs/17/sql-grant.html); the cleanup matches the documented dependent-grant behavior in [REVOKE](https://www.postgresql.org/docs/17/sql-revoke.html). This is a source-backed valid test construction, not a claim it has executed. |
| C26 authoritative compatibility | Actual admitted predicate in `supabase/migrations/20260802203000_canonical_runtime_consistency_hardening.sql:390–429` checks profile/user/assignment eligibility but does not require active role definition. Native C26 remains unchanged at fixture `:307`; D22 at `:457` deliberately admits that actor while shared grants disappear. D06–D13/D27 use predicate-relevant inactivation/removal, avoiding a silent predicate rewrite. |
| Bind the actual authenticated page actor | Page `:42–53` calls its real guard before `getAdminUserById(current.userId,id)`. Full `lib/admin/guards.ts:122–205` authenticates the session, checks the canonical context's user identity and uses the authoritative platform flag. No target form value supplies the diagnostic actor. |
| Fail closed before target service reads | `lib/rbac/getAdminUserById.ts:58–106` validates the diagnostic before `auth.admin.getUserById` or raw target-role/override queries. RPC/transport failures throw one generic Swedish diagnostic-unavailable error; server logs contain only bounded code/message. Existing raw display normalization and downstream error behavior remain separate (`:108–174`). |
| Distinguish valid empty from failure/malformed data | Loader `:60–78` validates target, scope, array, each nonblank string, duplicates, and a timezone-bearing ISO timestamp with finite Date.parse. The SQL JSON timestamp and fractional-second form fit this contract. Successful `[]` is copied and returned; null/malformed/error paths cannot become `[]`. This is a transport-shape/parseability check, not a strict calendar-date or freshness policy, neither of which the task requires. |
| Accurate Swedish count and complete retirement | Page `:53,214` reads `user.effectivePermissions` and says “Behörigheter via minst en aktiv bolagskoppling eller plattformsbehörighet”. It asserts neither all-company access nor company lifecycle status. `rg` found no remaining app/lib import or call of getUserPermissions; its sole remaining textual mention is an explanatory comment in apiGuards. The deleted helper is absent. Remaining ROLE_PERMISSION_PROFILES uses are catalog recommendations/role-configuration comparison (`lib/admin/accessModel.ts:590–594`, `app/admin/roles/page.tsx:96–122`), not target effective permission fallback. |

## Test fidelity

- **Native assertions are meaningful:** fixture `:103–119` defines invoker assertions. `expect_error` captures only the tested statement's SQLSTATE, then compares outside its exception block; unexpected success or wrong SQLSTATE raises P0001. It cannot catch its own failed assertion. `assert_true` rejects false and null. Native substrate `scripts/canonical-auth-provisioning-legacy-batch.py:250–277` uses ON_ERROR_STOP and requires both expected SQLSTATE and process success, so the case terminal marker cannot mask a prior SQL failure.
- **Native cases exercise actual policy:** D01–D17/D22–D27 use admitted actor/resolver bodies and real SQL role changes. D18–D21 replace only the canonical callee transactionally to inject calculation failure or invalid return values and verify wrapper propagation; these are correctly scoped fault-injection controls, not algebra evidence. `case_sql`/runner `:58–64,146–187` roll cases back, require terminal success, and compare baseline/repeat/recovery/final catalog and row snapshots. Auth, target existence, empty results, local/global precedence and explicit API-role denials are covered at fixture `:423–474`.
- **Permanent application tests import the tested policy:** `__tests__/permission-diagnostic.test.ts:1–29` imports the actual page/loader and substitutes service/request/Next runtime I/O only. The real guard, catalog and imported actions stay real. Positive/empty tests assert actual returned permissions and actor/target binding; malformed/error tests await rejection and assert no target reads; page tests assert element-tree text/count and guard failure (`:90–165`). The page's concurrent catalog reads are not target-detail reads and do not undermine the loader's independent gate.
- **Supplemental probes preserve failure fidelity:** `quality/audits/proofs/permission-diagnostic-regression.mjs:38–57` and `permission-diagnostic-page-regression.mjs:74–96` increment failures for assertion errors and exit nonzero. Their transport substitutions do not replace the resolver/guard. The page probe intentionally cuts only at the JSX boundary (`:65–69`), so it does not establish TSX rendering. The permanent test traverses the real returned React element tree, not a browser render.
- **Construction is not native evidence:** the runner constructor test's synthetic target intentionally checks orchestration only (`scripts/test-canonical-permission-native-runner.py:79–113`); neither its count nor its fixed SQL receipts establish database behavior. The author correctly labels this limitation.

## Issues

- Critical: **0**.
- Important: **0**.
- Minor actionable defects: **0**.
- Stable finding IDs: none assigned because no finding is confirmed. No suppression or permission-engine mock was identified. Reported Node VM/type-stripping experimental warnings are recorded environmental probe noise, not misrepresented as supported runtime success.

## Verification commands, outcomes and limits

| Reviewer action | Outcome |
| --- | --- |
| `sha256sum` on frozen diff/report | PASS; exact hashes above. |
| Dependency-free `python3 -B` hash inspection of every report-owned path; accepted SQL-prefix reconstruction | PASS: all 12 hashes match, old accepted prefix matches, deleted helper absent. No source mutation or test suite execution. |
| `rg -n 'getAdminUserById\|getUserPermissions\|ROLE_PERMISSION_PROFILES' app lib` and focused call-path/source reads | One migrated loader caller, no obsolete helper caller/import, recommendation uses only. Full loader/page, canonical guard, catalog helpers, admitted actor predicate, shared/company/platform bodies, private ACL sweep, fixture assertion and owned psql execution boundaries inspected. |
| Official PostgreSQL17 GRANT/REVOKE/role-membership documentation | Confirms explicit membership INHERIT option and ACL revocation reasoning. |
| One guessed bootstrap filename in a focused `rg` | Path absent; located actual source via manifest (`scripts/sql/gridex-supabase-compatible-bootstrap.sql`). No missing runtime tool was retried. |
| Author's reported 42 constructors and source loader13/page7 | Read and assessed as limited reported evidence; not rerun merely to confirm the report. |
| PG17 native129/baseline10, Node22 Vitest38, lint/typecheck/build | **NOT_RUN in this review.** Docker/psql/local dependencies and installed Next docs are unavailable; disk-full restriction honored. Existing hosted lanes are the required next gates, including first/repeat/inherited-ACL recovery, row/catalog preservation and exact cleanup. |

## Assessment

**Task quality: Approved for controller verification/publication; 0 confirmed findings.** The change removes the duplicate permission engine, centralizes diagnostic authority at the reviewed SQL seam, and supplies meaningful native and real-import controls without changing accepted Task11a behavior. Supported runtime acceptance, full replay/generated-types parity and production readiness remain unestablished; this review does not close those gates.
