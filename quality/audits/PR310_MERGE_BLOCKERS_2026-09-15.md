# PR310 — merge blockers and local auth evidence, 2026-09-15

Status: PARTIAL / LOCAL PATCH NOT PUBLISHED / PR NOT MERGED.
Scope: current remote head91575a3bf1a22ae64d515f9ee406d5471a1ff2e1,
source tree297fc94544268ae28de6f0039f0223400c36ac06. Preserve the entire PR branch.

## Skill routing and verification limits

Applied: repository operating contract, systematic debugging, test-driven
implementation, Supabase-specific privacy/privilege review, differential
self-review and verification-before-completion. Relevant installed local
skills were read. UI/React/performance/deployment work is absent. No independent
external reviewer, new native database execution or production operation is
claimed. Package installation was unavailable (sandbox network name resolution).

The present GitHub tools support reads, not writes or merge. Writer discovery
returned none; the plugin search only returned the already-enabled connector.
An additional user approval cannot substitute for a missing write action.
No push/PR update/workflow trigger/merge was attempted through an unrelated tool.

## Rechecked genuine native result (not a new run)

Ordinary OPS34904445623, clean job104177720124, artifact10372124801:
NATIVE_HISTORICAL_THROUGH77_VERIFIED.77 original source SHA256 values,
43 prepared first-prefix program hashes and19 unique support-file hashes match
this checkout and report. Prior ledger/no-op/negative controls and private owned
resource disposal pass. completeReplayVerified=false and generatedTypesVerified=false.

ZIP SHA256:ad34bba9699c31340233de2318fbac0f81b9a6ef2505a11d1149d908ae6f9f10.
Source archive10372296533 / run34904445663:
add8339ebdc1da7a509ee4c7d3d0444cfabdb4b11a40816ef2dbcd665219643e.
Both digests were compared with freshly fetched GitHub artifact metadata.
The archived source tree exactly matches the local baseline Git tree.
The remote PR description still describes63/pending68; it is stale evidence.

## Full reference comparison is not accepted

Run34904446567 / artifact10371643836, same91575a3b head.
ZIP SHA256:a6f1da93cdfcd8aef4623099c94241d6a3dd71d64aa5d8a49426ca8f2f2cafc3.
This separate portable execution applies144 foundation and514 timestamp inputs.
It does not certify native Supabase lifecycle or official migration ledger.
The final ordinary-owned-shell/reference result is BLOCKED; schemaAccepted=false.

| Section | Added | Removed | Changed |
| --- | ---: | ---: | ---: |
| Relations |62|0|5|
| Columns |937|0|741|
| Constraints |211|11|6|
| Indexes |451|51|5|
| Functions |34|0|2|
| Policies |486|59|128|
| Relation grants |1512|24|0|
| Function grants |110|3|0|
| Schema grants |3|2|0|
| Triggers |76|0|0|

These are differences from a pinned reference, NOT a claim that every difference
is a defect. Intentional forward changes need independent source-based admission;
unexpected differences need fixes. Do not regenerate the baseline to hide drift.

## Confirmed diagnostic gap and local correction

Same-head auth-email-source-effects job104177719552 fails step4. The connector
job-log read returned404. No auth-specific artifact is produced by the existing
job. Therefore the exact failing member of original16 and its SQL cause are not
reliably available from the retrieved evidence. Do not guess a missing table.

The local patch adds an OPTIONAL --evidence mode to the existing fixed runner.
It preserves the original selected commands, execution order, PG* environment
isolation and fail-fast exit status. Default and dry-run behavior remain intact;
--evidence plus --dry-run is rejected to prevent false execution receipts.

Private temporary output files avoid pipe deadlock/unbounded memory and are
closed/disposed. The exported JSON contains only fixed command identifiers,
actual checkout/fixture hashes, byte counts, output hashes, statuses, durations,
recognized SQLSTATE tokens and traceback coordinates in existing checkout files.
Exception messages, SQL text, rows, runner absolute paths, environment values
and credentials are not exported. Observed SQLSTATE tokens can come from
expected negative controls and are explicitly NOT a root-cause assertion.

The workflow uploads ONLY artifacts/auth-membership-group.json even on failure.
No continue-on-error, ||true, renamed passing gate or security waiver was added.
This corrects a diagnostic gap; it does NOT claim to correct the auth SQL failure.

## Local tests actually completed

16 evidence tests PASS, including actual subprocess failures, real runner
fail-fast/PG isolation, no-output disclosure, source-bound traceback locations,
spawn failure, signal/interrupts, unsafe/symlink outputs and dry-run rejection.
The original13 tests were observed RED before implementation. Additional tests
cover the completed runner wiring and traceback projection.

Existing controls:131 historical-prefix/57/63/68/77 +9 lock +22 legacy +44 repair
+12 required-gate =218 PASS. Total234 unittest cases PASS. They are source and
control tests, not new native SQL execution. The existing auth-membership group
self-test also finishes with exit0 (separately captured). AST parsing and workflow
structure checks pass. Migration integrity passes601 files/505 version groups.

## Exact continuation

A write-capable context must publish the whole local patch to the existing PR
branch and run ordinary CI. Integrate the post77 residual bridge/native78–144
and timestamps; then settle full schema/reference/final guards, fix the actual
auth failure, generate real types, and pass mandatory same-head E2E/CI/review.
Keep all earlier PR310 work. No subset merge and no forced branch update.
