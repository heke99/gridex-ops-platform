# Timestamped DB1: complete source-effects and dependency map (preparation)

Status: SOURCE-ONLY PREPARATION, 2026-09-11. This does not select, substitute, exclude, reorder, execute, or independently approve the source. Preserve accepted first43/actual63, foundation104, and immutable provenance pins. Native shapes, SQL effects, runtime privileges, and production parity remain unverified here.

`T` = `supabase/migrations/20260522_db1_schema_repair_backfill_foundation.sql`; all unqualified ranges below refer to T. Every line 1–3073 was read. `C1/C2/C3` are the selected split DB1 sources below; shared SQL was compared in full, with each changed counterpart inspected.

| Source | Lines | SHA256 |
|---|---:|---|
| T | 3073 | `aff5a3e4fb3aae6ebe682081cbce4876c5731be1c124650b19d8151abf6efc73` |
| C1: `01_db1_schema_repair_core_helpers_and_canonical_tables.sql` | 1197 | `85f3561be4d91cee063bbf626302de7726a09c5ce08743b250e62cee959bb5f2` |
| C2: `02_db1_operations_ediel_billing_dedupe_and_storage.sql` | 968 | `0413f4dca84aca387297954b900a163aa63d0f84552570c372c12e8f8abdd693` |
| C3: `03_db1_backfill_functions_rls_reports_and_finish.sql` | 950 | `877e395df0050a36ec71298d279c72fb0e6cb13d8b90082277450012e196f169` |

Counts are source declarations/attempts, not successful native effects: 1 extension, 62 CREATE TABLEs, 32 function definitions (18 SECURITY DEFINER), 15 views, 102 literal ADD COLUMNs, 2 compatibility loops (20 + 144 possible ADD attempts), 15 attempted unique indexes, 8 direct nonunique indexes, 9 DO blocks, 5 direct top-level DML statements, 49 RLS-enable targets, at most 197 new named policies. Existing objects, data, exceptions, privileges and execution failure can change actual results.

## Complete immediate declaration map

| T range | Declared objects / effects | Dependencies and boundary |
|---|---|---|
| 12; 17–111 | `pgcrypto`; 7 tables: `gridex_schema_repair_runs`, `gridex_schema_repair_findings`, `backfill_runs`, `backfill_run_items`, `canonical_record_links`, `duplicate_groups`, `duplicate_group_members` | UUID defaults, run-key/source-link/group/member uniqueness; item/group FKs. Bookkeeping and canonical link contracts persist. |
| 119–158; 292–329 | `gridex_db1_log_finding`, `gridex_db1_try_exec`, `gridex_db1_default_company_id` | Definer functions. Logger inserts findings; try_exec executes supplied SQL and catches/logs exceptions; default helper inserts a company if zero, selects if one, logs/returns NULL if multiple. Definition itself does not call these bodies. |
| 163–290 | 14 immutable SQL definitions: `gridex_normalize_email`, `gridex_normalize_phone`, `gridex_normalize_personal_number`, `gridex_normalize_org_number`, `gridex_normalize_facility_id`, `gridex_normalize_metering_point_id`, `gridex_make_source_hash`, `normalize_email`, `normalize_phone`, `normalize_personal_number`, `normalize_facility_id`, `gridex_make_idempotency_key(text[])`, `make_idempotency_key(text[])`, `make_idempotency_key(jsonb)` | Normalizers precede generated columns; hash/idempotency helpers require unqualified `digest`/`encode` resolution. Both text-array signatures are VARIADIC. |
| 335–511 | 9 tables: `companies`, `company_memberships`, `company_invitations`, `roles`, `permissions`, `role_permissions`, `user_roles`, `user_permissions`, `user_permission_overrides`; 31 direct compatibility ADDs plus one caught company generated-column ADD | Company/role/permission references and membership/override uniqueness. Not a generic role/permission seed: only a conditional company seed occurs. |
| 514–718 | 7 definer helpers: `gridex_user_has_role_key`, `gridex_user_is_platform_admin`, `gridex_user_company_ids`, `gridex_can_read_company`, `gridex_can_write_company`, `gridex_user_can_manage_company`, `gridex_can` | Depend on `auth.uid()`, role/permission/membership tables and `admin_users`. Role-key lookup adapts to columns; admin/role/permission PL/pgSQL helpers return false on caught errors. Company read uses active membership; write additionally checks four role labels. `gridex_can(text)` reads user/role allow rows without a company argument/filter. These are source facts, not proven effective permissions. |
| 723–1055 | 13 tables: `customers`, `customer_addresses`, `customer_contacts`, `customer_internal_notes`, `grid_owners`, `electricity_suppliers`, `price_areas`, `price_area_localities`, `customer_sites`, `metering_points`, `contract_offers`, `customer_contracts`, `customer_contract_events` | Tenant/customer/price-area references and generated normalization. Four SE1–SE4 price-area rows seeded at 852–854. Customer/site/contract relation names alone do not establish pre-existing column shapes. |
| 1059–1117 | 32 literal ADDs for `customers`, `customer_contacts`, `customer_sites`, `metering_points` | Four nested exception blocks log generated-column problems; ordinary ADDs outside those blocks can still abort. |
| 1120–1155 | 10-table loop adds `company_id` and `customer_id` if relation exists; 9 further literal link-column ADDs on agreements, delivery points, AI extractions, readiness snapshots | Does not create `contract_agreements`, `customer_delivery_points`, `customer_profiles`, `document_ai_extractions` or the other legacy relations. T lacks C1's separate contract prerequisite block. |
| 1160–1568 | 15 tables: `powers_of_attorney`, `customer_authorization_documents`, `power_of_attorney_scopes`, `supplier_switch_requests`, `supplier_switch_events`, `customer_operation_tasks`, `communication_routes`, `grid_owner_data_requests`, `outbound_requests`, `outbound_dispatch_events`, `metering_values`, `billing_underlays`, `billing_export_runs`, `billing_export_run_items`, `partner_exports`; 29 authorization compatibility ADDs at 1233–1269 | Company/customer and owned-child FKs. Creates operational schemas, not outbound sends, invoices, or switch requests. Adds scopes.status used by deferred scope backfill. |
| 1573–1808 | 10 tables: `ediel_actor_settings`, `ediel_route_profiles`, `ediel_message_rules`, `ediel_messages`, `ediel_message_events`, `ediel_message_validation_issues`, `ediel_aperak_error_rules`, `ediel_aperak_error_details`, `ediel_inbound_cases`, `ediel_tgt_test_data` | Company/message/error-rule references; TGT key uniqueness. No Ediel rule/actor/TGT rows seeded. |
| 1811–1899 | 6 views: `ediel_active_actor_settings_v`, `ediel_route_runtime_v`, `ediel_message_ack_state_v`, `ediel_overdue_message_acks_v`, `ediel_duplicate_ack_candidates_v`, `ediel_rule_ambiguities_v` | Actor/routes/messages/rules and preceding ack view. These definitions precede general compatibility ADDs at 2037; required existing columns/view replacement contracts are not automatically repaired. |
| 1904–2032 | 8 tables: `audit_logs`, `customer_portal_accounts`, `customer_portal_claims`, `customer_portal_events`, `customer_documents`, `customer_invoices`, `customer_invoice_lines`, `customer_invoice_documents` | Company references; operational/portal schemas only. |
| 2037–2057 | 36 named tables, four ADD attempts each: company_id, metadata, created_at, updated_at | Relation-existence guards; exact target array 2041–2048. This is not a complete per-table schema repair. |
| 2062–2101 | 15 try_exec unique-index attempts; 8 direct nonunique indexes (enumerated below) | Earlier tables, referenced columns, expressions and data. Only try_exec calls catch/log index errors; ordinary indexes are unguarded except IF NOT EXISTS. |
| 2106–2119 | 7 `storage.buckets` upserts: `customer-documents`, `contract-pdfs`, `customer-intake`, `billing-imports`, `billing-exports`, `ediel-files`, `actor-test-evidence` | Managed buckets relation/columns and id conflict arbiter required. Sets name, public=false, size=52428800 and each explicit MIME array, including on existing rows. No storage-object policy/DDL or file upload occurs. |
| 2125–2784 | 8 definer run/backfill definitions, detailed below | Persistent executable function contracts; none of the six public backfill entrypoints is called at T top level. |
| 2790–2861 | 49-table RLS loop; select/insert/update/delete policies; separate `gridex_db1_companies_member_select` | Enables RLS, creates only missing policy names; leaves existing same-name predicates untouched. Maximum 49×4+1=197 policy creations, including 49 DELETE policies absent from C3. |
| 2866–3021 | 9 views: `gridex_db1_schema_gap_v`, `gridex_db1_tenant_gap_v`, `gridex_db1_duplicate_customer_candidates_v`, `gridex_db1_duplicate_site_candidates_v`, `gridex_db1_duplicate_metering_point_candidates_v`, `gridex_db1_rbac_health_v`, `gridex_db1_rls_policy_gap_v`, `gridex_db1_storage_gap_v`, `gridex_db1_backfill_readiness_v` | Catalog/managed-bucket and customer/operational relation reads. Readiness aggregates 7 checks. Schema-gap checks object existence, not shape; RLS-gap checks enabled/any policy, not tenant correctness; RBAC callable row is a literal claim, not a call. |
| 3026–3073 | Conditional 8-table company-id DML, then repair-run completion update | Actual operational mutation occurs immediately; completion metadata is not evidence of native validation or successful index construction. |

## Index, RLS and immediate-DML detail

- Unique names at 2062–2073: `ux_companies_slug`, `ux_companies_normalized_org`, `ux_company_memberships_company_user`, `ux_customers_company_customer_number`, `ux_customers_company_personal_number`, `ux_customers_company_email`.
- Unique names at 2074–2083: `ux_customer_sites_company_facility`, `ux_metering_points_company_meter_id`, `ux_poa_company_customer_document_hash`, `ux_switch_company_meter_start_contract`, `ux_ediel_inbound_interchange`.
- Unique names at 2084–2091: `ux_ediel_outbound_source`, `ux_ediel_ack_related`, `ux_billing_export_items_source_period`, `ux_outbound_active_source_request`. Predicates/expression keys are part of each whole statement; a same-name index is not an equality proof.
- Direct indexes 2094–2101: `idx_customers_company`, `idx_customer_sites_company_customer`, `idx_metering_points_company_site`, `idx_ediel_messages_company_status`, `idx_ediel_messages_related`, `idx_outbound_requests_company_status`, `idx_supplier_switch_company_status`, `idx_audit_logs_company_entity`.
- RLS target set is the 62 declared tables minus exactly 13: `gridex_schema_repair_runs`, `gridex_schema_repair_findings`, `roles`, `permissions`, `role_permissions`, `user_roles`, `user_permissions`, `grid_owners`, `electricity_suppliers`, `price_areas`, `price_area_localities`, `contract_offers`, `power_of_attorney_scopes`. This says nothing about inherited RLS on those 13.
- RLS predicates 2820–2845: company-bearing SELECT = admin or readable company; INSERT = admin or writable company; UPDATE USING = readable, WITH CHECK = writable; tables without company_id use admin only; DELETE always admin only. No TO clause, FORCE RLS, GRANT/REVOKE, or replacement of an existing same-name policy is specified.
- Direct DML #1, 113–116: upserts one repair key, resets started_at/completed_at/summary on conflict. #2, 509–511: inserts the stated default company only if companies is empty. #3, 852–854: four price-area inserts, conflict-do-nothing. #4, 2106–2119: seven bucket upserts. #5, 3063–3073: marks the repair row completed with static safety/next-step metadata.
- Immediate nested DML 3026–3061: if exactly one company, fill NULL company_id in existing `customer_contracts`, `contract_agreements`, `customer_delivery_points`, `document_ai_extractions`, `customer_readiness_snapshots`, `customer_lifecycle_decisions`, `customer_duplicate_resolution_events`, `customer_merge_events`; otherwise insert an info finding. Does not call a named backfill engine.
- Immediate findings also arise from 15 failed try_exec attempts and five compatibility exception blocks (companies plus four customer/site/meter groups). try_exec logs SQLERRM, SQLSTATE and attempted SQL; no dirty-data exception is proof that an index exists. No direct DELETE/TRUNCATE or explicit transaction wrapper appears in T.

## Deferred execution graph and effects

| T range | Entry/body | Deferred writes and prerequisites |
|---|---|---|
| 2125–2194 | `gridex_db1_start_backfill_run(text,text)` / `gridex_db1_finish_backfill_run(uuid,text,jsonb,integer,integer,integer,integer,integer)` | Upsert/reset `backfill_runs`, then store status/counters/summary and return JSON. Reusing a run key resets counters; accumulated run items are not deleted. |
| 2196–2226 | `backfill_companies()` → default-company helper | Counts/selects/inserts company; run bookkeeping and optional ambiguity finding. |
| 2228–2325 | `backfill_customers()` → default helper | Requires full `customer_profiles` row fields; absent table/ambiguous company skips. Matches company+email/customer number, inserts/updates customers, upserts canonical links; failures insert run items. Source selects first matching customer; it does not prove ambiguity absence. |
| 2327–2432 | `backfill_customer_sites()` → `backfill_customers()` | Reads delivery points and profile→customer links; inserts/updates sites, fills delivery-point company/customer/site links, upserts canonical links; missing links skip into run items; per-row errors recorded. |
| 2434–2550 | `backfill_metering_points()` → sites → customers | Requires delivery-point external_metering_ref and canonical customer/site links; inserts/updates meters, delivery-point meter link and canonical links. Missing meter identifier is skipped; facility_id is not used as meter identifier. |
| 2552–2720 | `backfill_contracts()` → customers, sites, meters (which call nested predecessors again) | Fills NULL contract company IDs, optionally resolves customer_id via user_id, then reads agreements. Creates customers/sites/contracts, fills agreement links and upserts canonical links; already-linked contracts skip. Source/target apartment fields are an additional dependency at 2672–2674. Missing agreements returns only after earlier calls and contract updates. |
| 2722–2784 | `backfill_poa_scopes()` → default helper | Fills NULL POA company IDs, inserts a scope when none exists for a POA, records per-row failures. Reads POA identity/customer/site/meter/scope/validity fields; scope.status comes from compatibility ADD. |

All six engines use the run helpers. Five row-processing engines can finish with status `completed` and nonzero rows_failed; status text alone is not successful backfill proof. Default-company ambiguity usually returns skipped, but backfill_companies still finishes completed with a NULL chosen company. Link lookups and inherited constraints/triggers require their own tenant/identity proof; no exploit or deployment conclusion follows here.

## Four verified differences from the selected whole split sequence

Normalization removes only blank lines and whole-line `--` comments. Concatenated C1/C2/C3 has 2829 lines; T has 2801. The complete normalized unified comparison was independently inspected by root (256 diff lines); no other non-comment textual differences were found.

1. **21 CREATE-time FK actions:** T uses CASCADE where C1/C2 use RESTRICT, at T 57,102,385,403,443,445,478,754,775,794,872,908–909,1214,1312,1434,1523,1730,1742,1770,1780. These cover run/group children, memberships/invitations/overrides, role-permission parents, customer dependents, POA scopes, switch/outbound/export/Ediel children. Existing-table IF NOT EXISTS does not rewrite an existing FK; missing-table behavior still differs.
2. **Contract prerequisites omitted:** C1 1142–1174 is a 33-line DO block with 28 ADD COLUMNs (site/meter/offer, contract identity/type/start/end, termination/renewal, pricing/fees, metadata and audit fields). T goes from its shared legacy-column loop to agreement link columns at 1136; its CREATE TABLE only supplies these when the table is new. Omission is not a DROP of existing columns.
3. **Apartment data added:** T 2672/2674 inserts `customer_sites.apartment_number` from `contract_agreements.apartment`; C3 555/557 omits both. T's site CREATE/compatibility blocks contain no apartment_number. A bounded token check of every selected first63 source finds apartment_number only in C1's `customers` CREATE (747), so no supplier for the site column was identified there. This is a deferred-path source prerequisite, not an observed SQL failure.
4. **DELETE policies added:** T 2796,2818,2844–2846 adds the variable/name/creation branch; C3 674–725 only creates SELECT/INSERT/UPDATE policies. T can introduce 49 named admin DELETE policies, even when the 49 tables already exist. Effective deletion still depends on actual privileges, predicates, constraints and triggers.

## Existing order, managed boundary and named blockers

- **DB1-ORDER:** `scripts/gridex-aud-003-foundation-order.json` has 104 entries; C1/C2/C3 are positions 1/2/3, admin_users bootstrap is 25, RBAC helper bootstrap is 26. T has no slot. The recorded Task24 projection keeps T UNCLASSIFIED with execution=[] and derivedArtifacts=[]; dependency pinning for an excluded DB2 script does not classify T itself.
- Subsequent selected direct RBAC definitions occur at foundation35 (`20260519_final_saas_hardening.sql`:438/451), 37 (`20260519_batch_6d2_runtime_governance_completion.sql`:118–189), 39 (`20260520_batch_6e_rbac_tenant_stats_whitelabel.sql`:51–190), 41 (`20260520_batch_6e_hard_platform_roles_only.sql`:5) and 53 (`20260525_debug_batch_2_rbac_tenant_alignment.sql`:103). T recreates the early helper bodies; no position or later effective winner is inferred from its filename or this comparison. Later restoration sources remain binding.
- **DB1-SHAPES:** native existing columns/types/defaults/generated expressions, keys, view replacement contracts and function signatures/owners are unproved. C1/C2/C3 agreement is source evidence only. CREATE IF NOT EXISTS plus selective ADDs cannot certify the 62 existing table shapes; caught normalization/index errors can leave prerequisites absent.
- **DB1-LEGACY/APARTMENT:** full profile/delivery-point/agreement shapes and the two apartment fields must be resolved for reachable deferred paths. Existence checks skip some absent sources but do not validate present-row shapes; contract pre-loop updates are outside per-row catches. Do not invent tables or treat another project's catalog as this execution boundary.
- **DB1-PRIVILEGE:** 18 definer functions (11 mutation/helper, 7 RBAC), no execute revocations/role grants, and 15 views without security_invoker settings. Actual callers, owners, search paths, inherited ACLs/default ACLs and existing RLS/policy composition are not established by this map. Definitions can replace existing bodies while other inherited state persists.
- **DB1-TRIGGERS:** T creates/disables/drops no trigger and performs no sequence operation. Its immediate company, bucket, bookkeeping and eight tenant-update targets, plus deferred customer/site/meter/contract/POA/link/run/item targets, can encounter whatever triggers/defaults/constraints exist at the eventual boundary. That inherited graph is not certified; no provider action or complete transitive effect count is claimed.
- **DB1-MANAGED:** T needs public/schema creation rights, callable auth.uid(), pgcrypto/digest resolution and the existing storage.buckets contract. It does not create Auth/Storage schemas or managed roles. The local compatible bootstrap explicitly models auth.uid (154–160), buckets (178–188), function default grants (85–92), and search paths (64–78); those are harness definitions, not native Supabase ownership/grant evidence.
- **DB1-DISPOSITION:** whole source is schema/reference-bearing plus immediate operational DML and deferred engines. Neither operational-only exclusion nor duplicate substitution is supported. Next classification must account for every shared effect and all four deltas at a proved boundary, while retaining first43/actual63/foundation104. This preparation grants no new selection, source-order change, native acceptance, historical execution, or production action.

Independent scoped review approved documentation-only preservation on 2026-09-11 with no findings. The review inspected the high-risk immediate/deferred, privilege, policy, managed and delta paths; it did not upgrade source counts into native acceptance.
