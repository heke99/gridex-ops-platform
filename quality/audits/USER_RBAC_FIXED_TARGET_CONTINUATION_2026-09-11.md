# Fixed-target lossless continuation — implementation contract

Status: **BOUNDED DESIGN; NO NEW SOURCE SELECTED OR SQL EXECUTED.** Review base: `fe0379e47f7ec3ec3c0532aa2a608029211aa7a4`. Task15's 102 native cases at `2bbcdab8` supply characterization; final privacy acceptance of fix `49f00ff8` on its published head is a prerequisite to executing this continuation. This contract reuses Task9's complete effects and Task15's independent field/PK oracles. It does not repeat their matrix or approve historical activation behavior for production.

Evidence paths: bare migration filenames are under `supabase/migrations/`; Python filenames are under `scripts/`. The short citations `oracles.py`, `cases.py` and `fixed-target-selftest.py` mean `scripts/canonical-user-rbac-fixed-target-oracles.py`, `scripts/canonical-user-rbac-fixed-target-cases.py` and `scripts/canonical-user-rbac-fixed-target-selftest.py`; `replay.py` means `scripts/canonical-auth-provisioning-replay.py`. B0/C2/D2/F2 line numbers refer to the original pinned files in Task9's source-effects table.

## Binding boundary (Task9, verbatim)

The following is the existing six-step contract from `USER_RBAC_REPAIR_ADMISSION_CONTRACT_2026-09-10.md:137–142`:

1. Construct a fresh owned database from the accepted full prefix, with no application connections/provider/network hooks. Require all affected business tables and their enabled-trigger write targets empty, apart from independently snapshotted immutable catalog seeds. Reject any populated business input before seed/source submission. Preserve actual canonical constraints, RLS, triggers and role identities. No external/live database is eligible.
2. Capture independent complete catalog and row/sequence preimages in the private controlling process or separately owned private store; ON COMMIT DROP data cannot survive D2/F2. Seed only the minimum synthetic fixed slots through a reviewed constructor. Enumerate constructor effects as well as source effects, including Auth callback/profile creation, company trigger defaults/capabilities and audit/history. Missing prerequisites must be added through an actual reviewed canonical prerequisite, not table fragments or disabled guards. Pin an exact allowed dependency/write graph before execution.
3. Execute one complete source at a time with its original transaction control. B0/C2 can still use an outer transaction internally; D2/F2 commit inside the unpublished DB. A source reaching native failure does not count as selected completion. Follow with a newly reviewed forward cleanup/reconstruction if necessary, created from an actual CLI skeleton. The cleanup must restore **all** preexisting catalog values/IDs/rows and remove only exact constructor/source-generated synthetic rows by captured PK, in FK-safe order. Never broad-delete by email/company/status, TRUNCATE, CASCADE, disable triggers or restore a dump to substitute for source execution.
4. B0 specifically needs exact roles/permissions/role_permissions preimages, removal of its synthetic unscoped assignments/company/membership/profile/audit graph, original membership CHECK and actor/profile FK boundary. C2 needs company/profile/membership/invitation/role/activity/actor/timestamp/metadata and trigger graph reversal, including successful whole-file behavior that does not create a membership. D2 additionally needs global unrelated role-key preimages and a post-COMMIT cleanup. F2 needs both old/correct identity graphs, global email-alias preimages, company name, any disabled role and invitation expiry/status preimages, also post-COMMIT cleanup. Their fixture-created Auth/user sessions/credentials remain absent at final publication.
5. Compare exact independent final preimage plus only explicitly reviewed structural deltas; all synthetic row identities and downstream work disappear, no original data is lost, no newly granted permission or session remains. Sequence last_value/is_called and extension/catalog effects must be accounted for where changed. If enabled triggers make exact cleanup nonconvergent, that exact trigger/source effect blocks admission; implement its reviewed lossless reconstruction and new proof before continuing. Never claim effect-equivalence or partial extraction as full execution.
6. Only the controlling process may release the owned database handle to the next replay stage after final comparisons. Failure, crash or incomplete cleanup destroys that database and reconstructs the prefix on retry. Prove process death after each internal COMMIT and after cleanup/before publication cannot release an unverified handle. A distinct canary database remains untouched. This offers database-lifecycle isolation, **not PostgreSQL rollback of committed work**.

## One group, two actual forward identities

Preserve the complete first57, then insert **P → B0 → C2 → D2 → F2 → X**, before the unchanged former58–98 suffix. B0/C2/D2/F2 are the whole pinned originals identified in Task9's effects document, not copied statements. Each original and each new migration executes once in this foundation. Constructor and private assertion transport are controller support, not additional selected migration identities.

Obtain both empty skeletons in one hosted pinned-CLI2.101.0 job, following its actual help interface:

```text
supabase migration new canonical_user_rbac_fixed_target_prerequisites
supabase migration new canonical_user_rbac_fixed_target_restoration
```

P has logical suffix `_canonical_user_rbac_fixed_target_prerequisites.sql`; X has `_canonical_user_rbac_fixed_target_restoration.sql`. Retrieve the actual generated members and pin artifact/member checksums before authoring. No timestamp, generated filename or resulting selected total is assigned here. Two identities are necessary: P must run before B0's column references; X must run after F2's native COMMIT. No intermediate cleanup is needed: the admitted sources share compatible identities and constraints, and complete preimages survive the entire unpublished group.

P adds only `companies.industry` (text, NOT NULL, exact source-declared default) and `company_memberships.suspended_at` (nullable timestamptz). The authorities are `supabase/migrations/20260817210500_fix_canonical_provision_company_request_hash.sql:19–21` and `20260802014000_canonical_provisioning_access.sql:7–17`. The former continues into a provisioning request/function dependency at23–37; the latter creates jobs from19 and later defines acceptance/lifecycle behavior. Selecting either broad file merely to obtain these columns would import unaccepted dependencies. P is an explicitly new forward migration justified by those declarations; neither broad original becomes selected. Preserve these two columns at final release. Assert their exact shape, including default, nullability and absence of generated/identity attributes; reject an incompatible preexisting shape.

## Minimum shared constructor and successful path

The private pinned-source parser verifies that C2/D2 share U_target, U_actor, C_target and email, and F2 shares U_target/C_target/email. U_boot, U_target and U_actor are distinct; none is U_old. Bind only opaque symbolic slots in reports. Source binding is already defined by `canonical-user-rbac-fixed-target-selftest.py:68–109`; runtime must implement that boundary in a trusted runtime module, never import a selftest as authority.

After P and before B0, construct exactly three provider-free Auth rows and one company:

| Constructor row | Exact admitted purpose |
| --- | --- |
| U_boot | Fixed UUID from B0; fresh synthetic non-NULL email satisfying B0's guard. |
| U_target | Shared fixed UUID and privately bound exact source email required by C2/D2/F2. |
| U_actor | Shared fixed actor UUID required by C2/D2's actor FKs; synthetic email. Creating a fixture FK subject does not grant actor authorization. |
| C_target | Shared fixed company UUID, synthetic nonblank name, legal active status, and B0's privately bound slug predicate; other required fields use reviewed canonical defaults or explicit inert values. `created_by`/`updated_by` are NULL. |

All Auth credential, confirmation, recovery, session and provider metadata fields stay NULL/inert as in `canonical-user-rbac-fixed-target-fixtures.py:165–169`; create no Auth identity/session/refresh-token rows. Source-bound values are offline synthetic fixture slots, never reads of real accounts. Do not insert U_old, profiles, memberships, user roles, invitations, canary business rows or extra catalog seeds. Existing accepted role/permission/link seeds retain their IDs.

Require companies empty at admission and verify the constructor's C_target is the **one** company satisfying B0's entire slug-or-normalized-organization predicate (`B0:207–212`); the slug match alone is sufficient. This gives one company in the total graph. Moving D2 ahead of C2 could avoid the constructor company INSERT but would make B0 create a second company or require another constructor update; retain original B0/C2/D2/F2 order and the smaller total graph.

| Whole source | Required successful path and independent postimage |
| --- | --- |
| B0 | Existing pgcrypto; replace membership CHECK; upsert two role keys and23 permission keys; add all-permission super links and exact20-key company links with actual IDs. Create two unscoped U_boot assignments, update the unique shared C_target, insert U_boot membership with source `membership_role='owner'`, create U_boot profile and explicit audit row. Do not mislabel this membership as company_admin. Use `bootstrap()` at `canonical-user-rbac-fixed-target-oracles.py:261–352` and Task9 B0 map. |
| C2 | Auth/actor/company guards succeed. Actual membership_role column/type exists; U_target has no membership, so the admitted stale-FOUND path updates zero rows and skips INSERT. Create U_target profile and company-scoped role, and the accepted invitation through actual token/default/FK shape. Assert membership still absent. Use `activate()` at `oracles.py:167–257`; existing native case `actual_stale_found_no_membership` at `cases.py:36`. |
| D2 | Same company/target/actor. Global role-key fill executes with all original role preimages retained; no synthetic blank-key row is added. ROW_COUNT zero inserts U_target membership. Update the existing profile/role/invitation; preserve complete source BEGIN/COMMIT and private diagnostics. Actual both-alias and compatible membership/invitation type shapes are retained, so the reduced malformed-predicate/type failures do not apply. Sources `D2:42–44,74–155,211–241`; accepted insertion case `cases.py:64`. |
| F2 | No U_old membership/invitation/role exists, so retarget/disable branches affect zero rows. Fill the invitation's NULL alias, update shared company, conflict-update U_target membership, retain its D2 role_id, conflict-skip the existing scoped role INSERT and update role/invitation fields. Keep native BEGIN/COMMIT. F2's incompatible old-role `inactive` write is avoided by the minimum constructor, never by changing `user_roles_status_check`. Use `normalize()` at `oracles.py:355–415`, `cases.py:75–76,81,83`. |

After each committed whole file, compare complete rows/catalog against the independently composed existing oracle, bind each newly generated UUID once to its related fields, and capture its exact PK. No intermediate output is public. B0/C2 may use a transaction that commits on success; their Task15 outer **rollback** cases are not selected execution. New group success still requires hosted proof of this exact composition, especially actual B0 after P.

## Preimages, cleanup and finite trigger/sequence graph

Before P capture S0 in the private controller: the accepted portable complete catalog, every public/Auth/storage row and every sequence's `last_value`, `is_called`, **and `log_cnt`**. Reuse `scripts/sql/canonical-user-rbac-repair-catalog.sql:1–88` and `fixed-target-selftest.py:342–371` projections. Separately derive S1 = S0 plus only P's two column declarations, without executing P/X or any fixed source on the reference. Verify actual P equals S1 and changes no rows. S1, complete original seed rows and their PKs are immutable private preimages; later snapshots cannot overwrite expected values. Store current postimages/generated PKs separately. Do not use an ON COMMIT DROP table as durable storage across D2/F2.

The closed direct write set is `auth.users`, `companies`, `user_profiles`, `roles`, `permissions`, `role_permissions`, `user_roles`, `company_memberships`, `company_invitations`, `audit_logs` (`fixed-target-selftest.py:54–57`). Require all seven business relations empty, allowing only independently accepted roles/permissions/role_permissions seed rows. Require the remaining Auth credential/session relations and any reachable dependent business work empty as well; preserve every other snapshotted row. No source/constructor default or generated expression may call an unreviewed function.

**Actual trigger outcome:** Task15's accepted actual graph has no user trigger/rule on these ten tables, rejects enabled event triggers, and closes their outward FKs (`fixed-target-selftest.py:453–482`). Thus no Auth callback, company capability/default trigger or row-history writer runs in this prefix. This is a checked catalog property, not an assumption that all deployments lack such hooks. Constructor/source trigger-created work is exactly empty; B0's explicit audit row remains a captured deletion obligation. Do not bring later provisioning/job/lifecycle triggers forward with the two prerequisite columns.

Cleanup adds the incoming-FK check that INSERT/UPDATE characterization did not need: enumerate constraints referencing captured parent keys; require zero matching child rows outside the captured graph, and compute child-first order inside it. Retain all FK actions and definitions. A cascade/SET NULL action can issue a zero-row statement, so reject a reachable enabled statement trigger/rule or external write hook; do not rely on zero child rows alone. The existing operational triggers are `BEFORE INSERT OR UPDATE OF company_id FOR EACH ROW`, at `20260519_batch_6d_superadmin_tenant_governance.sql:156–174` and `20260519_batch_6d2_runtime_governance_completion.sql:387–401`; zero matching child rows cannot invoke their row bodies. Their complete registrations remain unchanged. This admits the existing empty incoming graph without disabling guards. Any newly observed action with work outside it is a concrete admission failure, not permission to expand cleanup.

**Sequence outcome:** the admitted constructor uses explicit Auth/company UUIDs. Actual source-created company/membership/invitation/role/permission/link/user-role/audit PK defaults are UUIDs, and timestamps/token defaults are the already characterized expressions: base `01_db1_schema_repair_core_helpers_and_canonical_tables.sql:339–465`, audit `02_db1_operations_ediel_billing_dedupe_and_storage.sql:751–763`, token prerequisite `20260909123000_canonical_invitation_token_prerequisite.sql:48–59,84–89`. The original files contain no sequence call; admit only the actual pinned default/generated/function graph with no sequence consumer. `auth.refresh_tokens` does have bigserial (`gridex-supabase-compatible-bootstrap.sql:133–141`), but no refresh token is constructed or written. Therefore every sequence must stay byte-for-value equal through P, constructor, each source and X, including `log_cnt`. Do not call setval on unchanged sequences: it is not an exact restoration of arbitrary `log_cnt`. Any changed sequence blocks release and disposes the target; there is no speculative sequence reset or reconstruction migration in this reachable admitted path.

X runs whole, once, in a new cleanup transaction. The controller materializes its immutable S1 preimages, exact captured PK sets and expected last source postimage into a private pg_temp envelope in the same session. Bind it to the exact owner/database, backend/transaction, ordered source hashes, completed F2 stage and one-use controller reservation. X fails without that context. Temp staging may be ON COMMIT DROP here because authoritative preimages remain in the controller and no historical COMMIT occurs inside X.

X must first match the complete last source postimage, then:

1. Delete captured audit rows, invitation rows, memberships, user-role assignments and profiles by exact PK, child-first under the verified actual FK graph. All descendants referencing a soon-deleted parent must already be absent; no incidental cascade is used for cleanup. Delete C_target by its captured PK, then the three Auth rows. In particular delete profiles before Auth/companies so cascade/SET NULL does not substitute for explicit reversal.
2. Delete only new captured role_permission link PKs before any source-created role/permission PKs. Retain every original link ID, pair, text alias, effect and timestamp; restore any changed original full rows by their original PK. Restore original complete role/permission rows, including B0 metadata and D2's global key effects; then remove only source-created role/permission rows that had no S1 identity. The actual graph must be clear of assignments/links before deleting these parents. Never rebuild original seeds by key or permit a replacement UUID.
3. Restore the exact S1 membership CHECK definition and attributes; revalidate it with all synthetic memberships gone. Preserve actor/profile FKs, all existing role/link arbiters, H2 indexes, extension identity/schema/version/owner, functions, RLS, ACLs and policies. pgcrypto and profile active_company_id/FK already exist in admitted S1, so B0 adds no retained structural delta. X creates no persistent support object.
4. Assert the exact S1 complete catalog and complete row/sequence multiset in the private controller after X commits. No synthetic Auth/profile/company/membership/invitation/role/audit row, new grant, credential/session or generated downstream work survives. Full relation snapshots also prove F2's global alias work and all old-user graphs are restored to their empty preimages. Dropped/recreated constraint comparison uses the accepted portable catalog, not database-local OIDs.

One terminal X suffices because original seeds and all source postimages remain independently known, UUID parents stay stable, C2/D2/F2 share the target graph, no old-role row causes F2's CHECK failure, and the admitted triggers/sequences produce no extra cleanup work. Do not split the group or author extra boundaries without a concrete failed dependency.

## Narrow lifecycle and private transport extension

`canonical-user-rbac-dedupe-batch.py:140–242` already owns FRESH → ACCEPTED56 → NATIVE → H2_COMPLETE → SUCCEEDED, terminal disposal and exact owner checks. Add the continuation **before** SUCCEEDED:

```text
H2_COMPLETE → FIXED_NATIVE → FIXED_COMPLETE → SUCCEEDED
any failure → TERMINAL → DISPOSED
```

The trusted scope selects continuation-required at reference preparation/fresh-target creation, before any execution; freeze that choice to the exact handle/reference identities. Historical dedupe57 uses the existing H2_COMPLETE finish. New `fixed-target` and eventual `full` scopes require FIXED_COMPLETE before finish. Do not expose a reset/rearm API, arbitrary expected snapshot, target URL, callback or caller-provided source list that could bypass the reviewed tuple.

Add one internal `continue_fixed(target, database, paths, staging)` entry in the existing lifecycle module. It requires unpublished H2_COMPLETE, the exact database, frozen continuation-required mode, exact trusted fixed runtime module/reference and actual validated StagedSources identity. It marks FIXED_NATIVE before submission, calls the pinned complete group, independently checks restored final state, then marks FIXED_COMPLETE. `finish` remains the sole release and also requires successful child exit, exact original/HOLD restoration and completed privacy checks. Every exception/timeout/native failure invokes the existing terminal disposal path before further requests. After continuation equality is established, full scope can run its preserved suffix and existing final checks; bounded fixed-target scope rejects generic post-foundation SQL. A SUCCEEDED or terminal handle can never enter this extension; repeat means a new owner/target and rebuilt prefix.

Task15's `Proof` intentionally requires SUCCEEDED and clones characterization databases (`fixed-target-selftest.py:384–404`); neither that class nor its test module is runtime authority. Put shared Task15-fix5 transport in a narrowly extracted runtime module and have its existing tests import it, retaining their behavior. Reuse its exact generated-admission memory transport, same-handle writer/source/staging provenance, readiness check, clean environment, complete-byte hashes, sanitized receipts and buffer clearing. Do not introduce global monkeypatches or generic log exemptions. New fixed-source bytes and generated constructor/preimage/cleanup input travel through private stdin; complete original inputs remain pinned, and generated sensitive input/raw stdout/stderr never become files or collector output. Preserve fix5's four existing whole-input exceptions and their provenance conditions; no filename-only exception is added. All later success/failure/cleanup paths run the same privacy boundary.

## Implementation ownership, insertion and essential acceptance

The following bounded implementation owns:

- The two retrieved actual migration files P/X; `scripts/canonical-user-rbac-fixed-target-batch.py` for trusted pins/reference/constructor/postimages/cleanup transport; `scripts/canonical-user-rbac-private-inputs.py` for the narrowly extracted fix5 adapter. Reuse existing runtime catalog SQL rather than adding another catalog format. Task15 selftest changes are limited to importing the extracted adapter and its associated controls; existing native cases/oracles remain evidence.
- `scripts/canonical-auth-provisioning-replay.py`, `scripts/canonical-user-rbac-dedupe-batch.py` and `scripts/gridex-aud-003-clean-replay.sh` for frozen scope, pre-release transition and actual loop. Insert the group immediately after H2 execute and before the former `data[57:]` suffix (`replay.py:163–181`); dispatch the group once instead of also running its files in a generic suffix loop. Keep legacy52/repair56/dedupe57 exact stopping points and their original behavior.
- The existing foundation-order/additions and history-additions manifests plus focused selection/accounting assertions to declare/pin the two actual identities and select the four whole originals. Preserve first57, old suffix order, fixture30–33/RBAC38/first43 and all immutable historical bytes/checksums.
- One continuation selftest, `scripts/canonical-user-rbac-fixed-target-continuation-selftest.py`, and its job in `.github/workflows/ops-hardening.yml`. Keep `canonical-auth-membership-group.py`'s original19 tuple and union unchanged; run the new proof separately, using existing exact-owner cleanup conventions.

The independent final reference is S0 plus P's two source-backed columns; it executes neither P/X nor the fixed originals as expected-result code. Per-source expectations reuse Task15's complete oracle logic as test evidence, with trusted runtime assertions independently implemented or extracted to a non-test module without changing their semantics. Every immutable seed ID comes from the actual preimage, not random IDs in a different reference database. The reference retains H2's exact index properties and all accepted repair/legacy catalog boundaries.

Required proof is limited to the changed boundary:

| Gate | Necessary evidence |
| --- | --- |
| Success and repeat | Standalone whole group and actual staged-shell fixed-target scope on the complete actual57 prefix; each P/original/X submitted whole once; C2 no-member → D2 inserted member → F2 update; independent per-source postimages and exact final S1. Repeat the entire success on a fresh owner/target and prove prior handle cannot reenter. |
| Admission | Dirty affected business input, changed seed/catalog/default/trigger or incoming-FK action, wrong owner/staging/hash/lifecycle stage rejects before constructor or fixed-source submission. Inject only the necessary sentinels; do not recreate Task15's branch matrix. |
| Native COMMIT failure | After D2 COMMIT and F2 COMMIT, an appended failure and a backend/controller death cannot release the handle; observe committed private work where possible, then verify exact disposal and untouched canary. Controller death uses the existing outer owned-job cleanup, never in-memory recovery. |
| Cleanup/release | A missing captured row, changed original seed/link identity, skipped restoration, catalog/sequence mismatch or X failure cannot release. Kill after cleanup COMMIT before finish; fresh retry rebuilds the prefix. Preserve `log_cnt` equality and deny stale/terminal/succeeded reuse. |
| Privacy and integration | Require fix5's final privacy gate first. At implementation head, verify private whole/generated input and outputs through success, native failure and cleanup; accepted original19 union, historical legacy52/repair56/dedupe57 proofs, mandatory quality/build and Ediel, and the new actual-loop job all pass on that same head. No extra optional characterization suite. |

Let k be the number of **retrieved, authored, independently accepted** new skeleton inputs; this contract requests P/X only. After actual generation, record exact filenames/hashes and verify that set before selection. Starting from accepted596 = 538/23/31/4 and focused342 = 295/20/23/4, final accounting is total `596+k`, FULL_FILE_SELECTED `538+4+k`, SUBSTITUTED23, UNCLASSIFIED `31−4`, EXPLICITLY_EXCLUDED4; unresolved50. Focused inputs `342+k`, selected `295+4+k`, substituted20, unclassified `23−4`, excluded4; unresolved39, explicitly include both forward files in the focused family. Foundation size is `98+4+k`; bounded endpoint is `57+4+k`, with old suffix shifted by `4+k`. Publish numeric totals only after actual identities/count are verified. Skeleton registration alone is not acceptance of B0/C2/D2/F2.

After independent design review, batch actual skeleton generation, implement this boundary and its essential gates, then continue the remaining restoration. This contract makes no full native/official-ledger replay, schema/fingerprint/types, provider/session runtime or production completion claim. Preserve R2's text[] versus later alignment RETURNS TABLE dependency for its proper downstream source-admission task.
