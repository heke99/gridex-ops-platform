# Historical user and RBAC repair admission contract — 2026-09-10

Status: **EXECUTABLE DESIGN; IMPLEMENTATION, HOSTED SQL ACCEPTANCE AND SELECTION PENDING.**

## Decision and unchanged authority

The next restoration batch is **complete R2, E2 and S2, followed immediately by one new fail-closed forward boundary W**. Aliases, hashes and all statement units are defined in `USER_RBAC_REPAIR_SOURCE_EFFECTS_2026-09-10.md`. This three-source batch can execute unchanged within one transaction: R2 supplies helpers used by S2; E2 shares the invitation prerequisite and has no target-identity prerequisite. R2 changes the role catalog and weakens the platform helper; S2 installs permissive PUBLIC policies and two sources create diagnostics. W must preserve preexisting role rows, restore the first52 platform helper and touched policy/RLS preimages, and make new diagnostic interfaces private before commit. Zero business targets alone does not secure catalog effects.

B0/C2/D2/F2 cannot successfully execute with their fixed Auth identity absent. D2/F2/H2 explicitly COMMIT internally. They have **separate concrete characterization and admission tasks below**, not exclusions, edited fragments, indefinite deferrals or a claim that errors count as complete execution. H2 is independent of hardcoded-user existence; its DML/COMMIT needs its own owned database lifecycle. B0/C2 have no COMMIT and can be characterized with genuine transaction rollback. Synthetic successful source execution does not by itself admit any of these five to the canonical selector.

Baseline is verified Task8 commit `5389f2b51dac46fd00e858a9cddb27b676f678ca`, tree `b8387fa0fef66019c6d27957f281138e45920390`; original16 and complete17/actual-loop hosted receipts remain accepted. Current595=533/23/35/4 and focused341=290/20/27/4 remain unchanged by this document. Foundation93, historical30–33, selected RBAC38, first52 and the former53–93 suffix remain fixed until the specific reviewed selection step. No repeated Tasks1–8 matrix, SQL proof or live catalog inventory is needed to author this design.

ADR-006 in `.agent-memory/decisions.md` prohibits mass replay or mass marking historical migrations live. This work never creates credentials, sends email, reads real users, alters a provider, deletes real data or writes production. Later authorized production convergence must reconcile the established `20260728170000_live_schema_code_canonical_sync.sql` repair point and actual ledger/runtime with separately reviewed forward delivery. An offline W must reject standalone invocation; it is not an executable production repair.

Skill routing and static verification scope are recorded in the companion effects document. This contract uses the accepted Task7 private runner/logging/cleanup model and Task8 actual-stage transport as interfaces, not proof that new sources pass them.

## Next implementation files and exact insertion

Implementation must create these bounded interfaces:

- `scripts/canonical-user-rbac-repair-batch.py`: strict reviewed R2/E2/S2/W batch, owned database handle only; no arbitrary SQL/database URL fallback.
- `scripts/sql/canonical-user-rbac-repair-admission.sql`, `scripts/sql/canonical-user-rbac-repair-assertions.sql`, and `scripts/sql/canonical-user-rbac-repair-catalog.sql`: owner-only context, exact row/catalog comparisons and safe output.
- `scripts/canonical-user-rbac-repair-selftest.py`: independent reference, actual first52, seeded/rejected/repeat/rollback/native/concurrency cases below.
- One **actual CLI-created** migration with descriptive suffix `_canonical_user_rbac_repair_boundary.sql`, called W here. Use the already verified pinned CLI2.101.0 hosted skeleton workflow: discover applicable help, run `supabase migration new canonical_user_rbac_repair_boundary`, retrieve the actual empty member and record artifact/member SHA256 before authoring. Do not invent its timestamp or reuse Q's empty skeleton. This is preparation; no source selection yet.

Only after independent review and complete hosted source proof may the implementation modify `scripts/gridex-aud-003-foundation-order.json`, foundation/additions metadata, history additions, `scripts/gridex-aud-003-clean-replay.sh`, `scripts/gridex-replay-input-accounting.py`, its focused-group classification and accounting/selection regressions, `scripts/canonical-auth-membership-group.py`, `scripts/canonical-auth-membership-group-selftest.py`, `.github/workflows/ops-hardening.yml`, and the actual-stage transport. The actual final acceptance must execute the **real staged shell loop**, not just the planner or standalone helper. Existing `scripts/canonical-auth-provisioning-replay.py` and `scripts/canonical-auth-provisioning-legacy-batch.py` remain the first52 construction/owned-target boundary; extend through a reviewed narrow adapter, preserving original semantics and command17.

Exact proposed order: first1–52 unchanged; R2 at53, E2 at54, S2 at55, W at56; old53–93 move to57–97 in identical order. Foundation97. Dispatch53–56 exactly once as one indivisible batch; timestamp scheduling excludes all already-selected members, including W. Reject missing, reordered, duplicated or partial group before SQL. Original16 runner command prefix and existing command17 remain byte/order exact. Append one new command18 for this batch and give it a complete exact-head hosted gate; require the all18 union, then an actual staged-loop proof including53–56.

Prospective accounting, **only if this exact group passes and is selected**: adding one W input and selecting three previously UNCLASSIFIED historical inputs yields596=537 FULL_FILE_SELECTED/23 SUBSTITUTED/32 UNCLASSIFIED/4 EXPLICITLY_EXCLUDED,55 unresolved; focused342=294/20/24/4,44 unresolved, with W explicitly included in the focused family definition. The five B0/C2/D2/F2/H2 remain UNCLASSIFIED. No source is substituted or excluded. Registering W alone beforehand adds one selected timestamp input under the existing timestamp interface but does not establish batch acceptance; record that intermediate count distinctly if published. Final source additions after this group require their own exact insertion, CLI-created boundaries where necessary and hosted acceptance.

## Admission for R2/E2/S2/W

Use one connection with `psql -X --single-transaction -v ON_ERROR_STOP=1`, separate complete `-f` files in order: admission,R2,stage marker,E2,stage marker,S2,stage marker,W,assertions. The three originals have no transaction control. Reject transaction/meta-command escape in support/W; immutable originals are validated by exact path/hash, not rewritten. Failure or backend death before outer commit rolls back this whole group.

1. Before connecting, verify exact file bytes/manifest pins, absolute owned paths, no symlink/path substitution, group order and disposable target ownership. Verify private log settings and host/socket allowlist as in Task7. No production credential or provider client is accepted. The actual-prefix lane is the accepted compatible bootstrap plus complete first52, including actual43 and actual44–52 batch, with no extra downstream helper smuggled before53.
2. In the connection acquire a database-local transaction mutex **before any catalog read or target lock**. Use the same existing mutex `(20260910,140053)` as the legacy batch when sharing that owned database; do not create competing lock order. READ COMMITTED, lock_timeout10s, statement_timeout60s. Lock touched public roles, user_roles, invitations and every present S2 target in qualified-name order with ACCESS EXCLUSIVE; lock Auth/company/profile/membership/admin-user/permission parents and all reviewed transactional trigger targets deterministically as required. Do not take a stale REPEATABLE READ snapshot before waiting. Validate all dependencies again after locks.
3. Require `public.user_roles` and `public.company_invitations` to be **empty** before R2. Empty means all rows, including inactive, NULL, orphan and unrelated tenants. Do not delete/hide/move rows to satisfy this condition. Profiles, memberships, companies, Auth users/sessions, admin_users, permissions and links may be synthetically populated but are retained full-row preimages. E2's diagnostic target-identity matches must be zero in admitted fixtures; private fixed identity diagnostics belong in characterization. There is no affected-row exception for expired/revoked invitations or unrelated role assignments.
4. R2 role seeds require additional nonempty admission. Permit the exact canonical first52 role catalog and compatible synthetic variants with unchanged legal unique keys. Reject blank/NULL keys with nonblank names that R2.1 would normalize, and reject any alias row that R2.1 would remap. This prevents intermediate key collisions and semantic identity changes. Existing canonical seed keys, noncanonical custom keys and NULL keys with NULL/blank names can be retained if constraints permit and complete preimages are captured. Reject ambiguous role key/name identities and duplicate non-NULL keys before source execution. R2.2 may update existing seed metadata; W must restore it exactly.
5. Require actual first52 relation kinds/owners, columns/types/defaults/nullability/PKs/FKs/checks/indexes and no unknown row/DDL triggers or rules. Require roles key arbiter and user_roles identity constraints. No replacement minimal table, disabled trigger/RLS/FK, generated touched column, unknown domain cast or privilege bypass is admitted. Record actual presence of **all17** S2 targets; require ordinary/partitioned table plus correctly typed company_id for each eligible target. Missing target legitimately follows source skip; a present wrong-kind/type target rejects. Do not create absent business tables merely to drive branches in the actual-prefix lane.
6. Snapshot all existing role fields by id; permitted existing columns; platform helper full definition/options/owner/ACL; every touched relation's RLS/FORCE RLS/ACL; all same-name S2 policy preimages including authoritative absence; complete catalog fingerprints and non-target row multisets. Owner-only temporary context uses ON COMMIT DROP and binds current database, backend, transaction, fixed ordered hashes and stage. Context is an owner-run protocol assertion, not an application authorization token. Unexpected trigger write targets reject, particularly roles UPDATE triggers that would make preimage restoration create history.
7. First52 has no gridex_get_user_roles, gridex_table_has_company_id, gridex_debug_batch2_rbac_v or gridex_debug_batch2_tenant_policy_gaps_v definition. First admission requires their absence. Repeated batch admission allows only the independently proven exact post-W definitions and ACLs; unknown same-name objects, return type, view shape, owner or dependency graph reject. For the first52 platform helper require the exact selected41 authority. The repeated lane checks the same restored authority. Do not catch CREATE OR REPLACE return-type failures and continue.
8. Run all three originals, including every diagnostic SELECT, to completion privately. Mark each completed stage in the same transaction. W requires S2 stage and matching context/txid; absence/mismatch rejects before mutation. Assertions must finish before commit and prove every allowed row/catalog delta below. No partial source success receipt, role-restoration warning or ignored SQL error permits continuation.

This is an isolated admission boundary, not a new rule that runtime tenant-role assignments or invitations must be empty. It is not permission to query production to find an empty window.

## W's exact forward obligations

W contains no BEGIN/COMMIT and no standalone fallback. Its design is deliberately a source effect repair with explicit preservation, not a whole invocation of another historical file.

| Object | Required committed state and independent authority |
| --- | --- |
| Preexisting roles | Restore every preexisting field exactly by original id, including NULL/false, key/name/description/scope/is_system/is_system_role/is_active/timestamps and any admitted extra field. Do not recreate IDs or change role_permissions/user_permissions/overrides. If an admitted old shape genuinely lacked a new column, separately define its source default; the actual52 lane already has is_system and must compare it exactly. No extra role trigger effects may survive. |
| New role seeds | Only missing keys among R2's exact16 tuples may be inserted; source-selected UUIDs are structural seed identities and have zero assignments/permission edges. Preserve exact source tuple metadata/default fields, with generated UUID/timestamps mapped privately into reference comparison. In canonical first52, determine actual missing set from the independently constructed reference, never assume all16 missing. Repeat adds no rows. |
| Platform-admin helper | Restore complete selected41 `20260520_batch_6e_hard_platform_roles_only.sql:5–21` catalog preimage, including options, owner and explicit/inherited effective EXECUTE boundary. Do not execute41's role-permission DELETE again or substitute its function fragment for a full-source selection claim. R2's historical admin_users branch must not survive this insertion. |
| New role-lookup helper | Keep R2's source-defined text[] body only as a private diagnostic compatibility object: remove EXECUTE from PUBLIC, anon, authenticated, authenticator and service_role, plus any inherited client path; owner retains capability. It is not newly enabled as canonical user authorization. Do not silently grant service role merely because current app code has a lookup caller; later runtime-function convergence must settle the table-return winner and reviewed caller access together. |
| New table-column helper | Keep S2's exact stable invoker body; revoke new direct client/service execution through the same role/inheritance boundary. The private policy-gap view is owner-readable. This helper is not a newly public catalog API. |
| Two new debug views | Keep complete source projections; set security_invoker=true and revoke all direct/column SELECT and write privileges from PUBLIC/anon/authenticated/authenticator/service_role and inherited client paths. Owner-only diagnostics. Assert definitions, reloptions, owner, grants and dependency identities. No new public API is inferred. |
| S2 touched policies | Restore each of its51 possible policy identities to the **exact preimage**, including absence, command, roles, permissive flag, USING and WITH CHECK. If absent before, drop only that newly created named policy; no CASCADE or blanket policy reset. Restore each touched table's exact preexisting RLS/FORCE RLS flags and grants. No altered or new permissive PUBLIC policy may remain. Existing independent policies are untouched. |
| Benign structural effects | Retain R2's correct user_roles_user_active_idx and E2's email/invited_email columns when absent under an explicitly admitted reduced catalog. Selected50 `20260528_final_user_access_schema_safe_repair.sql:44–45` already declares the same index on(user_id,status,is_active). Actual52 comparison must establish its presence and exact definition; same-name IF NOT EXISTS is not enough. Existing column values remain exact since the admitted business tables are empty. No DROP of additional constraints/indexes. |
| Existing verified boundaries | Preserve Q's actor/profile FKs, invitation token/hash/status, profile action constraint, G/R diagnostics, RBAC38–41 and all unrelated function/trigger/policy/ACL/catalog objects exactly. No new permission grant, membership, invitation, credential, session, audit event, intent/job/outbox or notification may survive. |

Independent oracle: construct a separate compatible database using complete first52 **before** moving source files into staging. Build its expected safe delta using reviewed source R2 seed tuples and source declarations plus selected41 catalog authority and baseline policy absence/presence; do not import W or parse W's own SQL into expected results. Source copies must be independent immutable bytes. Use portable catalog projections patterned on `scripts/sql/canonical-auth-provisioning-legacy-catalog.sql`, normalizing database-local OIDs by named identities. Catalog and data expected-state construction must not call the function under test to calculate its own answer.

## Exact rows, identity and work preservation

Use bidirectional EXCEPT ALL over complete row representations with explicit treatment of added columns. Counts/hashes alone do not prove identity preservation. Compare catalog separately. Snapshot values stay private in owned database or mode0600 files; publish case labels and SQLSTATE only.

| Relation / identity | Before→after admitted R2/E2/S2/W |
| --- | --- |
| user_roles / id and existing company/user/role uniqueness | Empty→empty. NULL activity, orphan company/role, duplicate, global, multi-company and multi-role rows all reject before R2 rather than normalize. |
| company_invitations / id, company_id, token/hash identities | Empty→empty. No alias fill, acceptance, expiry/revocation change, actor binding or durable delivery. Any populated row rejects, even both aliases NULL or already equal. |
| roles / id, key | Exact preexisting multiset plus only the proven missing16-key subset as above. Role keys and IDs are not reconstructed from names. No false→true or NULL→active state persists on existing rows. |
| companies / id; memberships / id,(company_id,user_id); profiles / id | Exact full synthetic multisets, including multi-company membership, active company, status, actor IDs and metadata. No assignment, tenant status/name/contact mutation. |
| Auth users/sessions and credential fields | Exact full synthetic multisets and schema; no provider lookup, password/OTP generation, sign-in, refresh/revoke or email confirmation operation. Retained synthetic profiles may be inactive; group must not activate them. |
| permissions/role_permissions/user_permissions/overrides | Exact identity/value multisets, including custom platform-related permissions and links. R2 seed insertion is not a grant. |
| audit_logs, company_user_audit_journal, auth_email_events, auth_provisioning_events, domain/history/outbox/job/intent/notification targets | Exact preimages, including tenant, actor, entity, customer and timestamps. Discover actual prefix trigger targets before admitting; reject unknown/external work. No database-notification delivery on rollback; no new notification expected on success. |
| S2's17 business tables | Exact full synthetic rows including cross-company and ownership values. Only source-intermediate policies/RLS may change, restored by W. Compare non-target company rows and canaries as well. |

## Proof matrix for the three-source batch

All cases below are future actual hosted PG17 checks, not claims of execution in Task9.

| Lane | Required observation |
| --- | --- |
| Actual prefix | Compatible bootstrap + complete accepted first52→whole R2/E2/S2/W→commit. Compare independent full rows/catalog. Enumerate all17 target-presence branches. Then run actual staged shell53–56; verify originals restored, no duplicate timestamp invocation and no ledger/artifact claim. |
| Seeded admission | Two synthetic companies/users plus preserved profiles/memberships, inactive/custom catalog roles, permissions/custom links, audit/session/job canaries; required two business targets remain empty. Existing role metadata and every canary remain exact. Missing seed keys are added without assignments. Repeat the complete group: no additional rows, exact final catalog. |
| Dirty data | Each populated user_roles/invitation category, alias-normalizable role key, conflicting canonical seed identity or unsupported trigger rejects before R2. Include duplicates/NULL/orphan/multi-role/multi-company shapes in suitable reduced fixtures; do not relax actual-prefix constraints to make them admissible. |
| Dirty catalog | Wrong role-key arbiter, wrong index definition under expected name, missing required relation/column, wrong view return shape/owner/grant, unexpected trigger/rule, generated touched column, domain/enum mismatch and S2 target non-table/wrong company_id type reject with exact preimages. |
| Whole-source native characterization | Reduced rollback-only fixtures exercise R2 alias collision/NULL role activation, existing table-return function incompatibility, E2 NULL-alias fill/conflicting aliases and diagnostic missing-column failures, S2 missing helper/wrong company_id type and actual PUBLIC policy semantics. Execute complete file; do not submit extracted DO children as acceptance. Preserve native primary SQLSTATE privately; do not force an expected code until source position is verified. |
| Atomicity | Inject synthetic sentinel after R2, after E2, after S2, inside W and after W before final commit; snapshot equality for full group. W alone/missing/wrong/stale context and reordered group reject. Kill backend mid-batch and verify rollback. Use actual trusted test markers rather than arbitrary SQL callback injection. |
| Concurrency | Observe two real whole-group contenders serialize on the mutex before relation locks; second uses fresh admission. Independently hold a target/catalog-DDL lock and observe55P03, not an injected error. Commit a dirty row while second waits, then prove it rejects after acquiring lock. No repeated retries masking an unsafe winner. |
| Effective security | Full role/inheritance/column grants compared before/after; new diagnostics deny anon/authenticated/authenticator/service_role including inherited paths; baseline platform helper behavior restored for active/NULL/inactive/global/tenant-bound cases. No claim that first52 is final session/lifecycle security. |
| Logs and teardown | Expected native, synthetic DETAIL/CONTEXT/statement/parameter and unexpected cleanup failures never expose raw content. Owned database/private socket/log teardown succeeds without touching a canary database. Timeout/backend death performs exact owned cleanup. |

## Private fixed-identity characterization: B0/C2/D2/F2

Create `scripts/canonical-user-rbac-fixed-target-selftest.py` and private fixture support under `scripts/sql/` in a **separate subsequent reviewed implementation**. Four independent owned databases/cases prevent B0's global grants or D2/F2's internal commits contaminating another result. Use the accepted managed-compatible Auth schema and actual first52 plus the proven three-source safe boundary when its helper/catalog dependencies are needed. Reduced fixtures are additional named branches; they never stand in for this complete actual-prefix lane.

Fixture identity protocol: the test code defines only symbolic U_boot/U_target/U_old/U_actor/C_target slots and synthetic unrelated identities. In the private disposable database, bind the fixed slots from the already pinned source constants in memory; where exact email equality is required, use that source-bound value solely on a newly created provider-free synthetic Auth fixture row. Never copy these literals to test code, logs, reports, artifacts or command arguments; no lookup to a real account verifies them. These database rows are fabricated test stand-ins, not imported users. Use no password/credential value or email provider; Auth sessions remain absent unless unrelated synthetic session preservation is explicitly tested. Reject any target that was not freshly created/owned or already contains one of the reserved fixture slots. If fixture defaults require credentials or unreviewed provider effects, stop that case with the exact dependency; do not fabricate credentials or alter auth-provider behavior.

| Source | Executable independent next cases and required postimage |
| --- | --- |
| B0 | Start with no U_boot and execute whole file inside genuine outer transaction: expect guard failure and verify extension/CHECK rollback. For successful characterization seed synthetic U_boot with non-NULL email; canonical role-key/permission-key/link arbiters, legal company status, membership pair uniqueness and profile/audit shape must exist. Exercise no matching company→insert; one slug/organization match→update; conflicting two matches/equal timestamps→characterize ambiguous winner without approving it. Exercise optional roles.is_system, each of four user_roles status/is_active shapes, optional permissions/link/profile/audit relations and audit company_id column. Assert two unscoped role assignments, all-permission super link, exact20-key company-admin links, CHECK narrowing, profile/active-company change and explicit audit row plus actual trigger effects. Repeat retains assigned IDs but adds audit/history as source dictates. Entire populated result rolls back or database is destroyed. |
| C2 | Seed synthetic exact U_target/email, U_actor and C_target; verify actor FK subjects are actually created, with no inference that fixed actor is authorized. Existing membership success→exact updates; no-membership with membership_role column→whole-file success may still leave membership absent due stale FOUND; this is a characterized defect, not a repaired successful activation. Column-absent branch tests actual INSERT. Test supported enum fallback, missing label/type failure, role key/name ties, target-user vs invited-email collision, target/NULL/other-company role rows, expired/revoked/conflicting aliases, each invitation email-column branch, missing token/default/actor FK and existing accepted row. Full multisets include company/profile/role/membership/invitation/trigger work; rollback all. |
| D2 | Seed exact U_target/email and U_actor; C_target may be absent because source upserts it. Run whole file with its BEGIN/COMMIT preserved using dedicated unpublished database. Verify ROW_COUNT insertion succeeds where C2 skipped; test unrelated blank role-key updates and NULL role-text overwrite with differing role_id. Test both aliases/email-only success; invited-email-only malformed predicate native failure; missing expires_at; mismatched invitation-vs-membership enum type; optional metadata/actor/key/time columns and no invitation insertion when none match. Run whole file twice and compare source-defined timestamps/notice counts privately. After internal commit, deliberately fail appended diagnostics/assertions and kill backend; committed effects remain inside disposable DB and **must be observed then destroyed**, never called rolled back. |
| F2 | Seed correct synthetic Auth identity and C_target/company-admin dependencies; seed U_old/U_actor only where required by actual FKs/rows. Test no old rows; old-only membership→retarget; both old+correct membership→native unique failure before upsert; old invitation retarget; other-company old rows preserved; NULL-company target roles moved; old-company roles disabled; inconsistent role_id preserved; missing role_id on inserted membership; all email-alias combinations and enum heuristic/fallback/no-label cases. Test missing memberships at early regclass cast separately from later optional guard. Whole file retains internal COMMIT. Postcommit sentinel/backend death must show committed private state before owned DB destruction. Never accept canonical invitation/role semantics from this source's success. |

For each successful characterization obtain exact pre/post multisets keyed by original PK, not just counts: Auth/company/profile, role/permission/link IDs, membership actor/time/role fields, invitation status/token/expiry/revocation/actor, audit/domain/journal/outbox/job rows, and sequence state when implicated. Source-generated UUIDs are captured privately and related to explicit source effects, not normalized away indiscriminately. Errors on dirty fixtures remain native-source characterization, not preflight success. No fixed-target execution can be authorized by an observation taken in another database/session.

Before-source rejection cases apply to every fixture runner: wrong database owner/name/host/port/socket, nonfresh fixture reservation, unpinned source, public logs, active network/external trigger or unreviewed FK/trigger target. Reject before sending source bytes. Source-derived sensitive literals never flow through the original16 cleanup path.

## H2: independent transaction-owning task

Create `scripts/canonical-user-rbac-dedupe-selftest.py` as the next independent proof after the three-source batch; it does not require B0/C2/D2/F2. Use an owned unpublished compatible database with actual first52 and the accepted safe53–56 group if selected. Whole H2 can succeed with user_roles empty; no fixed Auth target must be created for its final diagnostic. This is a true successful zero-DML source execution, but its two new indexes are real catalog changes.

First prove empty→empty with exact valid/ready unique expression indexes and unrelated Auth/company/role/session/audit canaries unchanged; repeat complete H2 preserves rows/indexes. Check all compatible first52 preexisting indexes/constraints: reject a wrong same-name index before H2 rather than accepting IF NOT EXISTS. The final independent oracle constructs the two exact H2 index definitions from original source, not implementation output. User-role population is rejected before H2 in any prospective clean-replay admission.

Then characterize populated fixtures in separate disposable databases: duplicate text/different IDs, duplicate role_id/different text, text-pass removes a candidate otherwise preferred in second pass, case differences vs whitespace differences, equal timestamps with distinct UUID tie-break, NULL timestamp ordering, NULL status/is_active versus explicit inactive, NULL role text/id/user/company, sentinel company collision, two real companies, two different legitimate roles, orphan relation shapes and dependent FK cascade/restrict/trigger history. Compute expected deletion survivors independently by the two sequential ranking passes, preserving complete row identity and every dependent effect. Unique-index NULL user behavior must be tested separately from window grouping. These commits are disposable characterization, never accepted data cleanup.

Concurrency is warranted by dedupe+unique-index creation: use a separate-session writer/index lock contender, observe actual blocking/55P03 or native uniqueness failure, and prove no externally published partial database. A failure **before H2.6** rolls back its current transaction; a failure after H2.6 leaves committed deletes/indexes. Test both, inspect privately, then drop only the owned database. No outer transaction, ON COMMIT DROP preimage or final assertion is allowed to pretend it spans the internal COMMIT.

A subsequent selection proposal for H2 must be reviewed separately: retain first56, insert whole H2 at57 ahead of old suffix57–97, total98, if its immutable no-repair empty result is accepted through the owned-database lifecycle gate below. This no-new-migration step would yield596=538/23/31/4 (54 unresolved), focused342=295/20/23/4 (43 unresolved). If catalog repair is needed, first obtain its actual CLI skeleton and recompute the extra input/order; do not use these counts for a larger group. No H2 selection is performed or implied now.

## Lossless empty canonical continuation for the fixed-target files

Synthetic characterization is deliberately not the final restoration contract for B0/C2/D2/F2. The concrete following task is to implement and independently prove an **unpublished owned-database lifecycle**, because internal COMMIT prevents shared-database transaction atomicity. This gate must exist before any such file can join the clean replay.

1. Construct a fresh owned database from the accepted full prefix, with no application connections/provider/network hooks. Require all affected business tables and their enabled-trigger write targets empty, apart from independently snapshotted immutable catalog seeds. Reject any populated business input before seed/source submission. Preserve actual canonical constraints, RLS, triggers and role identities. No external/live database is eligible.
2. Capture independent complete catalog and row/sequence preimages in the private controlling process or separately owned private store; ON COMMIT DROP data cannot survive D2/F2. Seed only the minimum synthetic fixed slots through a reviewed constructor. Enumerate constructor effects as well as source effects, including Auth callback/profile creation, company trigger defaults/capabilities and audit/history. Missing prerequisites must be added through an actual reviewed canonical prerequisite, not table fragments or disabled guards. Pin an exact allowed dependency/write graph before execution.
3. Execute one complete source at a time with its original transaction control. B0/C2 can still use an outer transaction internally; D2/F2 commit inside the unpublished DB. A source reaching native failure does not count as selected completion. Follow with a newly reviewed forward cleanup/reconstruction if necessary, created from an actual CLI skeleton. The cleanup must restore **all** preexisting catalog values/IDs/rows and remove only exact constructor/source-generated synthetic rows by captured PK, in FK-safe order. Never broad-delete by email/company/status, TRUNCATE, CASCADE, disable triggers or restore a dump to substitute for source execution.
4. B0 specifically needs exact roles/permissions/role_permissions preimages, removal of its synthetic unscoped assignments/company/membership/profile/audit graph, original membership CHECK and actor/profile FK boundary. C2 needs company/profile/membership/invitation/role/activity/actor/timestamp/metadata and trigger graph reversal, including successful whole-file behavior that does not create a membership. D2 additionally needs global unrelated role-key preimages and a post-COMMIT cleanup. F2 needs both old/correct identity graphs, global email-alias preimages, company name, any disabled role and invitation expiry/status preimages, also post-COMMIT cleanup. Their fixture-created Auth/user sessions/credentials remain absent at final publication.
5. Compare exact independent final preimage plus only explicitly reviewed structural deltas; all synthetic row identities and downstream work disappear, no original data is lost, no newly granted permission or session remains. Sequence last_value/is_called and extension/catalog effects must be accounted for where changed. If enabled triggers make exact cleanup nonconvergent, that exact trigger/source effect blocks admission; implement its reviewed lossless reconstruction and new proof before continuing. Never claim effect-equivalence or partial extraction as full execution.
6. Only the controlling process may release the owned database handle to the next replay stage after final comparisons. Failure, crash or incomplete cleanup destroys that database and reconstructs the prefix on retry. Prove process death after each internal COMMIT and after cleanup/before publication cannot release an unverified handle. A distinct canary database remains untouched. This offers database-lifecycle isolation, **not PostgreSQL rollback of committed work**.

The fixed-target selection task must first produce an exact reviewed source order and individual forward boundary identities after the then-verified prefix; no guessed timestamps/counts are authorized here. Required source-backed resolutions are finite: B0 CHECK/role-permission/audit reversal; C2 FOUND-success postimage plus invitation defaults; D2 predicate/type shapes and global-key reversal; F2 identity-collision/retarget graph; constructor/trigger effects; lifecycle publication crash gate. Each has a named implementation/proof above. If an immutable file has no successful state compatible with a lossless admitted constructor, record the exact native error/constraint and repair that prerequisite in a reviewed forward boundary; leave that file UNCLASSIFIED until actual success. No blanket exclusion or production replay is the fallback.

## Completion gates and nonclaims

Implementation sequencing is continuous: actual W skeleton→R2/E2/S2/W implementation and independent review→complete hosted proof→exact selection and actual-loop/all18 union→H2 and fixed-target independent characterization→owned-database lifecycle plus source-specific restoration→reviewed individual selection. No execution-method permission pause is required. Every stage retains immutable original bytes and publishes only after its applicable independent reviews/hosted gates.

Task9 verifies only static complete-source pins/units/branches, implicated object/caller dependencies, accounting arithmetic and document whitespace/literal safety. No synthetic tests mirroring this prose, local PostgreSQL probe, hosted rerun, SQL acceptance assertion, schema/types regeneration, production/ledger mutation or new credential is part of this commit. Native CLI/genesis/official-ledger full replay remains distinct from compatible fixture proof and from the prefix-only actual-loop lane. Do not refresh schema/fingerprint/types from an incomplete replay. Later final-chain RLS/helpers/session/provider/durable-delivery behavior, runtime database binding, live convergence and the system data-integrity acceptance contract remain open.
