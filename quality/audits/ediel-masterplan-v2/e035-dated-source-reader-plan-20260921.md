# E035B — dated received-source reader, actual UTILTS integration

Status: IN_PROGRESS / TEST_FIRST. Base actual accepted main5dc072d3127f386fb8a0a81ad5fbb34af290b4cf, tree d08521f733dfb08ae2fd47ed5f64e51ae3b002c6. PR367 acceptance5765219627 supersedes its pre-merge memory; source sealing must not be repeated. This is the next concrete READ path, not another source-sealing repair.

## Source and existing owners

U25-A-4 section3.6.8 relates UTILTS E66 register identities to received PRODAT Z04 and applicable Z06/Z10; appendix2 p132 requires E61/E62 comparisons against received structure, not incoming UTILTS observations. P26.A (2026-06-30) p50 distinguishes Z04 contract-start field210/DTM92 from Z06/Z10 change-validity field216/DTM157, both minute203. Pp76 distinguishes current224/MG from old225/Z02. Existing prodatDateFields.ts and render/dates.ts own strict calendars and fixed standard time UTC+1; existing parser.ts/parsedProdatObjects/prodatRegisterGroups own object+agency, register chain and local259 field. Reuse these owners, not a second parser, inferred register count, cached JSON or a duplicated PRODAT storage table.

Root inspected the supplied primary-document native text/page images for these clauses and current exact source, but did not rehash original PDFs. Frozen source manifest and prior qualified field/source work remain unchanged. Lexical new fixtures test this finite reader, not full national message validity.

## Finite boundary: candidate evidence is not yet authority

Add a real read-only service used by individual inbound UTILTS processing after canonical matching. It loads the already linked, received PRODAT source rows, verifies their byte seal and actual wire identity, and exposes dated object/register candidates in normalized diagnostics. It does NOT select an authoritative current version, establish accepted disposition/completeness/supersession, compare E61/E62, change guide/ACK/ingestion decisions, or certify transport. Every result explicitly says authorityStatus=not_established and selection=not_performed. The operation is useful independently: it supplies actual source IDs/hashes, legal parties, physical object/register positions and correct minute dates for the subsequent acceptance/timeline step.

The retrieval universe is explicitly `linked_received_sources`, not complete market history. Existing mutable linkage/environment/family labels, unlinked/multi-object applied cases, deleted sources and generic validated/applied flags cannot silently become proof of complete expected inventory. This limitation is carried in the typed result and docs; do not call an empty query a valid empty register set or `inspected` a functional acceptance.

## Read contract

1. Missing trusted company, invalid environment, invalid receipt timestamp, absent/ambiguous real wire transaction/LOC172 object+agency or unambiguous matched internal metering point: no database query for that unresolved scope. Parse original UTILTS wire via existing canonical AST, not cached payloads. Legal MS/MR identities must come from their single header NAD with SVK/260 components, not technical UNB or row labels. No parser fallback.
2. One batched SELECT via tenantDb(companyId), always exact environment, inbound, EDIFACT, PRODAT metadata, matched metering-point IDs, and receipt cutoff. No per-transaction query loop or `latest` lookup. Request exact count, at most101 rows for a100-row diagnostic budget; require complete, unique results in that bounded query before projecting. Missing count, truncation, duplicated IDs or count overflow yields incomplete, never a selected prefix. RPC/schema grants are unchanged.
3. Defensively reject a response containing another company/environment/point/direction/family/standard or post-cutoff row before parsing or returning any row identifiers. Read errors yield a fixed non-sensitive local diagnostic, not a national rejection or an exception that claims the existing meter-value operation failed.
4. For each remaining source, require nonnull exact raw bytes and matching lowercase SHA256 (PR367 seal). Reparse original wire with the existing PRODAT parser and canonical AST. Support single physical message only in this increment; multiple/mismatched envelopes stay unavailable, never first-message fallback. Match actual legal FR/DO against incoming legal MS/MR, and actual object ID+agency against the exact matched UTILTS scope. Technical intermediary IDs are not legal identity. Require valid canonical register grouping, not merely positive registerCount.
5. Extract field210 for Z04 or216 for Z06/Z10 using existing date owner. Keep optional contract-start separately on change messages; never use created/received/message date as effective date, never coerce malformed or duplicate dates, never replace change time by future contract start. Convert the valid standard-time minute to UTC with the existing fixed+01:00 semantics. Preserve current/old meter identities and each actual register's field259 with source order; do not infer missing identities or physical inventory from observations. Retain all dated candidates, including equal effective times and future changes; presentation sorting is not supersession.
6. Store a minimal evidence projection at normalizedMeteringPayload.receivedStructuralSources through the actual processor's existing diagnostics path. Do not copy raw source/customer/address payloads, database error details or received JSON into the report. Source read failure, missing authority or conflicts do not alter existing ACK/runtime/transaction-persistence decisions in this preparatory increment.

## Acceptance and workflow

Publish public-processor tests FIRST, observe actual ordinary CI failures at the missing diagnostics/read behavior, not nonexistent module imports. Existing actual parser/runtime/processor are used; only external database/matching/status/ACK boundaries are substituted. Query mocks deliberately return hostile rows independently of filters to prove defensive isolation. Cover correct source dates, legal parties, agencies, source hashes, duplicate/partial histories, wrong tenant/environment, future receipts, malformed dates, partial changes, multiple registers/objects and no national outcome change. Existing5222 assertions remain unchanged. Add direct boundary controls as review identifies concrete risks.

Independent SOURCE/DESIGN/ORACLE review before implementation. Then exact-head ordinary CI, completed independent TASK/SPEC, QUALITY, TENANT-BOUNDARY and WHOLE-PR review, expected-head guarded merge, actual-mainfull73/73 plus allOPS and inspected report/JUnit/hash/commit receipt. No silent gate changes, forced merge, temporary workflow or live operation. Local workbench is verified PR367 source artifact, without GitHub DNS/dependencies; no local full-suite run is claimed.

Skill routing: bounded code/consumer acquisition, writing/executing plan, spec-to-code trace, TDD, tenant query review, differential/false-positive inspection, independent requesting/receiving review, verification-before-completion and branch finishing. Debugging and generated/property cases activate on concrete failures. No UI, Next rendering, database migration, broad audit, infrastructure, deployment or performance-remediation campaign is in scope; no unavailable tool execution claimed.

After this reader: independently accepted source-context/receipt qualification, complete relevant source discovery, dated timeline and supersession; then E61/E62/guide/ACK/persistence integration and other F3 groups. D110/110+10/10 retained, fullE035/F3/masterplan incomplete. PR310 remains paused e961135199f292b8210884f07de3b616a670161a, untouched/excluded.
