# PR368 — source/design review boundary resolutions

Status TEST_FIRST, no production code. Review5765535492 accepts the non-authoritative source interpretation but requires additional query/wire/outcome evidence. Original49-case suite remains unchanged. A second public-processor suite now observes the real tenantDb wrapper, exact101-row limit, two independent wire transactions/one batched query, production environment, uppercase seal, invalid incoming MS/MR/LOC agency/cardinality/multiple-message boundaries, and exact receipt microseconds.

Paired cases retain the real parser/runtime and compare all pre-existing result, status/failure, ACK arguments/IDs, dispositions/persistence payloads, events and ingestion invocation arguments across empty candidate, source candidate, read-error and truncated-reader outcomes. Only receivedStructuralSources may differ. Separate rejected September and accepted October fixtures assert original E19 and ingestion behavior, so a pair cannot both silently switch branch. Ingestion I/O is mocked: this is actual decision/call-contract proof, not a physical database commit certificate.

The reader must finish before transaction persistence and diagnostics must be composed before that call. No reader evidence is added to national ACK decision inputs or the existing transaction RPC schema: those remain unchanged. The same report goes through existing normalized status/event/ingestion diagnostic payloads. Source-loading results are not a dependency of business acceptance.

Add only strict inverse prodatMarketMinuteToUtc to the existing prodat/render/dates.ts owner. It rejects nonexact/invalid minutes, uses fixed+01:00, handles Gregorian early years without Date's1900 offset, and is not host-zone dependent. Existing date APIs stay unchanged. No private date converter in the reader.

The optional diagnostic SELECT has a2-second wall-clock budget. A hung read returns a fixed read_failed report and cannot indefinitely delay existing ACK/processing. A late completion is discarded, not a later status/business write. This read-only bound is not permission to weaken authoritative writes or classify missing source as E61/E62. Parse budgets likewise mean diagnostic incompleteness, not national wire-size restrictions.

Actual original64389783 ordinaryOPS35638818984/job106462840903 finished2026-09-21T18:33:06.156Z:5271total,5222PASS/49FAIL;327files326PASS/1FAIL. Every failure reaches the public processor and observes the missing receivedStructuralSources field; no import/type/setup failure. All5222 pre-existing casesPASS. This is RED evidence, not corrected-headGREEN. Supplementary tests are published first and require their own terminal result. FinalCI/review/guardedmerge/main73+OPS remain required. PR310paused;fullE035/F3/masterplan incomplete. Prior source/acceptance/completeness/linkage caveats remain.
