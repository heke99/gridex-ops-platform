# Outbound Z08 unknown-wire dispatch fence — 2026-09-25

Independent read-only review of integrated PR #372 found a fail-open boundary in the actual SMTP path. `sendCorrectionFencedEmail` recognized a raw `BGM+Z08` while the row code could still be stale `Z01`; with a missing final terminator, the private SQL tokenizer returned NULL. `gridex_outbound_dispatch.mutate_v1` then returned `scoped:false` before sealed-original validation, and the callback allowed SMTP without an original/attempt/entry receipt. Conditional send guards did not cover every such row. The adverse effect is an uninstrumented external provider call and possible duplicate retry; this is a code blocker for the integrated PR, independent of E035 rollout acceptance.

The focused unit test `__tests__/ediel-correction-outbound-scope.test.ts` was RED before the fix: it resolved with a provider acceptance when an unscoped database receipt should have rejected. After the change it passes 2/2, including a valid non-Z08 control. The wrapper now sends every outbound PRODAT original through the database classifier and rejects `scoped:false` for a locally recognized Z08. Forward migration `20260925233000_e035_outbound_unknown_wire_fence.sql` preserves the existing private function and permissions while classifying unparseable or BGM-less outbound PRODAT as potential; sealed-original validation rejects it before any attempt or send. The native SQL test in `scripts/ediel-closure-wire-native.test.ts` creates only synthetic test rows and checks rejection plus zero original receipts. This native case has **not yet run**; the forward is not qualified until clean replay and generated-contract parity pass on the exact published head.

Local checks: focused unit 2/2; migration integrity and full `db:migrations:check`; app/test/script typechecks; scoped ESLint; large-file budget and `git diff --check` PASS. No hosted DB, SMTP, staging, counterparty or market send. Prior integrated head `0fbe6a5a` had all applicable ordinary workflows SUCCESS, including OPS `36190502740` verify, quality and clean replay; crawler was skipped. Those results precede this correction and do not qualify it.

Remaining boundaries: process archive retention/purge decision, pre-epoch `complete:false`, deferred staging/pilot, and automatic production deployment on a main push. Full E035/masterplan acceptance is not claimed. After exact-head native and independent diff review, the next implementation rule is S01–S07 BGM/C002/1131 = `SVK` through final UTILTS disposition/ACK.
