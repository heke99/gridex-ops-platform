# Outbound Z08 unknown-wire dispatch fence — 2026-09-25

Independent read-only review of integrated PR #372 found a fail-open boundary in the actual SMTP path. `sendCorrectionFencedEmail` recognized a raw `BGM+Z08` while the row code could still be stale `Z01`; with a missing final terminator, the private SQL tokenizer returned NULL. `gridex_outbound_dispatch.mutate_v1` then returned `scoped:false` before sealed-original validation, and the callback allowed SMTP without an original/attempt/entry receipt. Conditional send guards did not cover every such row. The adverse effect is an uninstrumented external provider call and possible duplicate retry; this is a code blocker for the integrated PR, independent of E035 rollout acceptance.

The focused unit test `__tests__/ediel-correction-outbound-scope.test.ts` was RED before the fix: it resolved with provider acceptance when an unscoped potential Z08 should have rejected. The wrapper now rejects that anomalous receipt. A separate RED showed the first version would block valid Z08 LK without Z25; the second forward `20260925234000_e035_outbound_lk_scope_reason.sql` gives only that canonical exemption a database-owned reason which the wrapper accepts with the matching profile. Forward `20260925233000_e035_outbound_unknown_wire_fence.sql` classifies unparseable or BGM-less outbound PRODAT as potential once the dispatch callback is entered; sealed-original validation holds it before an attempt or send. Unrelated non-Z08 originals retain their existing direct transport path. Native SQL cases use synthetic rows to check the malformed actor gate and the LK reason, both with zero original receipts. These native cases have **not yet run on the final correction**; clean replay and generated-contract parity are required.

Published intermediate head `9b62d5d9` had tenant, Ediel and browser CI success; its full E2E coverage job `108259269158` failed nine cases. The first error was `outbound_dispatch_callback_missing` for a non-Z08 send after the broad client classification; eight related/static cases also failed. The local candidate restores the existing non-Z08 direct path and passed those exact affected files plus the focused scope test: 52/52 in four files. The LK control is 3/3 after its RED. Earlier migration integrity/full `db:migrations:check`, app/test/script typechecks, scoped ESLint, large-file budget and `git diff --check` passed before this follow-up; rerun final checks before publication. No hosted DB, SMTP, staging, counterparty or market send. Prior integrated head `0fbe6a5a` had all applicable ordinary workflows SUCCESS, including OPS `36190502740`; those results precede the correction and do not qualify it.

Remaining boundaries: process archive retention/purge decision, pre-epoch `complete:false`, deferred staging/pilot, and automatic production deployment on a main push. Full E035/masterplan acceptance is not claimed. After exact-head native and independent diff review, the next implementation rule is S01–S07 BGM/C002/1131 = `SVK` through final UTILTS disposition/ACK.

Follow-up review of the published LK correction found that a mutable LK profile and Z23 token alone could exempt a row whose physical BGM was another message code. Forward `20260925235000_e035_outbound_lk_wire_identity.sql` requires outbound PRODAT, row code Z08 and physical BGM Z08 together before returning the LK exemption. A synthetic native adverse case keeps a Z05 physical wire in a stale Z08 LK row and expects the scoped actor gate, with no original receipt. The new case awaits clean native replay; earlier green CI cannot qualify it.
