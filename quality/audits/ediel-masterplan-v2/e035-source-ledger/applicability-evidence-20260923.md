# E035 remaining scope discovery at 95b5e391

Read-only investigation; no code, tests, migration, branch, memory or approval changes. HEAD was 95b5e391159e3164869ddabd8ec96b8f69189fe2 and working tree clean. This is a bounded source/owner map, not an independent acceptance review. Parent owns memory and final verification.

## Evidence limits and routing

Read AGENTS, active memory/checkpoint, domain-model, decisions/failures searches, E035 plans/owner map and actual owner/readset/wire/selection implementation. Inspected installed skill inventory and source-compliance/acquisition instructions: this narrow delegated discovery does not claim the full repository acquisition or compliance fan-out workflow; no UI, performance, dependency or live operations apply. Parent recovered frozen P during this investigation. I independently computed its SHA256 as 83c2f1d2915851d2e670731f6ab404ef06c9b9def282afbafdfa0eda836a6e95, matching the frozen manifest, and directly read sources/prodat.txt extracted pages13,50,65,123,130,133. Original P therefore supports the closure direction/date/correlation claims below; Handbok boundary/lifecycle material was not independently recovered here. Do not use the reportedly mislabeled U25-A-5 Library extraction as a verified frozen U25-A-4 original.

## Recommended next bounded implementation

**Add immutable received Z05L/Z05LK closure evidence and use it to bound an already witnessed post-ledger Z04 supply interval.** This has positive source-derived requirements and an actual business owner. Keep Z05C reopening a separate explicit transition until it has the same evidence quality; keep inbound Z08 unapproved because supplier Z08 is outbound in the frozen capability table. A Z08H request alone must not close a source interval.

Source-backed requirements:

- `docs/ediel/masterplan-v2/registers/prodat_message_cases.json` CASE-Z05L-SUPPLIER (Z22) and CASE-Z05LK-SUPPLIER (Z23): inbound grid-owner information terminates the affected supply at the stated valid end and preserves history; Z05L may refer to Z08H; Z05LK is not a Z09E response. Cites P sections 2.1–2.2/2.6 and HB chapters4/10/11.
- `prodat_fields.json` field211: required Z05/Z08, object-scoped SG8 DTM93, format203 CCYYMMDDHHmm; P17 and P50. Use original field211 at exact fixed normal time, not payload start-date fallback or UTC calendar truncation. Original P50 directly confirms this field/date format and supply-stop meaning; Handbok should confirm any additional final-reading boundary interpretation before acceptance.
- CASE-Z05C: retract the correct prior end after checking it; do not duplicate supply. CASE-Z08H: outbound monitored termination request, followed by relevant Z05L; CASE-Z08LK: bilateral, disabled without profile.
- `lib/ediel/stateMachines/prodatLifecycle.ts` maps termination to `supply_terminated`; `flows/inboundBusinessStateMachineLegacy.ts:182–214,441–456` performs the actual end write. This is an integration owner, not a certificate: it selects latest open row, accepts mutable payload date fallbacks, and overwrites `source_message_id` with Z05. Do not relabel its current row/status as complete immutable evidence.

Implementation boundaries:

1. New typed closure wire projection should bind original hash, company/environment, physical message/object/agency/LI, FR/DO and full field211, only the selected L/LK reasons. Preserve canonical disposition and explicit legal-party/facility decisions for all physical objects.
2. Capture original baseline identity plus before/after supply facts at an unambiguous successful end owner (or explicit fresh whole-original review with equivalent SQL revalidation). Persist closure assessment and separate committed witness, with forward migration and strict SQL ownership/reviewer/tenant guards. Do not infer the old Z04 source from the now-overwritten supply `source_message_id`.
3. Extend `sources/structuralSourceReadset.ts` to represent verified closure separately from meter/register versions; unknown/unapproved closure stays a hold. Extend selection to apply only matching supply/object/legal parties and only closure knowledge witnessed by cutoff. Unrelated closure cannot change another supply interval.
4. Preserve ability to select historical structure before closure and exact final-reading boundary; reject current-point/outside-coverage reads after closure. Closure carries no replacement meter/register inventory. Z05C, forks, changed end and unresolved correlation must hold until explicitly implemented.
5. Existing `reviewReceivedStructuralSource.ts:resolveCoverage` requires current status `confirmed_by_grid_owner` and `source_message_id` still equal baseline Z04; after legacy closure it cannot construct coverage. Thus closure integration must rely on immutable witnessed baseline plus qualified closure, not merely add Z05 to an allowlist.

Likely files: `lib/ediel/sources/{structuralSourceReadset,structuralSourceSelection,reviewReceivedStructuralSource,sourceOwnerPersistence,receivedSourceDecisionTimeline}.ts`, a separate closure projection/owner, lifecycle commit plumbing in `flows/inboundBusinessStateMachineLegacy.ts` and `flows/inboundProcessing.ts`, a CLI-created forward SQL migration and generated contracts, targeted unit/runtime/native fixtures. Keep original structural wire union separate or explicitly distinguish non-inventory closure.

Required proof: observed red then green pure/owner tests; genuine canonical fixtures; actual native SQL/HTTP tests proving cross-tenant rejection, immutable baseline preservation, unsuccessful writes never witnessed, effective versus knowledge cutoff, exact boundary, closure before baseline, ambiguous correlation, and retained Z04/Z06F/G/Z10/E61/E62/retry behavior. Exact-final ordinary CI plus independent TASK/SPEC, QUALITY, TENANT-BOUNDARY and WHOLE-PR review still mandatory.

Additional directly verified frozen P constraints: p13 defines Z05 from grid owner to old supplier and Z08 from supplier to grid owner; p65 restricts E34 non-death to bilateral agreement, Z08/Z23 to bilateral use, and Z25 for Z03/Z04/Z05 to bilateral use; p123 field226 requires Z05L LI to match corresponding *optional* Z08H, and cancellation LI to match the message being cancelled. Therefore do not require a prior Z08 for every valid Z05L; do require exact original case identity for Z05C. P130 gives a gas Z05L example with no meter/register inventory; it is illustrative only and must not be copied as electricity canonical fixture. P133 gives Z08H example. No sample personal content was copied into this report.

## Other remaining cases: implementation versus actual missing evidence

| Case | Assessment |
|---|---|
| Z06E death | Typed parser/policy exists (`prodatDeathStatus.ts`, `rulebook/prodatDeathStatusPolicy.ts`), but `prodatRegisterEvidence.ts:81` explicitly rejects persisted death assessment because no qualified producer exists. Implement authenticated, source/customer/object/LI/legal-party-bound assessment producer and native ownership persistence; an incoming Z41 or reviewer click alone is not independent proof of death. For actual records lacking independent lifecycle evidence, evidence is genuinely missing. |
| Z06E bilateral | Frozen CASE-Z06E requires bilateral basis outside standard lifecycle. Engineering can add explicit dated counterparty/process agreement owner. A specific actual bilateral agreement/profile is external business evidence; never manufacture it. Current `customer_only` is excluded in review and selection. |
| Agency89 | `structuralReviewReads.ts` documents legacy graph has no namespace column; review allows agency9 only. This is a schema/domain binding task, not user-policy permission. Add exact namespace identity owner and migrate/populate only from authentic identity evidence; numeric equality must never approve agency89. |
| Multiple physical messages | Current wire readers/readset demand `singleMessage`, index0, while per-register projection already preserves messageIndex and SQL retained tests cover message-local LIN uniqueness. Implement message-scoped physical parsing/party/date/business owners and all-message canonical evidence. A transport envelope first message is not the whole source. No external policy approval inherently needed, but fresh original T grammar verification and native all-message receipts are required. |
| Delegated sender | Existing tenant receiver transport relation and permission-flow temporal identity logic are reusable; sender still enforces UNB sender == FR in both wire readers. Need actual dated counterparty delegation records authorizing transport sender for facility grid owner, not merely reuse own receiver delegation. Engineering can add loader/owner; absent real relationship data remains unavailable. Do not claim SMTP authentication. |
| Changed-start Z04 | Current coverage requires original committed start equal baseline wire, current rows still point to baseline, and replacement is same-case explicit. A corrected start needs authenticated lifecycle correction/compensation and immutable transition from old to new dates; relaxing equality is not a valid implementation. Actual source date and committed lifecycle outcome can supply evidence; not an automatic policy blocker. |
| Pre-ledger history | No amount of bounded snapshot counting establishes missing historical originals or past acceptance. Engineering can create an import/reconciliation owner with coverage attestations anchored to authentic retained complete data. If that data does not exist, external evidence is genuinely required; keep historical intervals held. |

Current fail-closed behavior is appropriate while these owners are absent. None of this establishes full E035/F3 or masterplan acceptance. Do not touch paused PR310, change old assertions wholesale, weaken canonical/tenant gates, or infer acceptance from implementation/test counts.
