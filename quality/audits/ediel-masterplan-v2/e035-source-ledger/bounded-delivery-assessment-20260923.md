# PR370 bounded-delivery assessment — read-only

2026-09-23; inspected HEAD `d7666c7` and the current case/native adjudication. The user's latest instruction permits merging completed work and continuing. This changes the delivery choice in `continuation-20260923-evening.md`, which previously coupled PR370 merge to whole E035. It does not waive real verification or independent review. This is a scope recommendation, not code review, test result, merge readiness or E035 acceptance. Prior `remaining-scope-inventory.md` remains the detailed owner backlog. Skill routing: narrow code/evidence and delivery-boundary review; no implementation, full repository audit, hosted action or agent delegation.

## Recommendation

**A truthful bounded PR370 merge is conceptually viable after the case repair's real native/browser/generated-artifact qualification and exact-final-head whole-branch review.** Name the merged result a **post-ledger, direct-sender, agency-9, single-physical-message received-source/closure/retry and Ediel operational-case slice**, limited to genuinely committed and witnessed coverage and the applicable current-guide comparison path. Leave E035/F3/masterplan explicitly `PARTIAL`. Continue prior-guide and other source owners in subsequent reviewable changes. Do not cherry-pick SQL/code haphazardly from this coupled ledger branch merely to produce a smaller merge; the review should assess the actual 265-file branch and its forward-migration/replay contract.

The already green published checkpoints qualify bounded Z05L/LK closure (25 owner + 37 wire native) and successful retry binding (additional 62 consumption; cumulative 124 native), with ordinary CI at those respective heads. The current case task is **not yet qualified**: on published `bf6d4b5`, OPS35903332327 applied the new migration and passed retained124, then `ediel-case-view-native.test.ts:253` failed because it read an original migration at its removed working path while the replay script held originals in `HOLD` until shell exit. That prevented populated-legacy preservation, protected browser, generated types/schema and post-browser assertions. `case-fix5-native-adjudication-20260923.md` identifies a narrow checksum-bound test path repair; static SPEC/QUALITY approval of the SQL is not runtime acceptance. A sole implementer is addressing it. Current `d7666c7` is a documentation checkpoint, not an accepted final head.

## Dependency check

| Remaining item | Relationship to bounded merge | Existing boundary / review obligation |
|---|---|---|
| Prior 25-A-3 E61/E62 | **Known applicable gap, not a structural dependency of an explicitly current-guide/post-ledger slice.** `utilts25A4.ts` sets the prior flag false, and `qualifyReceivedStructure.ts` returns before comparison. The recovered source establishes pre-October applicability, so a merge cannot advertise E035 comparison coverage for that window or G01 completeness. | Whole-branch reviewers must explicitly adjudicate whether releasing the code with this known currently applicable gap is acceptable for the bounded delivery context, including whether affected current traffic is enabled. If its correct behavior is a merge requirement, implement the already reviewed source amendment first. Do not silently call the gap `not applicable`. |
| Z05C and corrected end | Positive cancellation/corrected-end owner remains absent; `continueSupplyPeriodFromZ05C` still uses a three-row legacy reopening and mutable date fallbacks. | The new structural readset represents unresolved C/closure as a blocker, and closure selection does not treat C as a positive named edge. Verify that this hold survives real legacy side effects and all relevant quantity/response paths; the old operational mutation is not independently qualified by the bounded merge. The process-basis and cancellation owners follow separately. |
| Z06E death/bilateral | No positive context producer; review throws `structural_e34_context_unavailable` and selection excludes `customer_only`. | Ensure an unqualified E34 cannot be laundered into another accepted marker or a false complete interval. Outbound death-sensitive producer remains independently held. Particular event/agreement evidence is per-instance, not merge prerequisite for the direct standard path. |
| Agency89, delegated sender, subaddress, multi-message | Current wire/review/accepted timeline requires agency9, messageIndex0 and direct sender; `singleMessage` requires one physical message. | `structuralSourceReadset` makes unparseable/unsupported physical sources unresolved and comparison holds rather than borrowing the first message/alias/party. Whole-branch review must confirm actual outer inbound processing and persisted outcomes do not claim whole-envelope acceptance or quantities from a partial child; no design-only approval substitutes for this check. The positive owners can be later changes. |
| Changed-start Z04 and pre-ledger history | Current committed Z04 owner requires live confirmed start/source equality; selection requires genuine baseline, ledger epoch and bounded complete readset. | Preserve pending/unwitnessed correction and pre-ledger holds, saved-cutoff/retry immutability and no fabricated E61/E62. The distinct Z04C/withdraw/new-Z04 lifecycle and any historical import remain later work. In particular, existing legacy activation behavior is not certified merely because a comparison readset holds. |
| Case navigation/event relation | **Actual merge dependency for this branch.** It is part of the proposed bounded deliverable and currently fails its required native fixture and browser path. | Finish narrow replay-path repair, run populated-row and ownership/rollback native assertions, protected browser and post-browser invariants, authentic generated contracts, scoped rereview and exact-head CI. Do not drop the test or describe the previous partial native run as a pass. |

The selector's `structural_universe_incomplete`, `structural_coverage_anchor_missing`, closure blockers and `structural_approval_gap` paths, together with `qualifyReceivedStructure.ts`'s `internal_review`/response `none`/no-quantity behavior, support this *conditional* separation. They do not prove every application caller, SQL sink or mixed-envelope case safe by inspection here. The full-branch integration/tenant review is load-bearing, especially because PR370 spans ledger, SQL owners, inbound processing, UTILTS persistence, migrations and admin UI. A discovered bypass that persists/sends/attributes quantity for an unsupported source would be a merge blocker, not a backlog label.

## Concrete final delivery gate

After the case path passes, reconcile generated types/schema from the authentic replay; run ordinary CI and required native/HTTP/browser tests on one published final head; obtain independent whole-branch TASK/SPEC, QUALITY, TENANT/DB and integration review of the **bounded claim and unsupported-path holds**; fix any material finding and repeat affected gates. Update PR description, source/verification matrix and memory to enumerate supported versus held windows/identities/cases and mark E035 partial. Then merging PR370 as that bounded implementation is truthful under the user's instruction. It is not truthful to mark the entire E035 finished, to claim historical/agency89/delegated/bilateral positives, or to treat missing real records as engineered approvals. No further user permission question is needed for the bounded merge once those substantive gates pass; the controller owns the actual merge decision and execution.
