# Independent bounded closure task review

Reviewed exact published head `2b4293b42732b0231dc00d5e43ceefe260cb4254`, tree `403087f3d4e57f21d3b13817ec85f0f1d7802dcf`, against whole-task baseline `0e81b119`. Local HEAD/tree were checked. Scope is the implemented L/LK closure task, not all E035, cancellation, queued retry/prior-guide work, or the masterplan. The approved closure design including mandatory 3A and the Handbok midnight amendment are the governing requirements.

## SPEC verdict

**APPROVED for the implemented bounded code scope; no concrete blocking specification defect found. Task completion remains gated on the pending exact-head native proof.**

The implementation preserves the actual Z04 history and does not reconstruct an invented root from ended mutable rows. Source observation and business approval remain separate: the parser can retain arbitrary original minutes, while producer, closed marker and SQL owner require midnight for L and the explicitly conservative LK subset. Unsupported C/BGM5/other reasons and missing qualification stay held.

## QUALITY verdict

**No blocking code finding. Verification acceptance is PENDING, not passed:** the new native owner replay, its mutation controls, and authentic generated-contract reconciliation were not complete when this review was requested. This review does not report 24 owner or 37 wire cases as passing on this head. The reported full 5902/359 coverage run, types and lint are implementer evidence; no broad optional suite was repeated here.

## Authority and original binding

- The private bounded lexer/projection/matcher retains the previously reviewed release-aware boundaries, exact physical indices, bounded input, original minute/UTC, LI/document/function/reason/party fields and closed JSON equality. The prior ordinal-parity observation and array-valued environment defect are fixed.
- The new SQL owner selects the sealed source by ID/company/environment/hash, then invokes `closure_wire_matches_v1(src.raw_payload,...)`. It never accepts marker-supplied raw text, token lists or offsets as authority. Midnight support is a separate guard, followed by independent timestamp/calendar projection and stop-after-start checks.
- The immutable committed Z04 root and separate reviewed coverage assessment must both be witnessed by the saved cutoff and present in its retained readset. Source hashes, facts hashes, physical identity, legal parties and customer/point/site/switch/supply identities are checked. The reviewed baseline must be the latest chain tip; a newer child disqualifies it even when unwitnessed, so there is no fallback to an older accepted approval.
- The added SQL uniqueness guards were inspected: exactly one latest witnessed reviewed baseline for the same supply/object/parties, and exactly one ended live supply attributed to this closure/customer/point. These address the implementer's earlier ambiguity observation. Current same-ID switch/supply/outbound rows corroborate the retained lineage, start and timestamps; expected closure-source/status/end-date mutation is explicit.
- The three append changes are explicit owner branches. Existing canonical/register membership, party proof and bidirectional owner-row comparisons, source lock, predecessor chain, owner readsets and separate witness persistence remain in place. The helper is private and STABLE within the append validation SELECT; no broad role grant is introduced.
- `source_switch_request_id` retains the older NULL-or-matching support rule. The immutable root never captured its prior NULL state; the implementation report correctly avoids claiming exact historical equality of that unrecorded field.

## Producer through live consumption

- `reviewReceivedClosureSource` authorizes the reviewer before canonical-ledger writes, obtains a fresh original/canonical receipt, binds physical membership, saves its own complete snapshot and freshly reads tenant/facility evidence. `resolveClosureCoverage` requires a unique latest witnessed reviewed baseline and genuine root; no latest-open supply heuristic is promoted to evidence.
- Every physical object remains represented. Unsupported or unqualified objects are unavailable/rejected; successful objects pass the database authority boundary before the existing separate availability witness. A whole-source accepted receipt requires every object accepted.
- Timeline acceptance still checks source/hash/tenant bindings, canonical physical membership, owner times and the closed marker. It does not replace historical ownership with today's mutable rows.
- Readset classification handles Z05 before the unsupported-message branch, separates closures from inventory, retains unavailable/rejected/unsupported object blockers, treats unknown party components as wildcards and unbounded physical identity as a global hold. BGM5 has no guessed late lower bound; C cannot silently reopen supply.
- Selection uses the existing inventory baseline and applies only closures matching object/agency/legal parties/supply and immutable coverage lineage. It returns cloned bounded coverage and closure provenance. Intervals ending at stop and closing points at stop retain the earlier inventory; crossings and current points at stop are unavailable. Conflicts and unresolved affected closures hold instead of choosing earliest/latest. Earlier/unrelated queries remain scoped.
- The actual `qualifyReceivedUtiltsStructure` snapshot path passes closures and blockers into `compareUtiltsStructure`, which passes them to selection. Existing inbound and data-request processors call this qualification boundary. Unavailable results continue through internal review, no positive response, and empty quantity persistence payload behavior; expected inventory is not selected from incoming identifiers.
- The message page exposes the separately labelled original-closure review. Its server action validates scalar IDs/environment/confirmation, rejects replacement input, obtains the authenticated user from the company-scoped guard, checks operational status, and supplies only identities/confirmation to the producer. Client reviewer IDs or date/approval facts do not become authority. No review-time operational date write or market send was introduced.

## Native oracle and coverage assessment

The retained/new test source distinguishes the controls correctly:

1. LI forgery with only raw binding removed can append; unchanged binding rejects it.
2. Same-date midnight-to-00:01 forgery with consistent UTC is rejected normally and with binding alone removed, because the midnight guard remains.
3. Removing both raw binding and midnight support permits that minute forgery. This is explicitly a two-guard historical-gap control, not single-guard binding isolation.
4. A genuine 12:34 original is held by producer and direct append. Removing only midnight support permits its otherwise-valid proposal while raw binding remains.

The native owner source also covers genuine L/LK baseline/end/review/witness flow, missing prior reviewed coverage, stale party/switch/outbound changes between reads and append, source/identity/reviewer/lineage forgeries, saved cutoffs, closure unwitnessed successors, retry revisions and latest-baseline no-fallback. Its positive fixture checks covered E61/E62; actual runtime qualification checks an internal hold and no quantity payload. Failed direct probes are transactionally isolated and the unchanged persistent assessment/witness state is asserted.

Full business-owner positive cases for custom advice, escaped references and several objects are not present. This is a real coverage limit, not proof that those full owner paths pass. I did not find a material unsupported authority jump that justifies demanding every lexical combination be repeated through the full lifecycle: dedicated lexical native cases already exercise escaping/custom advice/cross-LIN geometry, while business acceptance independently checks each selected object's original and live owners. Multi-object sources cannot claim whole-source acceptance with unqualified siblings. Do not describe lexical positives as full multi-object lifecycle or national-acceptance proof. An actual failure in the pending replay or a new branch-specific defect would justify focused additional owner coverage.

## Remaining gate

Inspect the exact-head native results before declaring this task verified, including real SQL compilation, owner positives, all mutation controls and retained wire/owner tests. Reconcile only authentic generated schema/type outputs and rerun the required final-head gates after any correction. No code issue is being held open merely because unrelated queued designs or whole-E035 work remain incomplete. Conversely, this bounded code approval is not evidence that those future owners are implemented or that native execution has already succeeded.

Only this requested review report was written; no production edits or broad tests were performed.
