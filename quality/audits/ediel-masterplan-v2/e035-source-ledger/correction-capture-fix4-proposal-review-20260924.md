# Task2 fix4 combined proposal — independent permission-contract review

**SPEC: APPROVE. QUALITY: APPROVE the bounded two-forward proposal for implementation and native verification.** No further policy amendment is required. The registry materialization repairs established canonical keys without assigning permissions; the direct-CTE restriction closes a demonstrated selected-company authorization gap. This is not approval of an unexecuted fix, new role policy or broader RBAC redesign.

## Independently checked evidence

Read task-2-fix4-report.md, catalog communication entries, May role-seed blocks, the current company-scoped resolver and actor wrapper, generated permissions/user_permissions schemas and historical gridex_can hygiene predicates. The native195/206 and11 missing-key failures are attributed to the supplied execution report; no SQL or tests were run in this review.

The May blocks reference communication.read/send in arrays but create role links by SELECT from existing permission rows. They do not materialize those two permissions. The catalog only defines application metadata. A new canonical forward inserting exactly these existing keys is therefore appropriate; a fixture-only INSERT would conceal the runtime registry defect.

The actual direct_permissions CTE in 20260902091000_company_scoped_permission_engine.sql190–195 filters user and normalized joined permission only. It ignores company_id/status/is_active/effect despite those fields existing on user_permissions. The latest actor wrapper checks selected-company membership and then consumes this resolver. Thus a non-platform actor belonging to A and B can use A's direct communication.send in B. A deny-only, disabled or removed direct row also contributes a positive key. This is load-bearing for the capture SQL; the existing no-membership-in-B negative does not refute it.

## Exact approved change boundary

1. **Registry forward:** insert only communication.read and communication.send with existing catalog names/descriptions/category. ON CONFLICT(key) DO NOTHING must preserve preexisting IDs, metadata and activation. No role_permissions, user_permissions, overrides, aliases, key renames or historical grant-list replay. Materialization makes already specified capabilities administratively assignable; it does not itself grant anyone a new capability. Existing platform-wide authority remains the existing policy.
2. **Resolver forward:** change only direct_permissions in gridex_get_user_permissions_in_company. Retain the inner join through permission_id and existing normalized key expression; do not accidentally add a permission_key fallback or new source of grants. Require active status/is_active and allow effect with the specified legacy null defaults. A non-null direct company must equal p_company_id and have active membership using the same membership predicate as the existing company-role branch. Null-company direct grants retain legacy global semantics. A null requested company must not expose a company-bound direct grant.
3. Preserve role CTE, platform fallback, output ordering/shape, signature, SQL stability, SECURITY DEFINER/search_path, owner and effective ACL. CREATE OR REPLACE must not regrant functions previously revoked from clients. Keep gridex_actor_has_company_permission and the capture owner's independent active actor/company/source/environment checks intact. No new capture-specific authorization algorithm.
4. Preserve union-of-positive-grants semantics: deny is excluded as a positive direct grant, but does not override an independent valid role/global/allow grant. Do not change user_permission_overrides or general shared-masterdata resolver behavior. Historical gridex_can supports the active/status/allow hygiene; it does not authorize importing unrelated precedence rules or its broader fallback matching into this function.

These restrictions can remove previously accidental cross-company/inactive grants from every consumer of the shared company resolver. That is the intended security correction, not a new role assignment. Valid role/platform/global paths remain as before; a broader permission-policy cleanup is outside this wave.

## Required verification before acceptance

- Genuine DB replay must produce exactly one row for each canonical key without test self-seeding. Verify first application grants nobody and reapplication preserves IDs/metadata/activation and all assignment/override rows. Use deterministic ordering for snapshot equality. Registry tests must also cover preexisting metadata/disabled rows rather than assuming clean insertion is the only deployment shape.
- Run a real dual-member, non-platform actor: active A direct allow captures A; B permission check fails and B capture produces zero concern and witness writes. Test inactive/removed/deny-only direct rows separately and keep the active profile/selected-company membership otherwise valid, so the intended predicate is exercised.
- Prove valid null-company direct behavior with selected-company membership; prove company-bound grants do not resolve for a null requested company. Preserve valid company role, platform-role/admin behavior and reject inactive/wrong-company role or inactive membership using established semantics.
- Include deny-only=false and deny plus independently valid role/allow=true to lock the explicitly retained union-positive policy. Do not strip the last admin to build fixtures; use dedicated limited actors.
- Preserve full capture/witness positives and negatives, including testing-only/read-only denial, paused company, wrong source tenant/environment, sealed bytes/hash and cutoff/witness transaction boundaries. Test action requirement agrees with SQL. Actual enabled C profile and sealed source prerequisite assertions should fail loudly rather than insert zero rows.
- Preserve published migration checksums and actual effective resolver ACL; obtain authentic final-head native replay and schema/types reconciliation. Do not label the proposal or local typechecking as resolved native evidence.

No new load-bearing flaw found in the proposed repair. Registry materialization alone would be insufficient; the combined resolver repair is necessary and appropriately bounded. No live operations, product/schema/memory edits, commits, delegation or broad tests occurred; wrote only this review.
