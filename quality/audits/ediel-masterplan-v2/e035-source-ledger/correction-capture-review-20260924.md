SPEC: ❌ Issues found in the sealed-source-only checkpoint: unsupported message grammar can produce exact scope instead of a wildcard concern. Full Task 2/document capture and Tasks 3/4 are not assessed as completed.

QUALITY: Needs fixes. Native acceptance remains withheld pending genuine isolated CI execution and actual schema/type artifacts.

## Strengths

- `app/admin/ediel/correction-actions.ts:9-17` rejects unexpected/duplicate inputs, obtains the actor from the authenticated company guard, requires `communication.write` with all-of semantics, and checks operational company status. It does not expose caller hash/bytes/reviewer/target authority.
- `supabase/migrations/20260923231731_ediel_correction_concern_capture.sql:194-230` independently checks actor/company permission, tenant/environment, source insertion provenance/context and original SHA-256, computes immutable facts/hash, and makes same-original retries idempotent. `:149-183` supplies forced RLS, direct-access revocation and update/delete/truncate rejection.
- `supabase/migrations/20260923231731_ediel_correction_concern_capture.sql:116-145` preserves unknown oldStop, records C's own date separately, leaves candidateTarget null and never grants positive C authority. The native regression at `scripts/ediel-correction-context-native.test.ts:33-39` checks this distinction and missing LI.
- `supabase/migrations/20260923231731_ediel_correction_concern_capture.sql:241-250` binds the witness to scope/hash and rejects the creation transaction's xid; `lib/ediel/sources/correctionContextCapture.ts:27-42` awaits separate RPCs, validates receipt/witness identity and microsecond times, and returns an allowlisted receipt only afterward. Native cases at `scripts/ediel-correction-context-native.test.ts:79-86` exercise same-transaction rejection and later recovery using real SQL/RPCs.
- `scripts/ediel-correction-context-native.test.ts:136-146` has a meaningful lost-witness proof design: actually commit capture without its witness, discover the sealed raw C through the real source snapshot RPC at a newer cutoff, and compare the persisted older snapshot's text/hash. This is source-ledger proof design, not evidence of completed combined correction-cutoff/UTILTS integration.
- `quality/audits/ediel-masterplan-v2/e035-source-ledger/correction-capture-checkpoint-20260924.md:3,33-46` accurately limits completion, discloses native/type artifacts pending, and leaves document retention/input and later integration unresolved.

## Critical

- None identified by static review.

## Important

1. **Unsupported message grammar can narrow the concern's scope.** `supabase/migrations/20260923231731_ediel_correction_concern_capture.sql:28` checks only UNB syntax version and the first UNH message-type component; it does not validate the UNH message directory/version/agency tuple or even its shape. An otherwise passing original with its UNH composite replaced by `PRODAT:BOGUS` still passes these tests, produces object/party scope at `:134-136`, and can receive `proposedStop:not_asserted` at `:137-144`. The latter whitelist checks segment tags, not this unsupported composite. Unknown oldStop protects the interval only inside the returned scope; it does not make the scope wildcard. Define and validate the supported physical message grammar before accepting exact observations, returning the existing wildcard result for unsupported/malformed variants. Add native cases mutating the UNH tuple/shape and UNB syntax identifier without changing counts. This violates the brief's conservative unsupported-namespace requirement and the report's malformed/unsupported-wildcard claim. This is a static counterexample, not a claimed executed native result.

2. **Large copied parser creates two owners for the same structural logic.** `supabase/migrations/20260923231731_ediel_correction_concern_capture.sql:6-109` reproduces the envelope/party/LIN/group/date parsing in `supabase/migrations/20260923113014_ediel_closure_original_wire_binding.sql:67-170` almost verbatim, with only a few intentional policy differences (Z24, one object, optional LI, receiver equality). The tokenizer is shared, but the substantial structural decoder is copied. The review rubric explicitly treats verbatim logic duplication as Important: fixes to physical parsing can diverge between closure and correction owners, as the separately added receiver comparison already illustrates. Introduce a private neutral structural decoder in a forward migration and keep the L/LK acceptance and C observational policy gates separate. Do not edit published migration history or broaden positive C acceptance; protect existing closure behavior with the existing native cases and the correction cases.

## Minor

- `quality/audits/ediel-masterplan-v2/e035-source-ledger/correction-capture-checkpoint-20260924.md:36` reports npm `http-proxy`/experimental EnvHttpProxyAgent warnings. These are disclosed environment noise, not a demonstrated product defect, but the reviewer template requires noting non-pristine output. Clean up the supported CI invocation/environment or preserve an explicit known-noise classification with the gate evidence.

## Verification limits and focused checks

- ⚠️ `quality/audits/ediel-masterplan-v2/e035-source-ledger/correction-capture-checkpoint-20260924.md:22-38`: full 6038/374, focused 35, type/lint and integrity results are existing reported results, not independently reproduced here. All 20 native cases are written/typechecked only. Require isolated native CI (including migration application, role/RLS behavior and real PostgREST RPCs) plus genuine generated schema/type artifacts before native acceptance. No broad reruns were performed.
- ⚠️ `lib/ediel/sources/correctionContextCapture.ts:23` explicitly refuses document input. The actual signed-document byte readback/negative substitution tests in the original Task 2 remain outside the parent-authorized source-only checkpoint; do not mark whole Task 2 complete.
- ⚠️ `scripts/ediel-correction-context-native.test.ts:125-146` does not establish Task 4's combined one-MVCC concern cutoff or actual UTILTS hold consumption; the report correctly assigns that owner elsewhere.
- Named risk check — authenticated actor versus arbitrary service input: checked `lib/admin/guards.ts:299-322` and `lib/supabase/service.ts:1-12`. The action uses the authenticated guard result; the internal helper uses the service client. No additional public caller accepting a reviewer UUID is introduced in the diff.
- Named risk check — source ledger could admit an outbound/non-EDIFACT original or lose evidence after operational deletion: checked `supabase/migrations/20260922095911_ediel_received_source_ledger.sql:19-39,85-112`. The source owner gates inbound EDIFACT PRODAT, seals by insertion, has a payload hash constraint and deliberately no operational cascade FK. This supports this checkpoint's reliance on that owner rather than mutable message state.
- Named risk check — copied decoder and bounded tokenizer could diverge: checked `supabase/migrations/20260923113014_ediel_closure_original_wire_binding.sql:5-170`. The inherited tokenizer bounds bytes/segments/components and handles UNA releases; the separate structural copy is the Important quality finding above.
- Named risk check — native fixtures might fabricate parser evidence: checked `__tests__/helpers/closureWireFixtures.ts:1-27`. Tests construct real synthetic EDIFACT and pass it to SQL; the lost-witness case uses actual snapshot persistence rather than a mocked snapshot.
- Review was limited to the frozen `189eb136d85398b458d72293cc35e551034e5490..4deb48baee345e05593704ba7f4a1a811e55546d` package and the named checks. Initial tool output truncated the package's middle; only the missing hunk portion was retrieved separately. No checkout, index, runtime, memory, commit or test-suite mutation was performed.

## Fix round 1 — facae00db64cc80dd0377d0ba70d29504476305d..993367eaabdbe7271f5bf4fa61c8e56618090e04

SPEC: ✅ Both scoped findings addressed for the sealed-source-only checkpoint. Native qualification, full Task 2/document capture and Tasks 3/4 remain outside this acceptance.

QUALITY: Approved for this static fix review. Native acceptance remains withheld pending genuine isolated CI and actual generated schema/type artifacts.

- **I1 — ADDRESSED.** `supabase/migrations/20260923231731_ediel_correction_concern_capture.sql:135-139` now requires complete JSON-array equality for `UNOC:3` and `PRODAT:D:97A:UN:E2SE6A`; missing, extra, empty or changed tuple components cannot retain exact scope. It separately keeps singleton-object, Z24 and direct-receiver requirements. `scripts/ediel-correction-context-native.test.ts:148-162` adds twelve real-SQL mutations asserting the complete wildcard observation, including the original `PRODAT:BOGUS` counterexample. These are meaningful native test designs, not executed native evidence.
- **I2 — ADDRESSED.** `supabase/migrations/20260923231731_ediel_correction_concern_capture.sql:7-112` is the single neutral structural decoder; `:117-126` and `:131-143` are short closure and correction policy wrappers. The existing closure function is replaced in the forward migration, so the older published function body is historical migration content rather than a second live decoder. The common return carries wire observations and grammar/count metadata, while only the wrappers assign subtype.
- **Named shared-contract risk — closure CREATE OR REPLACE preservation:** compared the fix delta with the original closure decoder inspected during the first review (`supabase/migrations/20260923113014_ediel_closure_original_wire_binding.sql:67-170`). The neutral decoder restores closure's allowance for multiple physical objects and differing transport receiver, while the closure wrapper reinstates exactly one LI and Z22/Z23 only. It returns the same wire fields plus the same L/LK subtype. Time interpretation still occurs inside the UTC-configured neutral decoder; the wrapper does no time conversion. C-specific namespace/direct-receiver/singleton restrictions remain in the correction wrapper. No new closure-authority broadening was identified by this comparison.
- **Strength — policy and privilege regression design:** `scripts/ediel-closure-wire-native.test.ts:80-83,100-138` extends private-function ACL assertions and adds real-SQL cases for Z22/Z23/Z24 separation, mandatory closure versus optional C LI, multiple objects and delegated receiver; the closure results are compared against the unchanged TypeScript oracle. The new neutral decoder and both wrappers revoke direct API-role execution at migration `:112,126,143`.
- **Critical:** none newly identified in the fix diff.
- **Important:** none remaining from I1/I2; no new load-bearing breakage identified in this scoped static review.
- **Minor:** the previously disclosed environment warnings remain classified as known noise in `quality/audits/ediel-masterplan-v2/e035-source-ledger/correction-capture-checkpoint-20260924.md:73`; they are not a new fix-round product defect.
- **Verification limit:** `quality/audits/ediel-masterplan-v2/e035-source-ledger/correction-capture-checkpoint-20260924.md:65-75` reports 273 tests/16 files, three typechecks, lint and migration integrity passing, with **204 expected native cases still unexecuted**. Those reported results were not rerun. They do not execute this SQL refactor or prove installed RLS/ACL, RPC, transaction or closure-equivalence behavior. Require the genuine native CI run and generated artifacts before native acceptance; the historical 6038/374 result is not final-SQL execution evidence.
- **Review scope:** read the supplied fix package once; reused the original decoder evidence from the named first-round risk check. No broad search/rerun, checkout/index/runtime/memory edit, commit or delegation occurred. Only this requested review report was appended.

## Fix round 2 — 18debd5c57a00bbe26893d8535c75d369bb7df96..b274585a3ff28a4b5c1e28b8ff09e2c31b0ab759

SPEC: ✅ Scoped native-fixture correction is compliant. The source is explicitly bound to the actual enabled C profile without changing the capture contract or the test assertions.

QUALITY: Approved for this scoped static review. No new load-bearing breakage identified. Final native acceptance remains withheld.

- **Fixture failure — ADDRESSED by inspection.** `scripts/ediel-correction-context-native.test.ts:30-34` replaces unbound VALUES insertion with a registry-backed SELECT of enabled `PRODAT:Z05:C:26.A:r3`, joining the profile's actual rule-pack ID and deriving all six binding fields from those rows. It does not fabricate a checksum, profile ID, snapshot or authority marker. The unchanged source-count assertion at `:35` fails if no row is selected; the source remains an actual inbound EDIFACT PRODAT INSERT into the normal sealed-source pipeline.
- **Named risk check — ambiguous inference versus guard bypass:** checked `supabase/migrations/20260901163500_bind_canonical_rule_pack_evidence_on_inbound_ediel.sql:21-47`. The installed trigger explicitly accepts a complete six-field binding before its fallback inference; the fallback filters family/code/date, not subtype, explaining the reported three-way Z05 ambiguity. The fixture takes that existing explicit-binding branch using joined registry values. This skips only the unnecessary fallback inference, not the trigger itself; no trigger disabling, session-replication setting, registry mutation or production guard modification appears in the fix. This fixture does not prove automatic subtype inference, nor does this task require it.
- **Named retained-pattern check:** `scripts/ediel-source-owner-native.test.ts:569-577` uses the same enabled subtype-profile join and the same six value derivations for retained L/LK fixtures. The C fixture preserves `clock_timestamp()` for receipt time, so the saved-cutoff/lost-witness tests are not made to pass by backdating evidence.
- **Strength — failure accounting:** `quality/audits/ediel-masterplan-v2/e035-source-ledger/correction-capture-checkpoint-20260924.md:83-87` explicitly reports genuine OPS 35935812643 as **195 passed / 9 failed**, identifies seed insertion as the failure boundary, and does not count the nine unreached capture/role/witness/snapshot behaviors as proven. The fix touches only the fixture and report; no SQL/runtime migration changes are in this package.
- **Critical / Important / new Minor findings:** none identified in the scoped fix diff.
- **Verification limits:** audit `:89-98` reports script typecheck, scoped lint and 71 tests/4 files passing, while expressly retaining **204 expected native cases pending rerun**. Local checks were not rerun for this review and do not establish PostgreSQL success. Require genuine CI execution of the nine previously unreached assertion groups, full native 204-case results and authentic generated contracts before native acceptance. Prior reported 195 native passes apply to the earlier failed run, not a claimed green run of this fix.
- **Scope:** read the fix package once and performed the named retained-fixture/installed-trigger checks only (including the related snapshot trigger, whose additional validation applies to outbound rows and is not claimed as inbound proof). No checkout/index/runtime/memory changes, broad repeat tests or delegation occurred. Only this requested report was appended.

## Fix round 3 — 90a9bd63326dffd2bf5f37568e093a4fd4d349d6..8ebbac3f6f2eb82fafff46d013bedba7cc1a5764

SPEC: ✅ Scoped permission amendment and native-fixture repairs are compliant with the updated Task 2 brief and approved `correction-capture-permission-amendment-review-20260924.md`.

QUALITY: Approved for this static fix review. No new load-bearing breakage identified. Native acceptance remains withheld pending the genuine 206-case replay and generated artifacts.

- **Nonexistent capture permission — ADDRESSED.** `app/admin/ediel/correction-actions.ts:14` and new forward `supabase/migrations/20260924001447_ediel_correction_capture_canonical_permission.sql:10` both require exactly `communication.send`, as authorized by the binding amendment. The action retains the authenticated actor, selected-company guard and operational-company check. The forward migration adds no catalog permission, compatibility alias, role grant, testing fallback, review authority or outbound operation. Its function EXECUTE grant at `:47-48` restores the already-existing service-only function ACL, not a new user capability grant.
- **Named risk check — replacing the private owner could accidentally change source/security semantics:** mechanically compared the replacement function in the diff with the published function in `supabase/migrations/20260923231731_ediel_correction_concern_capture.sql`. After normalizing only `CREATE` to `CREATE OR REPLACE` and the permission literal, the complete function declaration/body is identical. Thus `20260924001447...sql:5-46` retains search_path/timezone/security mode, active actor/company checks, tenant/environment, source/hash/2 MiB/context validation, unknown authority, immutable facts and idempotent behavior. The manifest retains the old migration checksum and adds a new version; there is no old-migration edit in the package. Witness and parser owners are unchanged.
- **Silent fixture grant failure — ADDRESSED.** `scripts/ediel-correction-context-native.test.ts:17,26-27,36-39` asserts a singleton canonical send permission before INSERT SELECT, queries the actual effective selected-company permission RPC afterward, and reads active company/user/membership state. Existing real capture, hash, idempotency and distinct-witness-transaction assertions remain at `:71-80`; success is not reduced to a mocked permission result.
- **Last-admin negative fixture — ADDRESSED.** `scripts/ediel-correction-context-native.test.ts:130-151` creates a separate active viewer instead of deleting or downgrading the tenant's functioning administrator. Both test-only and read-only variants assert their actual limited permission is true and send is false, then demand unconfirmed capture and zero concern/witness rows. This is a meaningful denial test rather than a fixture that fails before attempting capture.
- **Strength — additional denial evidence design:** `scripts/ediel-correction-context-native.test.ts:81-91` explicitly checks wrong-company permission is false and retains cross-company/source, environment and disabled-user negatives, with zero concern/witness checks. `:188-195` changes the company to paused, reads the actual persisted state, then checks denial and zero writes. No lifecycle or last-admin guard is disabled.
- **Critical / Important / new Minor findings:** none identified in this scoped fix diff. The report continues to disclose existing tool/environment notices; they are not new product breakage.
- **Verification limits:** `quality/audits/ediel-masterplan-v2/e035-source-ledger/correction-capture-checkpoint-20260924.md:101-125` reports the actual previous run as **196/204, eight failures**, the exact action expectation RED (one failure/30 passes), subsequent GREEN 71/4, three typechecks, lint and integrity 609/513. These were not rerun in this review. The native failures are not retroactively passes: require the genuine final-head **206-case** result, including actual capture and separate witness positives and all repaired denial cases, followed by genuine schema/type artifacts. Source-only scope remains unchanged; no whole-Task-2/document or Task-3/4 completion is granted.
- **Review scope:** read the updated brief, approved permission amendment and supplied fix package; initial tool output truncated the package middle, so only that missing portion was retrieved. Performed the single named replacement-body comparison, with no broad tests, runtime changes, checkout/index/memory edits, commits or delegation. Only this requested report was appended.

## Fix round 4 — dba554664ea2d6f240d709db57c3c664ea5ec9cc..05461a63c3a60b725524320285bb11fd58414171

SPEC: ✅ The scoped two-forward implementation matches the approved combined proposal and fix4 brief. Registry completeness and the direct-grant scope/status/effect defects are addressed in source.

QUALITY: Approved for this scoped static review. No new load-bearing defect identified. Native acceptance remains withheld pending genuine final-head 224-case CI and generated artifacts.

- **Missing registry materialization — ADDRESSED.** `supabase/migrations/20260924003708_communication_permission_registry_completion.sql:6-10` inserts exactly the two existing canonical communication keys and uses `ON CONFLICT(key) DO NOTHING`. It contains no role/user/override assignment or historical role-list replay. Named metadata check against `lib/rbac/catalog.ts:311-324` confirms the existing labels/descriptions/category. Existing IDs, disabled flags and metadata are untouched on conflict; there is no new permission concept or alias.
- **Direct-grant selected-company/state/effect defect — ADDRESSED.** `supabase/migrations/20260924003724_company_direct_permission_scope_repair.sql:47-70` retains the permission-ID join and normalized permission expression, filters active/status/allow with the approved null defaults, and requires matching non-null selected company plus active membership for company-bound rows. A null requested company cannot reveal a company-bound row. Null-company legacy direct grants remain global. Deny is excluded as a positive grant without overriding a separate valid positive role grant; overrides are not imported or modified.
- **Named shared-owner preservation check:** compared the complete replacement declaration/body against `supabase/migrations/20260902091000_company_scoped_permission_engine.sql:149-212`, excluding only direct_permissions. The remainder is exactly equal: role/platform branches, ordering/result shape, signature, SQL stability, SECURITY DEFINER and search_path. The forward contains no owner/ACL statement. Checked the consuming wrapper at `20260902100000_rpc_surface_and_permission_scope_corrections.sql:108-153`; its selected-company membership/auth-user and established explicit platform authority remain intact. Capture's independent actor/company/source/witness checks are not edited by this wave.
- **Strength — actual migration replay harness:** `scripts/ediel-correction-context-native.test.ts:53-61` reads the SQL from the current committed git revision, validates it against that revision's manifest SHA-256, and removes exactly one outer BEGIN/COMMIT pair for transactional rollback. It does not execute clean-replay marker contents or handwritten replacement SQL. `:65-103` compares deterministically ordered complete role/user/override snapshots, tests first application with no assignments, and reapplies twice over existing IDs/disabled metadata and nonempty synthetic assignment/override rows. These assertions would fail on accidental grant creation or conflict-row mutation.
- **Strength — private resolver proof design:** `scripts/ediel-correction-context-native.test.ts:104-117` snapshots owner, ACL, security mode, volatility and configuration across replay and asserts anon/authenticated cannot execute while service_role can. This is meaningful installed-contract verification pending execution; source comparison alone cannot certify the actual replayed ACL.
- **Strength — authorization proof design:** `scripts/ediel-correction-context-native.test.ts:267-372` uses dedicated limited actors and real effective-permission RPC/capture calls for dual-member A/B isolation, deny/removed/inactive direct rows, null requested scope, valid legacy-global behavior, inactive membership, scoped/inactive roles, deny plus valid role, global platform role and explicit superadmin. Denial helper `:288-293` requires both false effective permission and zero concern/witness writes. Positive A/global/role/admin paths retain recorded unreviewed capture assertions. Function/role fixture SQL is not substituted for the production capture owner.
- **Strength — prerequisite evidence:** `scripts/ediel-correction-context-native.test.ts:18,37-43` adds the actual enabled C-profile singleton and direct sealed-original scope/byte/hash/context/clock assertions before attempting capture. Existing capture/witness, paused/read-only/testing-only/tenant/environment and saved-cutoff cases remain; no fixture-self-seeded permission registry was introduced.
- **Named native fixture compatibility checks:** inspected the actual table definitions in `supabase/schema.sql:68180-68203,69411-69486` for assignment snapshot IDs, direct status/effect/is_active, override fields and synthetic role fields. Checked `scripts/ediel-source-owner-native.config.ts:18-19` for isolated serial-file execution. No incompatible field/shape was identified. This does not certify runtime triggers or test execution.
- **Critical / Important / new Minor findings:** none identified in the scoped fix. Removing accidental cross-company/inactive direct grants from other shared-resolver consumers is the explicitly approved security correction, not an unapproved role-policy expansion. Existing global/role paths are preserved.
- **Verification boundary:** `quality/audits/ediel-masterplan-v2/e035-source-ledger/correction-capture-checkpoint-20260924.md:127-146` and the fix4 report disclose the genuine previous **195/206 with 11 failures**, local **71/4**, scripts typecheck/lint and integrity **611/515**, and **224 expected native cases pending**. No native pass is inferred from local tests or source proof. Require actual first/reapplication, ACL, all new resolver cases and retained capture/witness native execution, then authentic schema/type reconciliation. Earlier published migrations are absent from this five-file diff; the two new forward checksums are added to the manifest. Full Task 2/documents and Tasks 3/4 remain unaccepted.
- **Scope:** read the fix4 brief/report and independent approved proposal. The requested git diff was read before the generated review package became available; its initial tool output truncated the middle, so only that missing part was retrieved separately. Performed only the named catalog/shared-owner/fixture-contract checks and a focused mechanical source comparison. No broad test reruns, runtime changes, checkout/index/memory edits, commits or delegation occurred. Only this requested report was appended.
