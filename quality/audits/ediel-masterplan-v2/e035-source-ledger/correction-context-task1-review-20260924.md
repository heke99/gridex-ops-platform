# Task 1 review — pure correction-context boundary hold (2026-09-24)

Frozen head: `4de528daa6c60df2a0943642bf5b5f999fc68a52`; reviewed `ecbd937b..4de528da` against the Task 1 brief and approved C-PB-3 design. **SPEC: APPROVED for Task 1. QUALITY: APPROVED for Task 1.** No material findings in the bounded pure projection and selector. This does not approve a production correction-context source, capture completeness, SQL owner, or positive C authority.

The projector validates both observations and their instants, receipt time, source ID, and scope. It chooses the earlier of two known instants, uses the one known instant only when the other is positively `not_asserted`, and sets a null lower bound when either candidate is `unknown` or neither is known. The matcher treats a null lower bound as affecting the entire matching interval, tests the inclusive period end (including both point kinds at the boundary), requires visibility at the saved cutoff, and excludes only concrete known scope mismatches. The selector applies this hold before source comparison and does not create or remove closure edges. Existing L/LK coverage and scoped closure-blocker functions are unchanged. The readset adds only an empty typed array; callers in the current runtime do not populate or pass correction-context blockers, as deliberately deferred to Tasks 2–4.

Concrete false-positive challenge: a day30 old stop with day20 proposed stop observed on day10 holds day25 and both day20 point queries at day10, but day9 saved cutoff and a pre-day20 interval remain selected. A later day40 proposal holds from day30, not day40. An unknown potentially earlier candidate holds even an interval ending on day9 at the day10 knowledge cutoff. Missing party or comparison scope cannot disprove the concern, while known company, environment, customer, supply, or object mismatch can. The tests cover these cases with a selectable baseline and also check invalid calendar instants and raw-C projection without a positive closure shape. These observations support the hold-only claim; arbitrary pure caller arrays are not provenance.

Verification reviewed from the implementation receipt: final focused run 67/67 across five files after the last matcher/test edits; Node 22 app and test typechecks, scoped lint and diff check passed. Full 6007/6007 across 373 files preceded the final small extraction/assertions; it was not repeated. I did not rerun tests. Remaining integration must classify an artifact with an unspecified changed end as `unknown`, acquire visible records at the exact saved cutoff with service-owned scope, and keep unresolved capture/history conservative before this pure selector can provide production protection.
