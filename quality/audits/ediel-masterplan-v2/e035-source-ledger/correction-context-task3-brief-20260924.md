## Task 3: Outbound Z08H dispatch and linked process-history universe

**Files:** Extend the forward migration with private outbound history/coverage and bounded process-snapshot RPC (or a following forward if Task 2 is already published); create `lib/ediel/sources/correctionProcessReadset.ts` and a narrow post-send hook in `lib/ediel/outbox/sendOutboxItem.ts`/`lib/ediel/transport/index.part-2.ts`; native tests in `scripts/ediel-correction-context-native.test.ts`.

**Interfaces:** `openCorrectionProcessSnapshotV1({companyId,environment,cutoffAt,customerId,pointId,supplyPeriodId})` returns sealed text/hash/ID, three owner-set counts, bounded `complete`, coverage start/end/missing spans and observed witnesses. It enumerates outbound Z08H original+attempt, inbound L/LK/C/replacement source/assessment, and linked customer/contract/termination/rescission changes. A failed producer or unknown process-initiation retention start returns `complete:false` with reason; no empty-list-as-negative inference.

- [ ] First write native failed-reader/over-limit/pre-retention and conflicting Z08H cases. Assert reversed outbound FR/DO matching, same LI on unrelated point not blocking, missing party/date as potential match, old queued/prepared Z08H not a dispatch, and provider-accepted with status/event persistence interruption reported uncertain rather than definitively unsent.
- [ ] Add immutable post-acceptance observation keyed to exact outbound `ediel_messages` ID, original raw/hash, company/environment, outbound request, actual provider attempt/Message-ID and server receipt. Keep provider acceptance distinct from delivered/acknowledged and from `message_sent_at` fallback. If capture fails after SMTP acceptance, mark history `unavailable` and retain transport's uncertain-delivery behavior; do not retry a market send to fill evidence. Historical sends without this producer have a missing coverage span, not a synthetic backfill.
- [ ] Snapshot the three bounded owner sets under one MVCC visibility boundary with exact counts and overflow reason, including captured process records before semantic review and every predecessor/witness. For customer/contract rows, append a source-owned historical observation when those actual transitions occur; mutable current status, generic `domain_events` or `customer_case_events` alone cannot prove process-initiation completeness. Record retention interval explicitly; `edifact_raw_payloads` archive policy does not prove old data survived.
- [ ] Run native table/RPC/tenant/race checks. Commit only with real hook and complete/incomplete receipts; if the actual send pipeline lacks a safe after-acceptance persistence boundary, retain `complete:false` for outbound and report the exact blocker instead of fabricating a dispatch receipt.


## Binding reviewed amendments — prepared, not dispatched
Read audit `process-history-preflight-20260924.md` and `process-history-design-review-20260924.md` in `quality/audits/ediel-masterplan-v2/e035-source-ledger/`, including the final scoped approval and five-point cross-task activation contract. They refine the original task text above: capture must reserve and fence per original BEFORE provider entry at BOTH shared send paths; post-send-only capture is insufficient. Partial provider acceptance and uncertain entry permanently prevent automatic resend of that original. Scope candidates conservatively before completeness counts. Do not claim third-set historical completeness until exact table/route inventory and actual native proof close it; truthful bounded partial phases are approved. Parent supplies immutable BASE and precise implementation phase at dispatch.

Global constraints: preserve merged PR370/371 tenant isolation, immutable source/cutoff/retry behavior and no positive C/basis/reopening authority. Use CLI-created forward migrations, never rewrite published history or hand-edit generated contracts. No hosted writes, deployment or market sends. Only synthetic native fixtures, actual disposable CI evidence. One runtime implementer and independent task review; no subdelegation or parent memory/publication work.

Activation is conditional: actual potentially relevant raw/rejected/unwitnessed/ambiguously linked correction concern or durable producer-gap signal reaching that correction context activates required history at saved cutoff. A new epoch alone does not activate a hold for unrelated ordinary supplies. Once activated, missing inception-to-cutoff coverage/reader failure/overflow holds that correction scope. Unknown dimensions are wildcards, proven unrelated scope stays unaffected. Paired oracle: qualified pre-epoch ordinary supply without matching concern unchanged, identical scope with new matching raw/unwitnessed concern holds on missing history. Saved earlier decisions and finalized retries remain immutable.

## Finite producer inventory
Read reviewed `process-producer-inventory-20260924.md` and `process-producer-inventory-review-20260924.md` in the audit directory. Exact operation table names and supplier-switch events/metering point graph additions are binding minimum coverage, with atomic actual state capture or scoped gap for silently dropped operation-event inserts. A trigger on a missing insert cannot prove event completeness. Parent must name the bounded added tables/routes at dispatch; actual native qualification remains required before complete:true.

## Exact fixture/schema prerequisites
Read process-history-fixture-preflight-20260924.md before implementation. It supplements the approved finite12-table producer inventory without changing policy. Assert real native catalog/FK/trigger order: switch events RESTRICT request deletion, supply periods restrict parent deletion, signed contracts reject DELETE, customer/site graphs have mixed restrict/cascade, and some composite SET NULL can erase tenant/customer fields. Preserve immutable OLD scope/links for tombstones, rollback records on rejected writes, and capture earlier committed partial archive mutations without claiming the archive as atomic. No trigger can witness the swallowed event insert; qualify actual consequential state or retain scoped incompleteness. Keep exact registry/profile preconditions and provider-boundary stubs while running real direct/worker functions; no transport-level mock substituting for the owner. These are readiness requirements, not native PASS.
