# Qualified delegated counterparty sender owner — implementation design

2026-09-23. Revised for independent review Q1 (atomic retrospective correction) and the genuine SOAP2016/export acquisition receipt. Read-only design against `6d2811af9b578aee042727b75747b265f286bda1`; no implementation or test execution. Continue only after the active closure slice. Full E035 remains incomplete. PR310 and accepted D110/parent work remain excluded.

**Goal:** admit a PRODAT structural source whose UNB sender is a genuinely authorized legal agent of its NAD sender, without treating actor configuration, a reviewer assertion or SMTP authentication as representation.

**Architecture:** add an immutable authority-evidence ledger and a source-specific sender owner. Both TypeScript and the append RPC independently resolve the same dated, scoped evidence. Persist the selected authority versions in the existing source assessment; downstream selection uses its immutable snapshot.

Skill routing: writing-plans for component sequencing; prior source/compliance and false-positive review workflow for evidence. This is a narrow design, not a repository-wide audit. Implementation remains sequential with the designated sole coder; no delegation or production edits are authorized by this document.

## Evidence and present boundary

Read `quality/audits/ediel-masterplan-v2/e035-source-ledger/remaining-owner-scope-20260923.md`, sender section. The following original clauses establish the semantic requirements, not a real counterparty's authorization:

| Original | Evidence and implication |
|---|---|
| T24-A-6 §5.2.1–2, printed pp26–27 | Valid Ediel actors; no obsolete internal proxy identifiers for PRODAT/UTILTS. A legal agent appears in UNB while PRODAT/UTILTS NAD retains the represented party. SMTP-only providers do not qualify. UTILTS has at most one agent across its variants. |
| T24-A-6 §5.2.3, pp27–28 | PRODAT supports subaddresses and application reference; an application reference is constructed by the sender, not a portal-issued authorization. Preserve all address components. |
| T24-A-6 §7.3–4, p37 | Ediel agreements and maintained portal information, including agent changes, are required. These clauses do not specify an authenticated historical export or its validity semantics. |
| P26.A/16.B SG4/NAD sender, p45 | FR uses field207 valid Ediel identity, qualifiers160/SVK and country. This is the represented legal sender; replacing it with the agent destroys the binding. |

Original PDFs under `/workspace/scratch/db7cad0629c3/sources/Start av elbolag/`: `260220_Ediel-anvisning-generella_tekniska_regler_version_24-A-6(4).pdf` SHA256 `5204d4514774b04b8eedb039e1f4799ed447c7fef14554577935e2d7bd93f951`; `260630_Ediel_PRODAT_APERAK_Anvisning_version_26-A_16-B(6).pdf` SHA256 `83c2f1d2915851d2e670731f6ab404ef06c9b9def282afbafdfa0eda836a6e95`. Locally read corresponding `technical.txt` lines1341–1390,1393–1480,1989–2018 and `prodat.txt`1864–1904. No external-source absence is asserted.

Current execution boundaries:

- `sourceOwnerWire.ts:19–24`, `structuralSourceWire.ts:42–45`, `closureSourceWire.ts:57–60` reject UNB sender different from NAD FR before an owner could qualify it.
- `receivedSourceOwnerSession.ts:20–48` requires actual Z04 commit, then canonical receiver and selected facility; reviewed structural/closure paths independently perform the same party/facility reads. `reviewReceivedStructuralSource.ts:162–165`, `reviewReceivedClosureSource.ts:65–71` bind NAD sender to selected grid owner.
- `20260922144906_ediel_source_object_decisions.sql` independently compares exact owner input sets with real rows (`owner_rows_match`, lines27–51). `20260922205926_ediel_reviewed_structural_source.sql:review_party_proof_consistent` also requires direct sender equality. `20260923113014_ediel_closure_original_wire_binding.sql:112` repeats equality in original-wire parsing. Changing only TypeScript cannot qualify delegation.
- `tenant/tenantEdielIdentity.ts` is the receiving tenant's owner. Its dated relation must remain independent. `prodatPriorPermissionFlow.ts` is an ESCO permission flow, not sender delegation evidence.
- Existing registry upload is insufficient: `actor-registry/parseActorRegistryXml.ts:103–113` copies partyId into interchangePartyId and derives verification from field presence; `importActorRegistry.ts:478–480` passes true for identifier verification. `importActorRegistry.ts:537–575` accepts uploaded XML and hashes it, without authenticating the issuer or establishing delegation dates. `ParsedActorRegistryActor` contains actors/routes/certificates, no dated principal→agent authorization. Do not promote these flags into authority.

## 1. Capture genuine evidence before enabling acceptance

Add a separate capture boundary, proposed `lib/ediel/sources/senderAuthorityCapture.ts`, and private append-only SQL tables. Do not extend the general XML-upload API with a caller-controlled `approved` flag.

A qualifying capture must preserve exact original bytes, SHA256, issuer identity, acquisition channel and server observation time, provider record/version reference, parser/profile version and exact fragment/record locator. The evidence itself must establish principal, agent, legal representation (not merely SMTP routing), message-family/market/direction scope, and validity or historical status. Actor identity/EDI eligibility evidence must be independently linked for both parties. Record tenant/environment visibility explicitly; an authoritative public relationship does not make every company's source records visible.

Accepted acquisition class for this slice: a server-controlled fetch/capture from the qualified official directory, with authenticated origin and a documented record contract which actually identifies legal delegation and validity. The connector chooses the allowlisted origin and record key; callers cannot supply arbitrary URL, bytes, issuer, observedAt or validity. Retain response bytes and provenance before projecting records. Fetch/parser failures produce unavailable evidence. Credentials remain outside the evidence record. The capture writer has a dedicated database identity; ordinary service-role assessment callers cannot directly insert authority records or invoke its writer function. An RPC taking a JSON claim from the ordinary service role is not an authenticity boundary.

If the provider cannot supply historical dates, store an observation, not a backdated interval. It cannot qualify a previously received source by assuming the relationship existed then. A signed mandate is a possible separately qualified evidence adapter, but it needs issuer/signatory authority, scope and revocation verification; an uploaded PDF plus a reviewer click is not that adapter. Do not silently add it to this slice.

### Acquisition result now superseding the initial source-search gap

Read the tracked `quality/audits/ediel-masterplan-v2/e035-source-ledger/delegated-sender-acquisition-receipt-20260923.md` and local candidate XML, XSD, WSDL and guide text. Recomputed and matched the receipt's hashes for the official XSD/WSDL/PDF and rendered XML. No new hosted operation or SOAP request was made for this revision.

The source chain is now concrete: [official portal webservice documentation](https://www.ediel.se/Info/webservice) → SOAP 1.2/WS-Security UsernameToken service [ExportEdielParties/2016](https://ws.ediel.se/ExternalServices/ExportEdielParties/2016), operation `ExportPartyAddressing`, namespace `http://www.ediel.no/Export`. The request requires boolean `IncludeUpcomingChanges`. A parser/capture interface can now target actual fields; a claim that no candidate service or distinct-party example exists is superseded.

The actual supplied record is EL/SE → company EdielId `64920`, Holmen Energi Elnät AB, role `Netowner` → `EDIFACTDetails Type="PRODAT"`: `InterchangePartyId IdCodeQualifier="ZZ"` = `81300`; `PartyId IdCodeQualifier="160" IdCodeResponsible="SVK"` = `64920`; `EDICharset=UNOC`, `EDISyntax=3`. Preserve this genuine pair and its locator in parser tests. It demonstrates distinct UNB/NAD addressing, not by itself a sending mandate. The supplied header claims `GeneratedAt=2026-09-10T06:35:24Z`, `SenderApplication=www.ediel.se`; neither authenticates uploaded bytes. The export has no detail ValidFrom or deletion records.

Local evidence directory: `/workspace/scratch/db7cad0629c3/sources/delegated-sender-candidates/`.

| Artifact | SHA256 | Qualified use |
|---|---|---|
| `ExportPartyAddressing.xsd` | `b88d768a4858fee5572fe2b7a7de5f6b5afa78924749007f1923a5d1534a2941` | Official standalone export grammar |
| `ExportEdielParties.wsdl` | `f35b539a4de093b305abe50d0952932fcc8995f75bf405c7cc24de2072caa42b` | Official SOAP2016 operation and embedded schema |
| `portal-guide-20141120.pdf` | `1dc4a6dfcc8e681bb9c7fa035970e211364b9358a1f751cbcc23e907885cd8ea` | Linked guide3.1.0, dated2014-11-20; bounded semantics below |
| `companies.rendered.xml` | `ff3bbd4de3ea045baeb41b0cbafbe9d3b36499b147f323f35fa72b15e7e334ff` | Genuine supplied content rendered by Library, **not original bytes**; 787191 bytes versus original metadata787190 |

Guide semantics (pp7–8,15–16,23–24): ASP handles applications on a principal's behalf, with principal responsibility retained; ASP supplies UNB addressing. ESP handles EDI transport/system service and can supply SMTP without thereby becoming the legal ASP. Approved technical changes have effective dates; message details distinguish UNB/NAD and actor-test needs. These support separating legal application delegation from transport, but do not certify that any particular current export row grants sending authority or that the service preserves retrospective history. Do not conflate the guide's older date with a new rule publication.

The receipt also recovered concrete June11/June15 import lineage for64920/81300; those uploaded records cannot be attributed to the September10 export. This is a completed acquisition step, not a reason to rerun broad record searches. `registry_selector_results.json` and original-byte archive materialization remain unrecovered in the bounded search; neither absence is proven. No real provider integration reference emerged, so generic integration-provider settings are not the next search target.

### Smallest buildable slice: typed addressing observations, with no authority promotion

Implement only a nonauthoritative source adapter and immutable candidate-capture contract first: proposed `senderAddressingExport.ts` and `senderAddressingCapture.ts`. This slice is independently useful because it removes the current lossy interchange/legal-ID collapse and preserves actual source change records for later qualification. It does not enable the sender owner or publish authority versions/revision barriers.

Two explicit adapter profiles are necessary:

- `ediel-addressing-file-xsd-v1`: no-target-namespace standalone `CompanyListMessage` against the linked XSD. Header and several qualifier/type attributes are required there.
- `ediel-addressing-soap2016-v1`: SOAP1.2 response wrapper, namespace `http://www.ediel.no/Export`, WSDL operation/action and embedded types. `ExportPartyAddressingResponse/CompanyListMessage` and its Header are optional in this grammar; detail Type, identifier qualifier/responsible attributes are also more permissive than in the standalone XSD. Missing values remain missing and cannot qualify an owner. Do not apply the standalone XSD wholesale to SOAP or strip namespaces to make it match. Reject SOAP faults, unexpected envelope/body multiplicity and wrong operation/namespace as capture failures, not empty successful directories.

The typed observation carries profile/schema hash, immutable candidate ID/hash, acquisition kind (`supplied_rendered`, `uploaded_original`, or verified transport capture), received-at observation time, exact generatedAt lexical value, market code/country, company identifiers/roles, and every physical detail/deletion entry in original order with a structural locator. Retain absent versus present-empty elements; preserve raw identifiers and qualifiers, optional SubAddress, charset/syntax, and communication address type. No fallback from missing interchange ID to company/party ID; no deduplication or first-row selection. Full payload bytes remain the authoritative locator basis; parser text rendering or canonical JSON hashes cannot stand in for original-byte hashes.

For each detail retain `detailValidFrom` as either absent or the exact schema-valid `xs:date` lexical value; for deletion require `deletionValidFrom` and retain `Type` and its company/market parent scope. Preserve timezone presence/absence in date lexicals. Never turn a missing date into epoch/capture/GeneratedAt, or convert date-only values into UTC midnight until a qualified provider temporal contract defines that boundary. No `ValidTo` exists here; do not derive it from the next row, omission, a deletion or generation time before change/replacement semantics are established. `IncludeUpcomingChanges=true` requests upcoming changes, not historical completeness. A deletion observation is not yet a legal retrospective withdrawal event under §3a.

Use a namespace-aware bounded XML parser; no DTD/external entity/network resolution, no regex projection, no silent truncation. Return parse/coverage diagnostics for unsupported structure and ambiguity. Any document/record limits must return explicit incomplete status, not a complete partial export. The candidate parser never returns `authorityAccepted=true` or an owner disposition. Keep candidate storage separate from restricted qualified authority publication, so even an authentically fetched but semantically unqualified addressing record cannot enter the SQL authority selector.

Capture interface: requests are fixed to the documented endpoint/operation/profile with explicit `IncludeUpcomingChanges`; record response bytes/hash, server observation, profile hash, request policy and nonsecret authentication mechanism/credential reference identity. Do not persist UsernameToken/password or secret-bearing request envelopes. A renderer/upload may only create its corresponding untrusted candidate provenance. The service-controlled authenticated adapter remains disabled/unqualified until permitted credentials and actual successful exchange evidence exist; tests cannot forge an authenticated capture by passing a boolean into an ordinary service-role RPC. This design specifies its interface now, not an executed authenticated producer.

Focused slice tests: exact64920→81300 and qualifiers survive supplied rendered XML; file and SOAP profiles remain distinct; missing detail date stays absent; dated details and required-dated deletions are preserved (synthetic mutations labelled as such); missing deletion date rejects; duplicate/conflicting details remain visible; wrong namespace/body/fault cannot masquerade as complete capture; optional SOAP fields produce incomplete ownership evidence rather than default IDs; date offset/absence remains lexical; payload mutation changes candidate hash; rendered bytes never claim original provenance. None of these tests counts as real delegation acceptance.

### Concrete remaining prerequisites for positive sender authority

1. Complete a permitted authenticated request through the documented2016 service using the documented webservice role and obtain an immutable success/fault capture. The acquisition receipt made no SOAP request; current uploaded header claims and public schemas are not that receipt. Use an existing authorized account/session path when available; no provider messages/account creation or hosted operations are authorized by this design task.
2. Follow official service/portal documentation and the actual target record's message-details/technical-change surfaces for the narrow contract: does this ASP/UNB record authorize **sending PRODAT** for principal64920 through81300; which approval/actor-EDI eligibility evidence applies; and what are the date boundary, deletion scope, replacement/correction and historical retention semantics? The current XSD/WSDL encode addressing and dates but no explicit sending-entitlement or EDI-agreement-status proof. Do not manufacture those from role names, schema validity or matching SMTP.
3. Obtain an authentic record/history covering the source receipt instant and independently qualify the selected facility→legal actor link. Optional ValidFrom plus no stated end/history coverage cannot prove an arbitrary historical interval. Even a current successful authenticated export does not retroactively authorize September or earlier originals. If history is unavailable through the observed surface, record that bounded result and follow the documented history/technical-change path; do not declare the provider lacks history without doing so.
4. Qualify the authority projection and publisher against that narrow contract, then implement positive owner/SQL and §3a barriers. Original byte-identical supplied XML and selector annex recovery may improve provenance/interpretation, but neither substitutes for the authenticated receipt and temporal/eligibility semantics above. If the supported service cannot provide one of those facts, name the exact fact needing a separately qualified evidence adapter.

Thus the typed candidate parser/capture boundary is now source-backed and buildable. The genuine authority producer remains a later, concretely gated slice; no administrator assertion, public schema, rendered export or synthetic fixture closes it.

## 2. Immutable records, dates and cardinality

Proposed private records:

- `sender_authority_captures`: immutable bytes/hash, capture provenance, issuer/profile, server-recorded capture/commit time, visibility, explicit validation result. Content hash remains a hash; reprocessing appends a new assessment, never a timestamp suffix to the hash.
- `sender_authority_versions`: capture FK plus record locator/hash, stable principal and agent actor bindings, exact Ediel identifier/qualifier evidence, market `electricity`, environment, direction `sending`, family `PRODAT`, allowed application scope, asserted `[valid_from,valid_to)`, knowledge/recorded time, supersedes/withdrawal link. Missing dates remain unknown. Explicitly separate source-stated time from system observation.

Use an independently qualified binding between selected `grid_owners.id` and the principal actor/Ediel identity; neither a matching name nor two unauthenticated uploaded ID strings suffices. Include this binding in the SQL readset. No actor ID merging or wider registry cleanup is part of this slice.

Policy: establish sender authority **at source receipt time**, using immutable `source_received_at`, not UNB local time, source effective supply date, review time or caller time. This is the conservative owner policy, not a claim that T mandates receipt rather than dispatch time. If a delegation changed between dispatch and receipt and the evidence cannot establish the selected instant, leave unavailable. Future dispatch-time support needs trustworthy dispatch-time evidence. Structural effectiveFrom/effectiveTo continues to govern supply/meter selection separately.

Knowledge cutoff is assessment creation time. Later acquired genuinely historical evidence may produce a new assessment of the same sealed original, with knowledge time recorded now; it cannot alter what a previous snapshot knew. Active-now relation alone does not prove authority at past receipt. Never extrapolate capture time backward. Expiry after receipt does not erase a formerly qualified source; a later withdrawal/correction is published with the mandatory snapshot barrier specified in §3a; future consumers cannot reuse the dependent accepted assessment while it is unresolved.

Select exactly one compatible authorization after applying explicit supersession as of cutoff. Two conflicting/overlapping candidates, even same agent with incompatible dates/scope, are unavailable; never `limit(1)` or latest-row guessing. UTILTS is outside this enabling slice; do not model per-TIM/SCH agent exceptions that would violate T5.2.2. Application reference narrows the proven scope; it never grants a relationship on its own.

## 3. Source-specific owner and SQL independence

Add `senderDelegationOwner.ts` with an internal resolver taking sealed source identity, complete physical object key, parsed original parties/application scope, selected facility, source receipt instant and assessment cutoff. Return either an explicit direct binding or `delegated-counterparty-sender-v1` with exact immutable capture/version IDs and hashes, temporal evaluation, complete candidate readset, actor binding evidence and reasons. Never accept a caller-supplied accepted owner.

Binding includes company/environment, source message UUID and payload hash, canonical assessment, physical message index/reference, object index/object identity agency, register scope, NAD FR/DO and full UNB sender/receiver composite components. Preserve existing one-message/agency9 limits. Subaddresses remain unavailable until their existing independent qualification gap is closed; do not truncate them to make a sender match. Direct sender retains existing facility checks and receives an explicit direct branch; receiver evidence stays unchanged.

Integrate resolver in all three production paths: committed `receivedSourceOwnerSession.ts`, `reviewReceivedStructuralSource.ts`, `reviewReceivedClosureSource.ts`. Keep commit/reviewer permission and coverage evidence requirements intact. Extract wire identity without imposing direct equality, then demand the correct owner branch. The three wire readers and the SQL original-wire parser must change together; rejecting unsupported components remains parser responsibility, legal delegation becomes owner responsibility.

Forward SQL migration must:

1. Create private immutable records with RLS/FORCE RLS, revoked direct writes, no mutation/truncate, strict hashes/FKs/scopes and restricted capture ingress.
2. Recompute original wire/source/hash/physical scope and NAD→facility owner binding; resolve authority versions itself from private tables under a fixed transaction snapshot. Compare complete candidate set and selected IDs/hashes with supplied evidence; do not accept JSON `is_verified`, `authority`, timestamps or selection as proof.
3. Apply receipt-time validity and knowledge-cutoff rules, reject gaps/ambiguity/scope mismatch and verify issuer/capture qualification. Record durable selected authority references in source assessment facts. Ensure concurrent capture/revocation is ordered by committed ledger visibility; no precommit timestamp can make later data appear in an earlier snapshot.
4. Preserve receiving tenant independent reads, roles and dated transport delegation. Extend `object_owner_proof_consistent` and `review_party_proof_consistent` through one internal sender proof routine while retaining their separate business checks. Remove direct equality only where that routine replaces it.
5. Preserve append-only assessment chaining/witnesses and whole-object coverage. A missing or invalid sender owner is `unavailable`, not a fabricated national negative ACK.

Old direct accepted assessments remain readable under their old version. Existing delegated originals gain no authority automatically: explicitly revalidate sealed bytes and append a new assessment only when genuine evidence qualifies. No historical delegated-row backfill. Snapshot consumers use persisted owner references; they must not look up the current directory again and rewrite old selection.

## 3a. Retrospective correction owner and atomic snapshot visibility (Q1)

Choose a **durable snapshot barrier**, not an asynchronous promise to rewrite all source assessments. Proposed private SQL owner `publish_sender_authority_revision` is the only publication path for a qualified replacement/withdrawal; `open_object_selection_snapshot` is the sole consumer that materializes its impact. The existing source timeline alone cannot discover an authority-ledger change.

Add immutable `sender_authority_dependencies` alongside each accepted delegated object, inserted by the same append transaction as its assessment. SQL derives each dependency from the independently selected authority, never a caller list: company/environment, source UUID/hash, assessment ID/facts hash, complete physical object key, selected authority version/capture and principal, source receipt instant. Require one complete dependency per accepted delegated object through the append function and integrity validation; no accepted delegated row may commit without it. Historical direct rows need no fabricated dependency.

Publish the new capture/version **and** an immutable `sender_authority_revision_events` row in one restricted database transaction. The event identifies the superseded version/principal, qualified correction evidence, asserted affected receipt interval or explicit unknown extent, and publication transaction identity. No intermediate state exposes a usable correction without its barrier. Unknown extent conservatively affects every dependent source of the superseded version/principal in its permitted visibility scope. A normal expiry starting after a source's receipt does not block that source when the earlier interval remains established. Missing/uncertain semantics cannot be labeled harmless expiry.

Extend the ONE MVCC statement inside `open_object_selection_snapshot` (`20260922150922_ediel_source_decision_snapshots.sql:50–91`) to read assessments, dependencies and committed revision events together. For each candidate source/object, join its selected assessment dependencies to visible events. Emit a versioned `senderAuthorityBlockers` array inside the immutable readset, including source/object/assessment identifiers, event/version hashes and reason; include dependency/event counts and a completeness marker in the hashed snapshot. An affected latest accepted object becomes unavailable **for this snapshot**, without altering the old assessment. Apply this overlay in `receivedSourceDecisionTimeline.ts`/`structuralSourceReadset.ts` before projecting accepted structural versions and closures; block the source identity against fallback to an older accepted assessment or predecessor. A compromised closure cannot become a harmless omitted closure that revives supply: retain an explicit affected-window blocker consumed by structural comparison.

Dependency discovery is a database join over all candidate accepted delegated objects for the requested company/environment, with exact counts; no background pagination or caller-selected impacted IDs. If a dependency is missing, a correction's principal/scope cannot be resolved, or event/dependency bounds overflow, mark the snapshot incomplete and return unavailable through the existing readset path. Do not emit a partial apparently complete list. Shared principal corrections are stored once with explicit visibility rules and independently joined in each authorized tenant/environment snapshot; publication must not rely on enumerating a currently known list of tenants. Cross-tenant payloads are never returned. The event foreign-key/reference model must allow the conservative principal barrier even where version-specific dependency matching is uncertain.

Publication and capture are atomic. A snapshot statement whose MVCC view precedes the correction commit may retain V1; one whose view follows the commit must include V2's barrier and cannot select affected V1. An assessment concurrently committing against V1 is still caught by its mandatory dependency in every subsequent snapshot; revalidating only during assessment append is insufficient. Record the committed event transaction and the consuming snapshot’s observation as the barrier visibility witness; no separate delayed witness is required before blocking. Positive replacement authority still requires its qualified availability evidence. Source/provider timestamps alone cannot backdate visibility. Safety barrier discovery considers all committed visible correction events at snapshot acquisition, even if the caller passes an older business cutoff; it cannot be evaded by requesting a fresh snapshot with a pre-correction cutoff. The historic result is available only by replaying its already persisted immutable snapshot.

Clearing requires a newly appended, fully qualified source-object assessment which records the correction event/version it resolved and independently proves authority at receipt from the new evidence. A later event affecting that selected authority blocks again. A pending reassessment, reviewer acknowledgment, new directory flag, or bare replacement capture cannot clear the barrier. `reviewReceivedStructuralSource`/`reviewReceivedClosureSource` own explicit source reassessment under their current permissions and complete business checks; committed-Z04 evidence must retain its existing genuine handoff requirements. If reassessment cannot be established, leave blocked, not automatically accepted.

Keep old snapshot bytes/hashes and old source assessments untouched. Existing nonheld UTILTS reservations/replays retain their recorded outcome and consumption binding; this correction never retroactively rewrites ACKs or quantities. A new UTILTS invocation receives the new barrier and holds the affected transaction; an eligible identical-byte held retry may requalify only after an explicit qualified successor clears it. Previously completed business-side correction/reconciliation is a separate authorized operation, not an implicit side effect of authority publication.

Add these decisive native+runtime cases beyond the generic revocation test:

- Accept source A with V1; snapshot S1 selects A. Publish genuine retrospective V2 covering A's receipt. Snapshot S2 must hold A and must not fall back to an earlier assessment/source; replay S1 is byte/hash identical. Repeat for a closure and verify supply is not revived by dropping the closure.
- Publish expiry only after A's receipt: S2 still selects A. Publish an event with unknown affected extent: S2 holds A conservatively. An unrelated principal/source remains unaffected when completeness is proven.
- Two real transactions interleave event publication and snapshot/assessment capture on both sides of commit. No snapshot sees published V2 without its barrier; late V1-dependent assessment is held in later snapshots. Requesting a new snapshot with an old cutoff does not bypass the visible barrier.
- Omit/forge a dependency; exceed a discovery bound; place dependent sources in two permitted tenant scopes and an unrelated tenant. Native append rejects missing linkage, new readset cannot claim completeness on truncation, each permitted scope blocks its own dependent source and returns no other tenant's records.
- After V2, qualified successor assessment resolves the event and only a new snapshot becomes usable. Cached JSON approval cannot clear it. Nonheld reservation remains immutable; identical-byte held replay follows the new snapshot and preserves consumption-binding checks.

## 4. Tests through real consumers and native append

Extend `scripts/ediel-source-owner-native.test.ts` and corresponding runtime suites; use real original PRODAT wire, canonical validation, owner entry points and actual PostgreSQL RPC. Mock only qualified capture transport where necessary; mark generated authority fixtures synthetic. No serializer-only acceptance tests.

| Case | Required outcome |
|---|---|
| Delegated Z04 actual committed handoff; reviewed Z04, Z06F/G, Z10M; Z05 closure/cancellation supported by current closure slice | NAD stays legal grid owner; distinct UNB agent qualifies solely through captured dated authority; selected source survives durable snapshot reading. |
| Delegated sender plus independently delegated receiver | Both resolve against separate principals and evidence. Receiver relation cannot authorize sender, nor vice versa. Direct/direct and direct/delegated receiver regressions remain green. |
| Same bytes with missing evidence, then later capture proving historical receipt interval | First assessment unavailable; explicit reassessment accepted at later knowledge cutoff; old snapshot unchanged. Capture without historical dates still unavailable. |
| Exact validFrom/validTo boundaries; revoked, future, gap, overlapping authorities; PRODAT vs UTILTS, electricity vs gas, wrong application/environment/company | Half-open dates and exact scope enforced in TypeScript and native SQL. No latest-row fallback. |
| Change source hash, message reference/index, object index/agency, NAD legal principal, UNB agent/qualifier/subaddress, selected point/grid owner, capture hash/version/locator | Native append rejects forged evidence even when submitted JSON claims accepted; runtime leaves owner unavailable. |
| Upload arbitrary XML with valid-looking IDs, verified flag, matching SMTP/certificate, tenant relation or reviewer statement | None qualifies sender representation. Ordinary service role cannot manufacture a capture via direct table write or unrestricted RPC. |
| Concurrent revocation/new capture vs assessment; replay old snapshots; changed directory projection after selection | Immutable selection and cutoff ordering are preserved. No retroactive authorization or corruption of the old snapshot. |
| Accepted delegated structural source → `qualifyReceivedUtiltsStructure` via real `processInboundEdielMessage` E30/E66/S07 flow | Matching readings proceed to actual transaction persistence, ACK plan and metering/billing policy; mismatch produces existing E61/E62 only; unavailable source produces internal review with no held quantities. Include mixed eligible energy/held-reading case and independent transaction behavior. |
| Identical-byte held retry after qualified source reassessment; changed-byte retry; preexisting nonheld reservation | Preserve consumption binding, held requalification and immutable nonheld ACK reservations. Do not use this slice to bypass or redesign retry authority. |

Acceptance sequence: bounded typed observation/candidate-capture slice first (no owner activation), then positive source-record semantics and authenticated capture qualification; failing runtime/native owner tests; implement capture/resolver/forward SQL together; public-consumer tests; focused schema/type and migration verification; independent scoped review, then root's exact-head delivery gates. This design has not run those tests and does not certify a real authority record. A positive mocked resolver alone is not closure of delegated sender E035.
