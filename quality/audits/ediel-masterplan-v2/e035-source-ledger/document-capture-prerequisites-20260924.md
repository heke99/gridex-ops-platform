# E035 Task 2b prerequisite gate: document reference context

Read-only preflight, 2026-09-24. Task 2b is PREPARED, not dispatched; first accept the exact Task 2a source-capture head and its native result. This note checks actual prerequisites only, without DB calls, tests or implementation. `document-reference-task2b-brief-20260924.md` and the approved reference design/review govern the feature.

## Gaps first

1. **`documents.read` lacks a proved clean-replay registry row.** Repository migration exact-key search found it only in `20260519_saas_ui_tenant_admin.sql:174`'s `permission_keys` role-link array, not an `INSERT public.permissions` VALUES. `lib/rbac/catalog.ts:297` is display metadata, not a DB writer. `20260924003708_communication_permission_registry_completion.sql` creates only `communication.read` and `communication.send`, and must not be repurposed. A new CLI-created forward should materialize exactly established `documents.read` with `ON CONFLICT(key) DO NOTHING`, without role/user grants, aliases or rewriting published history. First verify singleton key on authentic clean replay; no test fixture may insert the registry prerequisite itself. This is a static inventory, not an executed assertion that a hosted DB lacks the key.
2. **`customers.read` has an actual materialization path but still needs a replay oracle.** `20260519_bootstrap_div3rsa_superadmin.sql:98–126` inserts it, and `20260522_customer_flow_access_repair.sql:103–132` independently inserts it when `public.permissions` exists. The clean-replay script supplies the foundation before canonical migrations; assert exactly one active key in actual isolated replay rather than inferring it from role lists. Do not create a duplicate forward or change historical grants if the replay passes.
3. **Canonical capture permission is `communication.send`.** Fix4's new forward materializes it; `app/admin/ediel/correction-actions.ts` and `20260924001447_ediel_correction_capture_canonical_permission.sql` already use it. The document design's older `communication.write` wording is superseded by `correction-capture-permission-amendment-review-20260924.md` and the Task 2b brief. Require action `{allOf:['communication.send','documents.read','customers.read']}`, operational company and SQL selected-company rechecks; each key must exist and be effectively assigned to the synthetic actor. No test-only bypass or grant replay. Source-only Task 2a possession does not give document read permission.
4. **Task 2a's source concern has no established customer/supply link.** Its sealed original fixture checks enabled `PRODAT:Z05:C:26.A:r3`, actual inbound source `gridex_received_sources.sources`, hash/context/receipt, company/environment and observed wire object/agency/parties/old stop; its concern facts intentionally contain `customerId:null,supplyPeriodId:null`. Task 2b must construct and verify a *separate real tenant graph* before storage I/O: source's exact object/agency/legal parties to tenant `metering_points` and `customer_sites`, same-company customer, linked `customer_supply_periods`, `customer_contracts` (company/customer/site/point), and `customer_contract_documents.customer_contract_id`. Conflicting/missing graph remains an unresolved scoped concern, never a guessed link. Cross-company document failure discloses no bytes or metadata. The C is not an authenticated correction reason.

## Fixture and I/O readiness for implementer

| Check | Actual owner and required proof |
|---|---|
| Source | Use Task 2a's accepted sealed `gridex_received_sources.sources` original, not mutable `ediel_messages` status or a synthetic manually inserted source row. Assert singleton enabled C profile, original raw/hash/received context and a witnessed concern; input actor comes from authenticated server action. A relevant attempt must commit before slow storage read. |
| Locked PDF row | `customer_contract_documents` has `id,company_id,customer_contract_id,document_type,storage_path,mime_type,document_sha256,generation_snapshot`; `20260716183000_contract_canonical_finalization.sql:1499–1526` locks company/contract/type/bucket/path/MIME/hash/snapshot after non-null path and forbids DELETE. Require `signed_contract_pdf`, exact private `customer-contract-documents` bucket, non-null path, PDF MIME, exact row/document ID and same-company contract graph. Null path and forged cross-contract rows are negative fixtures. `generated_at`, `archived_at`, `verified_at` are not the immutable identity/cutoff. |
| Actual local bytes | `archiveSignedCustomerContractPdf` uploads with `upsert:false` to tenant/contract/content-addressed path; supplied digest and existing-row fast return alone do not verify bytes. Use synthetic local private-bucket storage and actual readback/hash. Revalidate at each new process read; replacement/deletion or unknown retention remains unavailable. No new PDF byte copy or retention category follows. |
| Bounded stream | Installed `@supabase/storage-js` 2.103.0 type/implementation supports `.download(path, {}, {signal, cache:'no-store'}).asStream()` returning `Response.body`. Existing `downloadAndVerifyCustomerContractDocument` awaits Blob plus `arrayBuffer()` and is unsuitable for in-flight 2 MiB cap. Add a distinct bounded helper: 2,097,152 actual bytes maximum, incremental SHA-256, 10-second fetch/body deadline with abort and deadline race, discard late success, and keep ordinary download callers unchanged. Unit stall/oversize mocks plus native local Storage readback are separate oracles. |
| Permission/native assertions | On clean replay assert registry singleton/active for send, documents.read and customers.read, then effective selected-company `gridex_actor_has_company_permission` for all three. A company-admin role's textual permission list cannot create a missing row. Use isolated actors for absent document permission, absent customer permission, read-only/wrong company/inactive or revoked membership, and show zero new attempt/witness and zero storage reads before authorization. Preserve legitimate role/direct-grant scope and no new automatic role grants. |

`app/api/admin/customer-contract-documents/[documentId]/route.ts` checks documents/customer read plus company access before ordinary download, but it has neither the E035 source graph nor in-flight byte cap. Task 2b needs its own server action and SQL validation, not a call through that HTTP route. The reference is context-only; a committed witness proves visibility of the receipt, not continuous PDF storage, independent grid-owner cause, positive C or historical process completeness. Task 2a's pending exact-head qualification remains the dispatch gate.
