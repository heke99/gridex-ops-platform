# E035 Task 2b fix round 2 — independent scoped review

Base `7a28b9ab086da88d320d7da96c54c503bcdafc75`; reviewed head `8089f34bfced7e72b36958c387cbf748f97bd89c`. Scope: supplied diff/report, canonical replay selection, the registry forward, manifest and native regression. No runtime changes or commit made.

**SPEC: APPROVE for this bounded fix. QUALITY: APPROVE for this bounded fix. No blocking findings.** This approves the candidate for publication and actual native qualification, not Task 2b acceptance or a native PASS.

The canonical replay selects explicitly ordered foundation inputs and files matching `^\d{14}_.+\.sql$` (`scripts/gridex-aud-003-clean-replay.sh:152–160,193–205,247–264`). Neither eight-digit legacy INSERT file is in the declared foundation/order, and those paths do contain the historical `customers.read` materialization (`20260519_bootstrap_div3rsa_superadmin.sql:103`, `20260522_customer_flow_access_repair.sql:105`). A search of fourteen-digit migration sources found only the document actor's reference to `customers.read` before this forward. The new timestamped file therefore supplies the missing clean-replay registry prerequisite. `public.permissions.key` is unique and `is_active` defaults true (`01_db1_schema_repair_core_helpers_and_canonical_tables.sql:435–443`). The inserted label, description and `Kunder` category match the established catalog (`lib/rbac/catalog.ts:220–224`). `ON CONFLICT(key) DO NOTHING` preserves existing disabled rows and metadata. The forward inserts no assignment, changes no authorization function, and leaves earlier migrations intact.

The native test checks singleton active registry state before its fixture can directly grant the synthetic actor (`scripts/ediel-document-reference-native.test.ts:66–68,88–92`). Its repeat case reads the committed migration from Git because the replay moves migrations into HOLD, obtains a real seeded actor/direct assignment, snapshots the row and all three assignment tables, disables and renames the row in a transaction, runs the INSERT twice, checks identical state, rolls back, and rechecks the actor's effective selected-company permission (`:94–107`). This is a meaningful preservation/repeat oracle against PostgreSQL, although it is not yet executed on this candidate. Existing per-actor checks cover all three effective keys in the fixture (`:81`). The first-application oracle is the actual clean replay plus singleton assertion, not the repeat test's already-present row.

Verification performed for this review: `git diff --check` over the stated commits passed; forward SHA256 matches registered `cc91587d97507a0f3ab810de11df02ed588c49914cf164d5e1fd3a213adb1243`; `npm run db:migrations:integrity` passed (613 files, 517 version groups). The author reports scripts/tests typechecks and scoped ESLint passing. Actual clean replay of this head, all expected 262 native cases, document DB/Storage assertions, generated artifact reconciliation and same-head CI remain **unverified**. The earlier actual result is 228/261 with 33 failures at the absent registry precondition, not proof that this candidate passes.

Skill routing: used differential review, code review, Supabase/Postgres schema reasoning and verification-before-completion for this scoped migration/test delta. Broad quality-playbook audit, UI/performance, static-analysis sweep and subagent workflows were outside the assigned review boundary. No material suspected security finding required a separate false-positive investigation.
