# E035 Task 2b — independent native evidence review (2026-09-24)

Published runtime `9b6ee291b2e5e0b74d89a620fbcbd39b4dadc8dd`. Scope: the supplied OPS `35948357290` / native job `107471286079` artifact `10786849836`, the actual replay log, the native document cases and their production contracts, and the generated types diff. This review makes no runtime edit or commit and does not reopen the earlier static implementation review.

**SPEC: provisionally APPROVED for Task 2b native behavior. QUALITY: provisionally APPROVED for the bounded native qualification.** The prior scoped static SPEC/QUALITY approvals stand. This is an evidence verdict for the native portion, not final Task 2b acceptance, Task 3 completion, full E035 closure, deployment or positive correction authority. The remaining generated-contract and final gates below must still pass on the reconciled head.

The ZIP SHA256 is `77600354e2a64d93a7eb0cf4dca9b3e5c31e82f01eaddd23e2e49950e2f85b0f`. Its log shows clean replay completion and five native files **262/262 PASS**: retained 224 and document 38. The document suite alone is **38/38 PASS**. The run also records case-view 1/1; the supplied parent status reports browser 2 and post-browser 1. The replay reaches generated types, which have SHA256 `f2a05bbb69ec298e78cf21cd6761a62aae82ba9bdde5b2aa38032b5e062d8e5f`, then stops solely because the manifest still expects `30c9f682c20f5e1d9ff203a445e24785d1058d3997f233d75e05b3eebee756e7`. No tenant/parity/schema or final same-head gate is inferred from this stopped run. The manifest and later parent-owned reconciliation remain pending at the time of review.

Native evidence is substantive, rather than mere case discovery. The actual local Storage bucket readback validates hash and the exact 2,097,152-byte acceptance boundary; 2,097,153 and 10,485,760 bytes become unavailable. The helper counts actual bytes incrementally, discards an overflowing chunk before hashing, bounds the operation at ten seconds, and retains no complete PDF buffer. Two 10-second loopback tests use the installed Storage SDK and actual Node fetch/TCP for header and body stalls, checking abort, connection closure and rejection of late responses. The local loopback seam is transport evidence; it does not assert that real project Storage was artificially stalled. The overshoot observation is an eligibility failure, not a promise that no oversized chunk reached the process.

The test establishes the source-scoped raw candidate and durable incomplete epoch after an injected failure before the first attempt. A successful begin commits an immutable attempt before the Storage read; injected failures before outcome and witness leave the expected unresolved or unwitnessed facts. SQL enforces different transactions for attempt/outcome and outcome/witness, and the saved cutoff preserves the original null-path document identity, xid pair and persisted witness snapshot after later row completion and a new verified observation. A fresh cutoff has its own visibility snapshot. No receipt claims continuous availability or existence at database commit.

Authorization and graph cases cover each of `communication.send`, `documents.read`, and `customers.read`; foreign document/environment; wrong-company actor; revoked actor at outcome append; and same-company customer, site, point, supply and party defects. The supply fixture now clears both contract aliases and verifies their persisted null state. Negative graph cases assert no Storage client access. The registry repeat test preserves existing metadata and assignments, and direct table access/mutation and authenticated RPC execution are denied. Upsert-false and immutable archive-row checks prevent metadata substitution. Delete/replacement before append, before witness and after witness preserve the earlier recorded receipt while new readbacks report unavailable; a byte replacement with unchanged metadata fails its hash. These are synthetic local fixtures, not proof that either real signing producer ran or that historical bytes are retained.

The generated types file in the ZIP and the working `supabase/database.types.ts` are byte identical at the artifact hash. Relative to published HEAD, the diff adds exactly 39 lines comprising four public JSON-returning RPC signatures: begin, observe, read and witness document reference. It changes no table types or other generated contracts. The signatures agree with the four public wrappers in the forward migration. This authentic bounded diff is suitable for the parent to register in the generated-types manifest, but registration and its ensuing checks are not part of this verdict.

Residual gates: register the genuine generated types hash, reconcile generated schema if required by the actual CI sequence, rerun the halted generated-contract and tenant/parity/schema/final same-head checks, and record their outcomes. The native pass does not erase unknown pre-epoch history, authenticate signing origin or correction cause, create positive C authority, or implement Task 3/4 composition and persistent combined readsets. Any later consumer must keep source-scoped activation, unresolved/unwitnessed concerns, saved-versus-fresh observations, and `authority:none` / `coverage:incomplete` intact.
