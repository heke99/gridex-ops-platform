# Task 2b preparation — 2026-09-24

Status: **PREPARED; NOT STARTED**. Read-only implementation inspection plus this requested preparation note only. Observed HEAD `6091edc7f4717aec3e17dda72c2529b247c7e91f` is not an accepted BASE. Await parent explicit START and immutable BASE after Task 2a native/tenant/parity/schema acceptance. No runtime edits, tests, migrations, commits, hosted operations or sends performed. Parent-owned checkpoint changes were left untouched.

## Confirmed prerequisites and implementation seams

- Read AGENTS, current memory/active plan, Task 2b brief, approved document design and DR-1–4 rereview, bounded-I/O and capture-prerequisite audits, Task 2a report, current source action/service/SQL, and actual document helper/schema. Older `communication.write` prose is superseded: current source action uses `communication.send`.
- Exact migration search confirms `documents.read` occurs only in the tenant-admin permission-key array, not a proved registry INSERT. After START, create a CLI 2.101.0 forward for exactly this established key with conflict-do-nothing and zero assignments. `customers.read` has actual INSERT owners in the two prerequisite paths; assert all three singleton active keys and selected-company effective authority in native replay. Tests must not self-seed missing catalog keys.
- Current source capture accepts only source IDs/actor and deliberately rejects document context. Its facts have unknown old stop and no established customer/supply association. Preserve its published migrations/API. Add a distinct reference action/service and private append-only attempt/outcome/witness RPCs. Public input remains only company/environment/source/document IDs and confirmation; actor comes from authenticated guard, with all three permissions and operational-company check.
- Document helper currently buffers Blob then `arrayBuffer()`. Keep ordinary callers unchanged. New bounded path must use installed streaming builder with third-argument signal, incremental SHA, actual 2,097,152-byte cap, fixed 10,000 ms fetch/body deadline, abort/cancel and a deadline race that discards late success. Return observation metadata only; persist no duplicate PDF.
- Actual schema has contract `customer_id`, `site_id`, `customer_site_id`, `metering_point_id`; point `customer_id`, `site_id`, normalized identity and grid owner; site customer/facility/grid owner; supply both legacy `contract_id` and canonical `customer_contract_id`. SQL must resolve/recheck the exact canonical graph and reject conflicting aliases instead of assuming legacy IDs are interchangeable. Bind source object/agency/legal parties/environment through actual source and tenant owners. Foreign documents fail without disclosing metadata or creating a foreign concern; missing/ambiguous same-company graph stays conservative.
- Locked document identity requires non-null path, signed-PDF type, exact private bucket, PDF MIME, expected SHA and generation snapshot. Signing/admin-import origin remains described context, never correction authority.

## Ordered work after START

1. Recheck accepted interfaces and working-tree ownership; read installed Next server-action guidance and test-quality guidance. Add focused RED cases for stream cap/deadline/cancellation and actual action sequencing/auth. Add native SQL/storage cases before implementation.
2. CLI-created forwards: registry prerequisite; private forced-RLS append-only reference state, narrow service wrappers, actor/graph validation and separate committed-row witnesses. Durable unresolved attempt must precede storage I/O. Outcome stores separate server storage interval, DB record clock/xid and predecessor; witness only establishes committed receipt visibility.
3. Implement bounded helper and capture/revalidation service. Include a durable completeness contract for failure before attempt commit: raw sealed-source discoverability plus explicit reference producer coverage must keep the reader incomplete. An in-memory failure flag is insufficient. Unknown historical document coverage always remains incomplete; no positive C, boundary clearing or historical byte-availability claim.
4. Verify exact limit/+1/10 MiB, byte hash and actual local synthetic Storage; fetch/body stalls, late completion, hash mismatch, null path, interruption and lost witness; graph/actor/cross-tenant negatives and direct DML/RPC denial; replacement/loss before append, between append/witness and after witness; new read revalidation with immutable saved-cutoff outcomes. Isolated fixture grant cleanup must preserve the accepted tenant invariant.
5. Run bounded relevant checks under Node 22.23.2, record exact RED/GREEN and native status, stage only owned files, commit/freeze for independent SPEC/QUALITY. Parent owns authentic CI and generated types/schema; no hand edits to generated contracts.

## Readiness and open gate

Ready for implementation dispatch; no newly confirmed design blocker. The sole dispatch blocker is pending parent acceptance/START/BASE. Exact graph queries and durable failed-producer/coverage mechanics require implementation-level qualification against the accepted source owners; they are required work, not assumptions already proved. Native tests and Storage execution have **not run** for Task 2b.

Skill routing: inspected using-superpowers (its explicit subagent exception applies); executing-plans preparation, test-driven-development, Supabase/Postgres and verification-before-completion apply. Parent-provided platform-preflight records same-day official docs/CLI qualification. Next/action and writing-good-tests guidance activate before code/tests; independent review/branch completion are parent gates. No delegated work, worktree/branch changes, UI, broad audit, performance optimization, dependency change, skill authoring or unrelated refactor is required.
