# E035 Task 2b — bounded generated-schema/native review (2026-09-24)

**SPEC: APPROVED for the bounded document native/schema evidence. QUALITY: APPROVED for the bounded generated-contract reconciliation.** No confirmed finding in this scope. This extends the prior provisional native verdict; final Task 2b acceptance remains conditional on publication and successful same-head ordinary CI. It does not approve Task 3/4, positive correction authority, whole E035 closure, deployment, or merge.

Scope was the published runtime `2fb06cb171deb7bee286ea0fcc0d974ffdad036f`, OPS `35949085146` / native `107473500225`, artifact `10788152004`, its generated schema/types and replay log, and the already published forward migration `20260924013820_document_reference_context.sql` (SHA256 `9d3bce11f58f704159b8c0b98ca12a617a8781dc0c9c90acf4ee4367c1ab042b`). No runtime code, migration, generated source, or project memory was edited by this review. Skill routing: differential/code review, spec-to-code, Supabase/Postgres security and verification applied to this bounded owner and artifacts. The repository-wide Quality Playbook and broad static/security/performance audits were already performed upstream and are outside this evidence-only recheck; no new independent agent or test implementation was appropriate.

The supplied ZIP at `/workspace/scratch/4b1d39503015/e035-pr372-document-schema.zip` hashes to `ed8a05257ac50f00624538e2242c468825617a8d1830690badf6c6ff92f2bccd`. Its `rem002-schema-snapshot/schema.sql` and `schema.fingerprint.json` are byte-identical to the current uncommitted `supabase/` copies. Schema SQL hashes to `b7aa3dc018528e93b7238e29bd3c2667ce4f446abacff6e04a02259c79e66ec0`; fingerprint content is `e28df3203db729c2db2d2f9ba82ba0d98cdd1247072d6c564616d0770ca2f67d`. The artifact's generated types and `supabase/database.types.ts` are also byte-identical, SHA256 `f2a05bbb69ec298e78cf21cd6761a62aae82ba9bdde5b2aa38032b5e062d8e5f`. The actual log records the schema snapshot writer producing this fingerprint, then the expected `db:schema:check` failure against the old committed `f6632eb6…` snapshot. Thus the failure identifies a stale committed artifact, not a live-vs-generated schema disagreement after copying the genuine bytes.

The schema diff adds 533 lines and no removals. It contains exactly four private document reference tables, ten document functions (six private including two helpers, four public service wrappers), their constraints/FKs/index, eight immutable update/delete/truncate triggers, forced and enabled RLS for all four tables, and function ACLs. All ten function bodies are byte-identical to the published migration despite pg_dump formatting around definitions. The migration explicitly revokes all four table grants from PUBLIC, anon, authenticated and service_role; the snapshot adds no table grant. Each new function revokes PUBLIC execution; the eight callable private/public functions grant only service_role, while actor/facts helpers gain no role grant. Public wrappers check `current_user='service_role'`; private functions and owner graph retain actor permissions, company/environment/source/document scope and separate committed attempt/outcome/witness transactions. This snapshot adds no policy and no permissive table ACL. The fingerprint section changes align with these owner additions; enums, extensions, policies, schema grants, sequences and views remain unchanged. The source-only `documents.read` permission seed is data, so its absence from schema DDL is expected.

The replay log reaches native **262/262 across five files** (224 retained + 38 document), case-view **1/1**, protected browser **2/2**, post-browser **1/1**, tenant invariants PASS, and parity self-test PASS for identical schemas and every injected drift class. The generated types matched before the schema gate. The schema check failed solely on the old checked-in snapshot, as above. These are the artifact's observed outcomes; no fresh database replay was run in this review. The prior detailed native analysis remains `quality/audits/ediel-masterplan-v2/e035-source-ledger/document-reference-native-review-20260924.md` and is not reopened.

**Open gate:** parent commits/publishes the authentic schema and fingerprint with its manifest/memory reconciliation, then verifies all required ordinary checks on that same final head. A clean final schema check and CI status cannot be inferred from the stopped old-head replay. No unrelated regression was found in this bounded generated diff; untested whole-delivery claims remain unverified rather than approved.
