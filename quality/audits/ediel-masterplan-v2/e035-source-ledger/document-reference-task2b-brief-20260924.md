# Task 2b: Reference-only document correction context

Status: PREPARED, NOT DISPATCHED. Depends on reviewed Task 2a sealed-source capture. Parent will supply immutable BASE at dispatch; never infer that current worktree is frozen.

Read the approved design and scoped review in `quality/audits/ediel-masterplan-v2/e035-source-ledger/document-reference-design-20260924.md` and `document-reference-design-review-20260924.md`. They are the full contract, including DR-1 through DR-4; this brief does not weaken their requirements. Read Task 2a report and actual final interfaces after it is accepted.

Implement only `context_document_reference_v1` as a non-authoritative concern. No copied PDF, new retention class, positive cause/issuer validation, closure-edge removal, C entitlement, deployment or external message. Use server-owned source/document IDs and confirmation; derive authenticated actor, enforce allOf communication.write/documents.read/customers.read and operational company. SQL independently rechecks actor and actual source/document/contract/customer/site/point/supply tenant graph. Unresolved same-tenant linkage must remain conservative; foreign-tenant details must not leak.

Commit an immutable unresolved attempt before actual bounded storage I/O. Verify the locked row and current bytes using the existing download/hash semantics with true bounded cancellable I/O, at most 2,097,152 actual bytes. Record separate storage observation interval, append transaction/server time and later committed-row witness; witness does not prove byte availability at commit or continuous retention. Any loss/failure/oversize/witness interruption preserves concern, and initial capture failure is explicit incomplete coverage. No metadata-only availability claim. Add new append-only private service RPCs in a CLI-created forward migration if Task 2a has been published; never alter published SQL or hand-edit generated contracts.

Tests must cover actual synthetic document readback/hash, exact limit/+1/10MiB ineligibility, cancellation and interruption, storage change/loss at the three design boundaries, scoped actor/tenant/link failures, original row lock versus null path, client/service direct mutation denial and separate witness. Preserve saved-cutoff outcome on later loss. Genuine native execution and generated artifacts are separate parent-owned gates; do not report native PASS without execution. Unit mocks establish action routing only and must be labelled accordingly.

One runtime implementer. No subdelegation, checkout/reset/push, or parent memory/plan edits. Stage only owned implementation/tests/receipt/report. Write the implementation report at this workspace task-2b-report.md with RED/GREEN commands/results, actual limitations and native status. Return commit SHA and stop for independent SPEC/QUALITY task review. The parent owns publication and next Task3/4 integration.

## Concrete bounded-I/O seam
Read audit `document-bounded-io-preflight-20260924.md`: installed storage-js2.103.0 supports `.download(path, {}, {signal, cache:'no-store'}).asStream()`. Preserve ordinary document downloads; implement a separate bounded helper with shared exact SHA comparison where appropriate. Use a fixed 10,000ms operation budget spanning fetch/body, actual 2,097,152-byte streaming cap, abort/cancel and deadline race discarding late success. This is an operation-specific engineering bound, not retention policy. Validate real synthetic local storage and injected stalls, never live customer bytes.

## Reviewed canonical permission amendment — supersedes nonexistent identifier
Independent permission amendment review in audit `correction-capture-permission-amendment-review-20260924.md` APPROVES existing `communication.send` as this operational capture capability. Earlier references to `communication.write` in this brief/design were an invalid identifier absent the canonical catalog. Use `communication.send` in authenticated company action and SQL, retaining all other source/actor/company checks and (for document references) documents.read/customers.read. No alias, new permission, role grants, testing fallback, semantic review or send operation. Published SQL is immutable; apply permission repair through CLI-created forward migration. Native preconditions must assert actual canonical permission and effective actor permission; distinct test-only/read-only/wrong-company actors fail without modifying last admin or writing concern/witness.
