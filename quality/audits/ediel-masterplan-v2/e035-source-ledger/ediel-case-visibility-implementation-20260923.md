# Ediel operational-case navigation — implementation checkpoint, 2026-09-23

**Status: fix round 3 awaiting disposable native/browser rerun; not yet accepted.** BASE `a2f5107abb1cdbee6dba61f7b4d20fa0c3e62b89` above qualified retry `fe63dd98`. The original finding and authoritative bounds are `ediel-case-visibility-review.md`, `ediel-case-visibility-brief.md` and `ediel-case-view-preflight.md`. This slice does not approve whole E035.

## Skill routing and implementation scope

Read `AGENTS.md`, current E035 memory, installed Next App Router page/searchParams, Link, forms/Server Actions and accessibility docs. Used `test-driven-development` and `writing-good-tests.md` for failing render/DB/route/action tests, `web-design-guidelines` and local accessibility guidance for semantic labels, headings and link destinations, `verification-before-completion` for evidence, and self-review under the one-implementer constraint. Existing preflight is the finite plan; no new design report or parallel agent was used. Security, broad database refactor, performance and reusable-skill workflows do not trigger for this bounded view. Root owns independent SPEC/QUALITY review, publication and exact-head CI.

## Concrete change

- Added `/admin/ediel/operational-cases?caseId=<uuid>` under the existing `customer.cases` read guard. List applies exact `ediel_inbound_state_machine` DB source predicate with company scope before its 200-row window. Exact-ID detail separately checks ID, selected company and source before loading scoped events. Missing non-platform company makes no service reads; platform list/detail remains read-only and shows company identity.
- Detail renders validated typed intent, reason, full description, next action, status, priority, timestamps and case events. Source reference uses `metadata.source_ediel_message_id`, verifies same-company message and links only for platform admin because the target message route is platform-only. Customer reference verifies same-company customer and requires `customers.read` or platform admin. Untrusted metadata does not grant an action.
- Control Tower has an exact Ediel exception count/card, source-aware recent row links to exact case only for case readers, and a nonlinked explanation otherwise. The all-source case count remains once in the exception total and no longer advertises Support as its destination. Genuine support rows retain their list destination only for its own capability; other operational rows are summaries. Missing tenant scope returns before any unscoped service query. Support page/action/predicate, portal, writer and market authority remain unchanged.
- Ediel status action independently requires `cases.write`, rejects platform/admin scope mismatch and paused/nonwritable selected membership, validates finite statuses and re-reads same-company/source case. The helper's optional expected-source condition is in the atomic UPDATE before event/audit recording; existing callers do not pass it. Only case status changes; no source review, ACK, market send, supply or billing owner call is introduced.
- The existing clean-replay job now provisions a disposable local GoTrue fixture through Admin API with synthetic companies, customers, company-scoped role permissions and actual `applyInboundBusinessStateMachine` Z06/G review cases. It creates an A case older than 201 newer support rows plus a recent A case and B case, writes only IDs/disposable emails to `$RUNNER_TEMP`, and runs a protected Playwright login→actual Control Tower link→detail/status test in the same live-stack step. Local URL and manifest-path guards fail closed. No hosted fixture, auth bypass, seed/migration change or key/password output is used.

## Verification evidence and open gate

- RED: actual rendered Control Tower Ediel row linked Support; source-filter and expected-source predicates absent; dedicated route/action absent; missing-scope Control Tower issued nine unscoped count queries. GREEN: focused retained/new Vitest 19/19 across 5 files, including exact link, no-case-read row, scope, detail/event ordering, cross-company/source IDs, platform read-only, forged action and atomic predicate.
- Initial local candidate: **5952/5952, 368 files PASS**, app/tests/scripts types, focused lint 0 errors/one existing warning and production Next build PASS. Published exact tree `d1e14fc75c90a83d66e31b2c2d72c906b7637f14` ran ordinary CI `35890710047`: existing native 124 PASS and full quality tests PASS, but tenant service-role ratchet rose from 2402 to 2403; native synthetic GoTrue context was authorized for the selected company yet received no permission grants, stopping before browser. Protected browser was **not executed**. Independent SPEC/QUALITY review required corrections to the open-card cohort, unsupported current statuses and post-browser side-effect proof.
- Fix round 1: related message/customer lookup uses `tenantDb(case.company_id)` and ratchet remains **2402 unchanged**; RBAC audit 24 checks PASS. The open Ediel card leads to `view=exceptions`, source/status DB predicates before range and deterministic `created_at,id` descending pagination with 200 rows plus lookahead, next/previous links and preserved view/page in exact-ID links. Invalid pages receive not-found. Status form shows current status and requires explicit permitted new selection, including from `billing_blocked` or `cancelled`. Disposable fixture inserts only missing standard permission catalog keys, grants the synthetic scoped role through `role_permissions`, and checks actual grant count before authenticated RPC. A second native verification pass after Playwright checks the browser's A-case status/event/audit and unchanged ACK fields/chains/lifecycle, source assessment, supply, billing and outbound rows. No baseline ratchet relaxation or auth bypass.
- Fix-round local RED→GREEN tests include >200 open source-cohort paging and invalid page values, exact count destination, unsupported current status, DB scope and status predicates. **5956/5956, 368 files PASS**; app/tests/scripts TypeScript PASS; focused ESLint 0 errors/one existing `_customers` warning; RBAC and tenant ratchet PASS; workflow YAML parse and diff check PASS. Fix-round production Next webpack build PASS with the dynamic Ediel route.
- Published fix-round tree `955e80043c7737184fc46b9b90b0a27f10b584a4` passed tenant ratchet, ordinary verification/quality/build and ancillary CI. Its disposable native run passed the existing 124 native tests and real-GoTrue permission setup, then failed before browser: inbound Z08 has no inbound dated rule profile, so the canonical insert trigger returned `23514 canonical_inbound_rule_profile_resolution_failed:PRODAT:Z08:2026-09-23:0`. Fix round 2 uses the existing inbound Z06/G dated profile and canonical association/application reference, checks the bound activation-row key, then invokes the genuine business writer. Browser expectation follows its `masterdata_update_review` intent. A dated policy test verifies this no-supply review outcome; affected focused tests 32/32 plus that test 5/5 PASS, app/scripts/tests TypeScript, affected lint and diff check PASS. No trigger or authority bypass was made. **That rerun failed before browser, as described below.** This workspace lacks Docker/PostgreSQL. Root must publish the frozen fix tree, run the disposable replay and inspect real writer, GoTrue permissions, browser navigation/status and post-browser state assertions. Public/staging browser jobs skip this explicitly local test; the replay job requires its manifest and executes it with local exports.

Published fix-round 2 tree `f9c9508baf3b9791d445358cd40eafff1ff6695d` passed its independent scoped review and tenant gate. The native run again passed existing 124 and GoTrue grants, then the inbound Z06 insert failed `23514 canonical_inbound_rule_profile_resolution_failed:PRODAT:Z06:2026-09-23:3`; protected browser remained unexecuted. The trigger matches code/date, not subtype, and cannot choose among its three genuine Z06 profiles. Fix round 3 checks exactly one enabled, active-window, inbound Z06/G registry row and inserts with that row's real pack ID, profile ID/key, guide version, checksum and snapshot, as the established source-owner native fixture does. It does not alter or disable the trigger. Its dated canonical review test and legacy writer tests pass 16/16; scripts TypeScript, affected lint and diff check pass. Actual source insert, writer and protected browser remain pending the next disposable replay.

No hosted write, deployment, real communication, main/PR310 change or whole-E035 approval is claimed.
