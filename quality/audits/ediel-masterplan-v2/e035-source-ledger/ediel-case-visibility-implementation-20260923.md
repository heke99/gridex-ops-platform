# Ediel operational-case navigation — implementation checkpoint, 2026-09-23

**Status: implementation frozen for disposable native/browser CI; not yet accepted.** BASE `a2f5107abb1cdbee6dba61f7b4d20fa0c3e62b89` above qualified retry `fe63dd98`. The original finding and authoritative bounds are `ediel-case-visibility-review.md`, `ediel-case-visibility-brief.md` and `ediel-case-view-preflight.md`. This slice does not approve whole E035.

## Skill routing and implementation scope

Read `AGENTS.md`, current E035 memory, installed Next App Router page/searchParams, Link, forms/Server Actions and accessibility docs. Used `test-driven-development` and `writing-good-tests.md` for failing render/DB/route/action tests, `web-design-guidelines` and local accessibility guidance for semantic labels, headings and link destinations, `verification-before-completion` for evidence, and self-review under the one-implementer constraint. Existing preflight is the finite plan; no new design report or parallel agent was used. Security, broad database refactor, performance and reusable-skill workflows do not trigger for this bounded view. Root owns independent SPEC/QUALITY review, publication and exact-head CI.

## Concrete change

- Added `/admin/ediel/operational-cases?caseId=<uuid>` under the existing `customer.cases` read guard. List applies exact `ediel_inbound_state_machine` DB source predicate with company scope before its 200-row window. Exact-ID detail separately checks ID, selected company and source before loading scoped events. Missing non-platform company makes no service reads; platform list/detail remains read-only and shows company identity.
- Detail renders validated typed intent, reason, full description, next action, status, priority, timestamps and case events. Source reference uses `metadata.source_ediel_message_id`, verifies same-company message and links only for platform admin because the target message route is platform-only. Customer reference verifies same-company customer and requires `customers.read` or platform admin. Untrusted metadata does not grant an action.
- Control Tower has an exact Ediel exception count/card, source-aware recent row links to exact case only for case readers, and a nonlinked explanation otherwise. The all-source case count remains once in the exception total and no longer advertises Support as its destination. Genuine support rows retain their list destination only for its own capability; other operational rows are summaries. Missing tenant scope returns before any unscoped service query. Support page/action/predicate, portal, writer and market authority remain unchanged.
- Ediel status action independently requires `cases.write`, rejects platform/admin scope mismatch and paused/nonwritable selected membership, validates finite statuses and re-reads same-company/source case. The helper's optional expected-source condition is in the atomic UPDATE before event/audit recording; existing callers do not pass it. Only case status changes; no source review, ACK, market send, supply or billing owner call is introduced.
- The existing clean-replay job now provisions a disposable local GoTrue fixture through Admin API with synthetic companies, customers, company-scoped role permissions and actual `applyInboundBusinessStateMachine` Z08/H cases. It creates an A case older than 201 newer support rows plus a recent A case and B case, writes only IDs/disposable emails to `$RUNNER_TEMP`, and runs a protected Playwright login→actual Control Tower link→detail/status test in the same live-stack step. Local URL and manifest-path guards fail closed. No hosted fixture, auth bypass, seed/migration change or key/password output is used.

## Verification evidence and open gate

- RED: actual rendered Control Tower Ediel row linked Support; source-filter and expected-source predicates absent; dedicated route/action absent; missing-scope Control Tower issued nine unscoped count queries. GREEN: focused retained/new Vitest 19/19 across 5 files, including exact link, no-case-read row, scope, detail/event ordering, cross-company/source IDs, platform read-only, forged action and atomic predicate.
- Full local Vitest: **5952/5952, 368 files PASS**. App, tests and scripts TypeScript checks PASS. Focused ESLint 0 errors, one pre-existing `_customers` warning in `lib/customer-cases/db.ts`. Production Next 16.3.5 webpack build PASS and includes dynamic `/admin/ediel/operational-cases`. `git diff --check`, shell syntax and browser spec syntax PASS.
- **Not executed locally:** Supabase/PostgreSQL/GoTrue native fixture and protected browser; Docker/PG unavailable in this workspace. The CI replay addition and its synthetic SQL/GoTrue setup require actual runner qualification. Root must publish this exact frozen tree and inspect native writer, authenticated context, status event/audit, browser navigation and same-head ordinary CI before acceptance. Native fixture failures are implementation findings, not skipped-test success. Public/staging browser jobs skip this explicitly local test; the replay job requires its manifest and runs it with local exports.

No hosted write, deployment, real communication, main/PR310 change or whole-E035 approval is claimed.
