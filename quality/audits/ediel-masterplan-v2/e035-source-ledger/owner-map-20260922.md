# E035 actual owner map — read-only investigation

Observed HEAD: 92d4980e5f1e068a3d33826f4ef75d086bfaf714. Git working tree was clean when inspected. No repository edits, tests, external tools or live operations. Scope is independent narrow architecture mapping; this is not a complete security audit or source-spec certification. Read AGENTS and current memory. Existing source review references were inspected, but original national PDFs were not re-read; temporal normative claims below are explicitly bounded.

## Existing reusable owners and actual gaps

1. **Canonical register rules exist; immutable per-object acceptance does not.** `lib/ediel/rulebook/canonicalPolicyFieldValidator.ts:105–114` invokes `validateProdatRegisterPolicy` with the resolved policy, raw segments, UNA, dependent facts and application reference. `prodatRegisterPolicy.ts:38–144` owns register conditional validation, exact object+agency facts, independent count mismatch and tariff distinctions. `:149–156` exposes matrix payload validation, with dependent conditions only when `requireConditions` is true. Therefore parser `validRegisterChain` or an empty matrix-only issue list is not the complete canonical register decision. Reuse actual resolved canonical policy invocation and project its typed diagnostics onto physical objects/lines; do not invent another register validator. Existing result is issues/handledFields/readings, not explicit object approval.

2. **Tenant routing owner exists, but is not independent accepted legal-party authority.** `lib/inbound-mail/inboundTenantResolver.ts:144–165` invokes shared `resolveInboundTenantFromIdentifiers`, using legal-role candidate and correlated outbound evidence. `lib/ediel/tenant/resolveInboundTenant.ts:426–449` immediately resolves an existing company_id as `existing_message_company_id`. That behavior is useful routing behavior and must be preserved, but cannot independently certify that the immutable source original tenant is authorized by legal FR/DO parties. A new acceptance facet must consume contemporaneous actual resolution inputs/output and explicitly distinguish routing-only/existing-id results from legal ownership verification. Never reconstruct this from persisted tenantResolution JSON (`flows/inboundProcessing.ts:311–325` explicitly reads that mutable report JSON for diagnostic propagation).

3. **Physical legal-party extraction exists, not persisted authorization.** `lib/ediel/utilts/receivedStructuralSources.ts:130–143,180–190,230–255` parses exactly one scoped PRODAT FR/DO and compares with UTILTS MS/MR with the appropriate qualifier/agency tuples. This proves literal wire correspondence, not legal sender authorization or receiver ownership. Its output deliberately remains acceptance=not_checked. A full per-object legal-party approval owner is absent from the received-source ledger. Do not promote correspondence, envelope routing or message hash into that owner.

4. **Business persistence owners exist, with different semantics.** `lib/ediel/flows/inboundBusinessStateMachineLegacy.ts:386–402` performs Z04 switch confirmation and supply-period writes. Z06/Z10 at `:499–512` create review cases, not accepted structural changes. Its returned `updated` list (`:541+`) is not an immutable all-object committed acceptance certificate. `lib/ediel/safeApplyReview.ts:277–345` applies individual entity patches and returns actual appliedCount/skippedCount; even its event says safeApplyDecision=applied when zero changes applied (`:323–339`). Never infer complete source approval from that event label. This legacy path does not qualify complete register inventories or physical object tuples.

5. **Per-object onboarding application exists but is expressly insufficient.** `lib/ediel/inboundCases.ts:980–1040` validates physical object+agency decisions, graph scope and sourceHash; `:1055–1083` calls onboardCustomerGraph and records per-object operation/customer/site/metering-point/application IDs, then marks the case applied. Those receipts prove customer graph application, not canonical register/legal party/full source approval. Capture any actual business acceptance only at the relevant committed owner, preserving all existing RPCs and behavior; do not use case.status or receipt presence as substitute. Namespace/schema guards at `:987–1000` mean this cannot universally approve all physical identities.

6. **Current immutable ledger is deliberately facet-only.** `lib/ediel/core/receivedSourceValidationEvidence.ts:10–14,18–50` binds original row/source context/hash and actual runtime result; emits sourceDisposition=not_established, objectDisposition/partyDisposition=not_checked. `flows/inboundProcessing.ts:300–309` calls it immediately after real registry runtime resolution, before subsequent business persistence. SQL `supabase/migrations/20260922095911_ediel_received_source_ledger.sql:149–156,301–345` enforces its owner and closed dispositions and serial predecessor chain. It cannot currently record full approval, and it cannot prove later business commits.

## Concrete integration boundary

Add a separate forward-migrated immutable disposition owner, leaving current canonical-runtime facet schema and RPC behavior intact. Bind original source ID/hash/company/environment, actual physical object identifier+agency and occurrence/line identity, exact legal parties, actual rule-pack identity, reasons and each actual owner result. Represent accepted/rejected/unavailable/review explicitly; require every required facet and every relevant object. Record full approval only when the business owner actually commits its source-specific accepted result. For Z06/Z10 pending safe-apply, unavailable or review is the correct state; do not autoapprove to make the feature appear operational. Preserve existing ACK generation/business decisions; evidence failures cannot silently alter established behavior.

A caller-provided proof JSON with `canonicalAccepted: true`, a receipt, status JSON, or checksum is not a replacement owner. The SQL boundary must validate allowed owner shapes and source/object scope; application integration must invoke only live owner outputs, not rediscover authority from mutable historical fields. Existing records without contemporaneous owner evidence remain unavailable unless a real explicit revalidation/acceptance process runs.

## Corrections, supersession and time

The existing SQL predecessor chain is an **assessment revision chain for one source**, not a market-message supersession rule (`20260922095911...:303,341–345`). It must not be interpreted as newest assessment/source wins. `receivedStructuralSources.ts:264–267` explicitly retains equal effective dates and does no supersession/current selection. No authoritative market correction selection owner was found in this path.

The existing source audit `quality/audits/ediel-masterplan-v2/e035-dated-source-reader-plan-20260921.md:7` cites U25-A-4 section 3.6.8 and appendix 2 p132 for received PRODAT Z04/applicable Z06/Z10 structure and E61/E62, and P26.A p50 for field210/DTM92 (Z04) versus field216/DTM157 (Z06/Z10), format203. Code `receivedStructuralSources.ts:241–252` implements those dates using the first physical register and fixed standard-time parser, checks timezoneOffset=1, and keeps field210 separately as contractStartMinute. It is sound reusable observation plumbing, not selection authority.

Keep receipt time, knowledge/assessment time and business effective minute separate. Require full immutable discovery/read-set evidence and approvals as of the UTILTS receipt cutoff before forming a timeline. Equal effective dates, conflicting corrections and unavailable acceptance need explicit unresolved results. Do not order by UUID, array order, row update time or receipt time to invent correction precedence. Obtain exact original national correction/reference semantics before implementing supersession; this investigation has not established a normative universal supersession algorithm.

## Verification limits

Read code and migrations and searched register, tenant, legal-party, business persistence, source disposition and supersession paths. No tests were run because this was a read-only architecture task. Nonexistence claim is limited to a complete immutable received-source owner; existing routing, register validation, application and safe-apply owners do exist and must not be described as absent. PR310 imports and changes were not used.

## Broader owner investigation — correction to overly narrow absence interpretation

There ARE reusable legal identity and temporal provenance owners outside the initial E035 path. Absence of a full ledger hook is an engineering gap, not by itself a reason to ask the user for policy or stop implementation.

- `lib/ediel/tenant/tenantEdielIdentity.ts:108–159` resolveCanonicalTenantEdielIdentity requires enabled electricity profile, unique legal actor/Ediel ID, market roles and explicit transport-agent relation. `:181–195` assertInboundTransportMatchesTenantIdentity and tenantHasMarketRole are existing explicit guards. The transport assertion currently has no external call sites in lib. This can supply the receiver/company identity decision by comparing actual immutable DO and UNB receiver with resolved legal/transport identity, including supplier role. Its `activeAtNow` at `:27–30` uses Date.now; historical qualification needs an explicit as-of parameter and returned record bindings, not a result from current settings retroactively asserted as past evidence.
- `lib/ediel/prodat/prodatPriorPermissionFlow.ts:42–60` priorPermissionScope is stronger existing temporal legal/transport validation: live temporal profile, exact identifier, role, explicit delegation and verified platform counterparty identifiers are checked at receipt and business instants. Loader `loadPriorPermissionFlow.ts:15–26` loads actual scoped temporal rows before raw historical messages. It is scoped to ESCO Z14/Z15 permission flows, so directly invoking it for supplier Z04/Z06/Z10 is incorrect. Extracting shared temporal identity/delegation checks while preserving its ESCO behavior is a concrete implementation route, not invention of a parallel approval engine. Its sender counterpart must equal transport sender and verified platform identifier; it does not establish a grid owner's facility-specific authorization.
- `lib/routes/dynamicReceiverResolver.ts:211–286,310–461,495–665` resolves a selected facility/site/switch grid owner through tenant-scoped graph reads, checks owner active/lifecycle/environment and actual Ediel ID. This provides reusable graph-to-grid-owner identity for comparing incoming FR; it is currently an outbound receiver decision. Its data are current graph state, not immutable historical facility authorization. Reusing its fact-resolution helpers is appropriate; relabeling outbound routing readiness as inbound legal acceptance is not.
- `lib/routes/agreementReferenceResolver.ts:17–67` and `gridOwnerAgreements.ts:77–84,128–150` provide explicit agreement status/uniqueness and date validity. `routeDecisionEngine.ts:609–640` invokes them for selected route scope. They resolve outbound access-agreement requirements, not a blanket national requirement for inbound PRODAT. Do not newly require such an agreement for Z04/Z06/Z10 without source evidence.
- `core/routeRegistry.ts:123–176` resolves outbound actor/route with receiver overrides (ACK actual sender override explicitly allowed), so its success alone does not certify inbound FR authorization.
- `inboundTenantResolver.ts:82–123` party-mirrored correlated-outbound lookup is ACK-only (CONTRL/APERAK/UTILTS_ERR), not a PRODAT source acceptance owner. Manual mail sender credibility is a separate non-EDIFACT correlation path and cannot authorize legal PRODAT actors. No authenticated inbound SMTP sender-to-FR authorization output was identified in mailbox poller paths.

Concrete positive path: use existing canonical tenant legal identity/role/delegation semantics plus actual selected physical-facility graph grid-owner identity and exact raw FR/DO/UNB tuples; capture contemporaneous results and record identifiers at the actual acceptance boundary. Extend existing temporal resolver plumbing to explicit as-of evaluation without altering old defaults. Where current graph has no qualified historical ownership, persist unavailable rather than infer it. This is implementable engineering for qualifying newly accepted messages; whether old evidence can be requalified depends on actual retained dated records, not user permission. An independently verified legal identity is narrower than cryptographic SMTP authenticity: do not claim the latter.

E61/E62 broader search: this checkout contains only `lib/ediel/utiltsEngine.ts` and `utiltsEngine.part-1.ts`. Read/search including ignored files (`rg -uuu`) found no E61/E62 literal or expected-register comparison in either; part-1 implements current E66 reading/energy checks around 818–970. Repository-wide lib search finds E61 only in the AGT test override allowlist, and E61/E62 source-reader comments. Thus an existing E61/E62 comparison owner was not found at HEAD92d4980e; this is an implementation gap, not a claim that rules are absent from external national specifications.
