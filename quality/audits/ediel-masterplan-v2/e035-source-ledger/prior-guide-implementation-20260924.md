# E035 bounded prior E61/E62 implementation receipt — 2026-09-24

**Disposition:** locally implemented and unit-verified; genuine native replay, independent review and same-head ordinary CI remain unexecuted pending publication. This is not full E035, G01, F3, or masterplan acceptance. PR310, hosted database and published migrations were untouched.

## Skill routing and source authority

Used the repository test-driven-development, spec-to-code and verification-before-completion workflows, and the approved bounded design. Native source-owner and tenant boundaries were inspected in the existing harness. Full repository audit, UI, deployment, performance and migration workflows do not apply to this policy/source-only amendment; native replay and delivery review remain subsequent gates. The plan's subagent-driven-development instruction is fulfilled by the delegated bounded implementer and parent reviewer; no extra parallel workstream was started.

The original prior 142-page PDF was recovered and SHA256 independently checked here: `fad5cf4f775f86258ab9d5827426d54e57881b6298836110359cf0e41706a798`. Its effective date is **2025-06-01**, distinct from translation/publication 2025-10-01. Actual body clauses inspected in the extracted source: §3.6.8 p38, §3.6.11 p42, §3.6.18 p48, Appendix 2 p138 fields 224/527; current U counterparts pp37/41/47/132 and frozen hash `0524c18f38864ebe081dec9d3d53f1797b224ef0af7b01986627e895f47d99be`. The additive [scoped source amendment](../../../../docs/ediel/masterplan-v2/amendment-prior-utilts-e61-e62-2026-09-24.md) records identity, precedence, dates, supported predicates and exclusions. No frozen manifest hash, register or existing source identity changed.

## Behavior and old-to-new assertion map

Only `UTILTS_25_A_3_POLICY.validateMeterAndRegisterAgainstStructuralInformation` changed from false to true. The policy exposes separate Appendix 2 and body-clause provenance for prior/current comparison; the old `source.section='1.8 Change log'` retains its original meaning for the general dated overlay, no longer falsely serving as the only stated comparison authority. Existing `qualifyReceivedUtiltsStructure` is unchanged: it fetches a fresh immutable tenant/cutoff snapshot, selects genuine witnessed expected PRODAT inventory, and holds unavailable cases. No approval shortcut, synthetic 2025 source, bilateral equality assumption, agency 89 bypass, generalized resolution permission or energy/retirement toggle was introduced.

| Old prior-window applicable reading | New prior-window outcome | National/quantity consequence |
|---|---|---|
| `not_applicable`, bypassed comparison even with no source | `unavailable` and `internal_review` if original/root/complete witnessed readset absent | No E61/E62, positive ACK or persisted quantities |
| `not_applicable` with genuine matched root | `matched` | Existing accepted disposition remains eligible |
| `not_applicable` with proved wrong meter / register membership | `mismatch` E61 / E62 respectively | Existing national mismatch path |
| 60/15 energy-only with no meter/register observations | `not_applicable` remains | No comparison source IO required |

Prior policy dates 2025-06-01 and 2026-09-30 and future continuity 2026-10-01 are explicit; pre-effective 2025-05-31 still rejects. The earliest date is a **policy selection**, not a fabricated old ledger. The native fixture deliberately uses policy reference date 2026-09-30 independently of post-ledger business supply interval 2026-10-01..15, present processing cutoff, actual source receipt/witness, and ledger epoch. Future policy date 2026-10-01 exercises the same genuine owner. The observation date and period are not used to select the guide. Missing source before review holds; after actual committed Z04 and reviewed/witnessed structure, meter/register mutations are independent. A later unwitnessed assessment blocks a fresh result while an earlier saved cutoff still sees its immutable accepted assessment. Existing finalized transaction/ACK retry persistence remains unchanged and needs native same-head execution.

## Verification

RED, supported Node 22.23.2, exact command: `npx -p node@22 node node_modules/vitest/vitest.mjs run __tests__/ediel-utilts-effective-date.test.ts __tests__/ediel-received-structural-qualification.test.ts`. Baseline produced **3 failures / 20 passes**: prior 2025-06-01 and 2026-09-30 policy flags false; prior missing-source runtime accepted/positive instead of internal hold. One initial fixture attempt also exposed existing prior E19 energy-volume validation; correcting only that fixture's energy-consistent QTY preserved the prior rule, then established the genuine three-failure RED.

GREEN targeted after implementation/additional side and 60-minute checks: **76/76** in three files on Node22. Final full Vitest: **5988/5988, 372 files** (`PATH=<Node22 bin>:$PATH NODE_NO_WARNINGS=1 ./node_modules/.bin/vitest run --silent`). `npm run typecheck`, `npm run typecheck:tests`, `npm run typecheck:scripts`, scoped ESLint, `npm run ediel:masterplan-v2:integrity` and `git diff --check` passed on Node22. The frozen package integrity reported 33 original files, 121 rules and 231 acceptance contracts; it does not assert application conformance.

The new `scripts/ediel-source-owner-native.test.ts` assertions are **written and typechecked, not executed**. The local environment lacks Docker/Supabase/psql; publication and native CI are owned by the parent and are blocked at this checkpoint. Native replay must inspect actual failures rather than count a static test as database proof. Its current proof covers genuine source missing/matched/E61/E62, quantities hold, saved cutoff and unwitnessed successor, with existing harness tests retaining review and closure checks. Genuine point-side transition through E20/E77/E24 versus E25/E67 remains covered by pure comparator tests; independent native point-side execution should be reviewed before a broader owner acceptance claim.

## Open delivery gates

Parent publication approval, authentic native replay, exact-head CI, independent source/task/quality review, and final retry/ACK behavior inspection. Authentic pre-ledger originals, bilateral mapping and unsupported resolutions remain unavailable rather than accepted. No historical committed result is rewritten by this code change.

## Review fix round 1 — canonical E30 and required integration matrix

Independent task review `prior-guide-review-20260924.md` identified three coverage gaps. A test-first actual canonical E30 singleton fixture exposed an additional concrete eligibility defect: the object-level `UTILTS_MISSING_DELIVERY_PERIOD` and profile-level `UTILTS_PROFILE_PERIOD_MISSING`/`UTILTS_PROFILE_RESOLUTION_MISSING` rejected all six no-period/no-resolution point-reason cases before structural comparison. RED command `PATH=<Node22 bin>:$PATH NODE_NO_WARNINGS=1 ./node_modules/.bin/vitest run __tests__/ediel-received-structural-qualification.test.ts --silent` yielded **6 failures / 16 passes**, each with those three missing-field errors and `guide_rejected` instead of accepted. Prior §3.6.2 p36 explicitly permits a singleton E30 meter reading without DTM324; §3.6.18 p48 says reading-only E30 omits resolution. Current counterparts pp35/47 agree. Both canonical missing-field owners now use one narrow predicate: exactly one physical, dated QTY220 observation, no energy, no period/resolution, E30 and IDE24. Multi-reading or energy E30 without those fields still rejects with all three errors. The source amendment records this expansion; no interval is invented and E03/start policy is unchanged.

Local GREEN: `__tests__/ediel-received-structural-qualification.test.ts` **29/29**, including six E30 reasons, second-reading/energy/invalid-calendar negatives, prior 15/60-minute canonical energy exemption, and missing/excess-register fixture eligibility. `__tests__/ediel-utilts-persistence-processor.test.ts` **12/12** including actual processor prior held/exempt/rejected sibling dispositions, persistence-call quantities, and ACK scoping. Final full Node22 Vitest **6003/6003 in 372 files**, exact command `PATH=<Node22 bin>:$PATH NODE_NO_WARNINGS=1 ./node_modules/.bin/vitest run --silent`. App/tests/scripts typechecks PASS, scoped ESLint **0 errors / 1 pre-existing unused-variable warning** at utiltsEngine.part-1.ts:1192, frozen source integrity PASS (33 originals, 121 rules, 231 contracts), `git diff --check` PASS. Sanitized logs are in sibling scratch directory `e035-prior-round1-evidence/`; this is local evidence, not native execution.

The native harness now contains actual committed/reviewed/witnessed Z10 E30 transition assertions for E20/E77/E24 closing and E25/E67 current, plus unqualified transition and E64 holds; prior E66 missing/excess membership, unknown genuine root, pre-ledger interval, latest witnessed unavailable review and unwitnessed successor. Real processor cases insert an E66 original in the local database and check held/no positive ACK/no series or sink, genuine matched/positive, E61/E62 national ERR, held-unfinalized release, identical retry, and finalized or post-persistence/pre-ACK interruption followed by a later unavailable source. **Eight new native test instances** were added to the previously wired source-owner suite in this round. For changed retry, the expected behavior is a **controlled `utilts_committed_transaction_retry_conflict`** with unchanged durable ACK reservation/final response, series, contract and saved qualification, and no new ACK/sink. Existing SQL guard and processor propagation justify this fail-closed oracle; no forward migration is warranted absent genuine contradictory side effects. These new native cases are **typechecked but UNEXECUTED** locally. Their exact outcomes require authentic native replay and independent inspection, not a claim of pass.

An incomplete/corrupt external snapshot is exercised at the qualifier RPC boundary by retained unit tests; this is a mocked service response, clearly separate from native database source states. No artificial DB incompleteness was seeded merely to pass a label. The native fixtures use a synthetic 2026-09-30 canonical policy date, actual post-ledger Z04 receipts/review, October supply interval/point, fresh processing cutoff and the database's actual ledger epoch as distinct values. Native same-head replay and source review remain open.

## Rereview 1 assertion correction

Independent rereview found that the successful ordinary retry native comparison included the entire ACK row although real finalization updates `finalized_at` and `updated_at` on a repeat positive ACK. The native assertion now excludes **exactly those two administrative timestamps** for the successful retry, retaining all stable ACK fields (including reservation, response type and response message ID), exact series/contracts and their membership. The contradictory changed-source retry still compares the **full** ACK/series/contract/qualification snapshot because its conflict occurs before finalization. This is a test assertion correction only; no product persistence rule changed.

The native ACK factory and normalized metering sink are mocked at external boundaries. The real processor, database transaction reservation/finalization, persisted series and consumption contracts are exercised, but these tests do **not** prove authentic ACK wire construction or completed downstream metering storage. Native execution itself is still pending. Rereview and this correction make written coverage reviewable for actual replay, not accepted native evidence.

## First native CI replay and fixture correction

Published head `f7c06f06255e57e3ef7372396f03d666a9723efd` ran genuine native job `107410965832` in workflow `OPS35929036489`: **165 passed, 1 failed (166 tests)**. The sole failure was the 2026-09-30 branch of `native reviewed Z04 qualifies prior/current E61/E62`, where an assertion expected the source-object RPC to accept copied facts from the approved review. The database returned SQL `23514 source_object_owner_snapshot_changed`. The approved facts name the original baseline as their `baselineCurrentAssessmentId`; after that review was recorded, its own assessment is current. Replaying its old facts as a successor is correctly rejected by the owner snapshot guard. The retained native `an unwitnessed later Z04 review cannot fall back...` case already asserts this stale replay rejection. This was a fixture error, not evidence for weakening SQL or changing production behavior.

The failing branch now composes a fresh review against the current approved assessment through `reviewReceivedStructuralSource`, lets the real append execute, and deliberately passes a wrong facts hash only to the separate witness RPC. It checks the resulting `unconfirmed` status, three assessments, latest unwitnessed successor, and its genuine current-baseline binding. It retains the prior policy hold/no national mismatch and immutable earlier-cutoff accepted assertions. The other 165 native cases passed on that first head; **the corrected branch has not yet been replayed in native CI**. This fixture mocks the witness RPC arguments to induce a real database witness rejection; it does not prove arbitrary corruption recovery or wire/sink execution.

On supported Node **22.23.2**, `npm run typecheck:scripts` PASS, scoped ESLint of `scripts/ediel-source-owner-native.test.ts` PASS, and `vitest run __tests__/ediel-received-structural-qualification.test.ts --silent` **29/29 PASS**. `git diff --check` PASS. Exact local logs are in sibling `e035-prior-round3-evidence/`; the first native CI failure log is sibling `e035-pr371-native-first-failure.log`. No local Docker/Supabase/psql native run occurred. Same-head replay of the corrected fixture and independent review remain open; this receipt does not replace them.
