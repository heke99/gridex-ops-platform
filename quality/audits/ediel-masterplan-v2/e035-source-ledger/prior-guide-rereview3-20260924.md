# Prior-guide scoped rereview 3 — 2026-09-24

Scope: independent SPEC and QUALITY review of `f7c06f06..6298f35f`, the sole native failure in OPS35929036489, and new breakage from its fixture correction. No product edit, commit, publication, or native execution by this reviewer.

**SPEC: ADDRESSED (static, bounded). QUALITY: ADDRESSED (static, bounded).** No new load-bearing finding in this change. This is approval of the written correction for replay, not native or Task 1 delivery acceptance.

The authentic first native job `107410965832` ran 166 cases at `f7c06f06`: 165 passed and the 2026-09-30 prior Z04 case failed with SQL `23514 source_object_owner_snapshot_changed`. Its test manually submitted the already-approved review's facts as a new successor. Those facts bound `baselineCurrentAssessmentId` to the preceding assessment, so the owner guard correctly rejected them once the approved assessment became current. The retained stale-facts case at `scripts/ediel-source-owner-native.test.ts` explicitly expects this rejection. SQL and product behavior were not changed.

The corrected case calls `reviewReceivedStructuralSource` again, using its actual canonical, tenant, point, coverage and current-baseline composition path. It spies on only `gridex_witness_source_objects_v1` and supplies a wrong facts hash to the real RPC, which should reject the separate witness after the real append. The spy is restored in `finally`; the test also asserts an `unconfirmed` receipt, three revisions, no witness on the latest revision, an accepted composition bound to the preceding approved assessment, a latest-source internal-review/no-response hold, and the earlier saved-cutoff accepted snapshot. `persistReceivedSourceOwnerDecisions` appends before witnessing and returns `unconfirmed` for witness error. The SQL proof requires the current witnessed baseline; the snapshot records assessment availability separately. These paths support the corrected fixture's intended semantics without fabricating coverage or weakening the owner guard.

The changed lines retain the earlier missing-root/pre-ledger holds and matched, E61 and E62 comparisons. The 2026-10-01 branch does not enter the new witness manipulation. The narrow wrong-hash injection demonstrates a database-rejected witness request; it does not demonstrate arbitrary corruption recovery, authentic ACK wire generation, or downstream metering storage.

Evidence limits: the implementation report records Node 22.23.2 scripts typecheck, scoped native-file lint, qualifier unit 29/29, and diff check as passing; this reviewer did not repeat those checks. The other 165 native cases passed on the *previous* head. The corrected branch and final head require ordinary authentic same-head native replay and inspection, followed by the parent delivery gates. Until then, the fixture's expected outcome is a reviewed assertion, not an executed result. This bounded rereview does not accept full E035, G01, F3, or the remaining lifecycle/delegation/historical owners.

Skill routing: `code-review`, `spec-to-code-compliance` principles, and `verification-before-completion` informed the scoped diff/source-path check and claim limits. The repository's compliance command requires fan-out agents, but this task expressly forbade delegation; the conclusion is confined to this single correction rather than a whole-spec compliance claim. Database/security review was limited to the directly relevant immutable source owner and witness guards. UI, performance, broad security/static scans, remediation, and full quality-playbook generation were outside this two-file fix.
