# Task 3 preparation — PREPARED, not started

2026-09-24. Read the Task3 brief and approved process-history preflight/design review, finite producer inventory/review, and fixture preflight. Checked the bounded live code seams in shared transport, direct orchestrator and outbox sender. Preparation only: no runtime/schema/test edits, execution, native claims, commits, HEAD changes, sends, hosted operations, delegation, or parent-owned publication work.

Task2a acceptance and Task2b publication/native status are parent supplied. Task2b is not accepted by this report. Implementation remains blocked on explicit parent START, immutable BASE, accepted Task2b and precise checkpoint scope.

## Proposed sequential checkpoints

Recommend **3a outbound fence**, independent review/native acceptance, then **3b finite process facts**, independent review/native acceptance. This partitions the approved implementation; it changes no correction policy or completeness gate. Parent should dispatch 3b on the accepted 3a immutable base. Keep the three-owner-set composition contract for Task4 explicit rather than building sequential client snapshots which would later require replacement.

### 3a: physical outbound identity and exclusive provider entry

Use a new CLI-created forward migration after published Task2b. Add private immutable original/attempt/event facts, per-original current reservation, explicit prospective epoch and separately committed visibility witnesses, with forced RLS and service wrappers enforcing tenant/actor/operation. No cascading ledger dependency on mutable message/outbox parents. Reserve only from canonical outbound original identity and hash; preserve original UTF-8 bytes/hash independently of the normalized transmitted EDIFACT latin1 bytes/hash/length. Bind route, MIME mode, actual recipient/from envelope, encoding and actual provider result/Message-ID/response. Original ID remains the exclusion identity across route/MIME/status/outbox-attempt changes.

The shared `sendEdielMessageViaSmtp` presently accepts only actor and MIME options. Extend its internal call context to carry actual worker outbox ID, attempt and worker fence from `sendOutboxItem`; SQL must bind them to the actual currently claimed row/message/company. Direct entry receives a separately generated owned attempt and authenticated actor, not a fictitious worker claim. The five `sendEdielEmail` branches must all invoke one shared fenced provider wrapper. Prepare after route/readiness/payload checks; construct mode-specific packaging before conditional provider entry where practical. Conditional DB entry must produce a one-use proceed decision; duplicate callers and stale paused owners cannot enter.

Result capture must precede the existing rejected-recipient branch. Nonempty accepted plus rejected is partial acceptance, permanently suppressing another send of that original. Accepted, uncertain, empty/malformed outcome, crash after entry and post-result DB/status/event/outbox failure must all preserve suppression independently of mutable status and the worker's late local `providerAccepted` flag. Provider entry without witnessed result remains uncertainty, never proof of acceptance or non-submission. Explicit negative/pre-connect observations stay distinct, but do not grant generic automatic retry. Only authoritative known-never-entered preparation release is enabled by ordinary safe release; any explicit all-rejected new attempt requires separately authorized reconciliation. A release must revoke the old owner before another owner can enter. Projection repairs must invoke no provider.

Write native failure/race fixtures first; retain real direct `sendQueuedEdielMessage`, `processEdielOutbox`/claim and both `sendOutboxItem` modes while stubbing only the external provider boundary and explicit persistence failure seams. Count provider calls. Required matrix includes failed preparation (zero calls), duplicate same attempt, direct/worker race, stale owner after release, route/MIME changes, accepted/partial/uncertain plus status reset, result-capture failure, later status/event/outbox failures, all rejection, malformed/empty result and DATA ambiguity. Assert physical latin1 versus UTF-8 non-ASCII identity and envelope binding for every route. Prepared/queued is not dispatch; historical status cannot backfill missing receipts.

### 3b: exact finite process state/event facts

Instrument these twelve tables transactionally: `customer_contract_events`, `customer_contracts`, `customer_sites`, `metering_points`, `customer_supply_periods`, `supplier_switch_requests`, `supplier_switch_events`, `customer_cases`, `customer_case_events`, `customer_operation_jobs`, `customer_operation_tasks`, `customer_operation_events`.

Install locked prospective per-table epochs. Capture final normalized INSERT/UPDATE facts and immutable OLD plus NEW scope/links, with deletion tombstones independent of surviving parents. Capture relevant status/date/identity/operation facts and bounded source fields from the fixture inventory; preserve prior aliases and relational linkage, never treating arbitrary JSON links or actor/source strings as authenticated correction provenance. Child cascade capture must recover scope from OLD/previous immutable linkage rather than require a surviving parent. Nullable/composite SET NULL tenant/customer removal must retain prior scope. Missing/conflicting ownership requires an atomic scoped gap or rejected mutation; size overflow cannot silently omit a transition. Rejected transactions roll all their observation rows back.

First inspect actual native catalog/defaults/generated columns/FKs/trigger ordering at BASE; fixture preflight is not executed proof. Exercise CRUD for every table plus actual contract-event RPC, legacy supply writer, supplier-switch activation, operation enqueue/claim, case routes and profile archive. Verify switch-event RESTRICT, supply restrictions, signed-contract deletion rejection, valid draft deletion, case/customer/site cascades in valid isolated graphs, point/job SET NULL, and archive failure after earlier transactions committed. Retain earlier committed tombstones without claiming the entire archive was atomic. Deny or explicitly invalidate unobserved TRUNCATE/disabled-trigger paths.

Exercise actual `emitCustomerOperationEvent` swallowed insert failure. Captured consequential state can establish that state transition, not manufacture the missing event. Event-only omitted facts and unqualified producers retain scoped incomplete coverage. Third-set `complete:false` remains mandatory until producer/native qualification is real; prospective epoch is never inception history.

## Readset handoff and limits

Expose bounded owner facts for one SQL MVCC composition in Task4: outbound originals/attempts/witnesses, existing received Z05 L/LK/C/replacement originals/assessments/witnesses plus qualified document-owner evidence when relevant, and the finite transition facts/gaps/witnesses. Task4's public `openCorrectionProcessSnapshotV1` must save exact text/hash/ID, exact candidate counts, coverage intervals/epochs/missing spans and cutoff-visible predecessors/witnesses. No independently queried owner timestamp is a substitute for one MVCC visibility boundary.

Candidate scope precedes limits, semantic acceptance follows candidate inclusion. Missing party/date/object links are wildcards; mutable conflicts, failed assessment and malformed subtype cannot exclude a potentially same-scope concern. Proven unrelated point does not consume selected-point capacity even with the same LI or an unrelated gap. Reverse outbound FR/DO appropriately when matching inbound sources. Preserve reviewed bounds (1,000 originals/transitions; 262,144 original bytes; 65,536 transition-fact bytes; 6 MiB owner inputs) and exact overflow reasons; inbound bounds remain the existing owner's contract. Reader failure and unknown retention yield `complete:false`, never empty-set negative proof.

Activation is conditional on actual potentially matching raw/rejected/unwitnessed/ambiguous concern or durable failed-producer signal reaching the context. New epochs alone never hold ordinary unrelated supplies. Once activated, missing inception-to-cutoff history holds that correction scope. Required paired Task4 oracle: qualified pre-epoch ordinary supply unchanged with no matching concern; identical scope held after a newly visible matching raw/unwitnessed concern. Add wildcard and proved-unrelated variants. Preserve saved earlier cutoffs/finalized retries; no positive C, process-basis, reopening or legal-validity authority.

## Named remaining uncertainties / dispatch prerequisites

1. Parent acceptance of Task2b native evidence, immutable BASE and exact 3a/3b dispatch boundary remain absent. No implementation should start from current mutable HEAD.
2. Native catalog ordering, exact effective constraints and enabled registry/profile rows must be asserted after full replay, not inferred from source. Permission keys `communication.read`/`communication.send`, company scope repair, active membership/profile and PRODAT 26.A r3 Z08:H / Z05:L,LK,C pins are required fixtures.
3. Select the smallest provider-boundary injection seam permitting real direct/worker native calls with no transport-level substitute; current shared signature needs explicit trusted worker-fence plumbing. This is an implementation choice to close in 3a, not a request to weaken race proof.
4. Raw MIME routes can bind produced raw bytes; nodemailer attachment route must bind the actual EDIFACT Buffer, encoding and concrete envelope/options without claiming unseen nodemailer-generated full MIME bytes. Verify helper semantics at BASE before choosing persisted fields.
5. Exact native process-writer fixture prerequisites (especially canonical signed contract setup and activation) and child cascade immutable-link lookup need implementation-head proof. Failed event-only insertion remains a declared gap unless a qualified state owner actually captures its consequential fact.
6. Independently committed witness/read visibility integration and one-statement Task4 owner composition must reuse accepted Task2a/2b contracts at BASE. Do not rework their approved source/document design or imply retrospective completeness.

Ready for parent review of this partition and later explicit START. These are finite implementation/native obligations, not a broad design reopening. This report grants no runtime or native acceptance.

## Parent partition approval and exact checkpoint boundaries

Parent subsequently approved the routine sequential 3a/3b partition, without policy/scope change. Full Task3 acceptance requires both native-qualified checkpoints; Task4 single-MVCC composition remains unchanged. Still no START or immutable BASE has been supplied.

**3a runtime files:** one CLI-created forward `supabase/migrations/<allocated_timestamp>_correction_outbound_dispatch_fence_v1.sql`; new narrow owner adapter `lib/ediel/sources/correctionOutboundDispatch.ts`; `lib/ediel/transport/index.part-2.ts` for shared reserve/entry/result integration; `lib/ediel/outbox/sendOutboxItem.ts` for real worker fence propagation and uncertainty handling; `lib/ediel/orchestrator.ts` only if direct-owned context/error propagation requires it; `scripts/ediel-correction-context-native.test.ts` for native provider-boundary/race fixtures. Any necessary transport parameter export/type lives with its actual declaring module, with no unrelated refactor. Generated schema/contracts remain parent-owned; the implementer reports regeneration requirements. Do not edit third-set producers or Task4 consumers in 3a.

**3a acceptance:** independent review of immutable original/physical-byte/envelope binding, tenant/actor/worker authorization, all five route entries, conditional one-use ownership, safe known-never-entered release, permanent accepted/partial suppression, uncertain no-resend despite failed result/projection persistence, and real direct/worker race evidence. Actual disposable native results must establish the required provider invocation counts, private access denial and prospective incomplete coverage. Parent owns native execution/publication coordination; merely adding assertions is not PASS.

**3b runtime files:** separate CLI-created forward `supabase/migrations/<allocated_timestamp>_correction_process_facts_v1.sql` defining private immutable facts/gaps/witnesses, locked twelve-table epochs and trigger/helper capture; `lib/ediel/sources/correctionProcessReadset.ts` for the bounded owner adapter/contract; `scripts/ediel-correction-context-native.test.ts` for exact twelve-table, actual route, cascade/SET NULL/rollback/archive and incomplete coverage fixtures. Existing business producer files are not pre-authorized for broad edits; atomic table capture is the implementation surface. If a particular event-only swallowed fact requires an additional producer edit, report its narrow seam and keep incomplete rather than silently expanding scope. Task4 composition/consumer files remain outside 3b.

**3b acceptance:** independent review plus actual native evidence for all named finite tables and required direct/RPC/batch/legacy/archive routes, OLD/NEW immutable scope surviving parent loss, rejected-write rollback, prior committed partial archive facts, bounded gap handling, RLS/tenant denial and epoch truthfulness. Swallowed event-only failures and historical gaps cannot become complete. Readset contract must support conservative candidate scope/counts and later one-MVCC composition, with no activated universal epoch hold. Full Task3 stays unaccepted until both checkpoints meet their native gates.
