# Task 3a — outbound Z08H provider-entry fence and immutable receipts

**Status: PREPARED, not STARTED.** Parent-approved first checkpoint of Task 3. Start only on the parent's explicit START with an immutable BASE after Task 2b native acceptance. One runtime author; independent review and actual disposable native evidence before acceptance. Task 3b starts from the accepted 3a base. This brief preserves the original Task 3 brief, reviewed PH-1–4 amendments, producer inventory/review, fixture preflight and the prepared Task 3 report; it does not add correction policy.

**Runtime boundary:** one CLI-created forward `supabase/migrations/<allocated_timestamp>_correction_outbound_dispatch_fence_v1.sql`; new narrow `lib/ediel/sources/correctionOutboundDispatch.ts`; shared `lib/ediel/transport/index.part-2.ts`; `lib/ediel/outbox/sendOutboxItem.ts`; `lib/ediel/orchestrator.ts` only for direct-owned context/error propagation; declaring transport type/export only if required; native fixtures in `scripts/ediel-correction-context-native.test.ts`. Generated schema/contracts are parent-owned; report regeneration needs. Do not edit the twelve third-set producers or Task 4 consumers.

## Contract and implementation steps

- [ ] At BASE, inspect actual schema, route/readiness and shared send code. The real direct `sendQueuedEdielMessage` and worker `processEdielOutbox` → actual claim → `sendOutboxItem` both enter `sendEdielMessageViaSmtp`; all five `sendEdielEmail` branches need one common fenced wrapper. Supersession after direct send, mutable `message_sent_at`, best-effort pre-send payload storage and S/MIME archive are not dispatch receipts. Assert the enabled PRODAT 26.A r3 Z08:H profile/pack and `communication.read`/`communication.send` DB registry and active company/member/profile/permission fixtures. Recheck native catalog and transport helper semantics at BASE.
- [ ] Write failing actual-flow native cases first, stubbing only the external provider boundary and explicit persistence failure seams, never replacing the direct/worker owner. Count provider calls for failed preparation (zero), duplicate same attempt, direct/worker race, stale owner after safe release, route/MIME/status/outbox-attempt changes, accepted/partial/uncertain and mutable status reset, failed result capture, later status/event/outbox persistence failures, explicit all rejection, malformed/empty result, pre-connect failure and DATA ambiguity. Exercise `sendOutboxItem` already-claimed and independently claimed modes. Test every route's envelope and non-ASCII physical bytes.
- [ ] Install private append-only original/attempt/event facts, per-original current reservation, prospective epoch and separately committed visibility witnesses, forced RLS, revoked direct DML and narrow service wrappers checking tenant, environment, authenticated actor, operation and worker claim. No cascading ledger FK to mutable message/outbox parents. Derive Z08/H eligibility and immutable original identity/hash from the canonical sealed outbound `ediel_messages` original, never supplied subtype/status. Preserve canonical raw UTF-8 bytes/hash separately from actual normalized EDIFACT latin1 `Buffer` bytes/hash/length; bind actual route, MIME mode, encoding, recipient/from envelope and provider response/Message-ID. Raw MIME may bind produced bytes; attachment mode binds its actual EDIFACT Buffer and concrete envelope/options without pretending to have observed nodemailer's generated MIME bytes.
- [ ] Prepare after route/readiness/payload checks and before any provider call. Reserve once across direct and worker paths, regardless of route/MIME/status/new outbox attempt. Worker passes the real outbox ID, current send attempt and worker fence; SQL verifies its currently claimed row/message/company/environment. Direct uses an independently generated owned attempt and authenticated actor, not a fictional worker claim. Conditionally transition only the current owner to `provider_call_entered`, returning a one-use proceed decision immediately before the provider. Failed reservation or entry means zero provider calls. Duplicate and stale paused owners cannot enter. Authorized safe release requires proof entry never committed and revokes the old owner before replacement.
- [ ] Record immutable result before the existing rejected-recipient error branch: exact accepted/rejected lists, provider Message-ID/response and server observation. Nonempty accepted plus rejected is partial acceptance and permanently forbids another send of this original. Accepted, partial, uncertain, empty/malformed result, ambiguous DATA/socket outcome, provider-entry crash and post-result persistence failure suppress automatic resend independently of mutable status and the worker's later `providerAccepted` local flag. Entry without witnessed result is uncertainty, not proof of acceptance or non-submission. Distinguish explicit all rejection and pre-connect negative; a new attempt after all rejection needs separately authorized fenced reconciliation, not generic retry. Repair status/event/outbox projections without invoking provider. Provider acceptance is neither delivery/ACK nor legal rescission authority; observed server time is not exact unobserved acceptance time.
- [ ] Run native tenant/RLS/direct-DML denial, worker-fence, race, payload/envelope and failure matrix on disposable replay; inspect actual provider call counts and receipt membership. Preserve uncertainty when accepted result capture fails, and prospective `complete:false` for old uninstrumented sends; queued/prepared is not dispatch and historical mutable sent status cannot backfill. Run targeted type/lint/migration gates and obtain independent checkpoint review. Report exact commands/results and remaining gaps; no PASS claim from test source or preflight alone.

## Handoff and invariant boundary

The outbound owner exports immutable originals, attempts, events, committed witnesses, epoch and scoped gaps for Task 4's one-statement MVCC three-set composition. It cannot alone assert inception coverage. Keep conservative candidate scope before completeness counts: reverse outbound FR/DO against inbound, include unknown party/date/links as wildcards and raw/rejected/unwitnessed potentially matching concerns, while an immutably proved unrelated point (even same LI) does not consume selected-point capacity. Bounds carried forward are 1,000 scoped originals, 262,144 raw bytes per original and 6 MiB original+attempt inputs, with exact scoped overflow reason. Reader/producer failure, unknown retention and older sends remain incomplete. Task 4 activates this history requirement only for an actual potentially matching correction concern or known context-reaching producer gap, never merely because a producer epoch exists; saved earlier cutoffs/finalized retries are immutable. No positive C/basis/reopening authority, hosted write, deployment or market send.
