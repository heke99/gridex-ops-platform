**SPEC — CI source-contract failure:** ADDRESSED. `__tests__/synthetic-customer-journey-pre-smtp.e2e.test.ts:230-257` now finds the actual `sendFenced` call within the S/MIME branch, after certificate resolution, encryption, and the expected-recipient rejection throw. It follows `sendCorrectionFencedEmail` to `sendEdielEmail` and checks archive-before-provider order. Actual transport `lib/ediel/transport/index.part-2.ts:519-648` and provider helper `lib/email/sendEdielEmail.ts:47-58` agree. The reported exact targeted RED1/GREEN1, tests types, and scoped lint cover the changed contract; this remains a source-order check, not a new provider-flow execution.

**SPEC — global fixture actor collision:** ADDRESSED. `scripts/ediel-correction-context-native.test.ts:418-463` gives each actor its UUID-bearing name, asserts the persisted generated normalized name and actor ID, and inserts its own identifier. The two-seed test at `scripts/ediel-correction-context-native.test.ts:584-596` asserts distinct names, actors, identifiers, and retention of both actors. The SQL uses no shared-row rewrite, upsert, cleanup, or weakened constraint.

**QUALITY — finite Ediel identifier collision:** ADDRESSED within the authorized fixture scope. `scripts/ediel-correction-context-native.test.ts:428-444` takes a transaction advisory lock before a separate allocation statement, so its READ COMMITTED candidate read observes previously committed fixture allocations. `available` checks the actual global `(identifier_type,identifier_value)` owner, while the data-modifying `actor` CTE feeds the newly inserted actor ID into the identifier INSERT. The five-digit format and persisted owner are asserted at `scripts/ediel-correction-context-native.test.ts:445-446`; exhaustion yields no identifier and fails the format assertion. The transaction commits before subsequent async work. Existing schema confirms the global indexes in `supabase/migrations/20260611123000_actor_registry_message_semantics_tenant_automation.sql:28,48`.

**New breakage in fix diff:** None identified. Only the two tests and report changed; published migration, runtime, and generated contracts are untouched. The report records scripts/tests TypeScript, both-file lint, helper PASS4/skipped87/collected91, and diff check as passing. The fixture change itself has no local PostgreSQL execution; native 301 must be genuinely rerun by the parent, so this review does not claim a native GREEN.

**Out-of-scope observations:** None.

**Fix round:** SPEC and QUALITY findings addressed, with no new Critical/Important breakage found; actual native 301 qualification remains pending.
