# Task 3a bounded native acceptance review

## Verdict

**SPEC: PASS for the supplied native outbound proof. QUALITY: PASS for this bounded native review.** The actual disposable replay executed all 301 native tests in five files, including the 91-test correction-context file and the required outbound scenarios. No new Critical/Important finding is established. This is not whole-Task3a acceptance: tenant/parity/schema reconciliation and final same-head gates remain pending. The prior minor formatting observation remains deferred to whole-branch final review.

Review scope: Task3a brief, final/fix3 report, original static review and fix1–3 reviews; artifact inspection and focused reads of the named executed test oracles only. No author tests were rerun, no runtime/SQL/generated file edited, no commit made. The reviewed test file and CMS source have no diff from published runtime `541619c011eda41bc7c531a5c5f07a611e63d57a`. Run/job identity OPS35956416625/native107495592215 is parent-supplied metadata; this review independently verifies the artifact bytes and their contents, not remote run provenance.

## Artifact and execution evidence

- `/workspace/scratch/4b1d39503015/e035-pr372-outbound-types.zip`: independently calculated SHA256 `716e75b2e824283a33520fcf53b82f8d0f0c8b0977b52b178102c62f72ded821`. Both extracted files match their ZIP entries byte-for-byte.
- `rem002-clean-replay.log:16998,17049` records application of migration031626 and successful empty local Supabase/checksum-pinned canonical-history replay. `:17854` records correction-context 91 tests passed; `:18081-18082` records **5 files/301 tests passed**, with no skipped count. This is execution evidence, not collection or restricted helper/preflight output.
- `:18106-18107`, `:18412`, `:18419-18420` additionally record case1, browser2, and postbrowser1 PASS. The CLI status help/stopped optional services earlier in the log do not constitute an abort: real native and browser execution follows.
- `:18428` and the final line identify the stopping gate: generated types hash `32ae2f06418bfb57bd3da0361f856bb544a360782967f33b962f9b9765518940` differs from old manifest `f2a05bbb69ec298e78cf21cd6761a62aae82ba9bdde5b2aa38032b5e062d8e5f`. The workflow is not claimed wholly green. Independent hashing confirms the generated file; comparison with published541619 types shows exactly one added public signature: `gridex_outbound_dispatch_v1: { Args: { p_input: Json }; Returns: Json }`. The current checkout copy matches the artifact exactly. No hand generation is needed or accepted.
- Published SQL independently hashes to `8eff8a86530af7cb46fdb3cf5c650522f2b70db755c6561ab97ea211daa281a0`, unchanged.
- Earlier reported runs267/34 and298/3 were blocked respectively by seed collisions and the CMS/fixture causes; they are not relabelled PASS. The supplied final log actually passes the previously failing S/MIME, count-bound and inactive-membership scenarios. Full6072/377 and quality/build/bundle PASS are parent-reported contextual evidence, not independently re-reviewed in this native artifact review.

## Mandatory proof and oracle qualification

| Boundary | Actual execution and focused qualification |
| --- | --- |
| Direct outcomes and receipts | Log17881–17887: accepted, partial, empty, malformed, all-rejected, pre-connect negative and DATA ambiguity pass through actual `sendQueuedEdielMessage`; one provider call, witnessed entry/result, correct classification and suppression after mutable reset are asserted. Partial asserts exact accepted/rejected lists, provider ID and response. Negative outcomes never invent reconciliation permission. |
| Real worker owners and races | Log17888–17890,17904: direct versus real `processEdielOutbox`, independently claimed `sendOutboxItem`, duplicate already-claimed calls, duplicate attempt and safely released stale owner pass. Source477–579 and695–712 observes two distinct prepare arrivals and both actual SQL decisions before entry, one scoped prepared-state loser, one winner, real worker identity and one witnessed accepted result. A readiness failure cannot satisfy the two-arrival oracle. The barrier intentionally controls interleaving; it does not fabricate SQL outcomes. |
| Current worker claim | Log17911: successful real claim and prepare, followed by requeue and replacement through actual `claimEdielOutboxItem`, produce exact `outbound_dispatch_worker_fence_lost`, witnessed preparation, zero entry/provider calls (source766–803). |
| Persistence and uncertainty | Log17892–17895,17902,17912: result-event/status/message-event/outbox-status faults prevent a second send. Real entry commits before its response is replaced with loss; initial and retry provider count remain zero. A trigger failing only result-witness INSERT preserves accepted unwitnessed result, witnessed entry, one provider call after trigger removal/reset/retry, and `unwitnessed_event` reader gap. |
| Authorization and private facts | Log17891,17905,17913–17915: tenant/environment/actor/forged worker rejection; inactive profile, inactive membership, paused company and denied permission; real direct send and SQL entry denial with zero provider/entry. Backup functioning admin permits legitimate membership deactivation without disabling the guard. Private DELETE denial is exercised for six tables and anon/authenticated/service roles; expected PostgreSQL denials in the log are assertions, not suite failures. This is the bounded executed DML evidence, not a claim that every possible DML verb was separately tested here. |
| Witness separation | Log17907 and17912: same-transaction witness receives `outbound_dispatch_visibility_unproven`; independently committed facts/witnesses and interrupted result witness retain conservative uncertainty. |
| Bytes, envelope and archive | Log17896–17901: five MIME modes bind actual non-ASCII latin1 Buffer bytes and hash separately from UTF-8 original hash; attachment mode compares actual provider attachment Buffer and does not assert generated MIME observation. Raw modes compare bound raw bytes to provider options. S/MIME uses real OpenSSL certificate serial1234, encryption, actual archive/readback and helper; at provider boundary it asserts archive_verified and witnessed entry, and final raw binding equals provider raw. Helper preparation/callback denial cases are included in the passing91/301 file, though fast tests are not individually printed by this reporter. |
| Projection/history/bounds | Log17903,17906,17908–17910: accepted projection repair after route/MIME change invokes no second provider; historic sent state remains uninstrumented/complete:false; unrelated point excludes originals before capacity. Persisted1001-row assertion precedes exact count overflow; native case completed23400ms within unchanged budget. Per-original262145-byte and aggregate32x200000 inputs produce their distinct exact overflow reasons. |

## Limits retained

Only nodemailer's external `sendMail` provider is globally mocked in these outbound native paths; the direct owner, worker claim/send owners, transport, dispatch RPC/SQL, helper, PostgreSQL and S/MIME Storage archive execute. Narrow RPC observation/barrier/committed-response-loss seams and explicit persistence-fault triggers are disclosed above. Synthetic fixture identity/readiness and provider accepted/rejected responses do not establish real market delivery, ACK, exact unobserved acceptance time, or legal rescission authority. Selected seam-level scenarios intentionally call SQL or shared transport directly; they do not purport to be separate direct/worker end-to-end flows.

The prospective ledger remains incomplete for older uninstrumented sends. No Task4 three-set completeness or positive correction authority is accepted here. Final schema artifact reconciliation, tenant/parity execution and final same-head CI are still required before the parent accepts Task3a or starts dependent work from an accepted base.
