# Task 3a readiness — PREPARED only

2026-09-24. No START received; no immutable accepted BASE supplied. Inspected mutable HEAD `7581966b75165b2fd88de05756913cb673cd4a32` only for preparation. Parent reports Task2b native262/types/tenant/parity and independent review passed, with final same-head CI pending; this report does not accept Task2b. No runtime/schema/test edits, native execution, DB calls, provider calls, commits, delegation or cleanup performed. Parent owns memory/publication/generated artifacts.

## Routing and evidence

Read AGENTS, current memory/checkpoint/active work-plan sections, domain model and relevant decisions/failures, approved task-3a brief, process-history-task3-prepared-20260924.md and process-history-fixture-preflight-20260924.md. Used Supabase and Supabase Postgres security/short-transaction guidance for preparation and verification-before-completion for truthful status. TDD is conditional on START (failing actual-flow cases before implementation); executing-plans was inspected but execution is not authorized. Using-superpowers exempts dispatched subagents. No new planning/brainstorming, broad codebase inventory/audit, frontend, performance, supply-chain, hook installation, worktree, branch-finishing or delegation workflow applies to this focused preparation. Native execution and independent review remain parent coordinated.

## Actual seam ownership

* Direct: `sendQueuedEdielMessage` in `lib/ediel/orchestrator.ts` reaches real shared transport after payload/context validation; its outbox supersession follows SMTP and cannot arbitrate entry. Generate a distinct direct owned attempt with actor; do not invent a worker claim.
* Worker: `processEdielOutbox` invokes actual `claimEdielOutboxItems`, passes current_send_attempt_id/workerId/alreadyClaimed into `sendOutboxItem`. Its independent-claim mode invokes `claimEdielOutboxItem`. Both must carry the actual row/attempt/worker to shared transport; current transport signature carries only actor/MIME. Already-claimed local status/locked_by checks do not establish supplied attempt ownership; SQL must verify current row/message/company/environment/worker/attempt under lock at reserve and entry.
* Shared: `lib/ediel/transport/index.part-2.ts` has exactly five sendEdielEmail calls (attachment, S/MIME, multipart validation base64, singlepart base64, remaining singlepart modes). One owner wrapper must cover all. Raw EDIFACT snapshot storage is best effort. Result classification currently occurs before the post-acceptance try/catch; partial acceptance currently throws before worker providerAccepted becomes true. Persist receipt before that rejection branch, propagate uncertainty across caller/projection failures, and use durable state for subsequent suppression.
* Byte identity: canonical original UTF-8 bytes/hash are separate from normalized EDIFACT latin1 Buffer bytes/hash/length. Preserve the actual raw MIME Buffer where produced. Attachment branch supplies from/to/subject/text/attachment Buffer; helper adds replyTo and lane/provider headers. Record the concrete envelope/options and Buffer; do not claim nodemailer's full generated MIME was observed.

## Concrete seam ambiguity to resolve at START

`lib/email/sendEdielEmail.ts` invokes `archiveSmimeRawMime` with durable archive/readback **inside** its raw branch, before `transporter.sendMail`. Wrapping sendEdielEmail in index.part-2 fences conservatively before archive, but cannot truthfully mark the actual provider-entry boundary immediately after archive or safely release an archive-only preparation failure. Proposed narrow scope addition: an internal awaited provider-entry callback/type in sendEdielEmail, invoked after raw archive and immediately before each of its two sendMail sites. Shared wrapper owns reservation, callback, result/error capture; tests stub nodemailer.sendMail only (plus explicit persistence failure seams), retaining real helper/archive/direct/worker paths. This is not authorized by the listed runtime files except possibly its declaring-type clause; parent should explicitly settle this narrow helper inclusion. No policy decision changes. The helper also normalizes malformed provider result arrays to empty; capture must preserve an empty/malformed uncertainty distinction only where actually observable, never reconstruct discarded values.

Parent subsequently inspected the actual helper (archive before raw sendMail; separate attachment sendMail) and confirmed this necessary helper-file extension **in scope**, with no runtime START. Lifecycle:

1. Shared wrapper determines scope from the canonical DB owner check, captures prepared bytes/envelope/options, and creates the owned attempt. It supplies a required callback for scoped Z08/H; non-scoped sends retain ordinary behavior. No caller-supplied subtype can bypass the DB scope decision.
2. Helper performs readiness/config construction and S/MIME archive/readback first. Immediately before either actual sendMail call, it awaits callback. Callback uses a local consumed guard plus SQL current-owner/claim conditional entry; local guard is defense in depth, SQL is authoritative across processes. A duplicate, denied, malformed or failed callback result throws and the helper never invokes sendMail. There is no unfenced retry/fallback.
3. Preparation failure before callback can request the narrow DB safe release; SQL permits it only if entry never committed and atomically revokes the owner. A lost callback response cannot prove no entry: do not release or retry locally. Once callback entry commits, any helper/provider throw is captured as an observed error and conservatively suppressed even if result/event persistence fails; distinguish proven connect-negative or explicit rejection without granting automatic retry. Missing/failed observation stays unresolved entry.
4. The helper's two branches each invoke callback exactly once per attempted provider entry, only after their actual preparation. The wrapper records the returned result before rejected-recipient branching and witnesses the result separately. A missing callback on a **scoped wrapper path** is a hard pre-provider error; implementation must make this omission unrepresentable in the scoped adapter contract and test it without changing ordinary non-scoped helper calls.

Native assertions added to the matrix: keep real sendEdielEmail and stub nodemailer.sendMail; S/MIME archive/readback rejection yields zero calls and no provider-entry event; failed/replayed callback yields zero calls; both helper branches call callback once immediately before sendMail; callback SQL response-loss after committed entry yields zero initial calls and no later resend; normal non-scoped sends preserve existing behavior. Assert immutable receipt membership and ordering, not only callback mock counts. The archive remains a real required preparation step; explicit archive persistence failure injection is allowed, replacing its successful owner with a blanket mock is not qualification.

## SQL fence preparation

One CLI-created forward migration; private append-only originals, attempts, events, separate visibility witnesses and prospective epoch, plus mutable current reservation. No cascading FK to mutable messages/outbox. Forced RLS, revoke direct DML and PUBLIC function execution; narrow service wrappers validate actor/company/environment, communication capability, allowed tenant operation and real worker claim. Canonical outbound sealed message determines Z08/H and original/hash, never caller subtype/status. Confirm seal fields, effective catalog and accepted Task2a/2b witness contracts at BASE before choosing exact SQL names.

Reserve under a unique original identity and row lock. Persist attempt binding to original, route/MIME, bytes, concrete envelope, owner and claim. Conditional entry checks current owner and claim again; only a newly committed entry returns proceed, duplicate returns no proceed. Network invocation occurs outside the SQL transaction. Safe release requires no committed entry and atomically revokes old owner before replacement; timeout alone proves nothing. Result append/witness follows separately committed entry and preserves exact accepted/rejected lists, Message-ID, response and observation time. Failed result append leaves entered unresolved and suppressed. Accepted/partial/uncertain are permanently unsafe to resend. Explicit rejection/pre-connect negative remain distinct observations with no generic automatic retry; all-rejected replacement requires separately authorized fenced reconciliation. Projection repair has no provider call. Witnesses prove committed availability, not exact unseen provider acceptance time, ACK/delivery or rescission authority.

## Native matrix ready for implementation

| Actual flow / fault | Provider-count and receipt oracle |
|---|---|
| Direct, worker batch real claim, independent sendOutboxItem claim | Each reaches same owner fence with real identity; only controlled external provider used |
| Failed readiness/payload/packaging/reservation/entry | Zero calls; preparation is not dispatch |
| Same attempt duplicate; direct/worker race | At most one call; exactly one current admitted owner |
| Safe release followed by paused stale owner | Stale entry denied; replacement can enter once only |
| Route/MIME/status/outbox-attempt mutation after entry | No second call for same original |
| Accepted; accepted+rejected | Immutable full/partial outcome; later status reset cannot enable resend |
| Empty/malformed result; DATA/socket ambiguity; crash after entry | Uncertain/unwitnessed fact, no automatic resend, no fabricated acceptance |
| Failed result capture/witness; later message status/event/outbox failures | Entered state suppresses resend independently of worker local providerAccepted |
| Explicit all rejection; reliable pre-connect negative | Distinct negative observation; no generic retry authorization |
| Every MIME branch with non-ASCII wire | UTF-8 original differs from actual latin1 bytes; bound actual recipient/from/route/options/raw bytes where visible |
| Wrong tenant/environment/actor, inactive member/profile/company, denied permission, stale/wrong claim | Denied before provider; anon/authenticated/direct private DML denied |
| Old sent status; prospective epoch; unwitnessed append | complete:false; no status-based historical backfill or queued-as-dispatch claim |

Fixture prerequisites carry forward unchanged: actual unique communication.read/send registry rows from 20260924003708 and company-scope repair 20260924003724; active company/auth user/profile/accepted membership and explicit scoped grants; assert permission resolver true and denial variants. Assert enabled `PRODAT:Z08:H:26.A:r3`, exact pack status/validity/hash/version and pin canonical_rule_pack_id/rule_profile_key/rule_profile_version_id/rule_profile_version/rule_pack_checksum/rule_pack_snapshot. Real synthetic test-route/mail-readiness and valid wire must pass actual direct/worker preflight without mocking owners. Recheck catalog/defaults/triggers after actual disposable replay.

## Handoff and exact next action

Task4 receives bounded owner originals/attempts/events/witnesses/epoch/scoped gaps for one-statement MVCC composition, not independent client snapshots. Preserve conservative reverse FR/DO scope, unknown party/date/links as wildcards, raw/rejected/unwitnessed concerns, and exclusion only for immutably proved unrelated point. Bounds: 1,000 scoped originals, 262,144 raw bytes each, 6 MiB original+attempt inputs with exact scoped overflow reason. Unknown retention/old sends/reader failure remain incomplete. Epoch alone never activates universal hold; saved prior cutoffs/finalized retries immutable; no positive C/basis/reopening authority. Twelve process-fact tables and Task4 consumers are outside 3a.

Next: parent inspects the completed helper proposal, supplies explicit START and immutable accepted BASE, then author rechecks only changed dependencies/catalog, writes/runs failing actual-flow cases, allocates migration via CLI and implements within scope. Report generated schema/type/contracts regeneration needs to parent; do not manually edit generated files. Actual disposable native result counts plus targeted type/lint/migration gates and independent checkpoint review are required before acceptance. Preparation commands were read-only file/rg/git inspection; no runtime correctness or native PASS claimed.

## START / implementation milestone

Parent START received at immutable BASE d0d95006ad442ffeb1f319351d257f647c0ee688, clean worktree. Parent accepts Task2b/runtime7581966b/native262 and all same-head CI; parent owns native CI and generated contracts. Node22.23.2 helper regression executed: 2 intended failures (raw/attachment ignored denied callback), 52 skipped; initial missing synthetic anon key was harness setup, corrected before RED. Actual direct/result/reset, direct-worker claim race and independent worker native fixtures authored before implementation. Their native RED is NOT EXECUTED locally by explicit parent scheduling; native guard remains intact. Implementing only approved runtime files and narrow helper seam.

Implementation progress: CLI2.101 allocated 20260924031626. Callback runs only after archive/readback and directly before provider; adapter distinguishes entry response loss from preparation failure. Private SQL handoff includes prospective epoch, scope-before-bounds and old uninstrumented rows. Scoped historical completeness remains false. Accepted witnessed receipts can repair mutable status without reusing provider; repair does not label newly prepared route/MIME as sent. Canonical raw BGM plus row code conservatively route potential Z08s to SQL; no subtype/status shortcut. Existing non-Z08 helper contract preserved. Initial app/scripts typechecks and lint passed; 50 targeted existing transport tests passed; expanded native matrix authored but unexecuted. Address/invoicee fixture preflight now passes with independently supplied tenant-bound source facts, retaining real owner checks. Successful S/MIME native fixture uses locally generated synthetic public certificate plus real archive/Storage, not a replaced transport/archive owner.

## PAUSED — shared draft ownership reconciliation required

Parent ordered runtime edits paused after unexpected source/checksum drift, before freeze/commit. Parent confirms no root SQL/manifest edit, immutable HEAD/remote remains d0d95006, and is preparing an isolated exact-draft worktree before resuming this author. Do not treat this mutable shared draft as a frozen implementation.

Confirmed observations:

- First finalization registration attempt refused an already-present new checksum f265cbb5c2b0f9a1f12dad4da243bb61774d05a5208f07b354faea8ad207a0b3. Current SQL SHA256 is 3056c8f400a1148a59092dd2d5f8c1bf381ee6ae7bdca0d2261661852d5712f7; no historical manifest entry changed. Author had not knowingly registered this migration earlier. Do not replace checksum before reconciliation.
- SQL profile query contains an unaccounted `pack.status='active'`/validity predicate immediately preceding the author's later active/transition/validity predicate. Exact provenance unresolved; neither silently discard nor endorse it.
- Concrete self-review bug awaiting correction: eligibility's predecessor CCI qualifier reads `{elements,3,0}` but the real fixture/grammar is `CCI++Z13`, so Z13 is `{elements,2,0}`. Current SQL would reject the valid H fixture. No correction made after pause.
- Further review concern to resolve in isolated copy: uninstrumented readset point exclusion currently derives scope from raw text without explicitly requiring its matching non-null canonical immutable hash. Potentially mutable/unsealed raw must remain wildcard, not prove an unrelated point. Consider this a concrete implementation review obligation, not native acceptance.
- Local fixture collection now reports 86 total (4 selected helper/preflight PASS, 82 skipped); initial retained count was52. Account for all draft additions before declaring the native expected count. The authored matrix is unexecuted against a database.

Verification on the current draft before pause: app/scripts/tests typechecks and scoped ESLint exit0; transport compatibility68/9 and owner regressions6/2 pass; helper/preflight4 pass. Migration integrity fails solely on the above unpublished new-file checksum conflict. Native PostgreSQL/Storage/races remain NOT EXECUTED. Successful local tests do not qualify migration or unknown concurrent changes.

Reproducible local-only config saved as `quality/audits/ediel-masterplan-v2/e035-source-ledger/process-history-task3a-local.config.mjs`. Command: `npx --yes node@22.23.2 node_modules/vitest/vitest.mjs run --config quality/audits/ediel-masterplan-v2/e035-source-ledger/process-history-task3a-local.config.mjs`. This config restricts selection to helper/preflight only and uses synthetic local env; ordinary native config/status guard is unchanged. Initial RED used equivalent temporary config with `-t 'outbound helper callback denial'`:2 failures because denied callback was ignored, followed by2 GREEN. Native RED explicitly deferred to parent CI.

Other exact local commands: `npx --yes node@22.23.2 node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` (also scripts/tests); `npx --yes node@22.23.2 node_modules/eslint/bin/eslint.js lib/ediel/sources/correctionOutboundDispatch.ts lib/email/sendEdielEmail.ts lib/ediel/transport/index.part-2.ts lib/ediel/outbox/sendOutboxItem.ts scripts/ediel-correction-context-native.test.ts`; `npx --yes node@22.23.2 scripts/check-migration-versions.cjs` (checksum conflict, not PASS). Existing regressions: ediel-smtp-outcome, ediel-smtp-post-acceptance, ediel-transport-security-resolution, ediel-prodat-free-text-smtp, ediel-prodat-death-status-smtp, ediel-prodat-meter-change-smtp, ediel-prodat-reporting-smtp, ediel-prodat-date-event-smtp, ediel-inbound-ack-transport-guard (68/9), ediel-post-send-source-projection and ediel-send-lock-canonical-boolean (6/2), all under __tests__ with .test.ts extension and `npx --yes node@22.23.2 node_modules/vitest/vitest.mjs run`.

Next exact action: receive exclusive isolated worktree path and explicit resume from parent; reconcile draft provenance, fix named issues, re-register only this unpublished migration with parent authorization, complete required gates/report/owned commit, then freeze for independent SPEC+QUALITY and native qualification. No committed Task3a runtime exists yet. Parent memory and unrelated audit changes preserved.

## Isolated reconciliation — resumed

Parent supplied exclusive worktree `.worktrees/e035-task3a-isolated`, branch `codex/e035-task3a-isolated`, exact draft snapshot6eece160b0b60cbf538d0fd52cd40b8e8b5907fb. Full review BASE remainsd0d95006. Original shared workspace remains preservation-only. Parent confirms migration031626 has never been published/applied and authorizes replacement of only its draft registration through the canonical registrar.

Reconciled additions: retain the four native cases appended at649+ (same-transaction witness rejection, scoped count overflow, original-byte overflow, aggregate-byte overflow). They directly exercise required brief obligations with real SQL, and do not replace runtime owners or bypass native guards. These bring this file from52 to86 tests; mandatory suite total should therefore be296 (retained262+34), subject to genuine CI collection. The pre-existing active/validity profile predicate is retained as the exact enabled active26.A r3 pack contract; remove redundant active/transition predicate rather than broaden status policy. Correct CCI++Z13 token index to2. Readset candidate scope now requires matching immutable original hash (and canonical rendered seal for uninstrumented rows) before any party/point exclusion; otherwise dimensions are wildcards. No unaccounted runtime change is accepted merely because it appeared in the shared draft.

## Implementation freeze report — 2026-09-24

Status: implemented and locally verified; **NOT native qualified or accepted**. Review the complete Task3a diff against d0d95006ad442ffeb1f319351d257f647c0ee688, including snapshot6eece160b0b60cbf538d0fd52cd40b8e8b5907fb and the final correction commit, not just the latter delta. Sole resumed runtime author used only the isolated worktree; original draft remains preserved. Parent checkpoint edits are excluded from this author's commit.

The direct transport and real claimed/already-claimed worker now converge on one adapter. The helper's bounded callback extension binds the actual raw MIME/envelope or attachment options after encryption/archive/readback and immediately before each provider call. Each scoped callback is one-use; any failure aborts with no fallback. Ordinary non-Z08 sends retain their existing helper path. Z08 eligibility, canonical profile, original seal, tenant/actor and worker ownership are revalidated in SQL; parsed subtype and mutable sent status never establish permission to send.

The private ledger stores sealed originals, owned attempts, immutable events and separate-transaction visibility witnesses. Reservation and entry are separate durable transitions; entry response loss cannot safely release or resend. Exact accepted/rejected observations and transport errors are retained. Partial, malformed, uncertain, all-rejected and post-entry failures suppress generic resend. A witnessed wholly accepted receipt can repair mutable projections without calling the provider or relabelling new route/MIME preparation as previously sent. No all-rejected reconciliation authorization has been invented. The readset is a private future-composition seam, conservatively scopes before fixed limits and keeps historical completeness false. No Task4 consumer or twelve-table Task3b facts are added.

Native fixture preparation explicitly enables only the synthetic tenant's `ediel_test` capability and asserts `canonical_tenant_operation_decision(...,'ediel.test.process')` permits it. Catalog inspection showed company creation deliberately seeds this capability disabled/not_configured; runtime capability policy is unchanged. This is the sole late fixture compatibility change. No existing non-Z08 regression file was edited.

Authored new native coverage: actual direct outcomes and mutable reset suppression; direct/worker claim race and independently/already-claimed worker; stale owner, safe release, duplicate entry, forged tenant/environment/actor/worker, disabled actor and private DML; same-transaction witness rejection; committed-entry response loss; result/message/event/outbox persistence faults; exact UTF-8/Latin1 and MIME/provider bindings; real S/MIME archive/readback before entry; accepted projection repair after route/MIME change; historic uninstrumented scope and count/raw/aggregate overflow. Only the external provider is replaced on native send paths. All database/Storage/native race assertions remain unexecuted locally. Expected mandatory suite count is 296 (262 retained +34 new), subject to parent genuine CI collection.

Final local evidence (Node22.23.2):

- Helper/preflight config command recorded above: PASS4, skipped82, collected86. Two helper callback-denial cases had meaningful local RED before implementation, then GREEN. This restricted config is tracked and does not bypass or modify the ordinary native status guard.
- Existing targeted compatibility command below: PASS74 in11 files. Includes non-Z08 transport, post-acceptance/outbox effects, direct source projection and canonical boolean send lock.
- App, scripts and tests TypeScript checks: exit0; scripts check rerun after synthetic capability fixture correction: exit0.
- Scoped ESLint command recorded above: exit0; rerun after fixture correction.
- `git diff --check`: clean.
- `npx --yes node@22.23.2 scripts/check-migration-versions.cjs`: PASS614 files /518 version groups, checksums verified.

Exact compatibility command:

```sh
npx --yes node@22.23.2 node_modules/vitest/vitest.mjs run __tests__/ediel-smtp-outcome.test.ts __tests__/ediel-smtp-post-acceptance.test.ts __tests__/ediel-transport-security-resolution.test.ts __tests__/ediel-prodat-free-text-smtp.test.ts __tests__/ediel-prodat-death-status-smtp.test.ts __tests__/ediel-prodat-meter-change-smtp.test.ts __tests__/ediel-prodat-reporting-smtp.test.ts __tests__/ediel-prodat-date-event-smtp.test.ts __tests__/ediel-inbound-ack-transport-guard.test.ts __tests__/ediel-post-send-source-projection.test.ts __tests__/ediel-send-lock-canonical-boolean.test.ts
```

Unpublished migration registration reconciliation: parent expressly confirmed031626 was never applied/published. Removed only its conflicting draft entry f265cbb5c2b0f9a1f12dad4da243bb61774d05a5208f07b354faea8ad207a0b3, then ran `npx --yes node@22.23.2 scripts/register-migration-checksum.cjs 20260924031626_correction_outbound_dispatch_fence_v1.sql`. Final SQL SHA256 and manifest entry both equal `8eff8a86530af7cb46fdb3cf5c650522f2b70db755c6561ab97ea211daa281a0`. Verified all428 BASE file entries and all non-files manifest metadata remain byte-value identical, with exactly this one new entry. Registrar and published SQL are unchanged.

Remaining gates belong to parent: independent SPEC+QUALITY full-BASE review; genuine disposable PostgreSQL/Storage replay and native296 qualification; generated schema/types via canonical machinery; applicable same-head CI and final publication. No local native PASS, migration execution or production/provider operation is claimed. Any native failure must be fixed and requalified before acceptance. No unresolved correction-policy question is being deferred; database execution is the explicit remaining proof limitation.

## Fix round 1/5 — T3A-R1 and T3A-R2

Parent authorized this scoped fix on docs-only HEAD5da7192e over runtime90a7c653, after independent review reported important native-proof gaps (no confirmed runtime defect). Read the exact review in task-3a-review.md. Changes are confined to the existing native test file and this report; SQL, checksum, runtime owners and generated contracts remain unchanged.

T3A-R1: replaced both weak race oracles with a narrow observation/barrier around the real `gridex_outbound_dispatch_v1` RPC. Both actual owners must arrive at `prepare` with distinct attempt IDs, and both real SQL decisions must finish before the winner can reach `enter`. The losing response is held until real entry returns, preventing ordinary losing-worker status projection from replacing the claim before the selected winner enters. Assertions require exactly two observed reservation arrivals/decisions, one `proceed:true`, one `scoped:true,proceed:false,state:prepared,acceptedReceipt:null`, one successful entry, and one witnessed accepted result/provider call. Direct/worker owner kinds and duplicate-worker actual claim identities are explicit. Actual invocation outcomes are inspected, not discarded. The bounded barrier timeout releases pending callers for cleanup; missing contender arrival still fails the exact two-arrival assertion. No SQL result is fabricated, no owner is replaced, and neither contender can win merely by the other failing readiness before dispatch.

T3A-R2: added five native cases. A genuine claimed worker successfully prepares with its real claim; before returning that real prepare response, the fixture requeues the synthetic outbox row and obtains a new claim through the actual claim owner. The old worker then receives the actual `outbound_dispatch_worker_fence_lost` entry error, with witnessed preparation and zero provider/entry. A separate trigger rejects only result-witness INSERT for this message, after successful provider/result-event commit; assertions retain the witnessed entry, accepted unwitnessed result, exactly one provider call after persistence restoration/mutable reset/retry, and scoped `unwitnessed_event` reader gap. Three valid-scope authorization variants invalidate membership, pause company, or deny the direct send permission after removing that synthetic actor's role grants. Each starts with successful canonical preparation, asserts SQL entry denies actor authorization, then checks actual direct invocation fails with zero provider/entry. The denied-permission fixture explicitly checks the canonical permission owner returns false.

Local checks after these changes, all Node22.23.2:

```sh
npx --yes node@22.23.2 node_modules/typescript/bin/tsc --noEmit -p tsconfig.scripts.json
npx --yes node@22.23.2 node_modules/typescript/bin/tsc --noEmit -p tsconfig.tests.json
npx --yes node@22.23.2 node_modules/eslint/bin/eslint.js scripts/ediel-correction-context-native.test.ts
npx --yes node@22.23.2 node_modules/vitest/vitest.mjs run --config quality/audits/ediel-masterplan-v2/e035-source-ledger/process-history-task3a-local.config.mjs
git diff --check
```

Both types and lint exit0; diff check clean. Restricted helper/preflight run PASS4, skipped87, collected91. The collection increase is exactly5 cases, so expected full native total is now301 (262 retained +39 Task3a), subject to genuine parent CI. These new DB cases were NOT executed locally and no native RED/GREEN is asserted. No concrete runtime failure has been observed; there was no authority/need to edit runtime or migration. Minor dense formatting finding is deferred to the final review ledger, per parent scope. Freeze again for focused independent review and actual native qualification; parent dirty memory and review audit are preserved.

## Fix round 2/5 — published source-contract compatibility

Parent supplied published HEAD45d3e5c921fd5a68b9a2ee1f216c487839dd4c81 and FullE2E run35954321218/job107489289984 outcome6061PASS/1FAIL. Native run35954321157 remained in progress. Migration031626 is now published immutable; no SQL/checksum change is made in this fix.

The failing synthetic customer journey source contract searched for `sendEdielEmail(` inside the transport after recipient certificate resolution. The actual transport now calls `sendFenced`, which delegates to `sendCorrectionFencedEmail` and then the actual email helper. Reproduced the exact local RED: test233 expected -1 greater than66963. Repaired only `__tests__/synthetic-customer-journey-pre-smtp.e2e.test.ts`: constrain the ordering proof to its actual S/MIME branch, require resolution then encryption then recipient rejection gate before the first wrapped send, verify the wrapper/helper delegation (both scoped and ordinary lanes), and require raw archival before actual provider sendMail. All existing application/legal/PDF/mail/Z03/certificate business expectations remain unchanged.

Exact commands:

```sh
npx --yes node@22.23.2 node_modules/vitest/vitest.mjs run __tests__/synthetic-customer-journey-pre-smtp.e2e.test.ts
npx --yes node@22.23.2 node_modules/typescript/bin/tsc --noEmit -p tsconfig.tests.json
npx --yes node@22.23.2 node_modules/eslint/bin/eslint.js __tests__/synthetic-customer-journey-pre-smtp.e2e.test.ts
git diff --check
```

Targeted actual test RED1 before edit, GREEN1 after edit. Covering tests types and scoped lint exit0; diff check clean. No runtime/business-outcome modification and no native count change (expected301 pending genuine CI). Holding final fix2 freeze briefly for parent native outcome/additional concrete failures, as instructed. Parent checkpoint/publication receipt preserved.

### Same fix2 — native seed-isolation failure

Parent then supplied native run35954321157/job107489288645: 34 failures at outboundSeed's actor INSERT, before tested behavior, with `platform_market_actors_normalized_name_uidx` / `(dispatch electricity grid)` already exists. Parent reports the first direct accepted case and retained cases pass (aggregate267PASS/34FAIL301 being preserved by parent). Failed-run log-only artifact10789494594 has parent-provided SHA256 `78369bd89d54d4b44d8921e41605b0d3383b36fe657809872b724f2989003df0`; no generated schema/contracts were produced. This is genuine remote native RED, not evidence of a local native run or a runtime gate failure.

Catalog inspection confirmed normalized_name is a stored lowercase/whitespace-normalized expression with a global unique index. Every outboundSeed used the same literal; the disposable suite intentionally retains fixture rows. Moreover, the impersonation test seeds two outbound tenants inside one test, so afterEach deletion would not solve the defect. Fixed only fixture ownership: name now includes its existing actor UUID; assertion checks the actual persisted id/name/normalized_name. The two-tenant case explicitly requires distinct actors/names and both rows still present. No shared-row rewrite, cleanup, upsert into an existing actor or uniqueness weakening.

The same seed also used a finite random five-digit Ediel identifier, while actual `platform_actor_identifiers_uidx` globally owns `(identifier_type,identifier_value)`. Parent authorized removing this analogous collision risk. A short transaction takes a fixture-specific transaction advisory lock, inserts the uniquely named actor, selects the first unused valid synthetic value from60000–89999 against the actual identifier owner, and inserts that actor's identifier via a data-modifying CTE. The allocation statement follows the lock statement so it gets a fresh READ COMMITTED snapshot. The transaction commits before further preparation; no lock crosses async transport work. Assertions require canonical five-digit format, persisted identifier-to-actor ownership, and different identifiers for the two-tenant case. Exhaustion fails the fixture instead of reusing another actor. Existing actor/identifier rows and all constraints remain intact.

After the complete bundled fix: scripts TypeScript exit0; tests TypeScript rerun exit0; scoped ESLint on both changed tests exit0; tracked helper config PASS4/skipped87/collected91; diff check clean. Commands are the previously recorded Node22.23.2 scripts/tests tsc, helper-config vitest and ESLint with both `scripts/ediel-correction-context-native.test.ts` and `__tests__/synthetic-customer-journey-pre-smtp.e2e.test.ts`. Native count remains301. No local PostgreSQL/Storage execution is available; fixture SQL needs genuine parent rerun. Published migration031626, its checksum, runtime, and generated contracts are unchanged. Freeze bundled test/report fix for scoped review against published45d3e5c921fd5a68b9a2ee1f216c487839dd4c81.

## Fix round 3/5 — real CMS serial compatibility and two native fixture causes

Parent authorized fix3 against published954b0e06 after genuine native run35955234101/job107492044271 finished298PASS/3FAIL301. Failures were the actual S/MIME recipient check, bulk1000-row fixture seed exceeding sql()'s10-second subprocess timeout before the reader, and membership fixture mutation blocked by the legitimate last-functioning-admin guard. Parent evidence artifact10789599035 is log-only, SHA256 `a728a0119bc712159798e24ee9a6741d6b8cee5985f4b441fd5ef5f032ad703b`. FullE2E6062/376 and coverage passed at that published head (parent-reported). Published migration031626 remains immutable and unchanged.

A: confirmed actual runtime representation defect. `openssl req -set_serial 1234` creates certificate hex serial04D2; real `openssl cms -cmsout -print` writes decimal1234. A large serial0xFEDCBA98765432100123456789ABCDEF prints with that0x prefix. Existing inspection treated decimal digits as hex and regex truncated the0x form to0. Two new actual-crypto tests (real generated public certificates, actual encryptSmimeEnvelopedData and inspectCmsRecipientInfo, no crypto/process mock) first failed with expectedReceiverPresent=false at the intended matching assertions. Parent expressly authorized the necessary narrow index.part-1.ts representation seam and rejected disguising the issue with serial9; native fixture retains original1234.

Runtime correction parses the entire OpenSSL serialNumber line, accepting only full decimal digits or0x-prefixed hex, converting with BigInt to hexadecimal without Number precision loss. Expected certificate hex normalization, recipient matching policy, encryption implementation, OpenSSL/forge fallback and DER-byte fallback are unchanged. Unsupported printed forms return null. Tests cover the two real encrypted CMS forms plus wrong expected serial returningfalse, seven malformed/unrecognized complete values, and decimal9007199254740993 preserving exact precision. New unit/actual-crypto file contains10 tests; these are ordinary suite tests, not new native cases.

B: replace single1000-row insert with20 bounded50-row inserts. Every real canonical trigger still executes, and an explicit persisted outbound count1001 is required before readset assertions. Neither sql()'s10-second budget, native30-second test budget, reader1000 bound nor exact overflow/scope assertions changed. This addresses the observed seed-statement timeout; genuine native execution must still qualify total cost and the unchanged reader budget.

C: before deactivating the target membership, establish a second legitimate synthetic administrator using an active confirmed user/profile from the existing seed owner, a company_admin membership and corresponding company role. The real guard remains enabled and permits the target mutation only because another functioning administrator exists. Assert the backup satisfies active/confirmed/unbanned/undeleted admin predicates and target is_active=false, then retain exact SQL denial and actual direct zero-provider/entry checks. No membership/tenant security guard is bypassed.

Verification (Node22.23.2): meaningful actual-crypto RED2 then GREEN10; targeted crypto+existing archive/send-guard/security suite PASS34/4files; helper/preflight PASS4/skipped87/collected91. App/scripts/tests TypeScript and scoped ESLint exit0; git diff check clean. Exact commands:

```sh
npx --yes node@22.23.2 node_modules/vitest/vitest.mjs run __tests__/ediel-cms-recipient-serial.test.ts
npx --yes node@22.23.2 node_modules/vitest/vitest.mjs run __tests__/ediel-cms-recipient-serial.test.ts __tests__/ediel-smime-archive-send-guard.test.ts __tests__/ediel-smime-archive-storage.test.ts __tests__/ediel-transport-security-resolution.test.ts
npx --yes node@22.23.2 node_modules/vitest/vitest.mjs run --config quality/audits/ediel-masterplan-v2/e035-source-ledger/process-history-task3a-local.config.mjs
npx --yes node@22.23.2 node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json
npx --yes node@22.23.2 node_modules/typescript/bin/tsc --noEmit -p tsconfig.scripts.json
npx --yes node@22.23.2 node_modules/typescript/bin/tsc --noEmit -p tsconfig.tests.json
npx --yes node@22.23.2 node_modules/eslint/bin/eslint.js lib/ediel/transport/index.part-1.ts scripts/ediel-correction-context-native.test.ts __tests__/ediel-cms-recipient-serial.test.ts
git diff --check
```

Native total remains301 and native correction file91; expected ordinary full-suite increase is10 (6072 versus prior6062), subject to actual collection. No new local PostgreSQL/Storage PASS is claimed. No SQL/checksum or generated artifact changed. Freeze for scoped review of the evidenced printed-serial fix and these two fixture corrections, then genuine same-head CI. Parent memory/native audit remains outside the author's commit.
