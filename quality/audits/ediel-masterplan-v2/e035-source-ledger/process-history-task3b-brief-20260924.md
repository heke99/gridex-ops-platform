# Task 3b — finite customer and process transition facts

**Status: PREPARED, not STARTED.** Parent-approved second checkpoint of Task 3, dispatched only after independent/native acceptance of 3a, on its immutable accepted BASE and explicit START. This is prospective partial capture; it grants neither historical completeness nor positive C/process-basis authority. Follow the original Task 3 brief, approved PH-1–4/design review and five-point activation clarification, finite inventory/review, fixture preflight and prepared Task 3 report.

**Runtime boundary:** separate CLI-created forward `supabase/migrations/<allocated_timestamp>_correction_process_facts_v1.sql` with private immutable facts/gaps/witnesses, locked per-table epochs and capture helpers/triggers; narrow bounded owner adapter `lib/ediel/sources/correctionProcessReadset.ts`; native fixtures in `scripts/ediel-correction-context-native.test.ts`. Existing business producer files are not pre-authorized for broad edits. If an event-only swallowed fact needs an additional producer change, report its exact seam and keep scoped incomplete pending authorization. Task 4 composition/consumer files and generated contracts remain outside this checkpoint/parent-owned respectively.

## Exact capture surface

Transactionally capture final normalized INSERT/UPDATE facts, OLD plus NEW immutable scope/links and DELETE tombstones for each of these twelve tables (the inventory's minimum, not proof that no other route exists):

1. `customer_contract_events`: company/customer/contract, event type/time, actor, note/metadata; preserve OLD tenant/customer before composite SET NULL.
2. `customer_contracts`: company/customer/site aliases/point, status and start/end/sign/termination/withdrawal facts; signed-state canonical gates remain active.
3. `customer_sites`: company/customer/facility/parent site, grid owner, status and move/closure dates; point links run point → site.
4. `metering_points`: company/customer/site aliases, physical/EDIEL identity aliases, status and dates; row UUID differs from text `metering_point_id`.
5. `customer_supply_periods`: company/customer/point/contract aliases, interval/actual dates, status/source and source message/process/switch links.
6. `supplier_switch_requests`: company/customer/site/point/contract aliases, operation, type/status, LI, dates, process/subtype and inbound/outbound links.
7. `supplier_switch_events`: company/request link, type/status/time and bounded payload/metadata; no direct customer/point column.
8. `customer_cases`: company/customer and site/point/contract/switch/outbound/cancellation links, status and withdrawal/cancellation dates.
9. `customer_case_events`: company/case/customer, type/status/message/time and OLD parent linkage.
10. `customer_operation_jobs`: company/customer/site/point/operation, type/status/idempotency and bounded request/payload/result.
11. `customer_operation_tasks`: nullable company/customer/site/point, type/status/metadata/resolution; no relational row `operation_id`.
12. `customer_operation_events`: company/customer/site/point/job/operation, code/status/source/time and bounded payload; event status `in_progress`, no row `updated_at`.

Capture relevant identity/status/date/operation facts and bounded source fields without promoting JSON links, `source`, actor strings, mutable case/contract status, generic `domain_events` or `customer_case_events` alone into authenticated correction provenance. Missing/conflicting ownership must atomically produce a scoped gap or reject a relevant mutation. Oversize cannot silently omit a transition. For child deletion/cascade, retain OLD or prior immutable linkage before a parent disappears; composite SET NULL must retain old tenant/customer/point scope. A rejected business transaction rolls back observation rows. Each actual committed archive mutation gets its tombstone, even if a later archive transaction fails. Deny TRUNCATE/disabled-trigger paths or explicitly invalidate covered history.

## Native-first execution and acceptance

- [ ] Inspect actual `pg_trigger`, `pg_constraint`, defaults/generated columns, FK actions and normalization order after full replay on BASE; fixture preflight is only source inspection. Build two-company synthetic graphs with unique IDs/keys, proper permission registry and active company/member/profile scope. Do not supply generated columns. Capture final normalized row after BEFORE normalizers and preserve actual side effects.
- [ ] Write failing native direct CRUD on all twelve tables and route probes for `addCustomerContractEvent` → `gridex_record_customer_contract_event_v1`, legacy supply writer, supplier-switch activation, operation enqueue/claim, case db/engine/support and profile archive. Verify native wrong-company/direct private access denial, owner scope after parent loss, counts and independent committed witnesses.
- [ ] Exercise switch-event request-delete RESTRICT and rollback, explicit event-before-request deletion, supply restrictive FKs, signed-contract DELETE rejection with real canonical signing prerequisites, valid draft deletion after dependencies, case→event and isolated customer/site cascades, point/job SET NULL, and archive partial failure after earlier committed deletions. Do not bypass business gates to force a successful fixture. Test captured OLD/NEW scope and separately committed tombstones, no tombstone for rejected writes.
- [ ] Exercise actual `emitCustomerOperationEvent` swallowed insert failure. A trigger cannot see the missing event; an independently captured consequential state may qualify that state only. Event-only omitted facts and unqualified routes retain scoped `complete:false`. Confirm SQL direct/RPC/batch/legacy/delete paths and add any discovered missing producer to the acceptance inventory before claiming complete; no inference from a source-file table list alone.
- [ ] Install locked prospective per-table epochs and atomic facts/gaps, verify bounded 1,000 scoped transitions, 65,536 bytes/fact and 6 MiB transition input budget, exact candidate counts and scoped overflow reasons. Run real native rollback, RLS/tenant, cascade, SET NULL, archive and route evidence plus targeted migration/type/lint checks and independent review. Record exact results. Keep third-set `complete:false` until all named producer/native qualification and inception-to-cutoff retention are genuinely established; prospective epoch never repairs pre-epoch/deleted history.

## Task 4 handoff

Provide immutable transition facts, predecessors, gaps, epochs and committed witnesses to the one SQL MVCC statement that must materialize outbound Z08H, received L/LK/C/replacement originals/assessments (including qualified reference owner when relevant), and this finite third set. The future `openCorrectionProcessSnapshotV1({companyId,environment,cutoffAt,customerId,pointId,supplyPeriodId})` saves text/hash/ID, exact three-set counts, complete flag, retention start/end/missing spans and witnesses under the same visibility boundary; sequential client snapshots with equal timestamps do not suffice. Candidate scoping precedes limits and semantic review. Unknown party/date/object links are wildcards; proved unrelated point/gap does not spill over. Failed reader, overflow, swallowed event, missing producer or unknown inception retention is scoped incomplete. New epoch alone never activates an ordinary-supply hold: the paired Task 4 oracle is unchanged qualified pre-epoch ordinary supply with no matching concern versus identical scope held after a newly visible raw/unwitnessed matching concern, plus wildcard and proved-unrelated variants. Preserve saved earlier cutoffs and finalized retries, and no positive C/basis/reopening or invented national response.
