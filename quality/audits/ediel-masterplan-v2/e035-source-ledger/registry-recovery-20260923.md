# Disposable replay image registry recovery — 2026-09-23

Base: `528831d2302995d5f317641a51338f071162b7ac` (PR370). Native jobs `107318534277`, `107320203307`, and `107321599880` failed on GHCR `toomanyrequests` before database startup, per the read-only registry preflight. Those failures did not execute the native case/browser checks.

The workflow uses `supabase/setup-cli@v1` with CLI `2.101.0`. [Official setup action source](https://raw.githubusercontent.com/supabase/setup-cli/v1/src/main.ts) exports `SUPABASE_INTERNAL_IMAGE_REGISTRY=ghcr.io` for that version. [Official pinned CLI source](https://raw.githubusercontent.com/supabase/cli/v2.101.0/apps/cli-go/internal/utils/docker.go) defaults to `public.ecr.aws` and maps images to `<registry>/supabase/<original-final-image-name-and-tag>` for mirror registries. This supports an explicit step-level `SUPABASE_INTERNAL_IMAGE_REGISTRY: public.ecr.aws` on the whole clean replay/typegen step. It retains the selected image names/tags; cross-registry digest equality and image availability have not been established.

The only workflow change is this two-line environment override. CLI `2.101.0`, PostgreSQL `17.6.1.155`, all other upstream versions, replay and native/browser commands, assertions, schema/type comparisons, and other jobs remain unchanged. YAML parsed with PyYAML BaseLoader, and a structured comparison against the base workflow passed after removing precisely that new environment mapping; `git diff --check` passed. `actionlint` was unavailable locally. Product tests were intentionally not rerun for this infrastructure-only change.

Status: **candidate, not native acceptance**. Ordinary CI must demonstrate ECR pulls, local database startup and the unchanged replay/native/browser/type/schema gates. A later assertion failure must be assessed on its own evidence. Case-navigation fix round 5 remains pending native qualification; full E035 remains incomplete. No hosted operation or PR310 change was made.
