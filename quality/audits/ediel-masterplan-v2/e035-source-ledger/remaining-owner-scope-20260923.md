# E035 remaining owner applicability — bounded read-only inventory

Observed local HEAD77ea6d1fbf6bb83a1882ff4d123660a296ad601b; concurrent closure work is outside this report. No implementation/test/masterplan edits, execution or acceptance. Present fail-closed behavior does not establish universal completion.

## Original source identity

Original source texts read from `/workspace/scratch/db7cad0629c3/sources/{prodat,technical,utilts}.txt` with original PDFs retained alongside them. Verified hashes in this session:

- P26.A r3:83c2f1d2915851d2e670731f6ab404ef06c9b9def282afbafdfa0eda836a6e95 (140pages).
- T24-A-6:5204d4514774b04b8eedb039e1f4799ed447c7fef14554577935e2d7bd93f951 (60pages); independently recomputed in this task.
- U current frozen:0524c18f38864ebe081dec9d3d53f1797b224ef0af7b01986627e895f47d99be (135pages; cover/file25-A-4, running footer25-A-5 discrepancy retained).

P47–48/114–116 establish physical object/register semantics; U3.6.8p37/Appendix2p132 establish received PRODAT comparison and genuine E61/E62. These references do not certify any missing historical inventory or legal relationship.

## 1. Agency89: nationally valid, currently unqualified domain binding

**Source:** P47 field209 LIN/C2127140 allows up to25characters, agency3055=89 assigned by distributor versus9 GS1. P114 requires distinct possibly fictitious object IDs for separate meters. P116 requires the same object ID on its multiple registers. P120 field209 says agency must agree with identity. Thus an exact local identifier is not a malformed GS1 identifier and must not be normalized into one. U field/identity scope preserves agency in the comparator; PRODAT equality alone does not establish ownership of a database point.

**Existing owners:** `prodatRegisterFields.ts` validates agency9/89; `prodatRegisterGroups.ts` and `core/receivedRegisterValidationBinding.ts` preserve object+agency and physical occurrences; `structuralSourceWire.ts` can parse89. But `sourceOwnerWire.readCommittedZ04Wire`, `reviewReceivedStructuralSource` and timeline accepted-owner checks admit only agency9. `structuralReviewReads.findStructuralReviewPoint` reads legacy meter_point_id with no namespace column. `inboundCases.ts:991–1000` explicitly holds duplicate cross-agency IDs and punctuation because existing onboarding RPC normalizes identifiers. Facility checks compare plain point/site strings to wire identity. Current SQL object-owner proof assumes legacy_unqualified/agency9.

**Minimal engineering direction:** introduce an exact namespaced metering-identity owner linked to the existing tenant-owned point/site/grid-owner graph. Key at least company/environment, agency, exact identifier and authoritative assigning grid owner; avoid case folding/punctuation stripping. Persist dated valid interval and authentic source/authority provenance. Explicitly bind89's issuer so identical local IDs used by different distributors cannot collide. Reuse physical canonical facets, existing point/site ownership and legal-party checks. New versioned proof marker/SQL branches must validate the exact alias and current graph in the same MVCC owner statement, preserving old agency9 semantics. Historical snapshot must retain the binding used; re-reading today's alias cannot invent past knowledge.

**Genuinely missing evidence:** actual trustworthy local-ID to point/site/issuer correspondence for existing records, especially after normalization lost information. Do not backfill89 from numeric string equality or relabel all legacy points9/89 automatically. Missing owner plumbing is engineering; missing original correspondence may require retained market/source records.

**Tests:** same spelling9/89 remains distinct; same89 across two issuers/tenants remains distinct; punctuation/case/released delimiters retained; exact authorized89 success through real review/native owner; alias mismatch/expiry/overlap/unverified provenance holds; no global fallback; immutable historical selection after alias changes; all registers bind same namespaced object.

## 2. Delegated sender: expressly valid legal pattern, not transport equality

**Source:** T5.2.1pp26–27 requires Ediel IDs/valid EDI actors and disallows obsolete internal proxy identities for PRODAT/UTILTS. T5.2.2p27 expressly puts legal agents for PRODAT/UTILTS and related APERAK only in UNB; NAD remains the represented legal party. Its example distinguishes a PRODAT agent from a subcontractor that only transports SMTP and must not appear as the legal agent. An actor may use different agents for different exchanges; UTILTS has at most one for its time/month variants. Therefore UNB sender differing from FR is nationally legitimate when the actual relationship is established. Mere mail sender or UNB mismatch does not prove delegation.

**Existing owners:** `tenant/tenantEdielIdentity.ts` resolves the *receiving tenant's* legal actor, role and enabled dated transport-agent relation with exact-count record evidence; `sourceOwnerReads.readSelectedFacilityEvidence` binds selected point/site to grid owner. `prodat/prodatPriorPermissionFlow.ts:44–61` checks temporal identity at receipt/business instants, but is ESCO-specific and explicitly requires other legal party==counterTransport. It is not an existing delegated-counterparty sender owner. `sourceOwnerWire.ts` and `structuralSourceWire.ts` require transportSender==legalSender. SQL `review_party_proof_consistent` independently repeats that equality. Receiver delegation support cannot silently authorize the sender.

**Minimal engineering direction:** add a versioned sender legal-delegation owner for the identified facility grid-owner actor, delegated transport actor/verified identifier, message family/application scope, tenant/environment visibility and dated authorization. Reuse temporal evaluation/count mechanisms but do not call the ESCO permission process as supplier authority. Evaluate appropriate effective/receipt instants and explicitly separate knowledge/assessment time; store exact row IDs/values and independently SQL-revalidate permission/validity/scope. Wire parser preserves full UNB/NAD tuple and accepts mismatch only when this qualified owner accompanies it. Keep current own-receiver owner. Handle optional subaddress under a separately source-qualified binding; never discard extra UNB components to gain acceptance.

**Genuinely missing evidence:** the actual grid owner-to-agent authorization/portal relationship and its historical validity. A current verified Ediel identifier proves the actor identity, not that it represented that grid owner on the relevant date. Existing tenant receiver relation is insufficient. SMTP authenticity is a different property and must not be claimed by this owner.

**Tests:** positive valid delegated sender with unchanged FR; wrong represented grid owner, family, tenant/environment, time interval, duplicate relations, invalid agent ID, SMTP-only provider and unproved subaddress all hold; direct sender remains valid; invalid receiver relation cannot be compensated by valid sender relation; source/hash and raw tuples bound independently in SQL; later delegation changes cannot retroactively change saved knowledge.

## 3. Multiple physical messages: valid same-function interchange, first-message acceptance is incomplete

**Source:** T2.1.2p13 requires number of messages to match UNZ, each UNH/UNT reference and segment count to match, and UNB/UNZ reference match. T4.3.1p25 defines UNZ0036 as number of messages in the interchange. P2.1p14 forbids mixing different PRODAT functions within one UNA/UNB–UNZ interchange, not multiple same-function messages. P47 restarts314 for a PRODAT message; register-local258 restarts per object. Thus multiple Z04 physical messages are applicable; mixed Z04/Z06 envelope is not the positive oracle.

**Existing owners:** canonical AST/tokenizer and register grouping preserve messageIndex/messageReference/physical segment geometry; immutable ledger and message-local LIN migration retain all physical membership. But `utilts/receivedStructuralSources.singleMessage` demands exactly one UNH/UNT, `sourceOwnerWire`/`structuralSourceWire`/readset require it and index0. `prodatRegisterMessageSegments` selects one message for validation; `core/receivedRegisterValidationBinding` prevents accepted later-message projections. `receivedSourceOwnerSession` selects one actual business commit/point. These are several distinct engineering layers; removing singleMessage alone would weaken the boundary.

**Minimal engineering direction:** create a bounded physical-envelope projection preserving original byte hash and each UNH slice/ordinal/reference without rewriting the interchange into a fabricated original. Validate envelope/global membership once and actual per-message canonical policy/fields for every physical message, retaining original segment-index translation. Resolve each message's own FR/DO/BGM/dates/LIN business commit independently. Immutable per-object key includes source ID+messageIndex/reference+object/agency+occurrence. Overall source accepted only when every required physical message/object owner is accepted; unavailable/rejected later messages stay represented. Extend SQL accepted owner contracts and bounded snapshot inspection for exact new physical scope. A single-message callback/status cannot approve all messages.

**Genuinely missing evidence:** no external policy decision is needed for valid same-function packaging. Actual per-message successful switch/supply or reviewed owners may not exist for old envelopes that processed only the first message; those require genuine reprocessing/review, not historical approval reconstruction. Full UNSM certification remains separately open.

**Tests:** two same-function messages both genuinely canonical/business approved;314 starts1 each; same object in two messages not collapsed; differing own parties/case/dates and equal LIN numbers retain scope; second-only error/unavailable/canonical-rule gap prevents universal source acceptance; wrong UNZ count/mixed function/duplicate refs/missing UNT/escaped UNH-like data fail appropriately; budget/truncation fail closed; immutable full-message snapshot/native tenant boundary; later registers do not inherit first-message authority.

## 4. Date-changing Z04 correction: supported replacement syntax, insufficient universal lifecycle semantics

**Source:** P42 BGM1225 distinguishes9 original and5 replacement; this identifies function, not a unique predecessor. P50 field210 is DTM92/203 actual supply start; Z06/Z10 before-start changes also require contract start. P123 field226 binds Z04L/LK to corresponding outboundZ03. P121 explicitly delegates valid contract-start date rules to Handbok. None of these clauses says every BGM5 may unilaterally move an already committed supply start or that shared LI globally identifies a predecessor. Therefore blanket date-changing acceptance is not source-proven; nor is all replacement support nationally forbidden.

**Existing owners:** `reviewReceivedStructuralSource.replacementFor` requires explicit source/assessment/hash, same case/function/business/parties/supply and different document ID. `resolveCoverage` still requires immutable root original start==live confirmed/supply start==wire anchor date; SQL reviewed source checker also requires Z04 changeAt==valid_start. `receivedSourceOwnerSession` stores real committed switch/supply facts; `structuralSourceSelection` has predecessor/fork/gap handling and an unreviewed later-date BGM5 guard. The pure corrected-effective-date positive in selection tests is for Z06, not an actual changed-start Z04 lifecycle certificate. Original commit's date precision is market calendar day; it cannot prove new sub-day operational precision.

**Minimal engineering direction:** retain hold until an explicit source-qualified lifecycle correction owner can name original committed root, replacement original, old/new start, outbound case context and successful same-supply compensation/reconfirmation. Use a new immutable transition record/marker rather than editing root. Preserve known-as-of decisions; earlier cutoffs still use old witnessed state or hold as appropriate, later cutoffs project only qualified replacement. Earlier moved date must not extend completeness before ledger start or before authenticated coverage. Later moved date must not revive the displaced predecessor in the abandoned interval. Independently bind both original/replacement DTM92/LI/BGM/parties/object from sealed bytes in SQL, applying the source-binding principle from closure design3A; same-day minute changes must not pass DATE equality. Update actual switch/supply/contract consequences atomically or retain explicit unavailable, never just relax source equality in the reader.

**Genuinely missing evidence:** authoritative Handbok/counterparty workflow for which changed starts are valid, deadlines and already-effective compensation, and the particular authentic correction/committed lifecycle result. P/T/U originals establish syntax and case linkage but do not fully settle those lifecycle consequences. This is a concrete missing-source scope, not a generic request for permission. Existing authentic retained process evidence may resolve it without user intervention; no fake agreement/approval is allowed.

**Tests after source qualification:** move later/earlier/sub-day; before-ledger move holds; correction before original; unchanged-date replacement control; wrongZ03/case/source/hash; two possible predecessors; later unwitnessed correction; canonical rejection; successful/failed lifecycle commit; saved-cutoff immutability; no duplicate supply and no auto-reactivation; exact E61/E62 only after a complete qualified interval, otherwise internal review/no quantity/no invented national code.

## Delivery order and status

Agency89 and same-function multi-message support are clear national applicability with missing engineering owners. Delegated sender is equally valid but each instance needs an actual dated legal relationship. Changed-start Z04 needs both engineering and the specifically referenced Handbok/process qualification; BGM5 alone is insufficient. These are separate bounded additions to current retained owners, not reasons to restart D110/parent work or paused PR310.

Choose one owner at a time after closure delivery gates. Each needs source/owner design review, observed targeted red/green fixtures, actual SQL/HTTP/native qualification where ownership changes, authenticated source-value binding, retained suite checks and final exact-head independent review. No completion status changes follow from this inventory or from today's safe holds.
