# E66 field 533 GS1 digit — bounded requirement, 2026-09-26

Base: remote main `cb842b203191353f84559e589c9575f42c681bf3` after PR #389. Sole active author; PR #310 excluded. No staging, TGT/AGT, counterparty or live market traffic.

| Criterion and source | Owner and effect | Status / evidence | Still open |
| --- | --- | --- | --- |
| Annex C UG-123-15, U annex 1 p.123: nonblank LOC+175/3225, and check GS1 digit when the value is a GS1 number; UG-123-16 accepts agency 9 or 89. GS1 Sweden's modulo-10 weight-3 algorithm: https://gs1.se/guider/sa-beraknas-kontrollsiffran/ . Agency 9 is GS1, 89 is distributor-assigned in the PRODAT field register. | `applyE66RegulatingObjectGuide` on each IDE, before final ACK. An invalid 18-digit agency-9 GS1 number yields application ERC 42 / field 533 and its own negative APERAK; a good sibling is unaffected. | Specified, implemented, targeted tests 82/82; app/test typechecks and scoped lint passed. RED before code: bad digit had no field 533 issue and actual inbound held it without negative ACK. | Exact PR-head CI/native and merge. Other GS1 key formats, if applicable to this field, require original profile evidence; this branch makes no claim about them. |
| Invalid regulating-object IDE must cause no metering or billing. A correct-looking LOC+175 must not inherit LOC+172 authority. | Actual inbound `processInboundUtiltsMessage` → tenant-bound `gridex_persist_utilts_consumption_v1` → ACK plan. SQL locks `(company, environment, source message, transaction)` and reserves negative disposition as `not_applicable`; source QTY remains for audit, with no accepted series. | Existing native SQL guards plus real consumer mocked-boundary regression: rejected persistence/ACK, no ingestion or billing. Correct-looking LOC+175 remains `internal_review` and has no business value. | Separate immutable tenant/legal-actor regulating-object owner and SQL sink, real native mixed-IDE storage/ACK/retry proof before positive consumption. |

The 18-digit fixture `735999260731000007` passes the GS1 digit; changing the final digit to `6` fails. Agency 89 is not subjected to GS1 arithmetic. No database migration or generated contract change is introduced. Field 203 temporal uniqueness/ERC42 and E035 historical `complete:false`, retention/deletion remain separately blocked. This is a bounded criterion, not F3 or masterplan approval.

Skill route: repository code understanding, spec-to-code comparison, systematic debugging, TDD, code review and verification/branch completion apply. Supabase applies to the read-only SQL boundary review; native replay remains required in ordinary OPS CI. Next.js UI and performance/security-wide audit groups are outside this single guide rule; no agents are delegated because one code author is required.
