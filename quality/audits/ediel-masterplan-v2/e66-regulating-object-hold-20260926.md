# E66 LOC+175: bounded receipt decision, 2026-09-26

Base: `main` `116a979d9f332f1a24bd03365298a5d86cac0dd9`. PR #310 is excluded. No live market traffic, staging or counterparty test was run.

| Requirement / source | Owner and consumers | Proof / state | Remaining gate |
| --- | --- | --- | --- |
| U annex C UF-metering_settlement-209/533 p.54: E66 facility data uses LOC+172, regulating-object data LOC+175, scoped to the IDE. | Runtime parser/profile → structural receipt qualification → actual inbound flow → `gridex_persist_utilts_consumption_v1` → CONTRL/APERAK; metering-point tables are **not** the LOC+175 owner. | Reproduced false LOC+172 rejection for a valid 175-only IDE. The changed parser retains distinct `regulatingObjectId`; the profile no longer borrows a sibling's 172. An unsupported 175 identity reaches `internal_review`, no national error, no positive APERAK, zero quantities and no metering-point ID even with a stale match. A 172 sibling remains independently eligible. Tests cover 175-only, mixed IDEs, missing identity and ambiguous dual identity. This is a **tested safe hold**, not acceptance of regulating-object consumption. | Identify durable tenant/actor-bound regulating-object identity and separate SQL storage/ACK semantics; validate UG-123-14/15/16 (nonblank, GS1 check digit, agency 9/89), native concurrent/retry/history and genuine inbound transaction effects before enabling it. |
| UG-122-7 p.122 field 203 unique over time, duplicate ERC 42. | Header guide presence is merged; no proven durable legal-actor reservation. | **Specified, presence merged, uniqueness blocked**, per release-boundary audit. | Prove scope, immutable history, concurrency and retry identity. |

This patch does not add a database migration or activate traffic. The wire stays available in the raw source message while the nonaccepted transaction persistence row records only the disposition and no quantities; it does not pretend that LOC+175 is a metering point. The unchanged SQL schema cannot make this a positive regulating-object path. Release boundary remains as in `release-boundary-20260926.md`: web deployment is not Ediel market approval.

Local verification: five relevant suites 81/81, including the actual inbound consumer; app and test typechecks passed, scoped lint had no errors (one preexisting unused-variable warning). Ordinary exact-head CI remains required for acceptance. Local full Vitest under Node 24 ran 6116/6118 before the final ambiguity guard; two existing TAP-report parser tests (`ediel-unb-ack-request`, `ediel-prodat-free-text`) failed because their Node subtest output did not match the expected `# tests` footer, not because of the E66 path. CI Node 22 is authoritative; do not count the local full suite as green.
