# F3 closure qualification — 2026-09-19

Derived evidence only. Code base `51c6c06b5fd7f56f447c0bd907373dc3a76d4676`
(PR349); task-start memory HEAD `2eab8d2f63a65e152f4d6cbf28615948b66cc021`.
No runtime, existing assertion, source, codec, schema, loader, gate, coverage-ledger
or memory changes. PR310 remains paused/excluded at
`e961135199f292b8210884f07de3b616a670161a`.

**F3 is not closed. Preserve 98/110 historical numeric acceptance and 10/10
parent occurrences.** The remaining twelve have substantial bounded protocol
implementation; their missing positive producers are not twelve missing predicate
implementations. Neither the catalog test nor this audit grants 110-cell acceptance.
The smallest next source-qualified executable unit is **PRODAT field-error identity
from field validation through the canonical ACK plan and actual P-APERAK renderer**.
A complete valid Z01 control demonstrates missing LI becoming ERC41 without226,
and an invalid sender country becoming ERC42/082 instead of207. Extra506/p119 is a
separate confirmed receiving defect with its own qualified control below.

Root independently reported main349 full run35472852603/job105976888328:
73/73,0failed, Node22.23.2 at22:25:24Z; OPS35472852618 jobs105976888047,
105976888180,105976888182 allSUCCESS. Root accepted the bounded GAS code unit
and retained counts. This task did not fetch CI or execute those gates; these
are root receipts, not fresh author execution. Root memory commit `ba01d408`
is separate. New runtime/count changes still require independent review.

## Method and authority

Read the task brief first, AGENTS and required memory, active work-plan, relevant
canonical flows/domain/integrations and decisions/known failures; inspected local
skill inventory and actual implementations. Applied bounded spec-to-code tracing,
caller/callee review, direct false-positive verification and
verification-before-completion. The PDF skill was used read-only. The
spec-to-code skill's normal further delegation is superseded by this explicit
one-author/no-subagents dispatch; root supplies independent review.
Using-superpowers exempts dispatched agents. Full repository mapping/playbook,
security/DB/performance audit, UI/Next, refactor, deployment, skill authoring and
runtime TDD triggers are absent; none of their global deliverables is claimed.

Original PDF `/workspace/scratch/2a201d6d5897/prodat-original-source/PRODAT-26A-r3.pdf`
was freshly rehashed as
`83c2f1d2915851d2e670731f6ab404ef06c9b9def282afbafdfa0eda836a6e95`.
Read full p20,68,90,93,103–105,119,122; visually inspected p68,119,122,
including non-gray506. Prior accepted audits retain original field/parent/register
notes and workbook evidence; those units were not reimplemented or retested.
Read frozen masterplan §§18–21, AT-P-01–17, SC-043 and AT-ACK-04. Frozen status
text and inventories remain untouched. The companion JSON reuses the exact110
IDs and10parent occurrences from the original/accepted inventory and adds derived
disposition/evidence references. It is not a replacement rule catalog.

## What the acceptance contract actually requires

| Contract | Exact scope and consequence |
| --- | --- |
| Masterplan §18 F3 | 74 fields,110D, parents/registers, UNB0031, CCI/CAV, PAP/UAP/CONTRL/ERR, guide before function; approval requires independent golden/rejection and closure of old bug reproductions. A D counter is not the phase gate. |
| AT-P-01 | Explicit handling of74 numeric fields+3parent groups,110baseD+register overlay; fulfilled conditions and relevant error/boundary cases; results carry correct field number, group/component, object/case and source rule. Explicitly prohibits field-number regex over DTM/RFF text. |
| AT-P-02 / original p119 | Ignore inapplicable national extra data for business projection and negative APERAK; retain raw. Do not conflate unused national fields with genuinely excessive syntax elements. |
| AT-P-03 | Outgoing true/false/unknown; false means source-specific O or X. R fields use authoritative facts; O uses conscious choice; missing local facts blocks own send. It does not demand inventing an actor role or positive producer for every historical wire function. |
| §19 | Implementation-ready requires unambiguous source, actual code/model and independent known oracle. Production-ready additionally requires implementation/schema/transport/rights/formally applicable approvals on correct scope. Shared parser/renderer roundtrip or green catalog is insufficient. |
| §20 G03/G04/G05/G06 | Formal actor/scope testing, rights, actual operational evidence and full UNSM/consumer/schema inventory are distinct gates. Missing evidence blocks the affected capability; it neither waives a protocol defect nor stops unrelated available work. |

**Recommendation, requiring independent adjudication:** absence of a positive
persisted Z14/Z10/death producer is not itself an express AT-P-01 requirement to
build one. Source-valid synthetic golden/rejection tests through real field
consumers, plus tested unknown/unsupported persisted holds, can establish bounded
protocol behavior without release eligibility. But pure-helper tests alone do not
establish all reachable consumers, correct field attribution or final ACK effects.
The defects below prove that even accepted field units have remaining composition
work. Do not rebrand the historical98 counter as a complete protocol proof or
promote the twelve automatically. Keep separate per-cell protocol evidence,
authorized TGT producer, live eligibility and formal certification axes.

## Exact110+10 reconciliation

The JSON contains all110 distinct `PC-*` rows and10 distinct `PG-*` occurrences,
with one group/receipt per row. Original predicates remain in the unchanged source;
approved full-source qualifications, including Z09E and GAS240, remain in the
named audits. No predicate was regenerated from runtime values.

| Historical accepted group | Numeric cells | Accepted evidence |
| --- | ---: | --- |
| Source subtype6; Z04 reference319; Z06 product242 | 8 | Subtype/Z04/Z06 accepted audits and actual source/consumer tests |
| Z06/Z09 UD227/228/231/232/316 | 10 | `f3-d-ud-20260919.md` |
| Z14 positive/N twelve cells | 12 | `f3-z14-dependent-20260919.md`, PR338 |
| Optional IT233/234 inZ01/Z03/Z08 | 6 | PR340; subtotal36 retained in prior dependency inventory |
|258 inZ04/Z06/Z10|3|PR341, `f3-register-inventory-20260919.md`|
|214/218/259 inZ04/Z06/Z10|9|PR342, `f3-register-readings-20260919.md`|
|229 inZ01/02/03/04/05/06/08/09|8|PR343, `f3-end-user-address-20260919.md`|
|IV250/251/252/253/317/318 inZ03/04/05/06/08/09|36|PR344, `f3-invoicee-source-20260919.md`|
|210 inZ06/Z09/Z10;211 inZ09|4|PR345, `f3-date-events-20260919.md`|
|321/323 inZ13|2|PR346, `f3-reporting-permission-source-20260919.md`|
|**Historical total**|**98**|Acceptance receipts in `.agent-memory/completed-work.md`, not fresh recertification|

Parents: UD-Z06,UD-Z09,UD-Z14,IT-Z14 (4), plus IV-Z03/Z04/Z05/Z06/Z08/Z09
(6) =10. Optional IT parents inZ01/Z03/Z08 are not extra D occurrences. JSON
preserves that distinction; runtime catalog120 =110numeric+10parents.

The accepted paths are concrete: `canonicalPolicyFieldValidator.ts` calls the
specialized owners; independent TGT facts flow through save actions, source/run
selection, generic/profile/TGT builders, body-bound evidence, preflight and guarded
send. Per-group test/owner/audit paths are in JSON. Z13's
`ediel-prodat-reporting-chain.test.ts` exercises real manual/autopilot actions and
mocked send with exact association and revision checks. This is authorized test
source evidence, not live address/contract/permission authority or external approval.
The catalog test `ediel-canonical-policy-batch-regression.test.ts:102–137`
explicitly disclaims110wire validation and leaves unqualified root hints unknown.
Its iteration count provides identity completeness only.

## Twelve bounded cells: disposition without acceptance inflation

| Exact cells | Verified bounded implementation/evidence | Still unqualified |
| --- | --- | --- |
|PC-321-Z14,PC-323-Z14|Reporting owner and `ediel-prodat-reporting-policy-boundaries.test.ts`: positive request presence/value correspondence, bounded/indefinite end, purpose, N exclusion, false/U and physical scope; persisted authority rejects pure selection. Approved PR346 unit.|No current registered Gridex-outbound positiveZ14 producer, source reload or positive persisted send chain. Protocol closure requires independent consumer/ACK assessment, not a new DSO role by fiat.|
|PC-254-Z10,PC-242-Z10|PR347: `prodatMeterChangePolicy.ts` plus meter-change tests cover source OR truth table, changed/unchanged/unknown, exact event/customer/object/agency/LI/minute, supplied-code compatibility, actual inbound and preflight/guard/SMTP holds.|Positive persisted Z10 event producer absent. Four supplier Z10 test steps are portal-inbound, not outgoing authority. Manual/TGT unmapped selected faults correctly require review; automatic national error mapping is not certified by that hold. Separate506 remains open.|
|PC-310-Z05,PC-310-Z06,PC-310-Z09|PR348: `prodatDeathStatusPolicy.ts` and source/inbound tests cover own Z05LK/Z06E assessment, Z09E process-derived requirement without extra local death boolean, true/false/U and p119 ordering. Actual direct ACK gives41/310 missing,42/310 invalid, own object/LI; persisted eligible death sends held.|No qualified positive persisted death-event producer. This does not erase the protocol coverage or require local death evidence for source-valid received Z09E. Manual/TGT unmapped selected faults remain review-required.|
|PC-320-Z04,PC-320-Z06|PR349: `prodatGasApplicabilityPolicy.ts` and gas source/policy/disposition tests cover GASZ04 required, Z06F/G required/Eexcluded, ELexclusion, exact reference/value syntax, actual consumers and protected selected sends.|No GAS positive producer or operational role. Historical GAS ACK boundary is review-required; no live GAS certification. These320 conditions need market/subtype, not a fabricated change-event producer.|
|PC-240-Z04,PC-240-Z06,PC-240-Z10|PR349: Z04required; Z06Eoptional; Z06F/G and Z10M/E58 causal changedR/unchangedO/unknownU; strict event revision/ownLI/object binding, outgoing structural validation; inbound gray240 no-new-content-rejection; EL exclusion.|**TIM/SCH domain identity is not implemented/qualified by condition-only evidence.** P77 TIM actual series versus SCH installation identity requires source regime/value authority. Missing GAS role/certification is distinct from this remaining value-semantic capability.|

Relevant normative source is recorded in each approved audit; the old compact
`death AND subtype` predicate and market-only GAS shorthand must not override
full notes. No new producer, grid-owner/GAS role, schema or PR310 dependency is
proposed. The two320 and three240 cells must not be treated identically when
adjudicating residual protocol versus capability scope.

## Fresh bounded reproductions and false-positive qualification

Fresh local Node24.19.0 scratch probe:
`/workspace/scratch/2a201d6d5897/f3-closure-probes/observations.test.ts`;
4 observation tests pass. This means the recorded current behavior reproduced;
it is **not** a normative pass. Full qualified Z01 inputs, exact issues/plans and
renderer outputs are in the tracked JSON. No external operation ran; DB access
in manual probes was a mock throwing `UNEXPECTED_DB`.

### F3-Q1 — confirmed, medium: canonical field identity is discarded

Source: AT-P-01; SC-043; P90/93 require A904 to name the actual PRODAT field for
ERC41/42; P103 defines41 missing versus42 wrong content; P104 defines
`FTX+AAO++<field>::260+...`; P105 requires available own object/LI references.

Chain read: `canonicalProdat26AFieldRules` preserves each descriptor's
`fieldNumber` → `fieldMatrix.ts:validateFieldMatrixPayload` selects the rule and
physical scope, but emits `EdielRulebookIssue` without that identity →
`validateCanonicalPolicyFields` → `core/runtimeDecision.ts:applyProdatPolicyDecision`
→ `applicationErrorFromIssue` regexes `fieldPath` and unconditionally sets
object/LI null. Registry wrapping retains the base response. Actual inbound
`flows/inboundProcessing.ts:293–365,480–497` persists/uses that plan and passes
its `applicationErrors` through `createAckIfMissing` → `buildAperakDraft` →
`aperakEngine.renderAperakEdiel`. The renderer uses the supplied field code; it
cannot reconstruct the lost field. This is a reachable application flow, not an
unused helper. No real persisted ACK or provider send was executed.

| Qualified Z01 mutation | Current actual result | Required result |
| --- | --- | --- |
|None|No field issues; positiveCONTRL, no unnecessary positiveAPERAK|Retain|
|Remove only own RFF+LI|Required-field error; canonical negative41 with `fieldCode:null`; actual P renderer emits FTX without C107 reference|41/226; own available object, no invented missingLI|
|Change only sender country SE→SWE|Field shape error; canonical negative42/082 from `C082`; renderer emits `FTX+AAO++082::260...`|42/207, header scope; never082|

The low-level renderer was given explicit synthetic parties/document/object refs,
not invoked against a service. Single-object fallback may restore an available
object reference in this case; that does not restore A904 or prove multi-object
attribution. No claim of a wrong live delivery is made.

### F3-Q2 — confirmed, medium: extra506 causes a forbidden negative ACK

Source: P20 makes506 applicable only Z13/positiveZ14; P68 separates fourth
C889 component242 from fifth506; p119 protects false/X extras even if bad;
p122's506 code validation applies only after applicability. Non-gray status does
not override p119.

Add only `CCI++Z14`/`CAV+::::8716867000030` or `CAV+::::INVALID` to the otherwise
valid Z01: `validateRulebookMessage` emits blocking energy-product forbidden;
`resolveCanonicalRuntimeDecision` changes the control to negativeAPERAK42 solely
because of506. Thus this is more than a warning-only helper discrepancy.
Three-alphabet Z10 observations independently reproduce the validator error;
`decideProdatAperak` remains positive and raw preflight does not emit the same
506issue. Z10's initial fixture has other field/register errors, so that observation
alone does not prove a506-only final negative. The complete Z01 control does.

`parseProdatMessage` preserves the supplied506 value even inZ10. Raw/descriptive
parsing may retain extra content; no live business mutation from that value was
proved. `parseInboundProdatBusinessData` keeps242 distinct, and no506 business
write was identified in the inspected consumer path. The next506 unit must
qualify semantic projection separately, retain raw evidence, preserve valid242
in shared CAV, and retain applicableZ13/Z14 checks, syntax errors and outbound
exclusion. Do not suppress whole CCI/CAV pairs or use a false242 predicate to
suppress506. Existing meter-change test's explicit forbidden506 expectation is
an old implementation assertion, not the source oracle; any amendment needs
individual root adjudication.

### Old static LI script — failure reproduced, alleged missing validator refuted

`npm run ediel:rule-regression` still fails line133 expecting
`RFF_LI_MISSING`, as already recorded on accepted59785d99 in the IV audit.
Fresh exact original fixture/call returns
`CANONICAL_POLICY_VALIDATION_FAILED: canonical_policy_direction_required:PRODAT:Z03`
before field evaluation. Its wire also lacks a valid subtype and has historical
profile/element forms. A canonical runtime call with inbound direction rejects
that payload for missing subtype. It is not a source-qualified single-defect LI
fixture. The independent valid Z01 missingLI control proves the actual required
field validator works with `FIELD_MATRIX_REQUIRED_FIELD_MISSING`; its downstream
null226 is F3-Q1. Do not add obsolete code aliases or remove a check merely to
make this script green. No old script/assertion was edited.

### Limitations and non-findings

The first scratch run had an incorrect import name (`parseProdat`); corrected to
actual `parseProdatMessage`. A candidate invalid205 date reached the guide-date
gate before field evaluation; it was retained as interception history and not
used as field-identity proof. The qualified invalid207 control replaces it.
Manual/TGT Z10 observations reached the deliberate DB mock; they prove no
positive/negative stored disposition and no actual DB access. Previous accepted
Node22 full-suite counts are receipts, not rerun here. No claim that every field
consumer or all74values is newly audited follows from these probes.

## Remaining F3 work beyond the D counter

| Area | Evidence/disposition | Next boundary |
| --- | --- | --- |
|74numeric field identities/locators|Accepted locator, CCI/CAV, RFF, BGM, NAD, DTM and register audits cover distinct families; current matrix retains74numeric+3parents. Old `protocol.md` P3/P4 snapshots are superseded by those accepted units, not instructions to restart them. F3-Q1 proves shared error projection still violates AT-P-01.|Fix typed field attribution through ACK; independently reconcile remaining value/grammar/semantic coverage instead of declaring74complete from row count.|
|Extra national fields / p119|F3-Q2 confirmed; selected death/GAS/meter-change owners already have bounded applicability handling. This is not proof every unused field follows p119.|After identity unit, bounded506 incoming applicability/projection; do not globally ignore invalid syntax or applicable sibling242.|
|UNB0031 / CONTRL / PAP-UAP / ERR|Source-family correction and `ediel-masterplan-protocol-regression.test.ts` cover P/U ACK and storedERR alias; `ediel-aperak-source-profile-regression.test.ts` covers source version selection. ACK matrix keeps Z01→CONTRL/Z02 and ERR→CONTRL/APERAK, with loop controls.|No new full closure verdict for UNB0031, all ACK fields, every final-disposition path or guide-before-function. Require explicit contract-to-consumer golden/rejection evidence; do not infer completion from the D units.|
|Full syntax / G06|`core/syntaxValidator.ts` visibly checks envelope presence/count/reference and limited status handling. It is not evidence of complete UNSM component/segment cardinality. MasterplanENV rules andG06 still require full grammar qualification.|Qualify actual grammar/version and reachable parser evidence separately; keep unavailable source versus missing implementation distinct. No syntax waiver from national field tables.|
|Manual/TGT mapping|Selected Z10/death/GAS diagnostics can intentionally stop at `*_ACK_REVIEW_REQUIRED` before DB rather than invent ERC/field mappings.|Those holds are safe bounded outcomes, not automatic APERAK completeness. Extend only after source-qualified exact attribution/disposition.|
|Legacy formats|PR347 audit R4 records accepted-base0271e118 `UNH;` list→register-evidence error in row dispatch and quote-bearing list throwing in rulebook guard. Raw-routing fix was bounded.|Preserve those concrete remaining limitations; no blanket XML/list acceptance or reclassification as new GAS regression.|
|Live source/capability / release|Z14/Z10/death positive producers, current GAS240 identity, source/rights/transport/formal test scopes remain individually unqualified.|No invented GAS/DSO role, producer, agreement, certification or count credit. F4 registry market loss/import-routing remains later cross-reference, not activated here.|

The Z10 observation also exposes canonical runtime's reading214/218/259 U issues
without receiver-local facts. This is a **separate composition warning requiring
source/consumer qualification**, not a new adjudication of the accepted outbound
readings unit. The current audit neither accepts that receiving behavior nor
changes its tests. F3 closure must inspect actual canonical/runtime ACK composition,
not only the newer direct decision-engine paths.

## Next executable unit: typed PRODAT field errors through P-APERAK

This is a proposed source/architecture contract for independent review, not runtime
authorization. It requires no external producer, role, schema, source, SQL or gate
change. Keep the existing presence/content decisions and national ERC meanings;
change how a known field result reaches its actual ACK consumer.

1. **Authority and type.** The field owner supplies the existing numeric
   `RulebookFieldRule.fieldNumber`/source descriptor, the applicable group/component,
   and a typed `missing` versus `invalid` finding. Add optional structured metadata
   to the internal issue type, for example `prodatFieldError: { fieldNumber,
   kind, sourceRule, occurrence }`. `fieldNumber` must name a source-known numeric
   field; the three parent labels and arbitrary runtime codes are not national
   field numbers. `sourceRule` names the owning descriptor/rule and source locator,
   not a newly invented authority. This is internal metadata, no new wire field.
2. **Occurrence.** `occurrence` distinguishes header from own first-register/object
   or register scope and retains available exact decoded installation+agency,
   actual LIN occurrence and ownLI. Populate it where `fieldMatrix.ts` already
   selects the source-backed scope, before reducing to strings/issues. For a
   missingLI fault, preserve the existing object's identity and keepLI absent;
   never borrow another object/message, row cache or a guessed default. For a
   header207 fault, preserve header scope; do not assign the first object by habit.
   Preserve release-aware identity values and established source paths. Reuse
   grouping/tokenizer owners; no parser/codec rewrite.
3. **Propagation.** Preserve metadata through
   `validateCanonicalPolicyFields`, normal/registry validation, canonical runtime
   issue projection and response plan. Current `EdielRulebookIssue` lacks these
   members, while specialized owners already know selected field/object details;
   audit each producer feeding `applyProdatPolicyDecision` when adding the adapter.
   Preserve its explicit metadata where present. Do not replace the selected
   death/meter/GAS error mapping or its applicability decisions with a generic
   path match. The initial acceptance scope is the shared field-error channel,
   not a reimplementation or new acceptance of110conditions.
4. **ACK mapping.** P103 already authorizes41 for mandatory missing and42 for
   wrong field content; P93 authorizes the owning field number; P104 the P-family
   FTX layout. Map these typed kinds to those exact existing codes, with exact
   occurrence references underP90/105. Remove numeric extraction from
   `applicationErrorFromIssue`; neither `DTM+164` nor `C082` may become field164/082.
   Do not assign226 to every RFF or242 to every shared CAV. Preserve family-specific
   P/U layouts and already explicit application errors. Policy/authority/unknown
   diagnostics are not automatically field errors: no invented national field,
   generic41/42 or invented business outcome may be supplied to hide missing
   metadata. Any genuinely unmapped producer must be made explicit as a bounded
   review/unqualified disposition and independently reviewed before broad routing
   changes; don't claim this unit closes the remaining manual/grammar gates.
5. **Real consumer.** Verify the response plan carried by
   `resolveCanonicalRuntimeDecisionWithRegistry` and persisted by
   `applyCanonicalRuntimeDecision` remains the plan consumed by
   `createAckIfMissing`/`buildAperakDraft`/`renderAperakEdiel`. Use mock DB/provider
   only; inspect final P-APERAK ERC/FTX/RFF, not a synthetic helper-only assertion.
   Single-object renderer fallback is insufficient for multi-object errors.
6. **Independent oracles.** Start RED from this audit's complete control, missing226
   and invalid207; add sourceSC-043 missing327→41/327 (never164), an applicable
   characteristic field versus sibling242/506, two objects with an error only in
   the second, repeated registers, header fault and escaped exact refs under the
   three supported service alphabets. Own absentLI must remain absent; retain
   rawpayload and source object. Include valid/no-error and source-bound P/U ACK
   controls. Inspect any failure before changing a fixture. Do not replace an
   expected national error with a green internal helper code.
7. **Old assertion decision.** Keep `ediel:rule-regression` unchanged until root
   separately rules on its exact missing direction/subtype fixture and obsolete
   `RFF_LI_MISSING` expectation. The valid independent missingLI case is the
   normative test oracle; repairing that script may be a separately authorized
   evidence amendment, not a production alias added for green output.

This unit is smaller than creating unavailable producers and is mandated directly
by AT-P-01/P93. F3-Q2 should follow as its own coherent applicability unit; the
field-identity change must not also suppress506. Counts and frozen statuses stay
unchanged until independent acceptance adjudication.

## Verification record

- Fresh original PDF hash matches the immutable source identity above.
- Read-only source extraction/visual inspection and actual callsite traces as
  listed; no web/network/live calls, runtime modifications or broad suite rerun.
- `npm run ediel:rule-regression`: FAIL at existing line133; exact reason and
  qualified false-positive interpretation recorded above. Not a green gate.
- `node node_modules/vitest/vitest.mjs run --config
  /workspace/scratch/2a201d6d5897/f3-closure-probes/vitest.config.mjs --reporter=json
  --outputFile=/workspace/scratch/2a201d6d5897/f3-closure-probes/results.json`:
  4/4 observation tests,0failed; nine Z10 combinations, exact oldLI call, five
  qualified Z01 variants and two actual renderer observations. Initial import
  setup failure and intercepted invalid205 remain scratch history.
- Companion JSON checked:110exact original unique IDs,98historical+12bounded,
  10exact parent occurrences; all group audit/test/owner paths exist. The stale
  prior36+74 ledger is reused only as an identity/source inventory, not its old
  unresolved status or predicate interpretation.
- `npm run ediel:masterplan-v2:integrity`:PASS33originals/121rules/231contracts;
  conformance/readiness both explicitly false. `git diff --check`:PASS.
  Diff against base for `docs/ediel/masterplan-v2`, `lib`, `scripts`, `__tests__`
  is empty. No field/parent acceptance follows from integrity or probe counts.
