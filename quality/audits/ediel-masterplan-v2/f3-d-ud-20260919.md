# Z06/Z09 UD: local qualification, exact-head CI/review still required

Status: IMPLEMENTED_NOT_VERIFIED. Base dcd0e5fa0f6e8949d614bd09b941b22d70acdd1f, tree 8764838fa530791cfb82403f5c97a0c44f43e675. Tested code/test tree a1cc6bcc6de6fa285c5ed85cef1673d11a96e75a. Audit/memory are the only additions after that tree. The filename uses the Stockholm session date; execution log timestamps are UTC on September 18. Live PR/ref state supersedes this prepublication snapshot.

## Bounded source and behavior

Unchanged user-supplied P26.A r3 parent/field/table projections p22/79 require UD for Z06E/Z09E and forbid it for the valid non-E subtypes in these codes. This unit adopts ten numeric cells (227/228/231/232/316 in each code) plus two separate UD parent occurrences. It does not accept field229, IV, Z14, full message grammar, national identity checks or an ISO country-directory implementation. Existing party shape/length/agency checks are retained, not represented as broader certification. The original full PDF was not recovered/rehashed/visually inspected.

The frozen source registry determines the pure subtype outcomes. Wire validation instead uses one exact source-valid SG14 reason223 before RFF/NAD in each object's own first register. Aliases, duplicates, malformed extra elements, another object/agency/register/message and stale snapshots cannot supply authority. All physical UD occurrences are checked before narrowing. Empty/duplicate/forbidden parents cannot disappear behind missing child values. E requires one valid parent and the five actual field components. Existing outbound one-message, register, product and other send gates remain unchanged.

Generic and profiled builders share this UD validation. The generic builder chooses parent applicability per actual object reason, not the root subtype, and carries explicit postcode/city/name/address components. A missing-family adapter defect discovered by the profile postcode test was fixed by passing PRODAT into the internal field-matrix call. Prior Z14N behavior and unrelated parents remain unchanged.

Inactive UD does not require field229. Active/unknown UD leaves address availability undetermined until independently scoped evidence is implemented. Neither root address flags nor field presence certify that condition. Inbound parse-only preflight and the outbound-only send guards are tested separately; these tests are not blanket certification of all inbound business-consumer paths.

## Executed local evidence

Fresh exact accepted-base reproduction: 332 failed,78 passed of410 new actual-module cases. Same final tests on candidate:410/410 (176 source/scope,13 builders,221 real-row/preflight/send cases). Both environments, three UNA alphabets, both override switches, and independent earlier wire prerequisites are covered. Positive production partial rows still stop at their separate readiness guard.

Full application:3201/3201 in243files. Retained standalone source:913/913. All three TypeScript projects passed. Lint:0errors,99warnings. Integrity:33originalfiles,121rules,231contracts; this is source integrity, not application conformance. Mechanical,45qualitycases,RBAC,size/performance budgets passed. Unchanged coverage ratchet passed:lines35.92%,branches27.42%,functions41.47%,statements34.56%. Build/browser/disposable database replay not locally run; all normal applicable CI is required.

## Failure history and fixture corrections

Initial unit176case attempt had139 failures, including6 malformed concatenated-UNA test fixtures. Fixing those test prerequisites preceded final exact-base red proof. Initial full suite had15 failures caused by old positive E fixtures missing UD. Those fixtures now supply independently valid UD while every existing assertion remains. Two retained source profile cases had F/B reasons despite asserting a populated UD; only those fixture contexts changed to E, and an actual-E assertion was added. No expected values in frozen source files were changed.

Initial builder tests included masked date prerequisites; complete date fixtures retained the missing-UD failures. Two missing-postcode cases exposed the runtime adapter family omission and pass after the explicit-family fix. A fixture TS2345 was corrected without suppression. Several tool-interrupted app-typecheck/lint attempts are incomplete, not successes. The 2560MiB app-typecheck actually exhausted the heap (134); separate unchanged-project retry with3200MiB passed. No TypeScript configuration or CI memory setting was relaxed. Full successful command output/log hashes and final source blob IDs are in the companion JSON; failed evidence is retained locally.

## Publication, acceptance and next work

Publish only the exact native tree with real main parent, never synthetic archive history. Run all four ordinary PR workflows, obtain substantive independent source/assertion review at that same SHA, fix findings and repeat on each head change. Inspect fresh refs/checks/review threads before expected-head merge. A generic skipped/success bot status is not review. Local green results are not CI evidence.

Before acceptance:8 numeric cells accepted,10 candidate,92 other;2 parent occurrences candidate,8 other. After this unit is accepted those may become18numeric and2parent accepted, never full-masterplan completion. Field229 evidence remains separate. Issue332 retains prior full-main70/73 failures; PR310 remains paused at e9611351. No SQL, generated types, grants, dependencies, workflow thresholds, live DB/storage/market sends or explicit deployment/settings changes. An authorized main merge may invoke existing automatic Git deployment.
