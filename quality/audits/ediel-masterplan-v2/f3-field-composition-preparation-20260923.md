# F3C-02 preparation: 314 / 209 / 258 / QTY31-213

Read-only preparation while E035 proceeds. Observed HEAD at final source read: 0e81b11960ccd980901b8c43356ac8e2edcdf387. No code/test/memory changes or test execution. No acceptance, merge, new counts or changed E035/masterplan status. D110/110, parent10/10 and paused PR310 are untouched. Scope is this finite field composition, not complete F3 or grammar review.

Read retained criterion-reconciliation R1/correctedR2, register-source-map and field-inventory-20260920. Historical execution receipts remain historical; none was rerun here. Direct original source: hash-qualified sources/prodat.txt, original P26.A r3 pp47–48,54,114–116 and field-table context. The independently checked PDF hash in this session remains 83c2f1d2915851d2e670731f6ab404ef06c9b9def282afbafdfa0eda836a6e95.

## Exact normative map and implementation

| Field/composition | Original source requirement | Actual owner and call path | Existing independent positive/negative evidence inspected |
|---|---|---|---|
|314|P47 LIN1082, n..6; starts1 and increments per actual LIN in one message, independent of object/register count.|`prodatRegisterFields.prodatPositiveSequence` and local314 shape; `prodatRegisterGroups` resets ordinal at UNH, validates every LIN against ordinal; `fieldMatrix.validateFieldMatrixPayload` emits protected topology finding.|`ediel-prodat-registers.test.ts` literal six-LIN/four-object example asserts global1..6 and local indices; explicit duplicate/gap/start2/blank/zero/nonnumeric/n7 negatives retain all raw lines; leading-zero case preserves wire.|
|209|P47 C2127140 max25, agency9/89, own explicit identity; P116 same object across its registers; P114 different meters require different object IDs.|`prodatRegisterFieldState` checks actual LIN C212 components and agencies. `prodatRegisterGroups` tuple key includes messageIndex, decoded ID and agency; missing identities never become inheritance authority. Canonical/parse object scopes use those groups.|`registers` missing second-register identity negative; escaped/interleaved IDs retained; same ID with agency9/89 stays separate; `register-consumers` punctuation mismatch rejected by TGT; `register-builders` agency-injection negatives and supported/default agency positives.|
|258|P47–48 C8295495=1 and1082 n..6; starts1 per object. P115–116 only Z04/Z06/Z10, all multi-register occurrences, absent for singleton; no omission of equal-valued registers.|`prodatRegisterFieldState` checks exact C829 shape; grouping validates presence and1..m without sorting/deduplication. `resolveProdatRegisterRequirement` plus `validateProdatRegisterPolicy` separately check independent outbound expected count, namespace and object membership. Inbound parse does not invent external inventory.|`registers` literal original topology plus scalar/wrong-indicator/extra-component/missing/duplicate/skipped/reversed/singleton/unsupported-function negatives. `register-inventory` explicit own-object expected count positive; missing/ambiguous/incorrect-count/object/agency facts rejected. `register-consumers` TGT missing expected register rejects.|
|213|P54 QTY qualifier31/C1866060, integer n..15 without decimals; optional unit KWH/MTQ. Z04 required; P116 Z06/Z10 later register required when supplied for that object's first register. Local values may differ.|`prodatRegisterFieldState` selects own QTY31, unique, integer1..15 digits, optional permitted unit, shape limits; `prodatRegisterFieldValue` returns null for malformed. `resolveProdatRegisterRequirement` gives Z04 required and later-register conditional; parser reads own physical segments, not inherited segments.|`register-quantity` literal0/1/15digits positives,16digits/decimal/comma/exponent/blank negatives across UNA alphabets. `register-conditions` same-object first-presence conditional and duplicate local values. `registers` no other-register/object borrowing. `register-consumers` second quantity mismatch cannot be satisfied by another object's matching value.|

213 is conclusively QTY31, not QTY136. The old documentation error is already corrected; no runtime repair follows.

## Owner-to-consumer composition actually inspected

`canonicalPolicyFieldValidator` calls fieldMatrix then the specialized `validateProdatRegisterPolicy`, forwarding direction so independent inventory is outbound-only. It projects actual canonical register evidence with selected-rule completeness, not merely parser topology. Dependent-only selection is explicitly not a complete canonical acceptance receipt.

`prodatRegisterGroups` separates register-local fields from first-register common authority. For valid chains it combines own local fields with first-register nonlocal facts. Invalid chains receive no borrowing. P115 preservation versus use is respected: raw later repetitions remain available while parser semantic values ignore them. Local213 is explicitly excluded from common inheritance.

`parser.ts` reads213 from the line's own segments. `edifactSegments`/canonical AST expose group topology, while `compatAdapter` and `inboundCases.parseInboundProdatBusinessData` retain per-object registers. The inspected consumer test uses actual modules but mocks DB at the boundary: it verifies all registers and first-register identity authority, rejects malformed topology before customer lookup/write, and asserts the first selected site's annualEnergyKwh=10. That single site projection is not evidence of an all-register aggregate business meaning; no aggregate defect is asserted.

Both generic and profile builders use the shared register rendering path (`render/registers.ts` plus `prodatRegisterInput`). Existing builder tests independently assert global versus local numbering, per-row quantities, omission of singleton C829 and missing213 not filled from root/first register. They also use parser round trips; round trips alone are weaker than literal wire/field oracles and must not be the sole acceptance basis.

Actual row validator/sendGuards, payloadPreflight and transport/sendLock are exercised in `register-preflight` and `register-structure-guards`: malformed topology survives normal/missing/stale canonical context, invalid-test override cannot waive it; valid chains have no register-specific blocker while unrelated production locks remain. This is bounded guard evidence, not proof of a live SMTP send. Inbound syntax-only parsing correctly avoids invented independent-inventory uncertainty.

TGT source-selection tests concern candidate hints and exact object/register comparison. They do not establish real independent market inventory or source approval. Do not reuse their recency ranking as E035 temporal authority.

## Existing oracle quality and exact limits

`__tests__/fixtures/prodat-register.ts` explicitly anchors pp15–16/47/114–116; it constructs expected component positions independently of the renderer, with three literal separator alphabets and proper release encoding. Assertions in registers/conditions/quantity use explicit expected values or deliberate malformed arrays. This supports genuine independent expectations for the mapped behaviors, rather than comparing the implementation to its own generated descriptor only.

The fixture's `validate` selects a narrow set of canonical field rules, and its default is only314/209/258. A passing topology fixture is therefore not a fully accepted PRODAT message; many intentionally omit unrelated required fields. `register-quantity` executes the lexical owner, not final inbound APERAK creation. The consumer suite mocks all persistence, and send-boundary suites preserve other locks rather than proving authorized transport. These are material boundaries, not reasons to repeat already accepted unit work.

The retained inventory's74 identities/962 base cells establish descriptor equality only. It says nothing new about this composition's final ACK/persistence, and cannot close F3C-02 by arithmetic. Historical test counts in criterion reconciliation are not acceptance.

## Remaining finite qualification work

No new runtime defect has been demonstrated by this read-only pass. The core314/258 distinction, namespace grouping, own213 integer/conditional rules, first-register authority, missing-register count check and protected guard propagation have direct code plus existing positive/rejection evidence. Preserve those as covered bounded subrequirements; do not reopen D cells or rewrite expectations.

Concrete remaining evidence rows before a scoped F3C-02 judgment:

1. **Full selected-policy outcome/response for these fields.** Attach actual existing full-canonical original fixtures and exact typed field diagnostic -> final ACK mapping for malformed314, own209, C829258 and own-QTY213. Existing inspected tests often assert `blocking` or TGT fieldCode, not final APERAK references/ownership. Read retained typed-error tests first; if no exact case exists, add only a focused source-backed positive/negative original fixture later. This is an evidence gap, not a confirmed incorrect ACK.
2. **209 and QTY edge matrix completeness.** The main scanned register suites strongly cover topology/identity, but the complete original-qualified transcript for25/26-character IDs, missing/bad agency and forbidden C212 components, QTY qualifier136 not satisfying213, absent optional unit, MTQ valid gas and wrong-market explicit unit was not located in this finite read. Owners clearly check max25/agencies/shape/qualifier and allow KWH/MTQ lexically; contextual unit behavior must be traced through actual market policy before alleging a defect. Do not interpret lexical KWH/MTQ allowance alone as a wrong-market acceptance witness.
3. **Per-message completeness distinction.** `prodatRegisterMessageSegments` deliberately selects first actual message for a field-policy invocation. Grouping can preserve all messages. An all-physical-message canonical consumer certificate must be mapped separately before claiming interchanges complete; E035 already keeps unsupported multi-message owner approval held. This discovery does not demonstrate a defect in single-message field composition or permit dropping later messages.
4. **Caller-bound external expected inventory.** Existing independent count facts enforce correctness when supplied and keep absence unknown on protected outgoing routes. An arbitrary caller supplied count is not a physical market truth certificate. Map actual fact producer/source binding for the specific business route being accepted, using existing `register-fact-source`, inventory and preflight evidence, without conflating source workbook hints and immutable market evidence.
5. **Final criterion review receipt.** Produce a short per-field source/owner/caller/positive/rejection/receipt table and independently adjudicate this exact finite slice. Use relevant historical accepted receipts or exact-head targeted execution only when needed. No broad suite was run or is justified solely by this preparation.

Recommended next bounded action after the active E035 work: reconcile those five evidence rows, starting with typed final error ownership and the unlocated209/QTY edge fixtures. A reproduction through the real canonical caller must precede any runtime fix. If the controls prove current behavior correct, record the bounded reconciliation instead of creating unnecessary code changes. Keep F3C-04/05 final ACK/persistence, F3C-06 grammar and E035 as separate open criteria.
