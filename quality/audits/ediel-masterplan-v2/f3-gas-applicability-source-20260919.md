# Five GAS applicability cells: original-source qualification and bounded architecture

Status: source/architecture approved at `4fc1eaf2`; bounded runtime implemented under explicit root dispatch, pending independent runtime review and root acceptance, 2026-09-19. Scope: PC-320-Z04/Z06 and PC-240-Z04/Z06/Z10. Source phase changed no runtime or existing assertions. Runtime scope and the two individually authorized assertion updates are recorded below; schema, codec, loader, generated-type, gate, budget, frozen-source and memory remain untouched by this author. No sends, live DB/storage access, settings or deployment. PR310 remains paused. Counts remain 98/110 numeric D and 10/10 parents; pure protocol evidence is not a live GAS certificate or full-cell acceptance.

Branch `codex/ediel-gas-applicability-20260919`, accepted code base `4e41b85f0318c5871ffd62b493cbfc038522c81e` (PR348); root-owned memory updates are separate. Root reported actual main 73/73 Node22.23.2 and all OPS success during this audit. This audit does not independently recertify that receipt.

## Routing, method and immutable sources

Read task brief, AGENTS, prescribed memory README/current-state/current-task/checkpoint/handover/blockers/work-plan, relevant domain/architecture/flows/product memory and decisions/known failures; inspected installed skill inventory and actual implementation. Applied PDF visual inspection, spreadsheet read-only source extraction, spec-to-code traceability, direct false-positive verification and verification-before-completion. `using-superpowers` explicitly exempts dispatched subagents. The spec-to-code skill's normal subdelegation is superseded by the explicit one-author/no-subagents brief; root supplies independent review. Quality-playbook/acquire-codebase-knowledge were inspected but their complete-repository deliverables and broad scans are outside this five-cell source task. Writing-plans informs the bounded proposed sequence below; runtime TDD/execution/review activates only on root authorization. No security exploit claim, platform/database/performance/UI refactor, hooks, worktree creation, publication or branch-finishing workflow is claimed.

1. Original PDF `/workspace/scratch/2a201d6d5897/prodat-original-source/PRODAT-26A-r3.pdf`; SHA256 `83c2f1d2915851d2e670731f6ab404ef06c9b9def282afbafdfa0eda836a6e95`, freshly verified. Read full notes and surrounding context pp15–22,36–41,65,76–78,87–88,108–110,114–116,119,122–123. Rendered/visually inspected pp21,77,119,123; scratch images preserve the appendix shading and exact table alignment. The p40 status definitions and diagram p39 were read alongside the tables.
2. Exact original workbook `/workspace/scratch/2a201d6d5897/reporting-permission-probes/TGT_PRODAT_Bilaga_1-Testdata_per_testkund_version_el_4-0-5.xlsx`; SHA256 `475131fa17fe0b4a611bae4ecf3f42cd78c9565b963a7c7cbd918213721332c7`, freshly verified. Read OOXML cells and sheet identities without modification. The full cell scan found no current field-code240/320 or calorific/serial field label in the data sheets; historical version notes mention serial IDs. Specifically Versionshantering D49,D56,D73,D77,D123,D124,D125 are history, not current GAS facts. D123 says serial-ID changes were removed from Z06F/Z10; D125 records removal of the series-ID field. Neither is current data or producer availability evidence. Supplier, grid-owner and ESCO sheet names do not authorize additional runtime roles.
3. Frozen masterplan §§1.1–1.2,2.2,3,20–21 and original-derived registers orient the scope; they are not substitutes for full original notes. §1.2 explicitly makes EL active and permits GAS registry rows for historical import only. §20 blocks only the capability lacking evidence. No PR310 dependency is established.

## Normative source and exact wire meaning

P15 explicitly makes §2.2 override conflicting appendix4 conditions and applies its matrix to register1. The p21 field notes, not merely the five D markers, determine these cells:

| Cell | EL | GAS first register, source-valid function/subtype | Missing independent context |
|---|---|---|---|
| 320 Z04 | Forbidden outgoing | Required | Unknown market: U; no field-presence inference |
| 320 Z06 | Forbidden outgoing | F/E64 and G/E32 required; E/E34 explicitly not sent | GAS with unknown/invalid own subtype: U |
| 240 Z04 | Forbidden outgoing | Required | Unknown market: U |
| 240 Z06 | Forbidden outgoing | F/E64 or G/E32: required if serial ID changed because of this change; otherwise optional. E/E34 explicitly optional | F/G with unknown change assessment: U; invalid subtype: U |
| 240 Z10 | Forbidden outgoing | M/E58: required if serial ID changed because of this meter replacement; otherwise optional | Unknown own process/change assessment: U |

P65 maps E58 exclusively to Z10M; p110 explicitly defines meter replacement as Z10M/E58. E64 belongs to Z06F/Z09F and is not a valid Z10 reason. The pure event envelope and corrected scratch Z10 case below use M/E58.

The same p21 row also has240 optional in Z05, outside the five D-cell count. Its explicit GAS-only qualification still matters if a shared field owner is applied there; do not silently change unrelated Z05 runtime under this five-cell dispatch. For functions outside the selected cells, preserve the existing matrix and accepted owners. In particular320 is not a Z10 field.

False `required` status must preserve `optional` versus `forbidden`: unchanged240 is O, not X; GAS Z06E320 and EL320/240 are forbidden outgoing. No serial-change fact is inferred from emitted240, changed meter number, current product, nonempty source column, `byCell`, old snapshot status, or root market metadata. For GAS Z04 neither field requires a serial-change event just to establish cardinality.320 F/G needs market and own subtype, not a new business event producer.

| Field | Source-exact path, value and limits | Qualification |
|---|---|---|
| 320 | SG8/LIN → SG16/RFF, C506/1153=`Z08`, C506/1154 calorific-area identifier, `an..35` | p77 example `ZTSSOT001` is an example, not a universal regex or enumerated list. No source here requires a directory lookup for320. |
| 240 | Same group, C506/1153=`Z06`, C506/1154 MSCONS series identifier, `an..35` | p77: TIM uses the actual reporting series ID, never a substituted installation ID; SCH uses installation ID. P37–38 distinguishes monthly/annual arrangements and timed reporting. P108 prohibits å/ä/ö/Å/Ä/Ö in series IDs. Do not generalize that prohibition to320 without source. |

For both RFFs, 1153 is mandatory `an..3`,1154 required `an..35`; 1156 (line number) and4000 (reference version) are X. These RFFs have **no1131 or3055 elements**: the310 CAV qualifier inference must not be copied here. Decode C506 with the existing UNA/release codec before measuring values; escaped delimiters are literal identifier data. Source layout p39 permits SG8 up to999, SG16 up to10 groups, exactly one RFF per SG16. Treat a single own value as the selected field; reject duplicate/conflicting outgoing occurrences as ambiguity (engineering uniqueness policy, not an invented extra source count). Never take a1156/4000 value as1154, borrow a header/party RFF, or borrow another LIN.

P15 and appendix2 pp114–116: these fields are first-register common data. Register2+ may omit them; receiver uses register1 and ignores repetitions on later registers. Source grouping is message plus exact installation identity and agency; invalid chains confer no inheritance. Unrelated objects each need their own first-register decision and LI attribution. Existing `prodatRegisterGroups`, `prodatRegisterRuleScopes` and reference readers already implement useful scope and decoding foundations; reuse them unchanged.

### Direction and appendix precedence

Visual p123 evidence is decisive:240 is grey;320 is not. P119 says grey fields need not be checked whether present or absent. It also says X/false-D extra information must not yield negative APERAK even if its content is inspected and found wrong. P123 says320 is GAS-only and lists nonblank for Z04/Z06G; **p21/p77 still require320 in Z06F**, by explicit p15 precedence. P77's shortened240 wording omits the Z06E exception; full p21 supplies it. These are reconciled source differences, not authority to rewrite originals.

**S-GAS-N1 — bounded interpretation adopted after independent review:** p109 (appendix1) describes Z06E/E34 as customer-information-only and says other fields are omitted except mandatory fields under §2.2. That general instruction is in tension with p21's specific field240 note making it optional in Z06E. For this cell, root adopts the specific p21 field note as controlling the general p109 process description: GAS Z06E240 remains optional. This is an explicit bounded interpretation, not an express national precedence rule; p15 names appendix4 only and does not expressly settle appendix1. It does not alter current EL exclusion, incoming grey240 treatment, or rules for unrelated Z06E fields.

| Condition / occurrence | Outgoing field correctness | Incoming protocol outcome for these fields |
|---|---|---|
| EL320/240, including blank/overlong/misplaced content | Reject supplied fields; do not render | Ignore content for negative APERAK; no GAS masterdata effect. An independent real syntax defect remains separate. |
| GAS Z06E320 | Do not render; reject supplied field | False-D: ignore, including incorrect content/unused components |
| GAS320, known required Z04/F/G, own first register | Exactly one nonblank proper1154, at most35 characters; no populated unused components | Missing/blank320 is a source-backed application error, including F. In a pure historical protocol validator report it as320; it does not authorize the active EL service to send a GAS ACK. |
| GAS240 required Z04 or known true F/G/Z10 | Require value/format/reporting identity; unused components rejected | Grey-field policy: do not require a240 content/presence check and do not introduce negative APERAK solely for240 in this bounded work |
| GAS240 optional E or known unchanged F/G/Z10 | Omit or validate supplied value; unchanged is not forbidden | Same grey-field policy |
| Market or own320 applicability unavailable/conflicting | U prevents an outgoing correctness claim; persisted send must not guess | U absence is not a missing-field rejection. U supplied320 does not establish market/subtype and is not automatically invalid: keep local diagnostic/review separate from national ACK. |
| Known applicable320,1156/4000 supplied | Reject populated X components outgoing | P119 X tolerance: ignore these residual components where own1154 remains unambiguous; do not transplant310 qualifier rules |
| Register2+ duplicate common data | Omission permitted; renderer should keep common source in register1 | Ignore repeated common data and use own register1; never use a later register to fill absent first320 |

For applicable320, validating1154 maximum length follows its own segment definition. No closed320 code list exists here. Unknown source/placement that prevents identifying the field is not proof of a bad320 code; diagnose scope/syntax independently. Grey240 “need not check” is a source permission, not a universal prohibition of optional checks. The bounded engineering policy deliberately adds no incoming240 content/presence rejection; it does not represent that choice as a source-mandated ban on every optional check. Existing real EDIFACT syntax handling is not disabled by this field policy.

## Market identity, current source selection and product scope

P16 field311: EL application reference is23-DDQ-PRODAT or23-DGI-PRODAT; GAS is27-DDQ-PRODAT. P41 field312: UNH association isE2SE6A for EL andE2SE6B for GAS. These must be coherent with the selected source profile; a GAS field or row label does not change the actual message's market. Application reference is UNB/0026 (element7); UNB/0031 is the separate CONTRL request and remains under the existing ACK/envelope owner. No0031 or codec change is proposed.

Actual current paths:

* `prodatRegisterReadingMarket` reads exact own UNB element7 using the existing codec, recognizes23-DDQ/23-DGI and27-DDQ, and rejects ambiguous/malformed leading UNB context. Wire wins over its explicit body-fragment fallback. This is reusable **market evidence**, not role or send permission; it does not itself compare UNH or row/route conflicts.
* `canonicalMessage.ts` and `rulebook/messageParser.ts` decode the actual application reference. In contrast, `prodat/parser.ts:parseProdatMessage(string)` returns `applicationReference:null`, and its row overload returns row.application_reference even with contradictory wire. Fresh probe confirms it. Its retained320/240 fields are descriptive parser output, not market authority. Proposed policy must use wire, not that convenience property.
* `prodatDependentConditionEngine` currently uses root `facts.market==='gas'` alone for all five cells; no subtype/event precision. `canonicalEdielPolicy` spreads caller facts into that predicate rather than binding these five cells to its source application reference. Wire EL plus facts.gas causes two false requirements; wire EL without facts causes two U blockers in generic inbound. `core/messagePolicy` hardcodes electricity instead; these paths disagree.
* Active `guideRegistry`, `prodatApplicationReference`, `canonicalRulePackRegistry` and `fieldMatrixImport` are EL: E2SE6A, electricity rule packs and23-DDQ/23-DGI by process. Active canonical resolution rejectsE2SE6B/27-DDQ. `canonicalRules:evaluateApplicationReferenceGuard` explicitly returnsUNSUPPORTED_GAS. This product boundary is correct and must stay. Do not add GAS to the active guide catalog to get pure tests green.
* `engine.ts`/`profileRenderer.ts` seed electricity facts and use canonical route application reference. Copied caller facts can still override that seed, so shared wire/policy selection must prevail. Production context has no dedicated calorific/series projection; renderer has no320/240 RFF emitter. Broad generic reference/characteristic functions are not qualified GAS producers.
* `core/routeRegistry.ts` resolves a company-owned route, application reference from explicit parameter/runtime/actor, receiver and registered subaddresses. It rejects wrong tenant and production TGT routes, but is not a market validator. `sendContextConsistency` and existing route checks remain intact. A route being registered or a row sayingGAS cannot convert the active EL guide intoGAS.
* `prodat/parser.ts` already parses both RFF values in a synthetic27-DDQ/E2SE6B message and inherits only register1. This is historical/descriptive parsing support. The five-cell owner may supply explicitly scoped historical protocol diagnostics without acceptingGAS into the active canonical sender.
* Actual actor registry parser `parseActorRegistryXml.ts` has no market field in `ParsedActorRegistryActor`/route type; the syntheticGAS probe loses `<Market>GAS</Market>` and, when MessageFamily is present, does not populate separate applicationReference. `raw` contains fragment length/extractor metadata, not the full market-bearing fragment. Thus §1.2 is a preservation requirement, **not proof current registry import fully preserves or segregatesGAS**. Importer/routing F4 work is separate and immutable here. `actorAutoReadiness` and certificate refresh filter subaddressGAS, which is not equivalent to a complete market model. No live import/resolver readiness is claimed.

Proposed market resolution contract: actual unique UNB with exact recognized application reference supplies wire market; cross-check actual own UNH association and nonempty row/selected route reference without normalizing contradictions into agreement. Known wire EL remainsEL despite a forgedGAS fact; report metadata conflict separately on persisted sends. No UNB in an explicitly pure renderer fragment may use its already resolved canonical application reference; absent/invalid wire UNB must not silently fall back to row.market/byCell or the presence of240/320. Unknown or incoherent context producesU/diagnostic; outgoing persisted uncertainty blocks affected send, incoming uncertainty does not invent a negative320/240 APERAK. Reuse the market helper or a thin source-specific wrapper, not a second tokenizer.

## Actual producers and consumers

Fresh execution of `EDIEL_TGT_TEST_CASES` found17 affected steps, all `market:el`, role supplier:

| Registered subset | Actual direction/producer | Implication |
|---|---|---|
|1.2.5 step4|Gridex outboundZ04D (after outboundZ03), two-register microproduction test|Existing bounded EL test producer; preserve it. It is not a new operational grid-owner orGAS role.|
|2.1.1,2.1.2|Gridex outboundZ06F/E64|Existing EL producer;320/240 exclusion is implementable now.|
|2.1.3|Gridex outboundZ06G/E32|Same; noGAS serial-change event is supplied.|
|1.2.1,1.2.2,1.4.2,1.4.2B,1.4.3,1.5.1 twice|Portal inboundZ04|IncomingEL extras must not produce negativeACK.|
|2.2.1,2.2.2|Portal inboundZ06|Same.|
|2.3.1,2.3.2,2.4.1,2.4.2|Portal inboundZ10|No registered outboundZ10; existing PR347 hold remains.|

The initial scratch registry assumption omitted1.2.5 and misread undefined messageVariant asE; that observation assertion failed and was corrected from actual registry titles/step descriptions. That registry correction did not change normative expectations. A separate invalid Z10F normative fixture was subsequently corrected to source-valid Z10M after root review; its original receipt is retained below. Source constants, not memory shorthand, establish F/F/G. NoGAS cases or source-supportedGAS actor/route/event producer were found. Existing server draft actions and TGT/autopilot use registered Gridex steps, company/run context and active EL source builders. Availability of a grid-owner workbook sheet or a generic message-code union proves none of the missing authority.

| Surface | Concrete observation / integration need |
|---|---|
|Pure D engine|GAS Z06E320/240 and GAS F/G/Z10 unknown serial change all collapse to required. Correct per-cell owner and explicit requirement kind.|
|Canonical field validator|EL supplied320/240 currently produces no selected-field errors; unknown-market generic inbound produces U errors, and forgedGAS fact withEL wire invents requirements. Route five cells through new owner; remove duplicate legacy checks for only these cells.|
|Historical parser and register grouping|Already retain own values and first-register inheritance; keep raw/descriptive values but do not consume false extras asEL business facts. No gas-field mutation consumer found outside parser projection.|
|Actual `decisionEngine:decideProdatAperak` + public wrapper|Does not call general canonical field validation; currentEL extra probes andGAS missing320 probes return positive. Therefore generic U errors are **not** proven negativeAPERAK. EL positive is correct;GAS positive does not certify valid historical320 or liveGAS handling.|
|Manual/TGT `deriveProdatAperakValidationIssues` → `resolveAndStoreProdatAperakErrors`|Existing selected death/meter/register gates precede positive shortcuts. No320/240 selected policy or error map; the final resolver returned0issues/0errors/noDB for syntheticEL andGAS Z06G with unrelated local fields supplied. Do not fabricate ERC mappings.|
|`tgtAutoMatcher:compareInboundPayloadToTgtTestData`|Synthetic selectedEL source columns320/240 produce mismatches for absence. The actual current workbook does not select these fields. Apply applicability before comparisons; source column presence cannot activateGAS.|
|Raw/row preflight, normal/catch validator and guards|No selectedGAS owner. Existing generic profile rejection and independent register/IV/address blockers protect tested bad rows, so no claim the whole persisted path allowed them. Both protected prefixes/scope and early returns/intentional-invalid labels need targeted coverage.|
|Shared SMTP `transport/index.part-2.ts`|Death and selected meter/date/reporting boundaries run before transport work; noGAS-specific first boundary. SyntheticGASZ04 with missing receiver stops at receiver_email, with mocked DB/provider uncalled. This proves missing explicit earlyGAS boundary, not successfulGAS transmission.|

`inbound/productionInboundDecisionEngine.ts` and orchestration call the real decision engine; UI/backend manual TGT calls the rule-registry resolver. New pure diagnostics must be intentionally threaded to whichever final path is authorized. ActiveGAS canonical rejection remains a capability limit, not a national content error. A broad change to make realGAS ACKs possible would require separate source/route/role authority. For this unit preserveEL positive extra handling, expose historical diagnostics, and keep unsupportedGAS execution gated. If a selected historical320 failure reaches manual/TGT response selection, review-required is appropriate until a qualified source-specific mapping exists. Never use a generic field105/40 fallback as a fabricated substitute for unknownGAS authority. Existing legacy wrapper limitations are not evidence of aGAS ACK producer.

## Confirmed findings and false-positive limits

All findings below are Medium protocol correctness within the bounded source scope, not demonstrated production security exploits:

* **GAS-1: predicate contradiction.** Engine's market-only predicate contradicts p21 for320Z06E,240Z06E and unknown change for240F/G/Z10. Six fixed normative tests cover these andEL wire behavior. Impact: wrong required/optional/excluded decisions. Fix the five-cell owner without changing frozen catalog counts.
* **GAS-2: EL exclusion and market binding absent at selected validator.** Actual wireEL receives false required/U results depending on caller metadata; suppliedEL fields are accepted. Root condition is not wire evidence. Impact: false diagnostics and missing outgoing exclusion. Shared market/source decision fixes it; noGAS producer needed.
* **GAS-3: selected-source comparison ignores applicability.** SyntheticEL test source with240/320 is compared and flagged. Impact is conditional on stale/wrongly selected source; actual workbook provides no such current fields. Apply direction-aware applicability in comparator; do not rewrite source or invent a test expectation.
* **GAS-4: no specific persisted unsupportedGAS boundary.** Static path and mocked SMTP probe show no first boundary; generic profile rejection and unrelated protected blockers exist. Impact is defense-in-depth/capability uncertainty, not a proved live bypass. Add bounded unsupportedGAS/mismatch/U guard before side effects and bypasses; preserve allEL routes.

False-positive register: allfive needing a new producer is false (EL exclusion/inbound fixes and320 market/subtype semantics are implementable); allZ04 portal-inbound is false (1.2.5 exists); gas root fact overriding wire is not acceptable authority; grey240 missing causing mandatory negativeACK is false; generic U errors causing actual negativeACK is not established (realACK probes are positive); importer fully satisfying GAS-history scope is not established; successfulGAS SMTP is not tested or claimed. Prior general inbound506/p119 issue and known list/format guard limitations remain separate unchanged accepted-base limitations.

## Proposed bounded architecture, pending review and root dispatch

One shared pure source owner, e.g. `prodat/prodatGasApplicability.ts`, with a thin `rulebook/prodatGasApplicabilityPolicy.ts` adapter if needed. No backend/Zod dependency, parameter properties or imports that break VM consumers. Reuse exact tokenizer/service-character codec, first-register grouping, existing subtype registry and RFF decoding; no frozen matrix/codec/loader edits. Return per-object field requirement (`required|optional|forbidden|undetermined`), selected source/market and diagnostics separately from execution authority.

1. Establish wire/fragment market with explicit source evidence and own subtype. For activeEL allfive requirements become forbidden outgoing/not-required incoming regardless ofGAS facts. Enforce references even if blank/misplaced; false inbound ignored. Preserve unique message scope and released-data behavior. 320GAS Z04/F/G cardinality can be tested independently from any persisted producer.
2. For **pure historical**240F/G/Z10 requirement assessment, use the exact condition-only envelope below. It carries a causal event assessment, not merely a before/after difference or the emitted series value. The envelope deliberately does not claim to certify the reporting-series identity or emit240. Field-level value checks remain the independently described p77/p108 checks; full TIM/SCH domain identity is unqualified unless independently supplied and reviewed in a later capability. Raw historical content can be parsed withU; grey240 does not need this envelope to avoid negativeACK. No persistence/notes/UI/new role is proposed forGAS.
3. Consume the owner in canonical five-cell diagnostics, field validation, renderer final checks, raw/row preflight normal and catch paths, and source comparisons. Pure historical positive owner tests are appropriate (GAS Z04 required320/240, F/G320, optionalE240, changed/unchanged240). Do **not** widen `resolveCanonicalEdielPolicy`, active guide acceptance or the EL renderer to issueGAS. No newGAS renderer/protocol profile just for a count.
4. A small persisted-outbound boundary is limited to **Z04/Z06/Z10**, the selected five-cell functions: select when an actual PRODAT UNH/BGM names one of those codes **or** the queued row claims PRODAT plus one of those codes. A lying row family/standard must not hide an actual selected wire message; a mismatched row selected code must not hide behind an unrelated wire code. For these selected outbound rows only, actualGAS application reference/association, contradictory nonempty row/route references, or missing/invalid/incoherent wire market blocks before intentional-invalid/test bypass and shared SMTP external dependencies. Complete queued wire must carry its own validUNB; a row-only reference cannot fill missing wire. This is **not a new global PRODAT unknown-market hold**: Z01/Z02/Z03/Z05/Z08/Z09/Z13/Z14/Z15/Z18 keep their existing canonical/product/other protections, unless a contradictory raw selected message independently triggers this boundary. Inbound rows and acknowledgement families are excluded from this send boundary. Supplied320/240 on otherwise knownEL are field-exclusion errors, notGAS activation. Known coherentELZ04/Z06/Z10 does not acquire aGAS source requirement; existing independentZ10/death/date/etc holds still apply. The scope is the smallest boundary protecting the selected cells, not a certificate for unrelatedGAS functions or routes. Use existing protected issue scope/prefix paths; no global reception/ACK lock.
5. Actual finalELAPERAK and manual/TGT verification must prove false extra content, gray240 and U absence do not cause newly invented negatives. Historical320 required/value failures can remain pure diagnostics and manual review at an unsupported response boundary. Adding liveGAS acknowledgement generation/mapping is outside this unit; automaticGAS operational positive/negative certification is expressly withheld.

### Exact proposed pure240 condition contract (engineering design, not wire schema)

```ts
type GasSerialEventEvidence = {
  key: string; revision: string; reference: string;
  eventKey: string; eventRevision: string;
};
type GasSerialChangeSelection = {
  source: { kind: 'caller_selection'; reference: string };
  objects: readonly {
    objectKey: string;
    installation: { id: string; agency: '9' | '89' };
    lineItemReference: string;
    process: { code: 'Z06'; reason: 'E64' | 'E32' }
      | { code: 'Z10'; reason: 'E58' };
    event: { key: string; revision: string; reference: string };
    assessment: { kind: 'unknown' }
      | {
          kind: 'known';
          dimension: 'serial_id_changed_due_to_this_event';
          changed: boolean;
          evidence: GasSerialEventEvidence;
        };
  }[];
};
```

All listed members are mandatory in their branch. Strict copier accepts only plain objects/arrays and exact literal enums/booleans; all identity/evidence strings must be nonempty exact strings, never coerced or trimmed into valid identity. Reject unknown keys so a purported `server`/`tgt` source or unsolicited value cannot silently acquire meaning. A supplied selection requires a nonempty objects array; duplicate exact `(installation.id,agency)` or `objectKey` is invalid. Event evidence must name exactly its enclosing event key/revision. Each wire use binds the installation+agency, exact own first-register process/reason and exact own RFF+LI to that one object; another message/object/agency/event cannot borrow its assessment. Conflicting/ambiguous own scope yieldsU. No root market field is included: actual qualified wire/explicit pure source profile determinesGAS separately. An independently asserted `changed:false` yieldsO for240F/G/Z10, `true` yieldsR, unknown/missing yieldsU; matching wire240 never changes that decision. Z04 andZ06E do not require or consume this event envelope for their cardinality.

This is a **condition-only** API: it intentionally includes no before/after values and no reporting regime, and makes no claim to independently prove TIM/SCH series identity. Domain identity validation is a distinct missing capability, not a value-comparison option silently skipped by this API. The specification's TIM/SCH identity rule is recorded above; historical raw parsing and a condition test are not full end-to-end value qualification. Bounded executable tests cover the causal condition, own object/event binding and structural series syntax only. They do not assert that a supplied identifier is the correct TIM reporting-series identity or the correct SCH installation identity. The TIM/SCH distinction remains a recorded source requirement and an explicit unqualified capability; no regime/value producer or domain-identity acceptance test is proposed in this unit.

Copying must deep-copy every accepted member and preserve no caller aliases. Omitted/undefined or explicitnull at the integration boundary means **clear/no selection**; it must clear stale prior assessment and becomeU where the condition needs evidence. It never falls back to old persisted snapshots, root `byCell`, emitted values or a previously selected object. Unknown branch permits only `kind`; no fabricated evidence or defaultfalse. Malformed receiver-local selection is diagnostic-onlyU and cannot create negative240APERAK (grey); pure/outgoing strict input rejects malformed selection. This design requires review before any facts type/copy adapter change; saved notes, schema and persisted authority remain unqualified and untouched.

Runtime RED/GREEN acceptance, if authorized: retain the six source expectations after the explicitly recorded Z10M fixture correction; add all five cell true/false/U and requirement-kind rows; three service alphabets and escaped fake delimiters; own first-register/another object/agency/later-register cases; exact1154 length/empty/1156/4000; series umlaut restriction (TIM/SCH domain-identity verification remains outside this condition-only unit as stated above); unknown or mismatchedUNB/UNH/row/route; forged rootgas/byCell/snapshots; activeEL source draft exclusion/regressions; normal/catch/preflight/both guards/SMTP mocks; finalELAPERAK and manual/TGT source comparison outcomes. Retain accepted death OptionA, meterchange hold, product/readings/date/parent policies. Any old assertion that truly conflicts needs an individual root ruling; none is requested or edited now. Full masterplan acceptance remains root-owned and cannot increase from these pure tests alone.

## Verification and retained scratch evidence

Scratch directory `/workspace/scratch/2a201d6d5897/gas-applicability-probes/` contains the images, helpers/tests, original workbook observation coordinates and JSON reports. Initial observation mistakes were kept in the diagnostic history. Root review identified an invalid Z10F fixture and an erroneous proposed Z10/E64 envelope reason. Original p65/p110 independently establish Z10M/E58. Under individual root authorization, the scratch Z10 predicate fixture and its normative RED were corrected to M, and the proposed envelope to E58; the other five normative cases were unchanged. `invalid-z10f-history/` retains both original test files, the original combined JSON receipt, observations and SHA256 manifest. That initial Z10F failure is invalid-fixture history, not valid Z10 evidence. The corrected characterization/RED rerun below is the authoritative current receipt.

| Check | Command/result |
|---|---|
|Original hashes|`sha256sum` on both exact originals: required hashes match|
|Current-source observations|`node node_modules/vitest/vitest.mjs run --config /workspace/scratch/2a201d6d5897/gas-applicability-probes/vitest.config.mjs current-source.test.ts source-contract-red.test.ts --reporter=json --outputFile=/workspace/scratch/2a201d6d5897/gas-applicability-probes/combined-source-results.json`:11 observations PASS,6 expected normative RED; exit1 expected|
|Final disposition|`disposition.test.ts` with same config, report `disposition-results.json`:5/5 PASS. The minimal first probe hit an unrelated register gate; adding source-valid local fields and a synthetic valid-length identity isolated the selected behavior. Mocked DB/provider fail closed; final probes invoke neither; no external side effects|
|Frozen source integrity|`npm run ediel:masterplan-v2:integrity`:PASS33 original files /121 rules /231 acceptance contracts; applicationConformanceAsserted=false and productionReadinessAsserted=false|
|Diff|`git diff --check` and staged `git diff --cached --check`:PASS before audit-only commit|

No unrelated full suite was run. Independent SOURCE/SPEC plus ARCHITECTURE review and explicit root runtime authorization are the next gates. Source-candidate publication, memory, CI, merge and acceptance remain root-owned.


## Bounded runtime implementation and verification

Root authorized runtime only after independent source/spec and architecture approval of `4fc1eaf2`. `prodatGasApplicability.ts` owns the five requirement kinds, strict causal selection copying and release-aware own-message market resolution; `prodatGasApplicabilityPolicy.ts` binds actual first-register identity/agency, source-valid reason, own LI and unambiguous message occurrence. The pure owner never infers causal change from emitted240, meter number, root market or byCell. Complete wire needs its own unique UNB0026 and coherent UNH association. Resolved canonical EL profiles may supply only detached renderer-fragment context. The old five market-only predicates and duplicate legacy field checks are replaced, while unrelated owners remain.

The strict `gasSerialChange` selection follows the approved condition-only envelope, including Z10M/E58, event-key/revision binding, exact keys, nonempty object collection, duplicate rejection and deep copies. Omitted, undefined and null selections clear; absence stays absent where needed to preserve unrelated legacy snapshot object shapes. Empty replacement snapshots do not retain old selections. Serialization does not grant authority: persisted evidence readers reject nonempty GAS event selections. Malformed receiver-local evidence yields a warning/U and retains ordinary normal/catch diagnostics; strict pure/outgoing handling rejects it.

Canonical fields, sync/registry validation, normal/catch paths, generic/profile/TGT render final checks, raw/row preflight and both send guards share this policy. Known EL excludes320/240 even if blank, malformed or misplaced; input false-field precedence and grey240 policy run before value checks. Selected outgoing references use their exact1154, source-supported length and series umlaut rules. No TIM/SCH domain-identity comparison or GAS renderer/producer is introduced. Generic historical GAS320 diagnostics retain object/LI attribution; the manual/TGT resolver requires `PRODAT_GAS_ACK_REVIEW_REQUIRED` before unmapped ERC/DB processing. Actual EL final APERAK remains positive for false extras. The existing low-level decision engine's synthetic GAS ACK behavior is not a qualified GAS response or changed into live GAS support.

`prodatGasAuthority.ts` holds only selected outbound Z04/Z06/Z10, selected from actual PRODAT wire or a claimed PRODAT row. Row/code/family/format deception, ambiguous message scope, GAS, unknown/incoherent complete-wire market and contradictory nonempty application references cannot confer permission. Selection uses the existing release-aware tokenizer, not substring matching; a dangling release is completed only for classification and always causes a selected syntax-invalid hold, never valid market authority. Inbound, actual ACKs and unrelated PRODAT functions keep existing behavior. Known EL requires no GAS evidence. SMTP combines the GAS hold with existing pure protected diagnostics before route/context/provider/storage work. Root explicitly approved the necessary route ordering: inspect actual wire/row first; inspect the loaded route reference immediately after the existing company-scoped route read and before any provider/storage/send effect. No extra route lookup was added. The route-conflict test expects one mocked route read; the GAS-wire test expects zero external calls. Existing death/Z10/date/reporting diagnostics remain visible.

Actual registered supplier producers 1.2.5 Z04D,2.1.1/2.1.2 Z06F and2.1.3 Z06G were exercised with explicit synthetic source columns and existing draft entry points: no GAS fields, source requirement or GAS boundary appears. Missing source columns in the initial new builder probe caused an unrelated existing death-scope error/default reason; the probe was corrected by supplying the independently specified source columns, not by changing runtime rules. These draft checks do not claim full producer readiness or external portal certification.

Verification history distinguishes behavioral failures from setup failures. The qualified six source assertions failed before implementation and pass afterward; the original invalid Z10F history remains separate. Initial new policy/boundary collection failures (test syntax and absent new module) are not behavioral RED evidence. Two additional consumer tests reproduced EL-source comparison and missing historical320 manual-review gaps; later tests reproduced row/code ambiguity and plain preflight-code protection gaps. All were fixed in production. Targeted mutation checks independently demonstrate that removing causal event-revision, own LI, or UNH-association binding fails the unchanged corresponding tests; mutations were restored. The first full run was4305/4311: five production composition/shape regressions were fixed without changing their existing expectations. Only the source-refuted catalog expectation required the exception below.

Two old-assertion exceptions were individually authorized by root and recorded in root memory `69bbf2f6`: (1) `ediel-canonical-policy-batch-regression.test.ts` adds only `Z10:240` to unknownIds because GAS subtype V/byCell does not establish source-valid M/E58 or causal evidence; all110 registry coverage and other expectations remain. (2) `test-ediel-prodat-reference-fields.cjs` keeps all818 cases but makes only id240's wrong-content comparison expect no mismatch under the adopted grey incoming policy; its label/comment disclose this, while other field mismatches, parser identity tests and source immutability remain unchanged. The first eight-script run was817/818 solely on that assertion. No loader/gate change or broader assertion exception was made.

Final receipts are in `/workspace/scratch/2a201d6d5897/gas-applicability-probes/`: full suite `runtime-full-final.json`; all13 exact gate commands/results `runtime-final-gates/gate-results.json`; UTC/Europe-Stockholm/Pacific-Apia source/time/governance checks `runtime-timezones/results.json` (103/103 each); mutation evidence `mutation-results.json`. Final full suite4318/4318 in288 files and all13 gates pass, including818/818 Ediel scripts and unchanged service-role baseline2402 (actual2401). The full receipt precedes only a type assertion in a new builder test and final documentation; that file, test typecheck and targeted ESLint were rechecked without runtime behavior changes. Local Node is24.19.0; root owns exact Node22 CI, publication, independent runtime review, merge and main acceptance. No live DB/storage/provider calls, human messages, settings or explicit deployment occurred. Counts remain98/110 numeric and10/10 parents; PR310 remains paused. GAS producer/role, historical actor-import completion, TIM/SCH domain identity and full-cell acceptance remain unqualified.


## Runtime review correction R1

Independent runtime review of `17b548fe` requested changes for two confirmed Medium defects: R-GAS-1 descriptive LI parsing discarded blank duplicates and normalized nonexact qualifiers before causal binding; R-GAS-2 coherent EL Z04 could reach direct SMTP provider readiness with forbidden320/240 because market authority alone did not invoke field exclusion. Root authorized this bounded correction; original source interpretation and capability limits remain unchanged.

R-GAS-1 now counts every LI-like occurrence in the actual own first-register token scope before qualification, including blanks and malformed qualifiers. One exact RFF/LI in own SG16, before NAD, must have a nonblank an..35 1154 and no supplied unused components/data elements. Only its exact decoded value can bind the independent causal selection. Duplicate, normalized, misplaced or malformed references yield no binding/U; incoming grey240 gains no blocking content rule. The shared tokenizer and descriptive reference reader are unchanged. Valid release-escaped identity controls pass in all three service alphabets.

R-GAS-2 adds a thin persisted-row adapter to the existing shared five-cell field policy. It selects actual outbound EL PRODAT Z04/Z06/Z10 and returns only the shared320/240 exclusion issues. SMTP composes these with existing source holds and protected pure diagnostics before route/context/provider/storage work, even when market authority is coherent and test allowInvalid is set. The market-only authority API remains separate. Incoming, ACK/unrelated functions and coherent EL without selected supplied fields retain their prior behavior; no new GAS facts or producer requirement is imposed. The previously accepted loaded-route consistency check remains after its existing company-scoped read. Tests cover each field separately, including blank/misplaced forms, zero external calls and retained existing date-format diagnostics.

Applied receiving-code-review, systematic-debugging, TDD and verification-before-completion to the confirmed defects; the existing approved plan/source contract governed this correction. Platform/UI/database/schema/security-audit workflows are not triggered by these bounded pure/consumer changes; no subagents or memory edits by this author. Seven tests were appended to two existing task test files; no existing assertion changed. One new composition probe initially used Z04 with a Z09-only XOR expectation; inspection corrected that new fixture to source-valid Z06 with invalid date format, without a production change or assertion exception. Its initial failed receipt remains distinguished from the six correctly failing regression tests.

Immutable reviewer `consumer-adversarial.test.ts` SHA256 `4e182c964cd2e499255b0c0190b15319832ef2492fe61c434ec97f3aa802d8e8` and `results-original.json` SHA256 `397399857459cfa0757a6a8ed45b71efab858c7bdf1d17982c7535e8d3d253aa` remain unchanged. Explicit-file rerun reproduced0/3 then passed3/3 (`gas-applicability-runtime-review-probes/results-author-red.json`, `results-author-green.json`); unchanged controls pass4/4 (`controls-author-r1.json`). Six additional tests across three service alphabets went31/37 to37/37, then the corrected composition check passed; affected actual consumers pass146/146 in14 files (`gas-applicability-probes/runtime-r1-affected.json`). All13 relevant checks pass in `runtime-r1-gates/gate-results.json`, including all three typechecks, targeted ESLint on all five changed code/test files, budget/performance/integrity, tenant checks, ratchet,818 scripts, route readiness and diff check. These are local Node24 checks; the prior4318 full-suite receipt belongs to the pre-correction candidate, not this tree. Root authorized focused verification without another broad local rerun and owns exact new Node22 CI/scoped independent rereview. Counts98/110+10 and PR310 paused remain unchanged.
