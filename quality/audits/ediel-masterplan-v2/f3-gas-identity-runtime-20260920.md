# RD-4 GAS240 outgoing reporting identity — local runtime checkpoint

Task 3; 2026-09-20. Base `485a19e6`; runtime `153b7d5aafc42231526029cfd3cb55e6c51e8c95`.
User requested saving everything for another chat while final required gates were running. No independent completed-runtime review, exact candidate CI, PR merge, or Batch B actual-main acceptance has occurred. PR310 remains paused. Historical counts 98/110 numeric D and 10/10 parents are unchanged.

## Authority and bounded implementation

Read `.superpowers/sdd/remaining-d-plan/task-3-brief.md` first and original PDF P21/65/77/108–110/119/123 directly. Source `cc59c38d` and RD-4 architecture were already independently approved in `.agent-memory/remaining-d-source-review.md`; this worker did not restart qualification. Root explicitly authorized runtime after PR355 actual-main 73/73 and all OPS success, recorded in `afd3e121`.

P77 requires independently known actual MSCONS-TIM reporting series identity, or the exact own installation identity for MSCONS-SCH. The existing causal changed boolean does not prove that value. The fix adds `prodatGasReportingIdentity.ts`: strict plain-record selection/copy, explicit unknown/TIM/SCH, exact installation+agency, process+reason, own LI, source reference+revision and known-assessment revision binding. Causal Z06F/G/Z10 selection is additionally bound by object key and event key/revision. Malformed, duplicate, stale or cross-object selection cannot supply an expected value. No inference from wire240, meter number, root market, product, resolution, internal series UUID or arbitrary JSON.

The existing `prodatGasApplicabilityPolicy.ts` owns both source condition and wire value validation. Identity is consulted only for supplied outgoing R/O240 after source scope/requiredness/placement checks. Missing R remains REQUIRED, omitted O needs no identity, X excludes before identity lookup and U remains blocking. Specific P21 Z06E optionality is unchanged. A known TIM ID may equal the installation string when independently selected; there is no universal inequality rule. Existing decoded an..35 and prohibited Swedish-character checks remain.

`prodatRegisterEvidence.ts` copies and clears the new fact, but rejects a non-null serialized identity envelope before reading it as persisted authority. There is no qualified saved GAS identity adapter. Existing GAS send hold and canonical guide-resolution hold are unchanged. No schema, market column, role, source promotion, source rule, producer, guide activation, shared codec/grouping, loader, workflow or threshold changes.

## Actual consumer evidence and limits

* Real shared GAS owner: TIM success versus unrelated series/installation substitution, SCH exact installation, TIM independently equal installation, R/O/U/X, omitted/supplied optional and E34, source/event revision and own object/agency/process/LI, strict malformed/duplicate schema, clear/copy, three service alphabets, decoded length/character restrictions, first register, second object and repeated UNH ambiguity.
* Real `validateCanonicalPolicyFields`: supplied TIM/SCH success and mismatch across Z04/Z06F/Z10 and three service alphabets. No mocked validation owner.
* Real `buildProfiledProdatSegments`: required240 omission still blocks, optional omission stays optional, and ordinary EL rendering treats the selector as no GAS authority. The renderer has no240 input or projection. These tests use a synthetic fragment policy solely to exercise the existing owner; they do **not** prove a positive GAS renderer or persisted producer. Root explicitly confirmed this boundary.
* Serialized new envelope is rejected by `readProdatRegisterEvidence`. Row validation, registry validation, preflight, both send guards and actual SMTP remain blocked for each selected GAS message; external DB/route/provider/event/update sentinels stay untouched.
* Incoming gray240 remains unchecked even for invalid identity or malformed content. Batch A typed incoming behavior is covered by the full suite.

The only old fixture amendment is the root-authorized `decoded RFF value length and released fake tag remain source data` controls in `ediel-prodat-gas-policy.test.ts`, all three alphabets: independent qualified TIM SERIES added to valid/long/fake payloads. Existing [] and only320-invalid assertions are unchanged. No other old assertion changed.

## Durable TDD and disclosed corrections

TDD commit `5c2e494b`: corrected authoritative `red-02.json` has 48 tests, 8 pass / 40 expected assertion failures. These fail on real missing identity comparison/copy/persisted behavior, including nine canonical consumer cases. Initial `red-01.json` has 33 tests, 2 pass / 31 fail; one failure dereferenced the absent copy field rather than asserting its presence. `red-consumers-01.json` records a collection failure (incorrect import paths; zero executed tests). Both initial receipts are retained as non-authoritative setup history. The corrected RED run had no collection error.

Initial GREEN 48/48, focused111/111. Test typecheck initially found fixture union/code types; corrected fixture typing without semantic changes, fresh `targeted-types-02.log` passes. Self-review found implicit String coercion in the new strict process parser: `strict-red-01.json` has 38 pass / 1 expected failure before adding a string type guard. Final focused117/117 includes54 new durable tests. Runtime checkpoint `153b7d5a` then froze all implementation/test source before the full gate runner launched.

Every historical receipt remains at its unique path. Recoverable gate runner and corrected RED source snapshots are archived as non-executable `.txt` files. No past evidence was overwritten.

## Verification

`gas-identity-runtime-20260920/final-gates-01.json` is the exact command/exit/log/hash ledger for the required15 entries reused from Batch A. Each entry names runtime153b7d5a. The runner captured all tracked lib/app/test source hashes before launch and verifies they remain unchanged at completion; prior audit files have a separate preservation manifest. Gate output paths are new and unique. NODE_OPTIONS is `--max-old-space-size=4096 --require=./scripts/lib/refactor-safe-static-read.cjs`.

Final completion status is recorded in the appended verification result and companion evidence JSON. Local runtime is Node24.19.0, so this is not the pending exact Node22 CI certificate. Service-role ratchet remains2400 against unchanged baseline2402.

## Skill routing, review and ownership

Applied executing-plans to the approved finite task, spec-to-code tracing to the original pages/shared owners (explicit no-subdelegation overrides fan-out), TDD and systematic debugging to reproduced failures, code-review to the changed paths and verification-before-completion to claims/commits. Quality-playbook routing was inspected; generating a new repository-wide quality system is outside this already-qualified bounded implementation. Acquire-codebase-knowledge trigger absent: no onboarding/discovery request. Broad security scanners/fp-check omitted: no new security vulnerability claim, auth/SQL/provider or dependency change. Differential review was bounded to the changed pure validation and persisted rejection paths. No Next/React UI/platform or performance optimization work; no corresponding skill activation. No subagents spawned.

Tenant boundary: new code is pure and has no database, customer/tenant lookup, storage or external effect. Persisted new caller facts are rejected, and selected GAS sends remain held before provider effects. Existing tenant-integrity/shutdown/route and ratchet gates remain required. No claim of live producer, operational GAS route, ACK transport certification or full-cell acceptance.

Resume with independent completed-runtime task/spec, quality, tenant and whole-branch review of the saved candidate; address findings with bounded TDD, then exact Node22 CI, guarded merge, actual-main73/73+OPS, followed by separate per-cell AT-P-01/02/03 protocol/persisted/live adjudication. Root owns publication, CI, memory and acceptance. Do not reopen PR310 or advance counts automatically.

## Final local verification result

All15 required gates PASS at runtime `153b7d5aafc42231526029cfd3cb55e6c51e8c95`; full4968/4968 in313 files, quality45/45 in2 files, Ediel818/818, three typechecks, full lint, integrity, large-file budget, performance, route readiness, tenant integrity/shutdown, service-role ratchet and implementation diff check. No implementation/test changes during the run: 1699 source-file hashes unchanged; 638 prior audit-file hashes unchanged. This is local verification only; paused before independent completed-runtime review or exactCI/merge/main acceptance.
