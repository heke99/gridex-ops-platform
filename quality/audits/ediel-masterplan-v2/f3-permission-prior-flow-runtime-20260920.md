# Prior permission-flow runtime — implementation checkpoint

Authorized source proposal: `0fd41d86`, independently approved in `.agent-memory/permission-prior-flow-source-review.md`; runtime task brief `.superpowers/sdd/permission-prior-flow-plan/task-2-brief.md`. Base `58674ab620f3eecf4b1bd692513694ffa7c52aa0`. No final-runtime acceptance claimed at this checkpoint.

Skill routing: using-superpowers dispatched-agent exemption; executing-plans, TDD/writing-good-tests, systematic-debugging, scoped code/spec/tenant review, Supabase security checklist and Postgres tenant-filter guidance, verification-before-completion. Existing isolated task branch used. No subdelegation. UI/browser, broad repository scanners, migration/schema, deployment, state-producer and performance-optimization workflows do not apply to this bounded correlation fix. Root owns memory/publication/independent review/CI.

Fresh runtime RED was committed as `65ec8293`: eight real-loader/engine failures before production changes. Original receipt immutable. Root subsequently clarified the consumer boundary: standalone permission validator remains field-only; source-bound prior evidence is required by the actual manual resolver before event/return. The initial unchecked-Boolean oracle pointed at the standalone engine and was corrected to the authority guard; nine actual three-entrypoint/null/unchecked/unmatched tests now verify the consumer boundary. No old field assertion changed. Only the specifically authorized energy unmatched40/105 assertion changes to internal/no event/no progression with retained field assessment.

Checkpoint: 209/209 focused tests pass, including 81 new prior-flow tests; test typecheck passes. A fresh mutation oracle first failed (9 actual entrypoint controls passed), then bound evidence serialization as well as source fingerprint closed mutable-tag promotion. All receipts remain separately available in `permission-prior-flow-runtime-20260920/`.

Fixture corrections are disclosed, not runtime source reinterpretations: copied source-probe party swapping accidentally rewrote trailing object digits, so new runtime fixtures restrict swaps to delimited party IDs; canonical test indicator belongs at UNB element11 (earlier copied `++1` was element9); new historical scope explicitly supplies received timestamps and temporal records. A malformed appended second UNA probe was corrected to append only a complete second UNH. Earlier receipts remain immutable. Existing field-consumer adapters supply explicit synthetic owner/envelope/receipt/provenance records, preserving their physical field assertions. Standalone field tests drop the obsolete Boolean argument under root's explicit adapter authorization.

Implementation consists of a pure physical-occurrence assessor, bounded metadata/candidate loader, and manual pre-event guard. Actual schema is unchanged. Scoped tenant metadata tables: tenant_ediel_profiles, tenant_actor_identifiers, tenant_actor_roles, tenant_counterparty_relations; narrowly selected platform_actor_identifiers verifies counterpart and represented transport identity. Both wire DTM137 business time (fixed UTC+1) and received/sent timestamps constrain historical identity validity; created_at is only an insertion search cutoff. Present-day resolver/snapshots are not historical substitutes. Candidate query predicates include company/environment/direction/family/code/electricity application references/exact verified transport pair before raw retrieval; deterministic created_at/id pages, 50 rows × four pages per process, explicit incomplete result at cap. Exact own wire and columns are independently checked after retrieval.

Dispatch adapter follows actual `transport/index.part-2.ts` sent status+messageSentAt and outbox accepted/sent/delivered/acknowledged status path. A valid sent timestamp no later than the source receipt is required; draft/prepared/queued never qualify. Z15C uses inbound receipt evidence and same legal direction. Correlation supplies no grant or current-state authority.

Processing limitations remain explicit: source-valid unlinked/spontaneous Z15 and unavailable historical mappings stay internal; represented remote counterparties without existing historical mapping are held. Other direct/canonical/system ACK history integration, lifecycle state authority, generic209/261 and full F3/masterplan remain outside this unit. No national40 is inferred from local history. Full gates and final self-review pending.
