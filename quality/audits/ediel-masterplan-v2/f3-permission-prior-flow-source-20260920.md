# F3 prior permission-flow authority and tenant scope — 2026-09-20

**Source qualification only; P-ACK-R1 remains HIGH/open until implementation and independent review.** The finite correction is an object-scoped, typed prior-correlation result consumed by the existing manual ACK path, with database ownership filtering before reading candidates and exact selected-wire verification afterwards. It is not a permission state machine, grant producer, or market activation.

Source baseline: `7ea935cf` on `codex/ediel-permission-prior-flow-20260920`, containing PR353 merge `4e8b8588f9a6ff805ba433e542615ba6dd66fb98`. Root subsequently accepted actual-main73/73, all three OPS jobs,4703 application tests and45 quality tests; those are root receipts, not this author's execution. Root memory commits and paused partial evidence are preserved. Historical98/110 numeric +10/10 parents do not change. PR310 remains paused/excluded; PR352 and accepted322/324 work are not restarted. Runtime, production/existing-test/schema/frozen-source changes and live calls remain unauthorized in this task.

## Method, scope and recoverability

Read task brief first, AGENTS, active memory/checkpoint, current state/task/handover/blockers/plan, relevant architecture/domain/flows/tenancy and decisions/known failures. Read prior P-ACK-R1 source audit, its `contextRows`, and independent `.agent-memory/permission-ack-source-review.md`. Read actual selected consumers, schema/migrations/generated types, actor ownership and existing tests. Source beats stale memory.

Skill routing: using-superpowers dispatched-agent exemption; code-review, spec-to-code-compliance, systematic-debugging, fp-check and verification-before-completion applied to the bounded path. Mandatory Supabase skill/security checklist and local supabase-postgres-best-practices/RLS guidance applied to the privileged query; Gridex tenant guidance read for company/environment/role separation. PDF skill used read-only. Quality-playbook evidence/intent principles apply; no repository-wide regenerated requirements or codebase documents. Full security scanner/supply-chain/SARIF, performance, UI/browser, schema authoring, implementation/TDD and deployment workflows are not triggered by this finite source qualification; no dependency/auth/RLS/schema change is proposed. No live DB/CLI checks or current-platform API implementation occurred. Explicit source-only/no-subdelegation scope overrides skill fan-out; root owns independent review and memory/publication.

Original full140-page PRODAT26A-r3 PDF: `/workspace/scratch/2a201d6d5897/prodat-original-source/PRODAT-26A-r3.pdf`, SHA256 `83c2f1d2915851d2e670731f6ab404ef06c9b9def282afbafdfa0eda836a6e95`, locked source P, provenance `https://www.ediel.se/Portal/Document/3338`. Fresh layout extraction read relevant process/reference/response sections (pp13–17,20–22,73,75,78–79,85–87,90–94,103–105,114–116,119,122–123,137–140); rendered P33–35 diagrams inspected. Frozen masterplan §§5,7.3,7.6,8,12,18–19 and AT-P-01/02, AT-ACK-01/02 are contracts, not evidence that planned producers already exist. Relevant-page text is preserved as a reproduction aid, never a substitute authority for the PDF.

Fresh evidence: `permission-prior-flow-source-20260920/` and companion `.evidence.json`. Immutable executable-source archives end in `.txt`; no discovered tests were added. Reproduce by copying `probe-qualified-final.test.ts.txt` to a fresh scratch `source-qualified-final.test.ts`, copying `vitest-qualified-final.config.mjs.txt` to scratch, replacing scratch/repository paths in both, then running:

```
node node_modules/vitest/vitest.mjs run --config <scratch>/qualified-final.config.mjs --reporter=json --outputFile=<scratch>/result-qualified-final.json
```

The actual TypeScript function declarations for prior lookup, manual decision and `createAckForSourceMessage` are extracted/transpiled without body edits; extracted bodies and source hashes are in the receipt. Actual parser, permission field guard/engine, canonical decision and ACK builders run. The query mock applies actual eq/in/not/lte/order/limit/select operations, including projection; it does not merely return every supplied row. Auth, TGT selection, event insert, kernel persistence, DB/RLS and transport are mocked or statically inspected. No HTTP, production data, credentials, provider, market send or grant write was exercised. `persistCount` means the real draft reached the mocked canonical-kernel boundary, not an actual stored/sent ACK.

| Execution | Actual result and qualification |
| --- | --- |
| Paused v1 (root preserved) |42 tests:39 PASS/3 FAIL;44 observations. Failures were harness assumptions: missing cached sender fails draft after success event; plain query-error object becomes `Error: [object Object]`, not the expected message. Original bytes retained. |
| Paused v2 (root preserved) |45/45 observation tests PASS;47 observations. PASS means capture/control assertions, not conformance. Isolated substring/case/cache cases changed to Z14N so shared customer001 cannot explain the match; FTX segment count corrected. |
| Fresh final and qualified-final replay |Each69 tests:54 PASS/15 deliberate RED;55 actual observations.53 observation/control tests plus16 literal source/tenant assertions (1 PASS/15 RED). No remaining harness failure. |
| Complete controls |Real canonical Z13/Z14/Z14N/Z15/Z18 controls all syntax/application `accepted`; manual→draft positive. This establishes protocol-shaped fixtures, not authoritative grants. |
| Frozen source integrity |33 original files,121 rules,231 contracts; application conformance and production readiness explicitly false. |

Final supplemental Z14VH control includes historical end date; v1/v2 three-alphabet release cases used the same punctuation and only the default alphabet required releasing it. Final cases use each alphabet's actual separators/release/terminator and all three produce false unmatched40 despite exact decoded LI. v1 FTX count was malformed and is not normative evidence; final count is repaired. Candidate later-UNH fixture uses distinct M/M2 and D/D2 identifiers. Dual candidates have distinct BGM/UNH identities. Final packaging review found inherited generic UNB S/R identities while row/NAD identities were numeric and test environment lacked its wire flag. `qualified-final` aligns UNB parties and test indicator with the complete controls and replays the same69 tests/55 observations, with unchanged54 PASS/15 RED. Original final receipts remain immutable and are supplementary; **qualified-final** is the qualification receipt. Deliberate wrong-party/environment mutations remain adversarial inputs. No receipt was overwritten.

## Source contract and limits of authority

| Question | Original source / frozen contract | Qualified conclusion |
| --- | --- | --- |
| Functions/parties |P13–14; P137–140 |Z13/Z18: ESCO→DSO; Z14/Z15: DSO→ESCO. Function is own BGM1001; subtype is own223, not cached code/status. Local inbound/outbound is relative to the owning company. |
| Z14 positive and N |P22/78, P137–138 |Own226/LI is the case reference returned from Z13. Positive Z14 may enumerate several approved objects; Z13 and Z14N omit209, Z14N also omits227/325. Never require absent positive-only fields to correlate N or manufacture a Z13 object identity. |
| Z15 response to Z18 |P139–140 |The example's LI is the Z18 LI;325 is the permission identity,327 the end timestamp,209/260/227 identify the relevant object/customer. Reverse legal parties; compare the same owning physical objects. A reference conflict cannot be cured by another field's substring. |
| Z15C |P139; masterplan CASE-Z15C/ST-E10 |Cancellation repeats the cancelled Z15 LI and planned end timestamp. Source-supported prior function is Z15, with the same DSO→ESCO legal direction. It does not generally require an own Z18. Correlating cancellation is not authorization to restore an internal grant. |
| Other Z15 V/VH |P13/34/73/75; masterplan CASE-Z15V/VH/ST-E07/08 |Z15 can announce a permission end or completed history, not just answer a request. No universal prior-Z18 requirement. An old Z13 or positive Z14 raw message alone does not establish a currently active, correctly scoped market permission. V and VH rights remain separate. |
| Exact identity |P22/78/79; masterplan §§5,8,18 |Use decoded own qualifier/value and identity namespaces. LI, Z09, ANJ, BGM, UNH, internal UUID and NAD+UD are different identities; case/punctuation folding and substring inclusion have no source authority. An unambiguous matching policy is a conservative implementation inference, not a new wire rule. |
| Objects/messages |P87/103/105, P114–116 |Assess every physical permission LIN; permission functions have no multi-register inheritance. A second LIN or later UNH cannot donate missing source facts. Scope a candidate occurrence explicitly; an independently addressed request line with the correct LI may correlate, but its siblings cannot supply its missing customer/permission facts. |
|40 versus unavailable |P91–93/103 |ERC40 is a national application error. A903105 means the object cannot be uniquely identified with its grid-area identity; A905 is `Anläggningen kan inte identifieras`. Empty, truncated, inaccessible or ambiguous local request history does not itself prove that condition. Known semantic failures remain eligible for independently qualified national40; no global40 removal or remapping is proposed. |
| Field truth |P21/73/75/93/104; accepted PR353 |322/324 wire diagnostics remain independent of history. A Boolean correlation result cannot introduce/remove/normalize a field error, change applicability, or supply own references. |

P103's general “matching not possible”40 example must be read with P92's specific A903 meanings. It does not justify assigning105 to every failed local lookup. This task qualifies no new national40 producer and does not claim that genuine absence of a current permission can never cause a national application error. Such a semantic condition needs its own authoritative assessment and correct A903, not inference from a query miss.

## Actual source/callsite and tenant ownership matrix

| Boundary | Actual implementation and authority | Constraint / gap |
| --- | --- | --- |
| Manual entry |`actions.part-3.ts:581–697,701–953,956–1089` require write/send RBAC and `requireScopedEdielMessageForAction`; backend calls at604/743/979. |Current source message access is checked. It does not authorize raw reads from other companies. |
| Source ownership |`actions.part-1.ts:1014–1036` checks `assertUserCanOperateCompany`; companyless messages allowed only for platform admin. |That admin exception must not become permission-flow owner selection. Missing source company must stop correlation before candidates. |
| Privileged candidate reader |`actions.part-3.ts:212–293`, `lib/supabase/service.ts` service-role client. |Only PRODAT, cached code set, status exclusions, created_at and optional environment; no company/direction/party/market constraints. RLS is not a substitute for explicit scope on this client. |
| Actual consumer |`actions.part-3.ts:449–509` assesses fields, resolves TGT, loads history, validates context, emits success/warning event, returns. |Only production prior-context caller found. Guard must precede event and returned positive; no registry-only correction can cover it. |
| Boolean projection |`testing/prodatPermissionEngine.ts:429–458` |False adds40/105 for Z14 or error-free Z15; null/true allows handled-positive if fields pass. Only first line receives the40; Z13/Z18 do not consume context. Z15 with ready field errors skips40 while Z14 prepends it. |
| Event ownership |`db.ts:780–806` reloads parent and inserts its company_id. |Wrong candidate affects an event attributed to the correctly authorized source company. Event payload includes candidate id/reason; probe captures this boundary, no actual persistent disclosure claim. |
| Draft / persistence |`orchestrator.createAckForSourceMessage`→real ACK builder→`core/kernel.ts:148+`. |Kernel requires company, source rule pack, route and normal duplicate/outbound checks. It does not requalify this history match. Missing cached source party already fails draft, but after success event. Kernel/provider were mocked. |
| Separate helper |`prodatPermissionEngine.ts:174–373 resolveProdatPermissionAperakValidationIssues` |Company+outbound+environment+optional reversed parties, row ownership defense; only test callers found. Its normalization/message-wide reference pool/optional match semantics are not a safe replacement. Do not wire it into runtime to reuse a misleading “safer” name. |
| Other selected field consumers |direct decision, canonical inbound, registry, system selector |They use322/324 but do not call this history resolver. Do not inject an unqualified asynchronous history gate into all these paths or reopen accepted fields. Full safe permission linkage remains unclaimed. |
| Existing lifecycle |`flows/prodatPermissionLifecycle.ts`, caller `inboundBusinessStateMachineLegacy.ts:489+` |Separate Z15 update uses company-scoped metering_permissions and its own matching/manual-review rules. It does not consume this Boolean; no proven Boolean→grant mutation. Leave unchanged; do not treat it as qualified authority for new ACK positives. |

### Actual available schema and ownership facts

`supabase/database.types.ts:38037+` and migrations establish `ediel_messages.company_id` (nullable FK to companies), non-null environment/direction/family/status, sender/receiver Ediel IDs, application_reference, raw payload, cached references, created/sent/received dates, resolved-company/counterparty fields, execution_context_snapshot and routing/rule snapshots. **There is no ediel_messages.market or generic tenant_id column.** `customer_id`/`metering_point_id` are internal UUID links (base schema), not the raw wire customer/object identifiers. Querying them as wire values confuses namespaces.

Database policy migration `20260523_db3_tenant_isolation_rbac_enforcement.sql:245–283` includes ediel_messages and company read/write policies. Base migration supplies company FK and company/status index; actor-testing migration supplies company/direction/family/code/created index. This is checked-in migration truth, not a live RLS/grant attestation. No new index/migration is required or authorized by this source proposal; future query performance must be assessed against its final shape if needed.

`tenant/tenantEdielIdentity.ts` resolves company+environment+electricity profile, legal actor identifiers/roles and explicit transport-agent relation. Legal actor and transport actor are deliberately different concepts; shared mailbox or shared service user cannot select a company. `core/tenantResolver.ts:73–85,318+` uses UNB transport identity plus legal market receiver, separately. `prodat/parser.ts:120–125` exposes raw NAD+FR/DO separately from cached sender/receiver. `core/executionContext.ts` has typed company/environment/market/actor IDs/roles, but stored JSON presence alone is not validated historical authority. No candidate selection currently reads/checks those fields.

Consequently, use company ownership first; validate own legal parties against the owning legal actor and counterparty; verify UNB transport identities under explicit representation evidence. Do not require NAD=UNB for represented actors, or equate actorUserId with the ESCO market actor. Existing identity resolver evaluates `activeAtNow`; that is not historical validity at the source business time. Historical mapping absent/contradictory must remain an internal scope hold; do not silently resolve a changed present-day identity or invent a new actor lookup architecture.

Existing tables are **not absent**: `metering_permissions` has company/customer/grid-owner IDs, external permission/case references, message links and status; `energy_service_permissions` also has message links/state; `ediel_permission_cases` has company and references. The actual Z15 lifecycle uses the first. None is queried by this ACK resolver; these shapes alone do not prove a unique current permission under environment, actor, product/mode and period. No new table or positive grant producer is proposed.

## Findings, actual outcomes and false-positive challenges

**PF-1 / existing P-ACK-R1: HIGH, confirmed unsafe cross-company/counterparty prior evidence.** Wrong-company, wrong-direction, wrong-sender/receiver, wrong legal-wire-party, gas-market, missing-company and missing-environment synthetic candidates reach the real manual positive decision and success event. Except missing source party (draft later blocks), real builders reach a positive ERC100 draft/kernel boundary. Query traces prove no company/party/direction/application filter and projection omits company/party/environment/application evidence, preventing candidate rechecks. Root cause is the privileged unscoped read plus raw inclusion; source authorization is not candidate authorization. Fix: DB scope first, selected-wire tuple verification second. Severity remains the previously qualified cross-company influence risk, not critical live data exposure or grant mutation. A malicious end-user's ability to inject arbitrary stored candidate rows and a production exploit are unproven.

**PF-2: MEDIUM, confirmed reference/function/occurrence false authority.** Z14N sole-LI controls with prefix/suffix LI, unrelated FTX, case-folded LI or cached-only source reference all approve. A candidate cachedZ13/wireZ18 approves; source cachedZ04/wireZ14 skips the query and approves. Two independent candidates pick the first; unsent draft and cached-only candidate approve. First matching LIN approves the whole manual decision; when only the second N line matches, first-only40 is emitted. A later candidate UNH donates the substring. Current raw matching also rejects exact released LI in all three UNA alphabets. Complete-control and isolated-N changes defeat the shared-customer false-positive explanation. Correct cached family/code/environment/status filters do exclude their mismatches; the defect is missing dimensions, not a claim that no filters exist.

**PF-3: MEDIUM, confirmed unqualified national failure and incomplete Z15 process selection.** Empty/null history, legitimate env/function/status exclusions and an older exact request hidden behind the50-row cap emit40/105 and the English `The object could not be identified`; P93 requires Swedish text for a genuine105. Matching priorZ15 cancellation is excluded because the query uses Z18/Z14/Z13 for every Z15. Conversely any old Z13 sharing a customer can approve Z15, and exact LI ignores contradictory Z18 permission/customer/object/grid identity. Source does not authorize current-permission authority from that observation. Fix the local-history disposition and finite per-process correlation; preserve independently qualified40 elsewhere. No spontaneous Z15 is nationally rejected merely for lacking Z18.

False-positive / limitation register:

- Query errors already stop before event/draft. Final transient and ready42/324+failure cases preserve the field assessment. Do not regress this behavior; original plain-error wrapping loses message text but remains internal and is not this task's general error-handling redesign.
- Source-valid Z14N/A76 and A13 remain valid negative business responses with a positive application ACK when safely correlated. No permission grant follows. Z13/Z18 currently have no selected prior lookup and must remain outside this correction.
- Ready42/322 survives true context; absent history currently adds40 beside it. Ready42/324 survives absent context without40, and remains attached when query unavailable. No history result may become322/324 field authority.
- The manual engine uses first-line context, but the real field owner still assesses all322/324 physical occurrences. This task must not restart field ownership or overwrite ready diagnostics.
- Actual canonical controls validate source-shaped payloads. They do not certify every direct/system workflow: previously recorded generic209/261 faults remain outside scope.
- The current cap can hide a correct candidate. Returning no match is not proof of absence even when a mock enforces every existing filter. Candidate duplicates here are different source messages, not the same returned row twice or legitimate multiple beneficiaries.
- No lifecycle/grant update, database RLS execution, transport route, provider delivery or human communication was performed. Kernel safeguards may block an eventual send; they do not repair the earlier positive event/decision. Their presence limits demonstrated impact, not the query defect.

## Minimal implementation proposal — requires review and root authorization

1. **Keep two independent assessments.** Preserve the accepted pure322/324 owner and its pre-read readiness guard. Introduce a small pure prior-correlation assessor and a scoped loader; the manual resolver composes them. Do not move this into registry, create a generic tenant service or change the permission lifecycle. Use actual selected PRODAT UNH/unique BGM and physical-LIN positions; no cached-code dispatch bypass, first-line Boolean or whole-file reference pool.
2. **Typed authority, not a replacement Boolean.** Return `not_applicable` for the current non-consuming functions; otherwise a per-object result with source message/LIN occurrence, own LI/permission/object references, source function/subtype, scope evidence, candidate message/LIN identity and evidence provenance. Distinguish `correlated_request` (Z14→Z13 or Z15 response→Z18), `correlated_cancellation` (Z15C→Z15), and `internal_review` with finite reasons `scope_unavailable`, `history_unavailable`, `search_incomplete`, `unmatched`, `ambiguous`, `wire_conflict`, `state_authority_unavailable`. These names are design inferences. Neither correlated result is a permission grant/current-state proof. A plain Boolean or caller-authored tag cannot mint evidence: bind the result to source payload hash, company/environment, selected UNH/LIN and verified candidate scope in the loader/assessor composition; consumers reject missing or mismatched evidence. Synthetic true-context adapters must build the same evidence structure, never a bypass flag. Unsupported/unknown subtype does not create a322/324 rejection or a new blanket hold solely for subtype: preserve existing field behavior; qualified context only states the relation it can prove.
3. **DB scope before raw reads.** Require source company, environment and uniquely verified party/market scope. Retain action ownership checks, reject ownerless platform-admin correlation before candidate query. Use actual company_id/environment/direction/message_family columns, process-specific message_code shortlist, and application_reference market prefilter; use the verified transport-party pair (or finite explicit representation pairs) to restrict sender_ediel_id/receiver_ediel_id. Z14/Z15-to-Z18 reverse parties; Z15C uses same direction/parties as originalZ15. No generic `tenant_id` or nonexistent `market` filter. Missing mapping cannot justify omitting filters. Select and independently verify scope columns plus raw payload. Data-quality disagreement between cache/snapshot/wire is internal evidence; cached columns only narrow the search, never establish wire truth.
4. **Fetch completeness without cross-tenant fallback.** Do not fetch50 unrelated raw messages then cap/filter. Restrict by DB ownership/known scope before payload retrieval. Reuse exact trusted canonical reference/message links only when their ownership and provenance are verified; message-level cached transaction_reference cannot cover arbitrary second LINs. Otherwise page the already scoped candidate set deterministically (created_at plus id) to a finite resource limit and mark truncation `search_incomplete`. Never infer a national failure from truncation/unknown completeness. A schema-free scoped page/limit implementation is sufficient for this unit; do not silently add a reference index schema or widen to all companies. Existing created_at is insertion order, not proof of send/delivery or market chronology. Require available trusted request dispatch evidence for `correlated_request`; draft/prepared/queued alone cannot qualify. Missing evidence is internal, not a national error. Exact status/dispatch provenance adapter must be tested against real code during implementation; no whitelist is nationally prescribed here.
5. **Exact own matching, with process distinctions.** Decode with actual UNA; compare exact LI namespace/value, never substring/case/punctuation folding or UUID substitution. Z14V/VH matches own Z13 LI and legal parties; where supplied and applicable, customer identity must agree in qualifier/agency namespace and request mode must not conflict. Z13's absent209/325 is not missing evidence; Z14N relies on LI without requiring positive-only fields. Multiple Z14 objects may share the same request; assess each response object and never infer rights for siblings. Z15 response branch checks own LI plus permission/object/grid/customer namespace and relevant end date against the same Z18 occurrence. Z15C checks priorZ15 LI/permission/object and cancelled end timestamp with same legal parties. Contradictory tuple, duplicate matching physical occurrences or multiple independent candidates becomes internal ambiguity; no arbitrary first result. Historical positiveZ14 can be recorded as an observation for unsolicited Z15V/VH, but does not provide current-state authority. This bounded implementation must use `state_authority_unavailable` there unless an already authoritative, validated scoped relation is genuinely available; do not invent it or require a nonexistent Z18. Expanding that state source is a separate source-qualified unit.
6. **Finite manual consumer integration.** `resolveBackendAperakDecision` consumes the typed result before its existing permission event/return. Any relevant internal-review object stops successful qualification, event and draft progression, using an internal review error/result with the complete already-assessed322/324 evidence attached, as current query failure already does. No fallback to submitted positive labels, TGT or null context may bypass it. Do not convert these reasons to40/105/41/42. Correlated results allow the existing protocol decision; field negatives remain unchanged. If every required object is correlated, retain existing positive-control/event/draft behavior without claiming any business authorization. Incomplete mixed objects cannot yield a success event for the message. Leave other canonical/direct/system consumers' lack of prior authority explicit rather than adding unqualified new gates.
7. **National40 remains separate.** Remove only this Boolean-false→105 inference after root adjudicates the exact assertion below. A genuinely qualified semantic40 assessment, if already supplied elsewhere, retains its correct A903 and P93 text. Do not rename105 as numeric field105 authority or globally rewrite ACK text/rendering. No new40 producer or proof of active permission is part of this unit.

Operational tradeoff: safe, source-valid but unlinked/spontaneous Z15 can remain in internal review until authoritative state evidence is available. That is a local processing limitation, not a national rejection of the wire. This is intentionally bounded and must be disclosed at runtime acceptance; it is not full F3/permission delivery completion.

## Finite acceptance and exact old-assertion conflicts

| Acceptance group | Required implementation evidence |
| --- | --- |
| A1 DB ownership |Two synthetic companies with overlapping exact LI/permission/customer; inspect query arguments and returned rows. Wrong company never read; null company no query, including platform-admin source access. |
| A2 Party/environment/market |Correct reverse/same-direction controls, wrong cached and wire parties, represented legal≠transport positive control with explicit relation, missing/ambiguous relation hold; wrong environment/market excluded at DB and wire checks; no source scope omission. No live state claim. |
| A3 Exact identity |Z14V/VH/N A13/A76 positive controls; own LI exact, prefix/suffix/case/FTX/cache/headerless negatives; all three release alphabets. Preserve source-valid absence209/227/325/261 as applicable. |
| A4 Occurrence/cardinality |First-only/second-only match, two matched independent objects, shared request across response objects, duplicate physical keys, candidate sibling/later UNH, two distinct candidate messages. Retain every physical identity; no message-wide Boolean. |
| A5 Process |Z15→Z18 exact tuple; wrong permission/customer/object/grid/date/mode hold; Z15C→priorZ15 same legal direction and end identity; unsolicitedV/VH without qualified state is internal, not “Z18 required” national failure. Z13/Z18 prior behavior unchanged. |
| A6 History lifecycle |Empty/null/error, unsent draft, dispatched evidence, cached function mismatch on both sides, finite-page overflow and candidates outside insertion window. No miss→105; ambiguous/incomplete/history failure produces no success event/draft. |
| A7 Field preservation |Ready322-only/324-only and mixed errors under match/unmatched/error; same types, Swedish field text, values, own refs and physical occurrences survive. All accepted PR353 guards and partial selected-policy tests stay intact. |
| A8 Real boundaries |Extract/import actual loader/manual engine and real draft builder; assert query before retrieval, stop before event/write/positive, no fallback bypass in all three manual entrypoints. Preserve unrelated national40 builder input and source-backed105 Swedish text when independently qualified. |
| A9 Packaging/gates |No frozen/schema/lifecycle/producer change; exact source/input hashes and unchanged receipts. Root-authorized old assertion only, targeted tests then required full gates/independent runtime review/exact CI; source RED is not implementation completion. |

The sole presently identified **semantic assertion conflict** is `__tests__/ediel-prodat-energy-product-consumers.test.ts:46–49`, named `valid506 preserves independent unmatched-context40/105 and warning event`: its unqualified `{hasMatchingPriorPermissionFlow:false, matchReason:'synthetic unmatched context'}` expects negative40/105 and warning event. Under this finite proposal, that specific false/no-authority fixture must expect internal review with no permission event/draft; it cannot remain evidence of national105. **Root must adjudicate it; this task changes no test.** The old expectation was deliberately preserved by earlier506/322 units; it is not permission to weaken all40 tests.

Potential fixture adapter changes are distinct from assertion waivers: actual consumer tests use a mock Boolean true context; if the typed API replaces it, upgrade those mocks to explicit synthetic correlated object evidence while retaining their original outcome/field/event assertions. Files: `ediel-prodat-permission-ack-consumers.test.ts:21`, `ediel-prodat-energy-product-consumers.test.ts:18–26`, and `ediel-prodat-permission-ack.test.ts:9`. Unknown-subtype field preservation at energy-consumers60–61, Z18 behavior, valid positives and unavailable-history attached diagnostics at permission-consumers51–54 remain required. No permission to turn those green by dropping assertions. The test-only `resolveProdatPermissionAperakValidationIssues` document/party tests are not runtime authority and require no edits in this proposal. No schema or frozen-contract exception.

## Verification, residuals and next gate

Observed69 tests54 PASS/15 expected RED establish the defect and finite design divergence; they do not approve the proposed implementation. A separate manifest records artifact/input/prior-receipt hashes and final scope check. Immutable original log bytes are retained. No production, existing-test, schema or frozen file changed from task base. The source file under audit is1099 lines, helper458; no file split is warranted.

P-ACK-R1 remains open pending this proposal's independent source/spec, architecture and tenant-isolation review, root runtime authorization, actual implementation and reviewed evidence. Separate residuals: qualified current-state/producer availability, generic209/261 workflow faults, other ACK routes' lack of history integration, lifecycle relation correctness, and full masterplan/12 remaining-cell reconciliation. They are not silently accepted or claimed fixed. The scope does not authorize a new role, grant, beneficiary mapping, live eligibility, provider call or production deployment.
