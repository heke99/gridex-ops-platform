# Remaining PRODAT D dependencies — 2026-09-19

Derived analysis only, against accepted PR340 base
`1bbe4483f86d5b9fd93dfcb6e846341fd9762a42`.
Analysis HEAD `c1df0ea80dd84df1cc34188763716d29dea424da` includes root-owned memory.
No runtime, normative, prior assertion, gate, database, active memory or remote changes.
PR310 remains paused at `e961135199f292b8210884f07de3b616a670161a`;
no content imported. No new conformance, release, live or cell acceptance claim.

## Result and next bounded unit

Recommend **PC-258-Z04, PC-258-Z06, PC-258-Z10**, with a small prerequisite:
separate independently supplied physical register inventory from observed wire
topology in outbound condition resolution. The existing `registerObjects` contract
already has `expectedRegisterCount`, exact object ID and agency. The authorized
TGT register-fact producer carries it through source-bound run evidence into
body-bound row evidence and both protected send paths. No new schema, paused310
machinery, GAS support or blocked switch facade activation is needed.

This is ready for a bounded implementation/evidence task, not a statement that
these cells currently pass. First reproduce the exact absent-count/partial-count
behavior through actual modules; this mapping deliberately reports no confirmed
runtime defect. Existing register structural work is retained. The next task must
not use observed LIN count or `multipleMeterRegisters` as proof that the physical
inventory is complete. Where independent inventory is absent, unknown is the
correct outbound business result; inbound parsing still checks wire structure
without demanding local external knowledge.

The requested accounting is exact: **36 accepted + 74 remaining = 110 numeric D**.
Remaining: **44 parent-dependent + 30 other-context**. Parent occurrences are
separate: **4 accepted + 6 IV remaining = 10**. The four321/323 cells remain
unaccepted despite PR339's N-exclusion fix. JSON contains every exact ID, original
predicate/true/false/unknown outcome, field note, locator and segment evidence.
Nothing is accepted by counting, source hashing or this dependency classification.

## Source and skill routing

Read AGENTS, current memory and checkpoint, active work-plan, domain-model,
canonical-flows, integrations, decisions and relevant known-failure entries.
Reviewed local using-superpowers (explicit dispatched-agent exemption),
spec-to-code-compliance, verification-before-completion, acquire-codebase-knowledge
and quality-playbook routing. Applied bounded source/callsite tracing and fresh
verification. No full fan-out spec audit or global quality generation is claimed:
the root brief explicitly bounds this task to derived dependency mapping and
root owns independent review. Runtime debugging/TDD and review skills apply in
the next implementation task; UI/Next, DB modification, security audit, performance,
architecture redesign, deployment and skill authoring triggers are absent.

Source: unchanged P26.A revision3 retained fields/parents/conditions and complete
relevant segment tables in `docs/ediel/masterplan-v2`. Read P field-note pages
16–23; segment tables47,49–50,55,58,66–68,71,74,77,79,82 and116; parent notes and
annexB. Prior `f3-register-source-map.md` retains original p114–116 visual/source
inspection and p15 precedence. The original PDF hash remains
`83c2f1d2915851d2e670731f6ab404ef06c9b9def282afbafdfa0eda836a6e95`.
Original PDF bytes/images were not mounted or freshly fetched in this task;
this is retained-source qualification, not a fresh original-page certification.
JSON retains the entire original source manifest and fresh projection hashes.

## Dependency map

`R` required; `O` optional; `X` forbidden/inapplicable. Unknown means outbound
stop until the required fact is known; it is not automatically an inbound
protocol error. Predicate false is not generally X: each cell retains its actual
frozen result. All facts attach to the exact object and applicable first register,
inside one message, with exact identity agency and source-exact reason. Root
facts, later registers, sibling objects or later messages cannot fill a gap.

| Group | Cells | Source condition and outcomes | Current input/producer and next dependency |
| --- | ---: | --- | --- |
| 258 Z04/Z06/Z10 | 3 | Independent meter count >1: R for each index1..m; singleton: X; absent inventory: unknown. C829/5495=1,1082 n..6; separate global314. | Existing register policy checks supplied counts, but its requirement resolver receives observed wire count. Existing TGT producer makes this the next small unit. |
| 214/218/259 Z04/Z06/Z10 | 9 | Standings-sent predicate; Z06F required if true; Z06E/G first register expressly optional. EL259 only when standings sent. Appendix2 requires later repeats when first-register value supplied in E/G. | Per-object standings declarations and TGT producer already exist. Do not infer from supplied field259. Qualify exact reasons and false outcomes after258. |
| 210 Z06/Z10 | 2 | Change before supply start: R, frozen false X. Future supplier routing also follows full note. | `byCell`; date inputs provide values but absent210 cannot prove no future supply. Need exact process/contract supply-start source and change-effective time. |
| 210/211 Z09 | 2 | Own D reason + production start/end event; respective R/X; both dates forbidden together; unknown D event remains unknown. | `byCell`, date exclusion/exclusivity and renderer exist. Need independent production-contract event; date presence is insufficient. |
| 321 Z13/Z14 | 2 | Bounded R, indefinite X, Z14N X; missing term unknown. V does not establish indefinite. | Prior qualification remains blocked pending per-object permission-term producer and transport. Existing N fix does not qualify full cells. |
| 323 Z13/Z14 | 2 | Private R, frozen nonprivate X, Z14N X; missing kind unknown. | Root `customerKind` is not per-object permission authority. Existing candidate `customer_type` reader does not establish the needed process relation. |
| 254/242 Z10 | 2 | Settlement changed OR new meter below profile threshold; aggregation product changed in EL. True R, false O. | `byCell` and output values; need exact replacement event with old/new values and threshold applicability. OR is unknown unless enough facts determine result. |
| 310 Z05/Z06/Z09 | 3 | Death AND Z05LK/Z06E/Z09E: R; known false X; eligible subtype alone leaves event unknown. CAV code Z41. | Root businessContext consumers exist; no dedicated outgoing death-event producer identified in traced callers. Bankruptcy/E34 alone is not death. |
| 320 Z04/Z06;240 Z04/Z06/Z10 | 5 | GAS-only. 320 required Z04/Z06F/G, not E. 240 GAS Z04 required, changed F/G or Z10 required, E optional. EL excluded. | Legacy market predicates under-specify GAS branches. Keep active EL and existing capabilities; no full positive-GAS acceptance claimed. |
| 229 eight messages | 8 | Active UD AND address available R; inactive/unavailable X; active with unknown availability unknown. | Actual Z01 producer exists, using site.street. Exact end-user address selection/availability and per-object transport need qualification; not a universal missing-producer claim. |
| IV250/251/253/317/318 in six messages | 30 | Required when applicable IV sent; inactive X. Parent depends on independent address difference; Z09 only E. | Root difference flag currently reused. Profile invoicee renderer exists; generic object input lacks invoicee. Build source-selected parent/party contract; supplied IV cannot prove it was business-required. |
| IV252 in six messages | 6 | Active IV AND address applicable R; false X; unknown remains unknown. | Current engine reuses difference flag rather than separate applicability fact. Needs explicit availability/applicability separate from parent requirement. |

Parent-dependent44 = 229x8 + IV6x6. Other-context30 = first nine rows.
Parent IV six occurrences are not extra numeric cells. Unlike the already accepted
optional IT choice in Z01/Z03/Z08, IV applicability is a business-address comparison;
its presence cannot be promoted into proof of that comparison.

Source interpretation cautions remain explicit: frozen 214/218 no-readings falseX
contrasts with the existing register overlay's optional outcome; full notes require
readings-positive and expressly permit E/G but do not by themselves settle every
other false branch. Resolve with source precedence before nine-cell acceptance;
do not silently modify the frozen ledger. Frozen240 Z06/Z10 falseO must be composed
with the full-source EL prohibition. For323, retain the prior audit's frozenX target;
silence in the private-customer note does not authorize a new nonprivate O rule.

## Actual producer and consumer paths inspected

Paths below are static evidence, not a claim of executed application behavior.

* `prodatDependentConditionEngine.ts`: all remaining predicate groups, fact type,
  fallback `byCell`, register requirement overlay. `canonicalEdielPolicy.ts` composes
  facts; `canonicalPolicyFieldValidator.ts` recomputes migrated fields, runs register
  policy, and otherwise retains root statuses. Z06/Z09 field229 has only the special
  one-object availability path. This is not full229 acceptance.
* `prodatRegisterGroups.ts`, `prodatRegisterPolicy.ts`, `prodatRegisterEvidence.ts`:
  message/object/agency grouping, first-register authority, optional expected count,
  register fact allowlist and body binding. Binding is integrity, not authorization.
  Reusing this envelope does not confer customer/permission authority.
* `saveEdielTgtRegisterFactsAction` in `app/admin/ediel/actions.part-2.ts`:
  write access, scoped run, company permission, operational company and allowed
  gridex step precede source-bound notes and tenant/id/revision CAS. No action was run.
  `tgtRegisterFacts.ts` validates company/run/role/case/suite/step/code/source digest,
  exact identities/agencies and expected counts against independent workbook objects;
  operator source note and actor are required. It supports factual assertions, not
  certification. `tgtProdatSource.ts` preserves original columns/raw values.
* The manual draft action and `tgtAutopilot.ts:createDraftForStep` both read those
  facts. `tgtEdifact.part-4.ts:buildEdielTgtDraft` validates with register facts and
  stores `prodatEngine.registerEvidence`. Generic `buildProdatMessage` accepts root
  register facts with object input; profile engine/renderer accepts context/snapshot
  facts and persists diagnostics/evidence. These are real paths beyond switch facades.
* `core/messageBuilder/payloadPreflight.ts`, `rulebook/validator.ts`,
  `rulebook/sendGuards.ts`, `transport/sendLock.ts`: rehydrate register evidence,
  invoke register policy and protect register errors before invalid-test overrides.
  Preserve this ordering and separate inbound parse calls in the next unit.
* `intent/renderGateway.ts:renderAndQueueCustomerMasterdataZ01` calls
  `intent/renderers/customerMasterdataZ01.ts` after intent validation; renderer
  reads `cis/db-shared.ts:getCustomerExportContext`, checks company consistency and
  resolves site process. It constructs both end-user availability and output UD
  street from `site.street`, not a separate end-user address selection. This is a
  **static source-selection gap to qualify**, not a newly reproduced runtime defect.
  `types/customers.ts` has `CustomerAddressRow` (registered/billing/facility/other,
  active/date bounds); `customers/getCustomerById.ts` already reads these addresses.
  That reader returns a collection, not a unique EDIEL end-user/invoicee authority.
  Do not add a schema or equate installation, registered and billing addresses by guess.
* `prodat/compatAdapter.ts:renderProdatSegments` transports portal party/date/purpose
  values into `renderProdat26A`; snapshot values are not independent predicates.
  `buildProdatSwitchOutboundDraft` checks `isProdatCodeSendable`; admin part1 wrappers
  route through it. `prodatMessageSupportRegistry.ts` derives inbound-only status
  for portal-to-actor profiles. `flows/prodatSwitch.ts` deliberately implements only
  supplier Z03; Z04/Z05/Z06/Z10 are inbound there, Z09/Z13 need dedicated processes.
  These blocked wrappers neither prove producer absence elsewhere nor authorize activation.
* `prodat/builders/profileRenderer.ts`, `prodat/types.ts`, `buildProdat.ts`,
  `render/dateSegments.ts`, `builders/z09.ts`: invoicee/context inputs and source
  locators exist; Z09 start/end exclusion is enforced separately from event knowledge.
  Root output fields do not become change, death, reporting-term or address facts.
* `core/messagePolicy.ts`, `stateMachines/prodatLifecycle.ts`, `core/ackPolicy.ts`
  read parsed/report booleans or businessContext. These are consumers/projections,
  not independent outgoing fact producers. `prodatContext.ts` reads customer_type
  for supplier-switch candidates, not permission classification; it does not provide
  per-object reporting/death/change/IV facts. Prior `f3-reporting-context-20260919.md`
  retains the dedicated permission facade/lifecycle trace and reproducible probes;
  this task does not rerun or enlarge its confirmed findings.

Searches covered `lib/ediel` and `app/admin/ediel` for all fact properties, `byCell`,
`dependentConditionFacts`, `registerFacts`, `renderProdat`, generic builder and
compatibility wrappers, then followed the actual CIS/customer readers above.
“No dedicated producer identified” is limited to these relevant call paths, not
an assertion that no such domain record can exist elsewhere or in production.

## Exact next implementation contract

1. Scope only258 in Z04/Z06/Z10. Keep source-exact C829/5495=1/index1..m,
   ordinary314 and identity/agency structure, first-message boundary and inherited
   nonlocal fields unchanged. No other numeric acceptance credit.
2. In outbound business resolution, require one independent per-object expected
   count; distinguish absent/null/partial inventory from explicit singleton. Counts
   must be integer1..999999. Validate exact object matching, missing/extra objects,
   duplicate evidence and observed-vs-expected count mismatch. Raw wire still owns
   syntax and actual topology. Do not turn a hash or `byCell` into authority.
3. Reuse the existing authorized TGT source/run contract and body-bound register
   envelope. Generic/profiled callers must preserve explicit unknown and cannot
   fabricate expected count from the array they just rendered. No new live production
   grid-owner producer is implied; the current supplier/ESCO direction boundaries
   remain. A future production producer must separately establish its domain source.
4. Carry that same resolution through builders, canonical field validation,
   persisted row, preflight and both protected guards. Parse-only inbound must not
   gain rejection for unavailable local physical inventory. Avoid imposing knowledge
   requirements on unrelated functions or bare structural utilities.
5. Actual-module RED/GREEN evidence: independent two-register inventory with omitted
   trailing register; one/two/multi objects; wrong agency, swapped/missing/null count,
   explicit singleton, repeated/malformed/skipped C829, all supported UNA alphabets,
   stale body/source/run, both builders/TGT paths, both guards and inbound isolation.
   Keep prior assertions and mandatory gates. Root reviews/publication follows.

After258, the nine readings cells have an existing dedicated fact path but require
source false-outcome reconciliation; they are not blocked by permission facts.
Party229/IV need source-selection and object evidence; change/death/production
cells need event contracts; four permission cells retain their distinct blocker;
five GAS cells remain outside positive active-market qualification.

## Verification

`python3 quality/audits/ediel-masterplan-v2/f3-remaining-dependencies-20260919.py`:
exact110/36/74,44/30,10/4/6; no duplicate/overlap/omission; all frozen cell
conditions and field/parent records preserved; source hashes match; normative
package unchanged against accepted base. `npm run ediel:masterplan-v2:integrity`:
33 originals,121 rules,231 contracts pass; no conformance claim. `git diff --check`
passes. No unrelated application/type/coverage suite rerun for docs/data-only work.
