# F3 Task A — UNB/0031 source, owner and consumer qualification

Date: 2026-09-20. Candidate branch baseline: `e02387263bb3d179bc2548334f48463dadddb99c`.

## Fresh original-source evidence

Independent public retrieval in PR357 comment 5751649097 resolved the official Ediel index to the exact PDF endpoint and freshly downloaded the bytes.

- Official index: `https://www.ediel.se/Info/edielanvisningar`
- Official document endpoint: `https://www.ediel.se/Portal/Document/3314`
- Response filename: `260220_Ediel-anvisning-generella_tekniska_regler_version_24-A-6.pdf`
- HTTP 200, `application/pdf`, 1,124,807 bytes, 60 pages, PDF 1.7.
- Fresh SHA-256: `5204d4514774b04b8eedb039e1f4799ed447c7fef14554577935e2d7bd93f951`.
- Frozen manifest SHA-256 is identical. No source conflict.

Printed p12 §2.1/2.1.2 requires CONTRL for other Ediel messages, explicitly including APERAK, and forbids CONTRL as receipt for CONTRL. Printed p25 §4.2 defines UNB/0031=1 as the CONTRL request and requires omission in CONTRL. UNB/0035=1 is independently the test indicator and is omitted otherwise. Inspected pp11–17 and23–29 disclosed no contrary 0031 exception. This is the source oracle for E011/ENV-04; BGM/AB does not own this decision.

## Existing canonical decision owner

`lib/ediel/ack/canonicalAckEngine.ts` is the single family/code ACK policy owner. Its rules already resolve:
- CONTRL: technicalAck=none.
- APERAK: technicalAck=CONTRL.
- PRODAT: technicalAck=CONTRL.
- UTILTS and UTILTS_ERR: technicalAck=CONTRL.

`deriveEdielAckDefaults` projects that same canonical decision into persisted outbound monitoring fields (`requiresContrl`, `contrlStatus`, due-time inputs). No second family matrix is needed or permitted.

## Envelope owners and actual routes

The common serializer is `EdifactEnvelopeCodec.encode` via `buildEdifactEnvelope`. `serializeUnb` reserves element index9 for ACK_REQUEST but does not assign it. The decoder already reads index9 as `acknowledgementRequest`. Index11 independently owns test indicator0035.

Actual routes inspected:
1. PRODAT compatibility/switch builders call `buildEdifactEnvelope`, then derive canonical ACK defaults for persisted monitoring.
2. PRODAT intent renderers/customer-masterdata and flow wrappers use the same envelope plus the same canonical ACK defaults.
3. UTILTS outbound calls `buildEdifactEnvelope`, then derives canonical ACK defaults.
4. APERAK/CONTRL/UTILTS_ERR drafts in `lib/ediel/ack.ts` call `buildEdifactEnvelope`. CONTRL must pass an explicit false decision; APERAK and UTILTS_ERR use the canonical family decision.
5. The separate generic PRODAT path `buildProdatMessage` calls `serializeEdifact`, which delegates directly to `EdifactEnvelopeCodec.encode`; this is the alternate serializer route that must not be missed.

The monitoring path therefore already has canonical policy authority, while the rendered envelope does not receive that authority. This is the bounded E011 split-brain.

## Minimal permitted implementation

Add one explicit immutable envelope input such as `acknowledgementRequest: boolean` (or equivalently named typed value) to `EdifactEnvelopeEncodeInput`, `serializeEdifact`, and `buildEdifactEnvelope`. `serializeUnb` writes literal `1` at UNB index9 only when true; false omits it. Do not infer from BGM/AB, rendered strings, route flags, DB state, or a new family matrix.

Every real outbound builder must obtain the boolean from the existing canonical ACK policy before encoding:
- PRODAT/UTILTS/APERAK/UTILTS_ERR: canonical `requiresContrl` true.
- CONTRL: canonical `requiresContrl` false.
- Generic `buildProdatMessage`: resolve the existing canonical PRODAT rule for its businessCode and pass the same decision.

Persisted `requiresContrl` and `contrlStatus` continue to use that exact canonical projection. 0035 remains unchanged at index11. Existing application reference, parties/subaddresses, separators, escaping, UNT/UNZ counts/references and incoming behavior are untouched.

## Required RED before runtime change

A durable focused test must fail on this exact baseline by asserting literal UNB positions through:
- direct codec eligible request -> index9 must equal1;
- real PRODAT builder -> index9 must equal1 while persisted requiresContrl=true;
- real APERAK draft -> index9 must equal1;
- CONTRL draft -> index9 omitted;
- production/test opposing control -> index11 omitted/1 independently.

Setup/import failure is not RED. The failing assertion must show current index9 omission.

## Task A disposition

Source/hash/page gate: COMPLETE.
Consumer/owner trace: COMPLETE for the bounded E011 routes above.
Static defect: CONFIRMED and source-backed.
Runtime edit: NOT YET AUTHORIZED until independent Task A source/design/oracle review accepts this record.
Full F3/masterplan: NOT COMPLETE.
PR310 remains paused and excluded.
