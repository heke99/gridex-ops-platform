# F3C-05: UTILTS message date field 205

Base: draft PR #380 published `9678a63359170914f16ab59274e0ea38d52e8aca`, same tree as local `ab238ea8`. Separate stacked draft batch, no merge. PR #372 and paused #310 untouched. No staging, TGT/AGT, market or production send.

Source: 25-A-3 annex C common header field 205 requires DTM+137, and UG-122-12/13 requires a real CCYYMMDDHHmm date in format 203, not in the future. `utiltsFieldMatrix.ts` and `fieldMatrix.ts` retain the required field. `canonicalBusinessDate` selects the effective guide before `runUtiltsRuntimeForMessage`; the header guide and `resolveCanonicalRuntimeDecision` feed inbound ACK and persistence.

RED: a complete E66-S with a real E19 reading mismatch but missing DTM+137 returned functional ERR. A 30 February value entered `canonicalBusinessDate` as `2026-02-30`, causing policy resolution to fail and a generic negative APERAK plan without a typed application error. The separate old test fixture for missing DTM had UNT count adjusted so syntax did not mask the guide fault. The malformed-date test first assumed functionalDecision `accepted` on policy-resolution failure and then exposed the empty APERAK errors; it now asserts the typed final response.

`normalizeDate` now verifies a real calendar date before using an extracted document date for policy selection; invalid values fall back to the received/created date for guide selection. The shared header guide validates field205 presence, exact 12-digit format203 and calendar validity. When both a parsed UTC offset and a received/created timestamp exist, it rejects a future document instant. Missing yields ERC41, malformed/format/future ERC42, FTX205 and negative APERAK, with the final E19 suppressed. The original valid +0200 E66 remains a functional E19 control.

Actual inbound processor test with external DB/sinks mocked: 30 February reaches the persistence RPC as `guide_rejected`/negative response, emits only APERAK containing 205, and does not call meter, billing or request-completion sinks. Local 24 UTILTS test files 280/280 PASS; app/test typechecks and scoped ESLint PASS. After the final received/created fallback line, the two directly affected suites passed again, 25/25, plus lint/diff. No native database execution or multi-IDE acceptance is claimed. PR #379's four exact-head ordinary workflows completed successfully; PR #380's three non-OPS workflows completed successfully and OPS was still in progress at the last check. This batch requires its own ordinary exact-head CI.

The received timestamp is the authority for the not-future comparison when present; `created_at` is the fallback. Cases with neither timestamp cannot prove the temporal upper bound from source context and remain a specific follow-up. Custom UNA for timezone conversion remains fail-closed as recorded in field206 report. Next: other F3C-05 header fields 202/203/204 or per-IDE E66 209/533 ownership with a distinct regulating-object persistence identity; F3C-04 native Z04 mixed-object, F3C-06 grammar, F3C-07 ledger, F1/F2 and F4-F6 remain open.

Skill routing: continued repository `using-superpowers`, `spec-to-code-compliance`, `systematic-debugging`, `test-driven-development` and `verification-before-completion`. No UI, schema edit, security audit or parallel-agent work was triggered.
