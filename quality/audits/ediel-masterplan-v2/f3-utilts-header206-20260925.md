# F3C-05: UTILTS timezone field 206

Base: draft PR #379 published `7cd41b50b3459182cc54afe246fd7420691b3b9c`, same source tree as local `83c1e1e8`. Separate stacked draft batch; PR #372 and paused #310 unchanged. No staging or hosted execution.

Source: 25-A-3 annex C common header field 206 requires DTM+735 offset to UTC; UG-122-16/17 requires an offset with format 406, not a fixed Swedish +0100. `utiltsFieldMatrix.ts` and `fieldMatrix.ts` retain the required field; `utilts/timezone.ts` parses and bounds default-UNA offset. The canonical runtime facade and inbound processor consume the result. Without an offset, conversion previously returned the local wall-clock value unchanged.

RED: complete E66-S original with real E19 mismatch, removing only DTM+735 and adjusting UNT count, returned `functional_rejected` and UTILTS_ERR. The first final-decision assertion also revealed a bad test fixture when UNT was not adjusted; the fixture was repaired without changing product code. The shared header guide now emits ERC41 for missing offset and ERC42 for invalid +2560 or format405, FTX206 and only negative APERAK. The existing parser is the validity owner, so values outside the supported 14-hour bound do not flow into persistence as an implicit UTC time. A valid +0200 control still yields E19 and no field206 issue.

Actual inbound processor test mocks external DB/sinks and sees `guide_rejected`/negative response in the persistence RPC, no meter or billing sink, no request completion, APERAK draft containing 206 and no UTILTS_ERR. SQL persists only accepted business series. Local 24 UTILTS test files 278/278 PASS; app and test typechecks, scoped ESLint and diff check PASS. The first attempt at the test typecheck used the nonexistent singular tsconfig path; corrected `npm run typecheck:tests` passed.

The code still calculates legacy functional findings before final guide suppression. Custom service separators are not interpreted by the existing timezone conversion parser, so the new guide holds such offsets rather than allowing unsupported local-time persistence; proper declared-UNA conversion needs a separate source/consumer slice. Duplicate header DTM+735 and other header field205 coverage remain unreviewed here. This batch does not claim native saved multi-IDE acceptance or full F3/E036.

Conditional E66 field209/533 review: annex C permits `LOC+175` instead of `LOC+172` for regulating-object values. `profiles.ts` borrows a sibling's message-level 172 and both legacy/profile checks reject 175-only. The persistence payload has only metering-point and grid-area identities; accepting 175-only without a distinct identity and sink boundary risks conflation. Kept fail-closed. Next bounded action: field205 DTM+137 required date/format and final ACK, then a per-IDE 209/533 identity-to-persistence design with source-backed references. F3C-04 native Z04 mixed-object, F3C-05 literal staging/native ACK, F3C-06 grammar, F3C-07 ledger, F1/F2 and F4-F6 remain open. Staging, TGT/AGT, counterparties, production and market sends are deferred.

Skill routing: continued repository `using-superpowers`, `spec-to-code-compliance` rule/owner/consumer trace, `systematic-debugging`, `test-driven-development`, `verification-before-completion`. No UI, schema edit, security audit or parallel-agent work was triggered by this bounded runtime/consumer change.
