# F3C-05: UTILTS header field 313 and mixed IDE review

Base: draft PR #378, published head `eb1bf1463f6e66615fdea4bfe1fa1b433fa9610f`. This is a separate stacked draft batch. PR #372 and #310 remain untouched; no merge or hosted operation.

Source `25-A-3` annex C `UG-122-9` and common header rows: BGM/4343 field 313 is required, allowed AB or NA. `fieldMatrix.ts` owns that rule. The legacy kernel reads AB to decide whether to send a positive ACK, but did not validate missing/invalid values as a header guide failure. `resolveCanonicalRuntimeDecision` and the real inbound processor consume the canonical facade result.

RED: complete monthly E66 with actual 1000/500 E19 mismatch and only `BGM/4343` changed to XX returned `functional_rejected`/UTILTS_ERR. Missing request also had no field313 application error. The focused test failed at final classification. The shared tokenized header projection now applies the retained 313 rule using declared UNA. Invalid/missing value yields field313 ERC42/41, negative APERAK/FTX313 and no final ERR; valid NA has no 313 issue. The legacy functional calculation still executes before this final correction, so literal staged execution remains open.

Actual inbound processor qualification (external DB/sinks mocked): the rejected transaction reaches the persistence RPC with `guide_rejected` and negative response; raw received QTY evidence remains in its request. The existing SQL persistence function skips business series unless disposition is accepted. The test observes no meter or billing sink calls, no completion call, APERAK draft containing 313, and no UTILTS_ERR. The first added test incorrectly expected rejected raw QTY evidence to be erased; source and RPC contract retain it. That test expectation was corrected, with no product change.

Mixed IDE check: a complete two-IDE E66 with missing IDE+24 in the first and real E19 in the second already returns synthesized first reference `transaction-1` with negative APERAK, then ERR E19 for the second. No code change was warranted for this qualified case. A distinct diagnostic with the first IDE missing LOC+172 and the second providing LOC+172 was classified ERR for both because `profiles.ts` falls back to message-level `facts.meterPointId`. This is a concrete possible cross-IDE guide ownership gap. The retained E66 source makes 209 dependent and allows regulating-object field533 instead; choosing a correct field/error for an IDE with neither identity requires the conditional identity owner. This batch does not alter that profile or claim a valid acceptance; actual rejected disposition currently prevents storage, but its ACK kind needs separate qualification.

Predecessors #377/#378 each passed all four exact-head ordinary workflows, OPS `36179405219`/`36180030123` with native 340/340 each. This does not qualify the new batch. Local verification: one mixed test and the new 313 test initially failed; after replacing the ambiguous 209 fixture with missing IDE identity, the mixed control passed without product change. The 313 test remained RED until its guide fix. Final 24 UTILTS files 276/276 PASS, app/test typechecks, scoped ESLint and diff check PASS. No native saved multi-IDE DB or full E036 acceptance is claimed.

Next: qualify E66 conditional field209/533 per IDE and source-backed ACK reference; otherwise inspect the next header/code path. F3C-04 native Z04 mixed-object, F3C-05 literal staging/native ACK, F3C-06 grammar, F3C-07 ledger, F1/F2 and F4-F6 remain open. Staging, TGT/AGT, counterparties and market sends are deferred.

Skill routing: continued repository `using-superpowers`, `spec-to-code-compliance` source/owner/consumer trace, `systematic-debugging`, `test-driven-development`, and `verification-before-completion`. No security-vulnerability, UI, database-schema or parallel-agent workflow was triggered by this bounded runtime/consumer slice.
