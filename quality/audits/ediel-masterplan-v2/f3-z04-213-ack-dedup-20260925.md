# F3C-02/04: own QTY31/213 ACK projection

Base is draft PR #374 published `ca1aa11b6311088f70b0f8ada05befdfa55b6924`, stacked on draft PR #373 and draft PR #372. PR #310 remains excluded.

P26.A revision 3 p54 and pp114–116 require each Z04 register's own QTY+31/C186/6060; another register's quantity cannot fill it. The existing `prodatRegisterFields` and `validateProdatRegisterPolicy` enforce the local condition. `canonicalPolicyFieldValidator` also includes the required field-matrix row. In a complete selected Z04 original with two physical objects and two registers in the first, the control passes canonical guide validation. Removing only the second register's QTY+31 yields two qualified field 213 findings for the *same* physical occurrence, one from each validator. `projectProdatDiagnostics` projected both as identical ERC41/FTX213 application errors. The actual inbound `processInboundEdielMessage` uses that plan before the case writer (PR #374 guard), so the duplicate would appear in the saved ACK draft. A focused RED showed the two entries.

The projection now collapses identical qualified **field** diagnostics by field/error kind, exact message/line/register/object/agency, component and failure content. It leaves the internal validation findings intact, retains distinct physical occurrences and does not deduplicate special application diagnostics. The focused test exercises the actual inbound consumer with the complete original, verifies accepted control, rejected first object, negative CONTRL/APERAK draft with one 213 error and exact first-object reference, no positive or sibling reference, and zero case/business effects. A second test verifies separate errors for two malformed objects. It does not assert partial acceptance or native DB persistence for the good sibling.

Local RED: 1 failed of 10 in the focused file, showing two identical 213 errors. GREEN: four relevant files 103/103, app and test typechecks, scoped ESLint and `git diff --check` passed. Ordinary exact-head CI remains required after publication; no staging or hosted execution.

Next: F3C-04 native persisted response/case and object boundary for mixed Z04 (including an independently selected register inventory), then F3C-05 literal staged execution and mixed unreferenced UTILTS findings. F3C-06 grammar and F3C-07 finding ledger remain separate.
