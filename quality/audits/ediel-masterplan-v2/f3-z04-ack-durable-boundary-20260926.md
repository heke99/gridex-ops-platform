# F3C-04 — mixed Z04 final ACK boundary

Base main `af93ef2b69037b0033a40d1afc77168f57c039e8` after #397; #310 paused. No staged, TGT/AGT, counterparty or market sends.

| Source and exact requirement | Code and durable owner | Inbound/outbound consumer, permitted effect | Verification and status |
| --- | --- | --- | --- |
| Masterplan §18–19, P26.A r3 pp.54,114–116: a complete mixed Z04 with own missing QTY31/field213 yields negative APERAK referring to that physical object; no invented positive sibling or invalid business write. | `resolveCanonicalRuntimeDecisionWithRegistry` → `processInboundEdielMessage` → real `buildContrlDraft`/`buildAperakDraft` → `createCanonicalAckMessage` → `createEdielMessage` → `createOutboxItem`. `ediel_messages` owns immutable ACK bytes, tenant, route/profile and original; `ediel_outbox` owns queued transport intent and unique retry lock. | Source-valid two-object inbound; final CONTRL positive and APERAK negative FTX213/RFF Z07 first object only. Route/profile and tenant must match; absent route creates warning, no outbound ACK/outbox or business effect. Retried original reuses existing ACK/outbox identity. | Prior test mocked ACK kernel/DB and proved only draft. New actual-consumer test keeps real builders, kernel, message writer and outbox helper, with external DB transport and unrelated side effects isolated. 3 tests: positive, retry, absent route. Native rolled-back SQL proves real ACK/outbox relationship, hash, tenant guard and negative-only pair. Native CI and exact-head ordinary CI pending. This combination does not by itself certify a live route/actor configuration or an unmocked HTTP+SQL processor run; no whole F3C-04 closure or Ediel traffic activation is claimed. |

The native SQL is a relationship/constraint proof, not a replay of the TypeScript renderer. The test's fake Data API records the actual generated insert/outbox row, but its route and outbound policy responses are controlled; the real route/mandate and bilateral transport gates remain capability-specific. No schema or generated type changed. If the clean replay rejects any synthetic fixture, read the first failing constraint before revising the fixture. Do not alter the normative expected ACK outcome.

Skill route: project test-driven-development/writing-good-tests and Supabase (current docs/changelog checked), source-to-code and tenant review. No UI/Next rendering, performance audit or parallel author applies.
