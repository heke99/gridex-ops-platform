# F3C-02/04 — mixed Z04 register assessment at the durable owner

Base: remote main `19e2f7f50ca331abc81bb0923d56fe1f773329c7` after #393. PR #310 remains paused. No staging, TGT/AGT, counterparty or market send.

| Requirement and source | Code and database owner | Actual consumer, ACK and effect | Evidence status and remaining boundary |
| --- | --- | --- | --- |
| P26.A r3 pp.47, 54, 114–116: LIN314 increments per message, C829258 increments per object, Z04 QTY31/213 belongs to its own register. F3C-02/04 requires the malformed object to be identified in a negative application response. | `prodatRegisterGroups`, `prodatRegisterPolicy`, `projectProdatRegisterValidation`; `gridex_received_sources.validation_assessments` is append-only and bound to source UUID, company, environment, raw hash and rule-pack facts. | `processInboundEdielMessage` selects tenant and canonical decision, then its rejected-register guard creates CONTRL/negative APERAK before the case writer. Existing complete two-object fixture checks FTX213 and first-object reference, no second-object positive response and zero mocked business effects. | Source specified; JS runtime implemented, tested and previously merged. Native assessment test previously checked only row count. This change verifies the exact durable assessment bytes/hash and owner/tenant/environment/source binding for accepted and rejected mixed facets. Native clean replay and exact-head CI pending. It does **not** prove a persisted ACK/outbox or atomic partial-object acceptance; F3C-02/04 remain open. |

The reproducible gap was evidentiary: `scripts/ediel-register-validation-regression.sql` accepted an arbitrary newly written assessment for its mixed-object case if the count changed and the RPC returned `not_established`. The tightened test checks the row by the returned assessment ID against the exact facts, source payload hash, company and environment. It retains the negative malformed-facet cases and their zero-write assertion. `not_established` is deliberately not promoted to market/source approval.

Local real inbound consumer suite: `npx vitest run __tests__/ediel-prodat-field-identity-inbound.test.ts` 11/11 passed. Native SQL cannot run in this local workspace (no `psql` or Docker); the ordinary clean replay must execute it on the PR head. No migration, generated schema, runtime rule or test expectation was weakened. Next independent F3C-04 proof is the actual persisted ACK/outbox and case outcome for this original, with retry and tenant route checks; the current unit mock is not that proof.
