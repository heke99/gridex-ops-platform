# F3C-04 — actual ACK route and native inbound consumer

Base `main` `cdc19d319ad7e210d97cceb373b7ff298dc0be31` after #398. PR #310 remains paused. No staging, TGT/AGT, counterpart or market send.

| Source and exact rule | Code and database owner | Incoming/outgoing consumer; expected effect | Tests and status |
| --- | --- | --- | --- |
| P26.A r3 pp.47,54,114–116, masterplan §18–20: a mixed Z04 with missing own QTY31/213 receives negative APERAK for that physical object and no false sibling success. | `processInboundEdielMessage` → canonical ACK gateway → `resolveCanonicalOutboundContext` → `findBestCommunicationRoute` filters `route_scope='ediel_ack'`. `communication_routes_route_scope_check` in the replayed schema allowed six other values, so it prohibited a route the gateway can select. `ediel_messages` and `ediel_outbox` own final ACK and queued intent. | Tenant/legal actor and source profile resolved from native local Data API, one real `ediel_ack` route and profile. Expected stored CONTRL positive, APERAK negative FTX213/first-object RFF only, two queued outbox rows and no case/switch/supply write; same-original retry reuses rows. No transport worker. | #398 first native clean-replay job `108444459333` at `597ce345` failed at `scripts/ediel-z04-ack-durable-regression.sql:50` with `communication_routes_route_scope_check` for `ediel_ack`. This is the RED reproduction. Test-only fallback in merged #398 then proved relationships, not actual route selection. Current forward and native consumer test are **not yet replayed**, so implementation/test/merge/activation are open. |

The forward widens exactly the persisted route-scope check. It creates no route or mandate, grants no new public Data API access, and changes no transport/send gate. Native SQL uses an isolated synthetic tenant and no sender. It must run on clean replay before acceptance; typegen and schema snapshot must be compared to authentic artifacts. On actual native failure, inspect the first cause, make one targeted correction and qualify the new head. Do not rewrite the prior published migration.

First exact-head #399 CI `8f5d1ee0` had four other workflows and OPS verify/quality green; native 343/344, with the new test obtaining `[]` ACK rows after route insert. The fixture had omitted `ediel_actor_settings`, which the real outbound actor resolver requires in addition to the tenant profile/identifier/role. Add that same-tenant legal actor setting, retain the strict ACK expectation, and report any remaining real blocked event on the next native head. This does not claim a fixed product outcome yet.

On second head `690c55e`, OPS native `108455173862` passed 344/344 in six files, including the real Z04 consumer, and the route SQL notice; all other ordinary workflows, OPS verify and quality/build passed. The job then stopped at the expected committed schema snapshot drift. Authentic clean replay artifact `10912276132`, ZIP SHA256 `7e5db6b6dd704ef443eb96abce6c7c8c7bad7b94ceb0953babe1ea31655bc927`, contained regenerated type SHA256 `36e989375ef5313e506a04f7d6e79b4058316ed0f39eb7d9530658fa1cbbed6d` (byte-identical to the committed file) and schema fingerprint `27b18060dd20cfc525fcd832876980b1ff3d6a4602ddbb27fba33c7d35d5fece`. The schema diff is one added `ediel_ack` CHECK member, and the fingerprint only changes the constraint section and aggregate. These two schema files are copied byte-for-byte from the artifact. Final exact-head CI, merge and deployment are **pending**; no Ediel traffic activation.

Skill route: project test-driven-development/writing-good-tests, code-review, verification-before-completion, Supabase and Postgres schema checks. Conditional: systematic-debugging on a fresh failure; Vercel deployments on merge. Broad quality-playbook/codebase mapping, Next/React, performance and parallel-agent skills do not apply to this single constraint/consumer criterion. Project `spec-to-code-compliance` slash/agent runner is unavailable under the sole-author/no-subagent constraint; the exact one-row source-to-consumer trace above is kept explicit.
