# F3 Z14 dependent unit — local candidate, not accepted

Base: 168c21336bedcebef02da27bada55a6ea63f805e (accepted main337).
PR310 remains paused/excluded at e961135199f292b8210884f07de3b616a670161a.
No live DB/storage/send/deploy/settings actions; no normative register, migration,
schema, generated type, grant, prior assertion or gate changes. No remote mutations.

## Source qualification and design recorded before implementation

Unchanged P26.A revision3 source_tables and prodat_fields provide §2.2 fieldnotes:
209 p16;302 p17;508/326 p18;217 pp18–19;222 p19;506 p20;513/260 p21;
325/227/228 p22. V/S17 and VH/S18 require these fields; N/Z96 excludes them.
These exact reasons are already in the canonical subtype registry. Raw aliases,
unknown/foreign tokens, duplicate/malformed reasons cannot supply this fact.
The subtype-only conditional projections do not override §2.6 p57:217 is EL-only.
For positive217, missing/unsupported market remains undetermined, never optional.
The N exclusion is explicit and does not require positive EL evidence.

P p22 UD parent note excludes N; active UD carries227/228/316, with229/231/232
not used in Z14. P p22 IT note excludes N; active IT requires233=209 and234.
These parent effects prevent root-snapshot false missing children; they do not
migrate unrelated D cells321/323 or address229 availability. Applicable wire
locators/syntax remain existing descriptors: LIN C212 p47;DTM p49/51/52;
CCI/CAV p57/63/68/72;RFF p77/78;NAD p79/81.

Confirmed gaps before editing runtime: nine numeric cells still consult byCell;
209/326/325 accept loose non-N tokens; protected send overlay lacks Z14; root N
parent filtering can hide positive fields and root V falsely requires N children.
Supplied malformed/forbidden data can disappear through scope narrowing.
Severity: major, malformed outbound acceptance / false outbound rejection.
Actual-module red: `npx vitest run __tests__/ediel-prodat-dependent-z14.test.ts`,
60 failed /16 passed,76 total. Existing Z14N date exclusions and parent exclusions
are preexisting behavior, not new implementation credit.

Plan: immutable12 source outcomes; dedicated bounded Z14 policy using existing
strict first-register wire reason decoder; supplied syntax/scope scan before
narrowing; object-specific parent activation; canonical outbound and preflight
integration; generic/profiled builder verification; actual-module red/green and
all three typechecks/full application/integrity. Root owns independent review,
ordinary CI, full certificate and remote acceptance. No acceptance-count change.

Skill routing: systematic-debugging, test-driven-development and verification-
before-completion applied. Existing task brief supplies bounded plan; manual
source/code qualification uses complete relevant call path. Repository-wide
quality/codebase discovery, UI/Next.js, DB/security infrastructure, optimization,
skill authoring and external tooling are outside this pure protocol unit.
Active memory is deliberately not edited per task authorization.

## Verification and residuals

Status: LOCAL_VERIFIED_UNPUBLISHED; independent review and remote gates pending.
Accepted progress is unchanged:18/110 numeric D and2/10 parent occurrences;
92 numeric D and8 parent occurrences remain unaccepted. This candidate proposes
12 numeric cells plus the two Z14 parent effects, not completion of F3.

### Retained implementation

- `prodatSubtypeRequirement.ts`:12 immutable Z14 source outcomes. N excludes all;
  V/VH require all, with217 positive EL evidence required. Operator byCell does
  not override these outcomes. Unknown/foreign subtype remains undetermined.
- `prodatDependentConditionEngine.ts`:parent facts use the same source outcomes.
- `prodatZ14Policy.ts`:new bounded outbound overlay; reads exact field223 in each
  object using the existing strict reason decoder. Validates all supplied
  fields before scope narrowing: SG8 DTM, SG14 CCI/CAV, SG16 RFF, SG17 NAD.
  Duplicate/malformed/misplaced data cannot be hidden by a valid sibling value.
  Code vocabularies are the finite source tables p57/63/68/72. This is not a
  claim that vocabulary membership alone proves broader process or temporal
  authorization. UD/IT activation precedes child requirements;233 equals own209.
- `canonicalPolicyFieldValidator.ts` and `prodatSubtypePolicy.ts`:outbound
  source-bound overlay feeds existing validator, protected send/preflight and
  transport guards. Inbound keeps the base parse-only path and no new protected
  unknown rejection. No inbound parser implementation changed.
- `buildProdat.ts`:generic Z14 builder validates final wire and derives each
  object's subtype from its own emitted reason. Optional installation input
  allows positive Z14 to express the required parent instead of making a valid
  positive message impossible. Other-code date subtype behavior is preserved.
- `builders/profileRenderer.ts`:returns bounded Z14 wire-validation issues.
  Before UNB exists, validation uses emitted LIN-and-following detached fragment
  and canonical policy applicationReference, matching the existing detached
  market-evidence rule. Final wire validation still requires its actual UNB;
  missing/unsupported UNB cannot borrow cached market or application metadata.

### Findings, refuted claims and fixture corrections

Major confirmed: missing source-derived cardinality; root subtype suppressing or
requiring wrong object's children; physical supplied fields lost through scope
narrowing; generic/profiled builder lacked complete bounded validation.
The new source-cell tests and send-boundary tests reproduce these actual modules,
not implementation-text snapshots. No DB/provider mocking is needed.

Intentional N builder omission of reusable context is NOT itself a protocol
error: accepted date/party omission contracts remain. A proposed new rejection
of omitted N builder input was withdrawn after review of that distinction; no
such runtime tightening or new assertion remains. Supplied forbidden WIRE data
is rejected. This unit does not claim raw-byte padding validation: the existing
tokenizer trims segment-boundary whitespace. A new trailing-segment-padding
expectation was withdrawn; leading padding, decoded component lengths, unused
components, malformed and duplicate reasons remain covered. Tokenizer unchanged.

Two retained fixtures corrected, with every prior assertion unchanged:
1. Complete-facts catalog had Z14 market=gas. Only Z14 now joins Z06 as
   electricity (both repeated fixture expressions), because217 is EL-only p57.
   All registry coverage and no-undetermined assertions remain; explicit gas/
   unknown negatives are in the new suite.
2. Prior parent regression supplied only NAD fragments and rootN. Its same NAD
   values now follow actual `CCI++Z13` / `CAV+Z96`, so the original outbound
   forbidden/inbound-ignore assertions use source evidence, not stale root facts.

New fixture correction: profiled positive UD initially lacked agency89 and
therefore parser correctly withheld its identity. Added valid fixture agency;
no parser patch. Duplicate object identity is already register-policy territory,
not claimed as a new twelve-cell defect. No register machinery imported from310.

### Executed checks

| Check | Outcome |
|---|---|
| Initial new source/field/object suite |60 fail /16 pass before runtime fix|
| First new suite green |76/76|
| Additional boundary/builder/header checks |Corrected red4/97; one N-input claim later withdrawn as above|
| Generic positive installation regression |Red before installation support; green after|
| Code-vocabulary negative |Red1/80 before finite source-code check|
| SG8 late-date regression |Red1/82 before placement repair|
| Final new Z14 suites |104/104 in2 files|
| Targeted prior+new suites |1203/1203 in17 files before final two additive tests|
| Initial full application run |3 fail /3215 pass; two retained fixture issues plus withdrawn trailing-padding expectation|
| Final `npm test` |3220/3220 in246 files|
| `npm run typecheck` |PASS, final implementation|
| `npm run typecheck:scripts` |PASS, final implementation|
| `npm run typecheck:tests` |PASS, final implementation|
| `npm run ediel:masterplan-v2:integrity` |PASS:33 originals,121 rules,231 acceptance contracts; does not assert application conformance/readiness|
| `npm run quality:large-file-budget` |PASS; unchanged1800 default,0 grandfathered files|
| `npm run quality:performance` |PASS API tenant/performance gates, N+1 budget, SLO contract|
| `git diff --check` |PASS|

Targeted command:
`npx vitest run __tests__/ediel-prodat-dependent*.test.ts __tests__/prodat-dependent-condition-engine.test.ts __tests__/ediel-prodat-date-boundaries.test.ts __tests__/ediel-prodat-party*.test.ts __tests__/ediel-masterplan-protocol-regression.test.ts __tests__/ediel-canonical-policy-batch-regression.test.ts`.
Final new command: `npx vitest run __tests__/ediel-prodat-dependent-z14*.test.ts`.

Local runtime Node24.19.0 differs from mandatory CI Node22. Remote ordinaryCI,
independent review and full release certificate remain root-owned gates. No
coverage thresholds, prior assertions, source registers or mandatory gates were
weakened. No active-memory edits, live operations or remote mutations.

Residual scope:321/323 and all other remaining numericD predicates, full229,
other parent occurrences, F3–F7, live TGT and release remain unqualified here.
PR310 remains paused. No source conflict blocks this bounded candidate; positive
217 with non-EL/unknown market intentionally remains unknown and blocks send.


## Independent review R1 — supplied DTM qualifier padding

Review of candidate ad1b6a86 requested changes for one major confirmed gap:
`DTM+ 90`, `DTM+ 354` and `DTM+ 693` escaped exact-qualifier selection in the
bounded Z14 scan. A complete positive sibling date hid the malformed additional
segment; N also escaped protected transport under the existing test-send override.
The space is inside C507/2005 and survives tokenization. It is not the previously
withdrawn assertion about segment-boundary whitespace. The earlier supplied-date
coverage claim was incomplete for this case.

Source basis: P26.A §2.6 pp49/51/52 gives exact2005 qualifiers90/354/693 for the
three owned fields302/508/326. The repair recognizes normalized qualifier matches
only to report malformed exact spelling; it does not normalize them into valid
required date evidence. It runs before scope narrowing in the outbound-only Z14
helper. Global date readers, tokenizer, inbound parser, other message codes and
unowned dates remain unchanged.

Actual-module red: new115-case suite failed108/passed7 before the fix. Repeated
with `assertEdielSendLock` as first assertion: the same108 cases escaped transport.
Coverage:3qualifiers ×2padding directions inside qualifier ×3UNA alphabets ×N/V
×header/object/party placement. Seven controls prove complete V/minimal N send
success and no reinterpretation of unowned/different qualifier codes.

Green checks include canonical policy all/dependent_only, actual payload and row
validator, preflight, rulebook send guard, transport guard, and unchanged inbound
parse behavior for every padded case. Targeted7files/608tests pass. All3typechecks,
integrity33/121/231, unchanged large-file and performance gates pass.
Final R1 `npm test`:3335/3335tests in247files; `git diff --check` passes.
Runtime remains Node24.19.0 locally; remote Node22 and independent rereview are
pending. Root-owned memory commit b9a9ac57 retained. No remote mutations or memory
edits by the R1 implementer. Accepted counts remain unchanged.
