# F3C-05 / U-03 — literal guide before functional execution, 2026-09-26

Base: merged main `cb7b759799e96cae0c0a4eae14dfc5ced68292a5`, after PR #391. PR #310 is paused and untouched. No staging, TGT/AGT, counterparty or live market send.

Source: masterplan U-03 / AT-U-03, U §5.2 p.107: syntax, header guide, per-IDE guide, then functional control only for guide-approved IDEs. ST-U01/02 require separate persisted dispositions and correct APERAK/UTILTS_ERR. Previous `f3-staged-utilts-field209-20260925.md` explicitly left the literal execution order open: the canonical facade ran the legacy functional validator first and filtered its findings after the guide.

RED: a complete E66 with LOC+175/3055=`260` was correctly guide-rejected in the final ACK, but a spy on the real interval-count function recorded **two actual functional invocations** for that rejected IDE. The branch separates a guide-only legacy pass from a functional pass restricted to guide-accepted transaction references. The profile interval count, object processability, E66 quantity/timing/count, S02/E31 functional checks and SCH promotion accept that eligibility. Canonical E66 quantity validation receives the same set. The old post-hoc functional suppression was removed. The final facade combines preserved guide issues with functional issues from eligible IDEs, then rebuilds the transaction disposition, ACK plan and persistence payload.

Test matrix: bad agency 260 → no interval-count invocation, field533 negative APERAK and no ERR; agency9 good → functional count still executes; mixed IDE → exactly the single-good-IDE count, independent guide/accepted dispositions and persistence payload; bad BGM header → zero functional invocations. Existing mixed guide/E19 and real inbound processor tests retain separate ACK/storage and no business mutation. Local 27 relevant UTILTS files 352/352, new real-consumer focused 23/23, app/test typechecks and scoped lint (one preexisting unused `gridAreaId` warning) passed. The wider local Node 24 Ediel pass ran 5124/5126: two Node 22 TAP-format wrapper assertions failed on their output parser, while their invoked scripts reported all cases passing. Ordinary exact-head CI, including full E2E and native replay, is mandatory before merge.

This criterion establishes the stage order for the covered canonical runtime and consumers. It does not certify all remaining F3C-05 field rows, F3C-02/04/06/07, field203 temporal uniqueness, positive LOC+175 object ownership, or the E035 historical `complete:false`/retention and deletion gates. No migration or generated database contract changed. The current web release remains safe with Ediel traffic gated.

Acceptance receipt: [PR #392](https://github.com/heke99/gridex-ops-platform/pull/392), exact head `bf57d16e406842cb3325424f0b621b6588ed065b`: OPS `36247794182` (verify, quality/build, clean empty-database replay and native cases all SUCCESS), Ediel `36247794205`, public browser `36247794321`, full E2E `36247794364` (smoke, coverage and PR certificate) all SUCCESS. Squash merge `fc4e688db414314bce4a85a7172c1d63bc15b804` is the verified remote `main`. Vercel production deployment `dpl_75bdehtPGc6Sr13ZNUSkj6QP7g6Y` is READY for exactly that SHA. This is web deployment evidence only; no Ediel market capability was enabled or accepted.

Skill route: repository understanding, source-to-code compliance, systematic debugging, TDD, review and verification apply; Supabase boundary is read-only and native CI validates retained SQL. Next.js UI, full repository security/performance and delegated agent workflows do not apply to this bounded runtime rule. Single author retained.
