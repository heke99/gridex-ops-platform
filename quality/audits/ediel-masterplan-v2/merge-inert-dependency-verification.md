# Inert policy qualification follow-up

Actual ab0c8f49 run35004825344/job104501850825 passes the seven atomic rejection cases and executes the exact policy-removal candidate. Its following strict catalog comparison fails before the zero-count/repeat/client checks. The snapshot includes pg_depend rows owned by each policy; dropping a policy necessarily removes those rows too, while the verifier removed only the24 policy keys from its expected preimage.

The correction captures dependency keys directly from pg_depend constrained to the exact24 retained public policy identities before the candidate executes. Only those captured keys and24 policy keys may disappear. All other catalog entries, ACLs and row fingerprints remain subject to exact equality. Regression cases reject unrelated dependency loss, duplicate keys and invalid catalog keys. Candidate SQL and its hash remain unchanged.

The fallback cleanup also failed to import a sibling module from Python stdin. Its import path now explicitly includes the repository scripts directory. The owned controller and its cleanup constraints are unchanged.

The local execution environment became unavailable before this follow-up could run locally. GitHub CI must execute the new tests and actual isolated PostgreSQL qualification; no successful qualification or migration promotion is claimed here.
