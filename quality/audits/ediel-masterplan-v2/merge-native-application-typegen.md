# Native application type candidate continuation

Status: IMPLEMENTED_NOT_VERIFIED on the actual native database. No generated
application types, manifest, schema acceptance or complete replay acceptance claimed.

The ordinary wrapper still raises
`NATIVE_LATER_ENVELOPES_AND_FULL_ACCEPTANCE_REQUIRED` after selected-chain success.
The owned native lifecycle disposes its database before the workflow's older
localhost typegen commands. Synthetic typegen transport evidence does not fill
this application type provenance gap.

Skill routing: systematic-debugging (traced wrapper, owner, complete timestamp
runtime, cleanup, comparator and synthetic typegen), test-driven-development
(candidate tests first failed on absent module), verification-before-completion
(exact tests below). Database/Supabase project invariants govern the existing
owned CLI path. No SQL/schema/auth policy alteration, UI change or performance
claim; those review groups are outside this scoped transport addition.

New candidate generation runs after unchanged final SQL, native actor/removed
policy/added view witnesses, exact probe cleanup and the second actual schema
comparison. It rebinds the real Runner files and ledger using existing admission
functions. It requires CLI 2.101.0, 144 foundations, 514 timestamps and the current
complete forward registry, including post-cleanup ledger size and probe absence.
It does not require an empty schema diff or label differences accepted.

Official CLI public-schema typegen executes twice on the still-owned internal
network. Both raw UTF-8 byte streams must be identical. Catalog, rows, provider
events and ledger must remain unchanged. The existing source-pinned nullability
override runs against a private temporary candidate. Candidate bytes remain in
memory until successful lifecycle execution, resource cleanup and private
workspace removal; only then may the fixed candidate artifact be exported.
Raw bytes are removed from the parent report before JSON serialization, even
when cleanup fails. No raw CLI stderr, row dump, accepted type path, manifest,
historical migration or schema reference is exported or changed by this module.

Fresh local verification:

- `python3 -B scripts/test-canonical-native-application-typegen.py`: 7 PASS.
- `python3 -B scripts/test-canonical-native-timestamp-runtime.py`: 21 PASS.
- `python3 -B scripts/canonical-native-supabase-lifecycle-selftest.py`: 15 PASS.
- `git diff --check`: PASS.

The new tests exercise output rejection, the real existing override script,
repeat mismatch, CLI failure, state mutation, missing prerequisites, disposal
failure, exact export binding and success export. These are offline controls,
not a claim of real application type generation. The root agent integrates the
workflow artifact path and selftest entry. Actual native replay, review of the
candidate, accepted schema decision and subsequent real manifest generation
remain required before the wrapper's final acceptance gate can change.
