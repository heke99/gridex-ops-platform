# Exact tenth forward registration

Skill routing: source-preserving forward remediation and verification-before-completion apply. This bounded registration uses the already executed PostgreSQL qualification; no UI, performance, architecture or deployment changes are involved. Root owns final review/publication and current memory.

Actual qualification supplied by the coordinating agent: run35006218472/job104506509692 succeeded on owned PostgreSQL17. The CLI2.101.0-created filename is `20260915181448_drop_inert_inbound_client_policies.sql`; SHA256 `b04ce7766f0d3e4655778cdd6aa6fcde1bb3a0661867e381aa0939f015c9e2c1`. Exact candidate bytes are promoted, including their historical candidate-only header. Qualification reproduced F14 count24, then0, with seven atomic negative cases, policy/dependency-only delta, unchanged rows/ACLs, client42501, service-role NOBYPASS CRUD, repeat and cleanup.

Registration adds exactly one manifest entry and one forward source. Inventory611, raw timestamps524 and FULL599 preserve original601 hashes, historical514 selection and first9 forwards. Complete actor/removed-policy receipts require all10; all267 required policy hashes remain unchanged. A regression verifies that the24 removed identities have zero overlap with these267.

Native predicate10 checks all24 exact identities absent, three RLS tables present, no direct PUBLIC/client policies, anon/authenticated table and column CRUD denial, and service-role SELECT/INSERT/UPDATE/DELETE ACLs. The owned candidate fixture now requires this predicate false before and true after. Actual execution of the new predicate and complete native ten-source replay remains pending; prior candidate success does not imply either or schema/type acceptance.

Local validation: source9, native-forward7, portable5, inert5, actor7, removed-policy11, frontier16, residual-admission20, replay-accounting38 and formula9 tests PASS. CLI lifecycle15 checks rerun after extending its positive ordinal set to10 and negative boundary to11. No schema reference, type manifest or acceptance flag changed. No production database write.
