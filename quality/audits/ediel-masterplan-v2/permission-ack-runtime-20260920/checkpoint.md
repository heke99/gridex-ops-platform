# Permission ACK runtime checkpoint

Task2 source-approved incoming322/324 migration plus root-authorized registry-only
missing258 amendment (a765bf7d). Runtime/tests are a recoverable implementation
candidate; broad gates, final report and independent completed-runtime review
remain pending. No permission business-linkage or live-service acceptance.

Skills: executing-plans, TDD, source-to-code tracing, code review/false-positive
challenge, verification-before-completion and bounded Supabase schema/upsert
review. Existing approved plan replaces new brainstorming/planning. Root owns
memory, publication, CI and independent review; no subdelegation. No UI, schema,
codec, dependency, broad performance/security audit or deployment scope.

RED: initial 56 tests15PASS/41FAIL before production; ten test assertions incorrectly
read responsePlan as an object. Consumer v1 has three missing-group.fields fixture
errors and one transient manual guard scope implementation error. All originals
preserved. Corrected tests ran in isolated baseline35009150 worktree:67 tests,
16PASS/51FAIL, qualified baseline receipt. New test source snapshots are archived
as .txt. Classifier1RED and amendment31tests20PASS/11FAIL are separate receipts.

The nine first inbound continuation failures were new overbroad assertions that
national F stops generic business invocation. Root confirmed existing ready-F
continuation differs from internal_review; corrected tests retain rejection,
exact typed persisted/reloaded response and real draft assertions. Internal text
hold stops business and retains independently ready F. No business flow changed.

Persistence uses deterministic physical LIN-qualified rule_key in both existing
upsert keys, plus physical field_path. Nullable error_rule_id verified in actual
migrations (02_db1_operations_ediel_billing_dedupe_and_storage.sql:617 and
20260525_debug_fix_batch_1b_schema_code_alignment.sql:340), generated types
supabase/database.types.ts:30475/30485, and existing unique indexes at455–486 of
that latter migration. Later columns are absent in the legacy generated table
view but present in migrations and existing runtime. No schema/type mutation.
Public Supabase upsert docs inspected; changelog.md fetch unsupported text/markdown.
No live DB query or mutation, only mocked actual write payloads and schema review.

Residuals: P-ACK-R1 HIGH prior lookup unchanged/unaccepted; generic209/261, canonical
duplicate-reference258, populated-scenario comparison hold remain. Counts98/110
numeric +10/10parents unchanged; PR310 paused, full F3/workflow unaccepted.

Focused checkpoint:141/141 PASS in four files; scoped ESLint0warnings/errors.
Final three new-test UNH controls initially concatenated two UNA interchanges,
causing dangling release tokenizer errors; corrected to two distinct UNHs in one
interchange, original failed receipt preserved. Final populated-scenario fixture
has required column testCase label; no runtime assertion waiver.
