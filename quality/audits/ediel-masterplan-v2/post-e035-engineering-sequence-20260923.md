# Engineering sequence after complete E035

Read-only preparation, 2026-09-23. Last observed HEAD `4303be2445bf8d99d3a177161b628869f05e9027`; sole closure implementer has active dirty work. No production/test/frozen-manifest edits, test execution, external writes or PR310 work. This file is a future execution sequence, not a new active work item or completion receipt.

Read frozen MASTERMASTERPLAN_v2 §§18–20 and annex E, open_evidence_gates, current-task memory, corrected F3 criterion reconciliation R2, remaining-plan-status, current F3 field-composition preparation, and the actual callsites listed below. Applied bounded code-review, source/implementation comparison and false-positive discipline. Unread later-phase internals remain expressly unqualified.

## Entry condition: E035 really complete

Do not start another implementation stream now. First finish the root-owned closure, successful-retry consumption/source binding, prior-25-A-3 scoped comparison activation, cancellation and remaining applicable owner scopes, then obtain actual exact-head/native/task/whole-delivery decisions. Current native parser evidence and previous green `0e81b119` are bounded historical receipts; they do not complete active closure or E035. When the root declares the E035 delivery qualified, import its evidence into F3C-05/E035/F6 rows; do not rebuild its owner/timeline/native tests merely because older reconciliation says the loader is missing.

Preserve D110/110 and dependent-parent10/10, bounded accepted PR351–359 field/ACK/permission/UNB/FTX work and subsequent accepted source/observation work. Do not restart them. PR310 remains OPEN/DRAFT/PAUSED at e9611351; do not import its source/proof infrastructure or silently mark dependent parity done.

## Sequence 1 — finish the finite F3C-02 register composition slice

Start exactly from `quality/audits/ediel-masterplan-v2/f3-field-composition-preparation-20260923.md`, not from a new 74-field rewrite.

Already implemented/covered in bounded evidence: global LIN314 versus object-local C829258, agency-qualified209 identity, own QTY31/213 integer/conditional rules, first-register common-field inheritance, independent outbound count enforcement and protected send guards. The preparation found no new runtime defect. The74/962 descriptor inventory is not behavior evidence.

Actual route: `canonicalPolicyFieldValidator.ts:48,93–107` → `fieldMatrix` + `validateProdatRegisterPolicy`; `prodatRegisterFieldState`/`prodatRegisterGroups`/`prodatRegisterRuleScopes`; parser/compat and `inboundCases.parseInboundProdatBusinessData`; then `core/runtimeDecision.resolveCanonicalRuntimeDecisionWithRegistry` → `flows/inboundProcessing.applyCanonicalRuntimeDecision` (line298; call at839) → final ACK creator. Outbound route is generic/profile renderer → `render/registers`/`prodatRegisterInput` → `core/messageBuilder/payloadPreflight` → `transport/sendLock.assertEdielSendLock`.

Next smallest deliverable: four field rows314/209/258/213 with original locator, owner, exact real caller, positive original, malformed own-object original, exact final response or internal-hold result, business-write prohibition and receipt. First inspect existing typed-field tests and retained error registry mappings; add tests only for genuinely missing end-to-end assertions. `ediel-prodat-register-ack-boundary.test.ts` is already present, but its actual implementation invokes **testing** `aperakErrorRuleRegistry` with synthetic TGT data and mocked DB. It proves no fabricated TGT positive, not national final production APERAK mapping. Do not claim this closes the production ACK row.

Qualify currently unlocated209 25/26-character/agency/component cases and QTY136-not213, omitted unit, MTQ gas-context versus wrong-market unit through real canonical caller. Lexical KWH/MTQ allowance is not itself a bug. Preserve the exact accepted raw topology suites. Map actual outbound expected-inventory producer via `register-fact-source`, not arbitrary fixture hints. Multi-message per-message completeness stays a separate caller evidence row; first-message field validation is not whole-interchange certification.

Once this finite slice passes independent review, repeat the same reconciliation for remaining field-family/header/first/later register overlays using existing family audit documents. Outcome can be “covered with receipt” without code changes. No new defect is confirmed by this preparation.

## Sequence 2 — F3C-04 and F3C-05 actual response/storage composition

Use one combined behavior table to avoid duplicate tests, but keep criteria separate. Rows distinguish syntax/CONTRL, national guide/negative APERAK, functional UTILTS ERR, accepted/storage/positive ACK, unknown local evidence/internal hold, and mixed independently scoped siblings.

Production response path anchors:

- `lib/ediel/core/runtimeDecision.ts:354,428,433,461` selects canonical policy/PRODAT/UTILTS decisions and registry resolution.
- `flows/inboundProcessing.ts:101` builds canonical ACKs; `:452,476,514` selects CONTRL/ERR/APERAK; `:917` dispatches UTILTS facade.
- `flows/utiltsInboundPolicyProcessor.processInboundUtiltsMessageByCanonicalPolicy` selects actual E30/E66 versus explicit nonbilling outcomes; `flows/utiltsDataRequest.part-2` prepares/persists actual transactions; part-1 `createUtiltsRuntimeAcks` finalizes each non-held transaction.
- `core/kernel.createCanonicalAckMessage` owns durable ACK creation, and `utilts/transactionPersistence`/current forward RPC own transaction persistence; after E035 use its final revised binding interface, not this preparation's older signatures.

Minimal missing proofs: real full-policy invalid register/field original → final ERC/FTX plus correct object/LI reference; first good sibling unchanged; actual guide-versus-functional competing errors per transaction → correct response and storage; internal local evidence failure → no invented national error. Reuse existing typed-field, selected-permission, E035 held/mismatch/retry/native cases where their final writer and source oracle exactly match. Wrong-guide, stale metadata/override, unsupported variant and duplicate physical identity need separate table rows with existing receipt or a narrowly added negative.

**Concrete remaining implementation fact, not yet a qualified defect:** current `utiltsEngine.part-1.ts:991–1318` still accumulates functional checks before `validateCanonicalUtiltsProfile(facts)` at1318; `resolveUtiltsTransactionDispositions` at182 prioritizes guide errors afterward. Final priority is not staged execution. `promoteE66SchMissingReadingToFunctionalIssues` at917 also remains. Next action is a source-backed actual-runtime witness of guide failure plus otherwise functional failure, including whether the functional owner is executed and what it changes. Only then refactor staging, preserving sibling scope. Do not change an old expectation merely to match present execution, and do not claim every functional check has externally harmful effects before reproducing one.

Native completion rows should reuse full E035 committed reservation/conflict/series proofs, then fill genuinely missing guide-rejection/ERR/positive/rollback/version-correction cases. Literal disposition arrays and mocked payload tests do not substitute for native transactions. Fixes require failing witness, targeted correction, source review and exact-head receipt; no full-suite rerun merely for ledger work.

## Sequence 3 — F3C-06 / G06 grammar and release inventory

Known current owners are `core/edifactTokenizer`, `edifactSegments`/`canonicalEdifactAst`, `core/syntaxValidator.validateEdifactSyntax` (line70), and `core/messageBuilder/segmentSchema`, with preflight/transport and inbound consumers above. The syntax validator visibly enumerates envelope/count/reference/status checks; this is not full UNSM certification. Source search of repository/audits and local recovered source files did not locate a qualified complete UNSM package. This is **not** an external-unavailability claim.

Next engineering action: obtain authoritative version-specific directory message/segment/composite definitions matching actual selected profiles, qualify identity/hash/precedence separately from frozen source_manifest, and build a coverage matrix: grammar construct → existing owner → reachable inbound/outbound caller → independent positive/negative. Existing UNECE leads in the prior work should be followed; do not ask the user to upload public grammar merely because it is absent from the seven-entry frozen manifest. Only missing demonstrated constructs receive implementation. Include actual physical UNH membership and release/custom-UNA/component cardinality; no rewrite justified solely by syntaxValidator size.

Generate exact accepted-release caller/schema/RPC inventory from the eventual tree and replay artifacts, not static documentation counts. Record PR310-dependent parity explicitly deferred under the standing exclusion. G06 cannot be globally closed while such required parity is excluded, but this does not block independent grammar engineering.

## Sequence 4 — F3C-07 original finding ledger, then phase decision

Create one non-frozen reconciliation row for each E001–E013 and E034–E037 with original acceptance text from annex E and one of accepted-current/accepted-historical-needs-current-link/superseded/unverified/confirmed. Populate throughout sequences1–3; final adjudication comes last.

E011 has PR358/UNB0031 acceptance; do not reproduce it from scratch. E003/E004 link existing typed diagnostic and actual final-reference proof; E005–E008 link subtype/date/end-user/reporting owner receipts, not global D totals; E009/E010 require occurrence/component-retention evidence; E012/E013 need their exact identity/AB rule callers. E034 links retained guide-selection/effective-date/decision-reuse paths. E035 imports final delivery. E036 maps the staged-validation evidence above; E037 maps final native persistence/correction/consumption binding. Frozen annex statuses describe the original review, so do not edit them to manufacture completion.

F3 closes only after every applicable criterion has its own evidence decision. Do not turn these five groups into five assumed PRs or a percentage denominator.

## Subsequent masterplan execution: inspect before claiming implementation gaps

The order below follows dependencies, not an assertion that each phase is wholly unbuilt.

| Step | Existing concrete entry points to inspect/reuse | Next finite engineering deliverable |
|---|---|---|
| F1/G01/G02/G07 source capability closure | `rulebook/guideRegistry`, `canonicalRulePackRegistry`, `utilts25A4`, `utiltsMessageSupportRegistry`; recovered `sources/utilts-prior.txt`, `utilts-aggregates.txt` and original PDFs | Qualify all remaining changed prior-profile and aggregate/product/role/period rows, beyond E035's bounded prior E61/E62 amendment. Reconcile explicit source conflicts by capability. Full prior and aggregate texts have been recovered: “missing fulltext” is stale, qualification is still required. |
| F2 rights/context + F4 import/route foundation | `core/actorRegistry`, `routeRegistry`, `partyRegistry`, inboundTenantResolver, registry import caller inventory to be completed | Map E014–E023 to actual import outputs → legal actor/market/role → company/route/cert resolution; one official fixture/diff per source format and ambiguity/cross-tenant negative. Reuse existing parsers after inspecting callers. G04 live agreements affect activation; they do not prevent building/qualifying technical isolation. |
| F4 transport + F0/G05 incident evidence | `transport/index.part-1/2`, `sendLock`, `outbox/readinessGuard`, `security/certificateStatus`, `certificateScope`, `inbound-mail/dsnClassifier`, inbound ACK transport guard | Trace one send operation/attempt/Message-ID/exact EML/DSN chain and cert validity/revocation/multiple recipient cert behavior. Separate existing code coverage from actual runtime/provider evidence. No guessed resend or real send authorized by this plan. Search connected read-only operational evidence before requesting unavailable artifacts. |
| F5 processes/deadlines | `stateMachines/prodatLifecycle.decideProdatLifecycle`, `flows/inboundBusinessStateMachine.applyInboundBusinessStateMachine` (168), `permissions/permissionStateMachine`, z13/z14/z18/z15 handlers, `calendar/deadlineCalculator.evaluateEdielDeadline` | Map the frozen42 transitions to actual atomic commands, timers and next-action owners; first unresolved start/cancel/end/permission transition gets real transaction/order/race/scope tests. E061 is expressly a withdrawn/currently unproved old deadline defect; do not “fix” it based on headline alone. |
| F6 metering/AI/BI lifecycle | final E035 consumer; `aiBiImportParser.parseAiBiListCsv`, `aiBiImportEngine.importAiBiListCsv` (46), `aiBiReconciliation.approveAiBiDiscrepancy` (57), normalizeMeteringValues and billing matcher | Reconcile E038–E046/E062: actual import/header/legal identifiers/product/period/history → discrepancy → approved mutation boundaries and grant/retention. Locate export callers before alleging BI role leakage; they were not traced in this bounded pass. Aggregate/forecast/no-billing separation must be proven through actual dispatcher. |
| F7 release/readiness + G03 | `certificationEvidence`, TGT registries, outbox readiness, CI/native replay/manifests and eventual deployed release metadata | Bind actor/role/environment/source/test ID/payload hash/result to exact release; distinguish unit/native evidence from formal market approval. Retrieve available test definitions/receipts before declaring external blockage. Honor paused PR310 dependency; do not restart it. |

For each later row, first search retained quality receipts and actual callers, classify covered versus missing integration versus missing operational/legal evidence, then implement only concrete gaps. This preparation inspected entrypoints and requirements; it did not adjudicate all later-phase code. No new later-phase runtime bug or externally unavailable source is claimed.

## Delivery discipline

One active implementation at a time after E035. Each finite change carries source oracle, production caller, minimal meaningful RED/GREEN or evidence-only reconciliation, independent review, and required exact-head checks. Preserve prior accepted work and current dirty closure files. No broad run, frozen-manifest mutation, live send, deployment or automatic merge is authorized by this document.
