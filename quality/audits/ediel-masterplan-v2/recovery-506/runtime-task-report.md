# Incoming field506 runtime recovery — candidate for independent review

Runtime/test candidate `e64d132dfa7bbd6f4eb6af66475cd2467bb72430`, tree
`928e4bc62e9cf354eae03f192830e47d7b518768`. This report and its compact ledger are
an evidence-only follow-up commit; the final delivery message supplies that exact
all-files commit/tree. Root's intervening memory checkpoint commits are not author
changes. Recovery start `20a2b410`; accepted runtime PR350 remains unchanged.

## Result and scope

One pure incoming owner, `lib/ediel/prodat/prodatEnergyProduct.ts`, binds actual
first-message BGM/UNB and own physical LIN reason to required/false/unknown506.
Own Z13 is required in the valid permission process; own positive Z14 requires506,
N excludes it, invalid/absent/aliased/duplicate/late/cross-object reason is unknown.
Unknown market/process never becomes a positive506 case. Receiver snapshots,
byCell, another LIN, later message and supplied506 cannot supply the predicate.
Idless source-valid Z13/N retain explicit absent209 and own LI metadata.

Required missing/blank fifth7110 produces typed41/506; supplied wrong value,
length,1131/3055 or placement produces typed42/506. False and unknown produce no
new506 error or hold. Required valid semantic value is separately exposed by the
owner; descriptive `parseProdatMessage.energyProductId` and raw wire remain intact.
No business506 writer/producer was created.

Canonical incoming506 bypasses the old generic matrix/D loops; knownfalse242 is
excluded only for its authorized shared-slot functions. Actual applicable242
owners remain. Z06 receives actual direction: incoming fifth506-only pairs do not
count as242 or trailing242 content; outgoing validation remains strict. Empty
false C889 tests assert no fabricated national242/506 error, never full grammar
acceptance. Syntax, independent207/223/311 faults, existing GAS boundaries and
applicable242 controls remain separately owned.

Direct `decideProdatAperak` projects the same typed result. The shared genuineF
review guard runs at actual `resolveBackendAperakDecision` after PRODAT/role checks
and before TGT lookup, permission context/validation, event writes or positive
return. It also runs at independently callable registry derivation, before empty
issues, positive TGT shortcuts and validation/detail writes. No invented manual
ERC mapping was added. Valid/false/U preserve established permission outcomes,
including unmatched-context40/105, invalid-status41/322 and warning events.
The original Z14N A76 manual status negative is preserved, not reclassified as a
506 failure. Incoming preflight no longer labels unused fourth242 as506; outgoing
component mismatch checks are unchanged.

## Fresh evidence and qualification

Original PDF SHA256 remains
`83c2f1d2915851d2e670731f6ab404ef06c9b9def282afbafdfa0eda836a6e95`.
Full original pages20/68/119/122 were extracted, rendered and visually read;
examples137/138 qualified permission fixtures, P65 resolved the own-reason edge,
and P140 qualified the final Z18 control. Synthetic data only.

- New initial source fixture attempts failed because UD carried excluded address
  fields, then positive Z14 lacked IT. These are preserved setup failures, not
  normative RED evidence. Qualified R2: five clean Z13/Z14 V/VH/N controls passed;
 21 source-literal defect cases failed across three UNA alphabets.
- Before integration, actual manual resolver body + real permission validation,
  independent registry and direct consumer had7 controls PASS/12 normative FAIL.
  This executes the actual exported resolver function body extracted with the
  TypeScript AST, with only external TGT/context/event dependencies substituted.
  It is not a whole-module/action invocation or live DB test.
- The first guard insertion accidentally targeted an earlier UTILTS helper TGT
  call. Actual manual tests caught all four missing/invalid Z13/Z14 cases still
  returning positive. The guard was moved to the exact PRODAT entry; no UTILTS
  change remains. Combined106/106 then passed.
- Expanded scope's first run included three missing policy application-reference
  setup errors and one repeated UNA setup error. These remain separate from the
  incoming preflight RED. Initial test typing found five annotation errors;
  corrected typing passed. No new explicit-any annotation was used.
- Actual inbound processing tests execute persistence and draft construction,
  replacing external DB/kernel/staging effects only. Persisted JSON and actual
  ACK wire retain41/506 or42/506 and own escaped LI; false506 adds no negative/hold;
  actual invalid fourth242 survives independently. These consumer regressions
  were added after the first canonical RED, not falsely claimed as earlier TDD.
- The only two old assertion edits are the preauthorized incoming506 expectations
  in meter-change-inbound and meter-change-review-regression. The earlier affected
  run had3209PASS/4FAIL solely at those two lines (one has three alphabet cases).
  Their242 assertions and all outgoing tests remain intact.
- Post-full self-review found an overstrict new own-reason gate: unused fourth/
  fifth CAV values incorrectly demoted exact S17 to U. The P65/119 probe gave
  1PASS/1FAIL. Only that helper gate changed; wrong1131/3055 still leaves the reason
  unknown. No general223 or outgoing owner changed. Root authorized this bounded
  correction. Final Z18 pass-through fixture was also refined from a Z14 mutation
  to complete original-example-based Z18; its clean and falseextra canonical
  controls both pass. Valid idless Z13 registry pass-through was added.

## Verification

Local Node24.19.0 only; exact Node22 CI remains root's gate.

| Executed verification | Result |
| --- | --- |
| Full Vitest,297 files, implementation492990fe | 4464/4464 PASS |
| Final focused,8 files, runtime candidatee64d132d | 295/295 PASS |
| Final app/test typechecks, scoped lint and diff check | PASS |
| Earlier same-unit scripts typecheck and full lint | PASS |
| Original-source integrity | PASS33 originals/121 rules/231 contracts |
| Eight unchanged Ediel strip-only scripts | PASS818/818 |
| Tenant integrity/shutdown/service-role ratchet | PASS |
| Large-file budget, performance, route readiness | PASS |

Full/route used
`NODE_OPTIONS='--max-old-space-size=4096 --require=./scripts/lib/refactor-safe-static-read.cjs'`.
**The full4464 receipt predates the final own-reason correction and final fixture
refinements.** Per root instruction, final targeted/type/lint verification follows
that correction; no optional full rerun was performed. This is not an exact-final
Node22/full-CI claim. Exact commands, results, task paths and SHA256 fingerprints
are in `runtime-evidence.json`. Large untouched logs remain in
`/workspace/scratch/2a201d6d5897/field506-recovery-evidence/`.
The first compact RED receipt accidentally had an empty hash map due filename
suffix filtering; its original bytes remain, and implementation/final ledgers
supply actual hashes. No unavailable historical probe was reconstructed or
presented as recovered/fresh.

## Remaining gates and limits

Ready for root's independent completed-runtime review, exact Node22 CI and guarded
publication/merge gates. No source approval, counts, F3 closure, production
readiness or live delivery acceptance is inferred. Historical98/110 plus10/10
parent accounting and paused PR310 are untouched. Source/frozen data, schema,
codec, loader, workflows, budgets, thresholds and live services were not changed.
No live DB/provider/send/deploy/settings action occurred. The existing idless Z13
legacy facility negative and independent permission semantics remain outside this
unit. Root owns memory, remote checkpoints, CI and acceptance.
