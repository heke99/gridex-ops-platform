# Exact362 nonunique added-index preservation

This bounded implementation applies the existing explicit positive review in `PR310_ADDED_CONSTRAINT_INDEX_INDEXES_2026-09-15.md` to283 ordinary nonunique access paths and79 source-generated FK access paths. It pins the complete review/register and all66 original source files represented by that index register. Every selected identity must retain its complete six-field catalog hash, exact declaration line and positive nonunique disposition. The source register's existence or schemaAccepted=false metadata is not treated as proof of runtime success.

The other89 additions—31 primary-key backing indexes, eight UNIQUE-constraint backing indexes,25 tenant composite unique keys and25 business uniqueness rules—remain outside this module. Changed and removed indexes are also excluded. Preserving a nonunique access path is not a performance or authorization claim.

The native parent witness reads all362 exact index definitions and private pg_index valid/ready/live flags from the admitted actual target. It rejects missing, duplicate, altered, unique or invalid indexes, including changed predicates or ordered keys because the full pg_get_indexdef row must match. Execution must preserve catalog/rows, ledger and all retained source bytes. No DDL or row mutation is performed. A distinct `addedNonuniqueIndexWitness` receipt is required by the decision context; a register or mocked test result cannot stand in for actual execution.

Root integration remains required: retain before staging; call execute_parent on the existing same Runner after finalSQL with before/after forward admission; copy the strict receipt into the same native comparison and final type/release witness bindings; add only approved()'s362 added-index mappings and validate_context(diff). Existing native provenance, full finalSQL and unsupported-difference gates must remain in force. This module does not accept a complete schema independently.

Seven tests PASS for exact362/89 separation, all68 pinned-file mutation controls, native definitions/state/missing/duplicate/unknown rejection, exact receipt/source/ledger guards, actual-parent ledger preservation, external-target rejection and read-only SQL shape. Actual native catalog execution remains pending. No central gate, runtime integration, historical migration or reference was modified by this task.
