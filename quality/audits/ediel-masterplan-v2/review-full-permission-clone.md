# Whole permission candidate: complete portable clone qualification

This adds an executable qualification harness, not a migration or an acceptance decision. Actual fa29 run35013041700/job104529480114 reported exactly `can_override_allow` and `can_override_deny` failing among the original24 changed-function cases, with cleanup verified. The harness must reproduce precisely those two failures on its own complete replay before testing repair.

The unchanged candidate SHA256 is `73eb8a8d89b8468783d69f2829658daeb71d7deda667b5f54a9c5650cd6c47ad`. The harness repeats the existing portable construction: selected144 foundation inputs,514 timestamp inputs, immutable10 forwards, and allfive pinned final SQL checks. It then creates `gridex_auth_legacy_atomic` from the replay database inside the same newly owned container. No external database selector, production connection or migration promotion is accepted.

Before mutation, complete non-system catalog, domain-row fingerprints and real ledger must match the original parent. The exact candidate is applied only to the atomic clone. Eight exact function identities, their candidate-source bodies and declared metadata are checked, including private functions. The full catalog permits only these function changes, gridex_private schema ACL change, and bounded source-function dependencies to declared schemas/languages/signature types. All other catalog objects, rows and ledger remain unchanged. Effective function ACL checks run with only a rollback-local assertion helper; they grant no application permissions.

The unchanged24-case SQL must then pass and roll back completely. Candidate repeat must preserve the full postcandidate snapshot. A deliberate poison grants only database-local schema/function privileges to existing roles; applying the exact candidate again must recover the complete postcandidate snapshot and pass effective ACL checks. Cluster role definitions or memberships are never changed. Custom-role/inherited-grant recovery remains outside this full-clone proof; it was separately exercised by the existing reduced-source qualification.

The separately reviewed full-schema seed adapter supplies129 original permission/storage/admin/diagnostic cases with bounded identity and seed adaptations. Every case owns its fixture transaction, must emit its exact terminal marker, and must restore the complete clone catalog, rows and ledger before the next case. No original fixture source-composition or production table/storage grants are replayed. See `review-full-permission-seed.md` for its exact scope.

Original parent snapshot, source bytes, immutable migration tree, owned cleanup and private workspace disposal remain required. Only fixed stages, validated SQLSTATEs, finite case labels, hash-only unexpected catalog keys and finite metadata field names may leave failure paths. Qualification success reports129matrix cases,24behavior cases, repeat/localACL recovery, and no promotion/schema/type acceptance. Live native SQL execution is still required; offline tests prove only fail-closed orchestration and privacy boundaries.
