# PR #372–#384 stack integration checkpoint

2026-09-25. PR #310 is excluded. `main` remains `2a148d39d631fc759c99cd1c69350b5e2147dbdb`. All original exact PR heads #372–#384 completed their applicable ordinary non-staging workflows successfully (crawler skips are expected). PR #382 had already merged into #381. On explicit user authorization, #384 → #383 → #381 → #380 → #379 → #378 → #377 → #376 → #375 → #374 → #373 were merged into their actual parent branches with expected-head locks. The final parent merge commit is `54c27ff6847a871c16ec04442812e893ceb020df` on draft PR #372. Its tree `d881795213c90d96d3355e55a24320e52c07cc29` is identical to original PR #384's fully green code tree and local `97254f78b910c9cc0593aedc9d4b801d73584ab4`. The new combined PR #372 head has its own ordinary CI pending. No staging/TGT/AGT/counterparty or market send was run.

Review of bounded child production deltas: #373 masks functional findings for guide-rejected UTILTS identities, #374 holds malformed PRODAT register objects before the case writer, #375 deduplicates the same physical field 213 diagnostic, #376–#384 project the source-backed UTILTS header 501/502/313/206/205/204/203/202 checks into final dispositions and ACK. Existing real-consumer tests cover the relevant APERAK/ERR and no meter/billing effects with external boundaries mocked; each child passed its own ordinary CI. The original PR #372 code head had an independent whole-diff read-only approval and native 340/340 with authentic generated types/schema per `pr372-acceptance-matrix-20260925.md`; that approval was bounded, not full E035 acceptance. The integrated final head is now 185 files and 31 SQL migrations against main, so it requires its own exact-head gates before any main merge.

Three inherited audit files had an extra empty EOF line, detected by `git diff --check` over the full main-to-stack diff; the follow-up removes only those lines. No runtime, SQL or test semantics change from that cleanup. Recheck diff and ordinary CI on the published cleanup head.

**Release boundary:** `.github/workflows/vercel-production-deploy.yml` runs on every push to `main` and creates a Vercel production deployment if `VERCEL_TOKEN` exists. The connected Vercel project lists a READY production deployment for each recent main merge, including #370 and #371. A main merge therefore causes production activity, while the current user scope still prohibits production runs. PR #372's process archive also has no assigned lawful retention/purge policy; `epochs.complete` remains constrained false and E035 authority holds, so broad rollout is not accepted. Do not describe a main merge as CI-only or call the full masterplan complete. The final main action requires reconciling this production consequence with the user's stated no-production boundary after all code and CI evidence is ready.

Remaining implementation groups stay open: field202 S-code C002/1131 and 1001, field203 tenant-scoped uniqueness/ERC42, E66 209/533 distinct identity/storage, F3C-02/04/05/06/07, source-backed F1/F2/F4–F6. External staging, counterparty and market gates remain deferred.
