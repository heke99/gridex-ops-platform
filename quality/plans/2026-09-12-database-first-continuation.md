# Database-first continuation through masterplan86

User authorized execution of the recommended order: canonical replay/types, the two retained runtime defects, complete RLS/permissions, billing transactions, jobs/starvation, paused API remainder, then verified merge/deployment.

Baseline: PR310 head728d9d4803d6d190279665f09f165fa7a719a343. No completed application batch is restarted. The pending partner-price patch stays unapplied until its turn.

## First action: find the next real SQL frontier

The normal full-effects admission correctly rejects23 substituted and14 unclassified inputs. It must remain fail-closed. The foundation loop already contains a selected118-source branch but accepted native evidence ends at77. A separate explicitly diagnostic runner now invokes that existing source-pinned foundation on a new network-disabled owned PostgreSQL17 container, retaining independent prefix references, private logging, source checksums, and exact cleanup. It does not weaken normal replay admission, mark a ledger, emit generated types, or claim complete replay acceptance. A successful diagnostic is not a verified full-effects migration chain.

Executed before publication: six source-selection/receipt/privacy constructor tests pass. The600-source inventory remains558 whole selected/23 substituted/14 unclassified/5 excluded. Native diagnostic execution remains pending GitHub CI. Local first43 SQL sources were additionally executed on an isolated native PostgreSQL17.11 database with no TCP listener; this is exploratory compatible-schema evidence, not hosted/canonical acceptance.

The source-only artifact is for restoring the exact current repository tree to the local isolated test workspace. No secrets, database rows or untracked files are included. Remove its export job after retrieval.

## Verification discipline

Retain the two existing red gates, all immutable migrations and manifests, the latest application code, and the paused API patch. Diagnose each failing frontier, add source-backed fixes and meaningful negative tests, then publish coherent batches. No production Supabase mutation, main merge, or deployment is authorized by a diagnostic success alone.

Skill routing: existing plan execution, systematic debugging, code/source review, database/tenant invariants, isolated workspace handling and verification-before-completion. Use TDD for actual defect fixes. UI, hooks and performance refactors are not involved. No independent-agent review is claimed.
