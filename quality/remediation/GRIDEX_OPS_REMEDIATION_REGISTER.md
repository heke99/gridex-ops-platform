# Gridex OPS master remediation register

Canonical status values: `CONFIRMED_OPEN`, `LIKELY_OPEN`, `IN_PROGRESS`, `CODE_REMEDIATED`, `AWAITING_STAGING`, `VERIFIED_CLOSED`, `BLOCKED`, `FALSE_POSITIVE`, `SUPERSEDED`.

No item may become `VERIFIED_CLOSED` until code/migration review, negative and positive tests, relevant two-tenant coverage, staging verification and CI on the exact PR head are green.

| ID | Source | Severity | Confidence | Status | Root cause | Components / database objects / code | Dependencies | Planned branch / PR | Test and staging requirement | Rollback / forward-fix | Verification evidence | Residual risk |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GRIDEX-OPS-BL-001 | Baseline BL-001; reconciliation | Critical | Confirmed | CONFIRMED_OPEN | Seven write-policy families accept raw tenant membership as an OR bypass instead of checking active status and the required write permission. | Seven baseline-listed tables/policies; canonical permission helpers; related UI/API writes | BL-002 reviewed first; serialize overlapping RLS work | `remediation/gridex-ops-bl-001-write-permissions` / pending | INSERT, UPDATE and DELETE separately for owner/admin/operations/compliance/read-only/inactive/cross-tenant/service role; staging two-tenant run | Drop only new policies and restore prior definitions if rollback is approved; preferred forward-fix is narrower helper/policy repair | Baseline policy query and role-catalog evidence | Highest remaining security item; exact seven-table inventory must be revalidated against latest main/live schema before implementation. |
| GRIDEX-OPS-BL-002 | Baseline BL-002; live non-production reproduction | High | Confirmed | CODE_REMEDIATED | Four platform-global read policies use `auth.uid() is not null`, exposing operational rows to every authenticated principal. | `actor_registry_conflicts`, `actor_registry_import_items`, `actor_registry_import_runs`, `ediel_certificate_refresh_jobs`; admin import-history page; service-role import/certificate workers | None; first remediation workstream | `remediation/gridex-ops-bl-002-global-read-isolation` / PR #84 | Persistent non-production apply completed; two-company role matrix and service-role CRUD passed; exact-head CI, browser/session smoke and log inspection remain | Recreate old policy only for emergency rollback; preferred forward-fix keeps explicit platform-admin/service-role policies and adjusts a proven consumer | Migration `20260806122255` applied on `gridex-ops-dev`; exactly eight explicit read policies; two-tenant rollback regression passed; existing-role smoke passed; service-role actor-registry/certificate-job CRUD smoke passed | Not closed until exact-head CI, human database/security review, browser `/admin/network-owners` verification and application-level worker/log smoke are complete. |
| GRIDEX-OPS-BL-006 | Residual BL-002 variant hunt; PHASE-45 O-005/O-006/O-007 | High | Confirmed | CODE_REMEDIATED | Broad authenticated SELECT remains on contacts and address/energy lookup caches; admin import history can silently empty when app/RLS platform-admin helpers diverge. | `platform_actor_contacts`, `platform_address_lookup_cache`, `platform_energy_lookup_cache`; `/admin/network-owners` import history; energy resolver cache; admin contact export | After BL-002 merge on main | `cursor/codebase-health-and-stability-6fc0` | Static regression + migration integrity passed; SQL two-tenant rollback pending staging apply | Emergency rollback recreates prior broad read policies; preferred forward path keeps platform-admin/service-role policies and service-role import-history read after app gate | Migration `20260807154500` checksum `eaf7b629…`; static regression green; O-007 consumer hardened | Not closed until staging SQL regression, exact-head CI and admin smoke are complete. |
| GRIDEX-OPS-BL-003 | Baseline BL-003; Supabase advisor; V3 SEC-001 | Medium | Confirmed advisor signal | CONFIRMED_OPEN | Leaked-password protection is disabled or not independently verified. | Supabase Auth settings; signup, reset and password-change UX | Separate non-production Auth test and explicit production approval | `remediation/gridex-ops-leaked-password-protection` / pending | Compromised test password rejected; strong password accepted; signup/reset/change paths; non-leaking messages | Revert non-production setting if provider incompatibility appears; production change only after approval | Advisor evidence only; dashboard/runtime verification pending | Configuration may differ by environment; connector cannot safely change production Auth setting. |
| GRIDEX-OPS-V3-BUG-001 | V3 `quality/BUGS.md`; reconciliation | Medium | Confirmed | CONFIRMED_OPEN | Customer Portal sync catch block converts controlled 400/413 input errors to generic 500. | `app/api/v1/customer-portal/sync/route.ts`; API error helpers; dedicated regression | Reproduce on current main; donor commit is evidence only | `remediation/gridex-ops-portal-sync-error-contract` / pending | Invalid JSON/field and oversized payload preserve controlled contract; unexpected exception remains generic 500 | Revert route-only change; forward-fix adjusts error classifier without exposing internals | V3 donor fix/regression exists but is not on main | Current main behavior must be rechecked before porting. |
| GRIDEX-OPS-V3-BUG-006 | V3 BUG-006; reconciliation | Medium | Confirmed | CONFIRMED_OPEN | Contract `2026-08-05.2` lacks a complete immutable release set on main. | OpenAPI release blobs, versioned routes, cache headers and release verifier | Reconcile current canonical schema bytes first | `remediation/gridex-ops-openapi-2026-08-05-2-release` / pending | Byte/schema parity, current versus immutable behavior, cache headers, mutation-prevention regression | Remove only newly introduced version route/files if release was never published; otherwise forward-fix with a new version | V3 donor commits and green donor workflow | Immutable published artifacts cannot be rewritten after release. |
| GRIDEX-OPS-V3-BUG-007 | V3 BUG-007; reconciliation | Low | Confirmed | CONFIRMED_OPEN | Reserved local `module` bindings fail Next.js lint. | `lib/customer-portal/tenantSync.ts`, `lib/legal/customerDocumentPackage.ts` | Reproduce lint on latest main | `remediation/gridex-ops-lint-module-bindings` / pending | Lint plus focused regressions, full suite and build; no rule disable | Revert variable rename | V3 donor fix exists | Only static rename expected; protect against unrelated formatting/refactor. |
| GRIDEX-OPS-V3-BUG-008 | V3 BUG-008; reconciliation | Low | Confirmed | CONFIRMED_OPEN | Public-contract tests use stale legal hashes/version fixtures while production validation is intentionally strict. | Four V3-listed tests; immutable legal evidence and current-version headers | BUG-006 release decision affects current-version fixture | `remediation/gridex-ops-contract-test-fixtures` / pending | Reproduce failures; retain strict non-null hash validation and explicit historical-null case; full suite/build | Revert fixture-only changes | V3 donor fixes exist | Avoid “fixing” tests by weakening production validation. |
| GRIDEX-OPS-BL-004 | Baseline performance/RLS advisor; reconciliation | Medium | Likely | LIKELY_OPEN | Large numbers of overlapping permissive policies and per-row helper calls may create OR bypasses and poor plans. | Policy families across public schema; helper calls; indexes and query plans | BL-001 and BL-002 policy repairs first; split by table family | Multiple `remediation/gridex-ops-rls-*` branches / pending | Export inventory; before/after `EXPLAIN`; positive/negative matrices; no mass rewrite | Roll back one family at a time; forward-fix helper or index only after measured evidence | Baseline counted 9,026 policies and 8,644 generated policy variants | Runtime impact and exploitability vary per family; no global one-shot fix. |
| GRIDEX-OPS-BL-005 | Baseline CI evidence; reconciliation | Medium | Confirmed assurance gap | CONFIRMED_OPEN | Deploy checks exist but the full OPS hardening workflow was not running on the audited main SHA. | GitHub Actions, migration/type/lint/test/API/security/build gates | Individual bug/migration branches should establish focused tests first | `remediation/gridex-ops-expanded-hardening-gate` / pending | Each intentional failure must make its own step red; artifacts/logs redacted | Revert workflow-only changes or forward-fix failing command/environment | Baseline found no workflow run on main; V3 donor workflow is evidence | Workflow may be expensive/flaky until dependencies and fixtures are normalized. |
| GRIDEX-OPS-V3-BUG-002 | V3 BUG-002 | Medium | Likely | LIKELY_OPEN | Billing webhook may resolve target before signature validation and expose distinguishable failures. | Billing webhook route, provider contract, signature/replay/idempotency logic | Authoritative provider retry/status contract and safe fixture | Dedicated security branch under WS10 / pending | Unknown ref, known ref/bad signature, stale timestamp, valid and duplicate event | Provider-compatible response normalization; forward-fix preferred | Static V3 observation only | Do not alter externally visible status semantics without provider evidence. |
| GRIDEX-OPS-V3-BUG-003 | V3 BUG-003 | Medium | Confirmed maintainability risk | CONFIRMED_OPEN | `customerApplications.ts` exceeds 8,400 lines and combines many transaction/domain concerns. | `lib/website/customerApplications.ts` and direct dependencies | Characterization tests and dependency graph before extraction | WS11 incremental branches / pending | Critical website, quote, legal/POA, application and two-tenant regressions; build-size/memory checks | Revert each extraction independently; no behavior-changing big bang | Direct file inspection in V3 | File size alone is not a runtime bug; cosmetic splitting is prohibited. |
| GRIDEX-OPS-DOC-001 | V3 BUG-004/005; current memory inspection | Low | Confirmed | CONFIRMED_OPEN | Agent-memory/checkpoint naming and stale phase context differ from `AGENTS.md` and current remediation work. | `AGENTS.md`, `.agent-memory/checkpoint.json`, stale phase files and consumers | Inventory all readers/writers before changing canonical format | Dedicated documentation/process branch / pending | Path-reader tests or script checks; no deletion of audit history | Restore previous paths; forward-compatible alias/migration preferred | `AGENTS.md` references differ from actual `.agent-memory` contents | Documentation drift can misroute agents but must not be bundled into security PRs. |
| GRIDEX-OPS-ASSURANCE-001 | Baseline blocked checks; requested WS9 | High assurance priority | Confirmed gap | CONFIRMED_OPEN | No complete deployed positive/negative two-tenant matrix spans all major domains and actor linkages. | Authentication, sessions, RBAC/RLS, website/customer portal, contracts, legal/POA, billing, documents/storage, jobs, notifications, EDIEL and webhooks | Functional remediations and stable staging fixtures | `assurance/gridex-ops-two-tenant-e2e` / pending | Full matrix with tenant/company/user/customer/contract/request/audit linkage | Test-only branch; failures produce separate remediation branches | Existing focused regressions only | Cannot claim system-wide isolation from sampled RLS checks. |
| GRIDEX-OPS-SEC-PIPELINE-001 | Requested WS10; baseline blocked scanners | High assurance priority | Confirmed gap | CONFIRMED_OPEN | SAST, secret-history scan, dependency reachability and privileged call-site assurance are incomplete on main. | CodeQL/Semgrep/gitleaks-equivalent, Dependabot, action pins, service-role/storage/webhook/input surfaces | Expanded hardening workflow and full checkout | Separate focused WS10 branches / pending | Redacted current-tree/history scans, reachability triage, intentional-failure gates | Revert scanner config separately; never commit discovered secrets | Baseline scanner commands blocked | Absence of findings is not evidence until scanners execute and results are triaged. |
| GRIDEX-OPS-DEPLOY-001 | Requested WS12; V3 deployment/observability report | Medium assurance priority | Likely/partially evidenced | LIKELY_OPEN | Staging health/readiness, structured correlation, saturation/failure alerts and rollback/forward-fix proof are incomplete. | Vercel/runtime, Supabase/Postgres, workers, webhooks, logging/alerts/runbooks | Stable staging and agreed SLOs | Deployment/observability branches / pending | Health/readiness, latency/error/job/webhook/dead-letter/DB saturation alerts and rollback drill | Per-component rollback plus documented forward-fix | V3 and current memory identify deployment/E2E gaps | Requires operational access beyond repository-only checks. |

## Status transition for the first workstream

`GRIDEX-OPS-BL-002` remains `CODE_REMEDIATED`, not `VERIFIED_CLOSED`. The migration is persistently applied on the non-production project as version `20260806122255`; the post-apply two-company regression, existing-role read smoke and service-role CRUD smoke passed. Remaining gates are exact-head CI, human database/security review, browser/session verification of `/admin/network-owners`, and application-level worker/log inspection.

`GRIDEX-OPS-BL-006` is `CODE_REMEDIATED` for the residual contacts/lookup-cache read variants and the related O-007 import-history consumer hardening. Staging SQL rollback regression and exact-head CI remain before `VERIFIED_CLOSED`.
