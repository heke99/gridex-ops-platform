# Gridex OPS remediation skill routing

Generated for the remediation program started from `main` at `ffe4d0b022d82108d902336755d26d5c5d3924ed`.

Status totals: **24 ACTIVE**, **13 CONDITIONAL**, **1 NOT_APPLICABLE**. The status applies to the full remediation program; the final columns identify the first workstream in which the skill is expected to contribute.

| Skill | Status | Concrete reason | Workstream | Files, systems or findings | Expected deliverable or verification |
|---|---|---|---|---|---|
| acquire-codebase-knowledge | ACTIVE | Establishes current architecture, consumers and migration/test conventions before changing security boundaries. | Program start; WS1; all later workstreams | `AGENTS.md`, `.agent-memory/*`, `app/`, `lib/`, `scripts/`, `supabase/` | Evidence-backed system and consumer inventory. |
| brainstorming | ACTIVE | Used to split the audit backlog into low-risk, independently reviewable branches rather than one bulk PR. | Program planning | Audit reports, reconciliation, branch dependency graph | Workstream plan with explicit dependencies and stop conditions. |
| code-review | ACTIVE | Reviews every remediation diff for scope creep, authorization regression and unintended compatibility changes. | WS1 and every PR | Migration, regression, consumer boundary, report | Review checklist and documented residual risk. |
| code-security | ACTIVE | The first finding is an RLS authorization defect and later workstreams include webhooks, storage, SSRF and PII. | WS1, WS2, WS10 | RLS policies, service-role call sites, security-sensitive APIs | Security invariant review and variant search. |
| code-simplifier | CONDITIONAL | May simplify only after behavior is covered; not used to cosmetically refactor security code. | WS11 | `lib/website/customerApplications.ts` and extracted modules | Smaller stable units with unchanged behavior and green characterization tests. |
| codeql | CONDITIONAL | Required for repository-approved SAST when a CodeQL runtime and full checkout are available. | WS7, WS10 | TypeScript/JavaScript data flows and generated SARIF | CodeQL database, SARIF, triage and false-positive decisions. |
| differential-review | ACTIVE | Donor audit branches must be compared to `main` without being merged or blindly cherry-picked. | WS4-WS8 | PR #74 commits versus current `main` | Per-fix donor diff map and main-based reimplementation decision. |
| dispatching-parallel-agents | CONDITIONAL | Useful only for non-overlapping read-only analysis; overlapping RLS migrations must remain serialized. | WS8-WS12 | Independent audit domains and reports | Parallel evidence collection with one active DB/RLS writer. |
| executing-plans | ACTIVE | Enforces the ordered workstream plan and one-finding-per-PR discipline. | All | Remediation register and workstream reports | Traceable execution against acceptance gates. |
| find-bugs | ACTIVE | Searches for adjacent defects while each root cause is being corrected. | WS1 and all later workstreams | Policy catalog, call sites, tests and failure paths | Stable IDs and evidence for any newly discovered variants. |
| finishing-a-development-branch | ACTIVE | Every branch must finish with diff review, evidence, draft PR and no automatic merge. | Every workstream | Git branch, commit, PR and CI state | Draft PR with exact head SHA and completion checklist. |
| fp-check | ACTIVE | Audit and advisor signals are not accepted without exploitability or contract evidence. | WS1, WS2, WS3, WS8, WS10 | `pg_policy`, grants, helpers, Auth settings, scanner results | Confirmed finding, false positive or explicitly blocked classification. |
| install-hooks | NOT_APPLICABLE | Installing repository hooks from a remote connector would mutate developer environments and is not needed for this remediation. CI and documented local commands provide the gate instead. | None | Local Git hook configuration | No hook installation; equivalent CI/local checks are documented. |
| property-based-testing | CONDITIONAL | Appropriate for authorization matrices, idempotency and parser invariants once deterministic fixtures are available. | WS2, WS9, WS10 | RBAC combinations, webhook replay, API inputs | Generated edge-case matrix without weakening explicit regressions. |
| quality-playbook | ACTIVE | Provides the common evidence, severity, test and completion model for all workstreams. | Program and all workstreams | Register, reports, CI and staging evidence | Consistent quality gates and status transitions. |
| receiving-code-review | CONDITIONAL | Activated when maintainers submit review comments; fixes must stay within the branch invariant. | Every draft PR | GitHub review threads | Addressed comments with rerun evidence and no unrelated refactor. |
| refactor | CONDITIONAL | Allowed only for stable boundaries with characterization coverage, especially the 8,400-line application module. | WS11 | `customerApplications.ts` dependency graph | Incremental extraction with behavior parity. |
| requesting-code-review | ACTIVE | Database and security PRs require explicit human review before staging or merge. | Every workstream | Draft PR description and required reviewers | Review request containing risk, rollback and test evidence. |
| sarif-parsing | CONDITIONAL | Used only after CodeQL, Semgrep or secret scanners produce SARIF. | WS7, WS10 | Scanner artifacts | Deduplicated, redacted findings linked to register IDs. |
| scan-secrets | CONDITIONAL | Required for current-diff and history scans when a full checkout/scanner is available; secrets must be redacted. | WS7, WS10 | Git diff, current tree and history | Redacted scan result with no secret values in reports. |
| security-threat-model | ACTIVE | Defines trust boundaries for platform-global data, tenant sessions, service-role workers and external providers. | WS1, WS9, WS10 | RLS, API keys, service role, webhooks, storage | Threat model deltas and security invariants. |
| semgrep | CONDITIONAL | Needed for expanded hardening and focused custom checks when scanner runtime is available. | WS7, WS10 | TypeScript, SQL and workflow files | Semgrep findings/SARIF plus fail-on-intentional-defect proof. |
| sharp-edges | ACTIVE | Identifies dangerous defaults such as broad grants, `auth.uid() is not null`, nullable tenant keys and service-role bypass. | WS1, WS2, WS8, WS10 | Policy/grant catalog and privileged helpers | Explicit sharp-edge inventory and defensive controls. |
| spec-to-code-compliance | ACTIVE | Maps audit/reconciliation requirements to concrete migrations, routes, tests and CI gates. | All | Baseline, V3 and reconciliation reports | Requirement-to-file/test traceability. |
| subagent-driven-development | CONDITIONAL | Can assist only on isolated non-overlapping modules; security migrations remain under one owner. | WS7, WS10, WS11 | Independent tests, documentation or scanner triage | Bounded patches reviewed by the primary owner. |
| supabase | ACTIVE | Required to inspect live non-production policy catalog, migrations, Auth advisories and execute rollback-only verification. | WS1-WS3, WS8-WS10 | Project `piidsfebjqjmnepdpnas`, RLS, Auth, migrations | Read-only evidence and isolated migration/test verification. |
| supabase-postgres-best-practices | ACTIVE | Guides RLS policy shape, `(select auth.uid())`/helper use, grants, indexing and plan analysis. | WS1, WS2, WS8, WS11 | PostgreSQL policies, helpers, plans and indexes | Fail-closed policies with measured performance and no OR bypass. |
| supply-chain-risk-auditor | CONDITIONAL | Activated for dependency reachability, lockfile and GitHub Action provenance work. | WS7, WS10 | `package-lock.json`, workflows, action pins | Reachability-based advisory decisions and remediation PRs. |
| systematic-debugging | ACTIVE | Requires reproduction before changes and verification of the precise root cause after changes. | WS1, WS4, WS6 | RLS reproduction, API error contracts, lint/test failures | Reproduce → isolate → fix → regress sequence. |
| test-driven-development | ACTIVE | Security regressions are written alongside each fix and must demonstrate negative and positive paths. | WS1, WS2, WS4, WS6 | SQL/Node/Vitest/Playwright regressions | Tests that fail before the fix and pass after it. |
| threat-model-analyst | ACTIVE | Performs attacker/asset/entry-point analysis beyond scanner output. | WS1, WS9, WS10 | Tenant data, global operational data, provider boundaries | Abuse cases and mitigations tied to register entries. |
| using-git-worktrees | CONDITIONAL | Recommended for local execution, but the connector has no authenticated local checkout. | Every workstream when run locally | Local repository worktrees | One clean worktree per active branch; no audit-branch mutation. |
| using-superpowers | ACTIVE | Coordinates the installed skill contract and prevents skipping relevant capabilities without evidence. | Program start and every workstream | `skills-lock.json`, routing table | Complete 38-skill routing and per-report activation list. |
| variant-analysis | ACTIVE | Mandatory search for adjacent policies/call sites sharing the root cause. | WS1 and all security/bug workstreams | `pg_policy`, nullable tenant keys, API error handlers, fixtures | Variant query, scope result and new stable IDs where needed. |
| verification-before-completion | ACTIVE | Prevents `VERIFIED_CLOSED` without negative/positive/two-tenant/staging/CI evidence. | All | Register statuses and exact PR-head checks | Honest completion state with blockers retained. |
| web-design-guidelines | CONDITIONAL | Relevant only when frontend/accessibility findings are remediated; WS1 has no visual change. | WS9, WS11 or dedicated frontend PR | Admin/customer portal UI and accessibility report | UI review without bundling into database fixes. |
| writing-plans | ACTIVE | Produces ordered, dependency-aware remediation and rollout plans. | Program start and every workstream | Workstream plan and branch sequence | Detailed executable plan with staging/rollback gates. |
| writing-skills | ACTIVE | Ensures reports, PR descriptions and runbooks are precise, redacted and reusable. | Program and all workstreams | `quality/remediation/*`, PR body, incident/rollback docs | Clear evidence records without secrets or unsupported claims. |

## Workstream 1 activation summary

The first branch directly uses: `acquire-codebase-knowledge`, `code-review`, `code-security`, `differential-review`, `executing-plans`, `find-bugs`, `finishing-a-development-branch`, `fp-check`, `quality-playbook`, `requesting-code-review`, `security-threat-model`, `sharp-edges`, `spec-to-code-compliance`, `supabase`, `supabase-postgres-best-practices`, `systematic-debugging`, `test-driven-development`, `threat-model-analyst`, `using-superpowers`, `variant-analysis`, `verification-before-completion`, `writing-plans` and `writing-skills`.
