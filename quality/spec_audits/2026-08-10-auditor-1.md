# Auditor 1 — Public API and data-flow architecture

Method: fresh sequential pass focused on request/data flow. No conclusions from the code-review report were used until citations were re-read. The no-parallel constraint means this is procedural independence within one model, not an independent model identity.

### lib/customer-portal/apiData.ts

- **Line 203:** DIVERGENT [Req: formal — master points 4–5]. listPortalContracts caps the database at 100 before the route-level pager. The same pattern is repeated in invoice/site/document/event helpers. Records beyond the cap cannot be traversed. Matches BUG-001.
- **Line 192:** DIVERGENT [Req: formal — master point 7]. listWithSchemaFallback returns [] after every schema query fails. This is false business data rather than platform_schema_not_ready. Matches BUG-003.

### app/api/v1/customer/invoices/[id]/route.ts

- **Lines 27–48:** DIVERGENT [Req: formal — master point 6]. Detail calls listPortalInvoices and scans its capped projection rather than resolving the public reference directly. Matches BUG-002.

### lib/integrations/writeIdempotency.ts

- **Lines 173–225:** DIVERGENT [Req: formal — master points 8–9 and 35]. Completion is best effort after the business mutation, so stored replay state may not describe the success already returned. Matches BUG-005.

### lib/integrations/webhooks.ts

- **Lines 57–175:** DIVERGENT [Req: formal — master points 36–38]. The public event contract is a recursive blacklist and secret resolution permits a global fallback. Matches BUG-009.

### lib/website/publicContracts.ts

- **Lines 2325–2355 and 2588–2605:** DIVERGENT [Req: formal — master points 31–33]. Hot reads select all view columns; the route-level ETag is evaluated only after feed construction. Matches BUG-012.

### lib/customer-portal/customerResolver.ts

- **Lines 118–170 and 234–348:** PHANTOM [Req: formal — master points 29–30]. A canonical atomic policy is present in connected dev, but the application still executes divergent fallbacks with inconsistent ambiguity limits. Matches BUG-013.

Conclusion: 7 defects, no spec bugs, no net-new candidate.
