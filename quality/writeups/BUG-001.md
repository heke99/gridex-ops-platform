> Generated by Quality Playbook v1.5.6 — 2026-08-10

# BUG-001: Portal list queries truncate before pagination

## Summary

The lib/customer-portal/apiData.ts:195-620; lib/customer-portal/publicDto.ts:424-453 code path violates REQ-002: Resource helpers apply fixed database limits before the shared offset cursor slices the materialized array. In practice, Create 1,001 tenant/customer invoices with shared timestamps and traverse pages of 100.

## Spec reference

- Requirement: REQ-002
- Spec basis: ../../upload/Pasted markdown(20260810-093430).md:70; quality/REQUIREMENTS.md (REQ mapping)
- Behavioral contract quote: “Pagination ska ske direkt i PostgreSQL med keyset/cursor.”

## The code

At lib/customer-portal/apiData.ts:195-620; lib/customer-portal/publicDto.ts:424-453, the current implementation does not enforce the required boundary. Resource helpers apply fixed database limits before the shared offset cursor slices the materialized array. The contract instead requires: Apply a tenant-bound tuple keyset in PostgreSQL, fetch limit+1 and derive the opaque next cursor.

## Observable consequence

Trigger: Create 1,001 tenant/customer invoices with shared timestamps and traverse pages of 100. The observable result is that resource helpers apply fixed database limits before the shared offset cursor slices the materialized array. The expected outcome is: Apply a tenant-bound tuple keyset in PostgreSQL, fetch limit+1 and derive the opaque next cursor.

## Depth judgment

The failure is masked in small or happy-path fixtures: it appears only with large histories, an injected persistence/schema failure, connected live capabilities, tenant-specific secret state, or conditional cache/auth paths. That explains why broad unit coverage can remain green while the external invariant is broken.

## The fix

The proposed change moves enforcement into the shared boundary named by the requirement so parallel routes cannot silently diverge.

```diff
diff --git a/lib/customer-portal/apiData.ts b/lib/customer-portal/apiData.ts
--- a/lib/customer-portal/apiData.ts
++ b/lib/customer-portal/apiData.ts
@@ listPortalInvoices
-    .limit(100)
    .order('period_start', { ascending: false })
    .order('id', { ascending: false })
    .limit(pageSize + 1)
    // Apply the decoded (period_start,id) cursor in PostgreSQL before reading.
```

## The test

- Regression test: regression_bug_001_reaches_row_1001 in quality/test_regression.test.ts
- Regression patch: quality/patches/BUG-001-regression-test.patch
- Fix patch: quality/patches/BUG-001-fix.patch
- Red receipt: quality/results/BUG-001.red.log
- Green receipt: quality/results/BUG-001.green.log
- Command: npx vitest run --config quality/vitest.config.ts quality/test_regression.test.ts -t BUG-001
- Current result: FIXED; green regression PASS on 2026-08-10.
