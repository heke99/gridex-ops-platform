> Generated by Quality Playbook v1.5.6 — 2026-08-10

# BUG-002: Invoice detail scans only the first invoice page

## Summary

The app/api/v1/customer/invoices/[id]/route.ts:27-48; lib/customer-portal/apiData.ts:312-358 code path violates REQ-003: The detail route calls listPortalInvoices() and searches its capped result in Node. In practice, Place a valid invoice behind 1,000 newer rows and request its listed public reference.

## Spec reference

- Requirement: REQ-003
- Spec basis: ../../upload/Pasted markdown(20260810-093430).md:72; quality/REQUIREMENTS.md (REQ mapping)
- Behavioral contract quote: “Detaljendpointen ska göra en enda tenantbunden lookup.”

## The code

At app/api/v1/customer/invoices/[id]/route.ts:27-48; lib/customer-portal/apiData.ts:312-358, the current implementation does not enforce the required boundary. The detail route calls listPortalInvoices() and searches its capped result in Node. The contract instead requires: Resolve company_id + customer_id + canonical invoice_reference through one indexed query.

## Observable consequence

Trigger: Place a valid invoice behind 1,000 newer rows and request its listed public reference. The observable result is that the detail route calls listPortalInvoices() and searches its capped result in Node. The expected outcome is: Resolve company_id + customer_id + canonical invoice_reference through one indexed query.

## The fix

The proposed change moves enforcement into the shared boundary named by the requirement so parallel routes cannot silently diverge.

```diff
diff --git a/app/api/v1/customer/invoices/[id]/route.ts b/app/api/v1/customer/invoices/[id]/route.ts
--- a/app/api/v1/customer/invoices/[id]/route.ts
++ b/app/api/v1/customer/invoices/[id]/route.ts
@@ GET
-  const invoices = await listPortalInvoices(identity)
-  const invoice = invoices.find((row) => publicPortalInvoice(client.company_id, row).invoice_reference === id)
  const invoice = await getPortalInvoiceByReference(identity, id)
   if (!invoice) return jsonError('Fakturan hittades inte.', 404, 'invoice_not_found')
```

## The test

- Regression test: regression_bug_002_invoice_detail_uses_direct_lookup in quality/test_regression.test.ts
- Regression patch: quality/patches/BUG-002-regression-test.patch
- Fix patch: quality/patches/BUG-002-fix.patch
- Red receipt: quality/results/BUG-002.red.log
- Green receipt: quality/results/BUG-002.green.log
- Command: npx vitest run --config quality/vitest.config.ts quality/test_regression.test.ts -t BUG-002
- Current result: FIXED; green regression PASS on 2026-08-10.
