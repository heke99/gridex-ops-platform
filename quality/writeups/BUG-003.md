> Generated by Quality Playbook v1.5.6 — 2026-08-10

# BUG-003: Exhausted schema fallback becomes an empty success

## Summary

The lib/customer-portal/apiData.ts:186-218; app/api/v1/customer/metering-values/route.ts:43-61 code path violates REQ-004: listWithSchemaFallback returns [] when all schema variants fail, allowing HTTP 200 with no data. In practice, Make every canonical/legacy list query return PostgreSQL missing-column errors.

## Spec reference

- Requirement: REQ-004
- Spec basis: ../../upload/Pasted markdown(20260810-093430).md:73; quality/REQUIREMENTS.md (REQ mapping)
- Behavioral contract quote: “Schemafel får aldrig maskeras som tom data”

## The code

At lib/customer-portal/apiData.ts:186-218; app/api/v1/customer/metering-values/route.ts:43-61, the current implementation does not enforce the required boundary. listWithSchemaFallback returns [] when all schema variants fail, allowing HTTP 200 with no data. The contract instead requires: Raise PlatformSchemaNotReadyError and emit the canonical retryable 503 envelope.

## Observable consequence

Trigger: Make every canonical/legacy list query return PostgreSQL missing-column errors. The observable result is that listWithSchemaFallback returns [] when all schema variants fail, allowing HTTP 200 with no data. The expected outcome is: Raise PlatformSchemaNotReadyError and emit the canonical retryable 503 envelope.

## The fix

The proposed change moves enforcement into the shared boundary named by the requirement so parallel routes cannot silently diverge.

```diff
diff --git a/lib/customer-portal/apiData.ts b/lib/customer-portal/apiData.ts
--- a/lib/customer-portal/apiData.ts
++ b/lib/customer-portal/apiData.ts
@@ listWithSchemaFallback
-  return []
  throw new PlatformSchemaNotReadyError({
    capability: 'customer_portal_read_model',
    blockers: ['portal_schema_variants_exhausted'],
  })
```

## The test

- Regression test: regression_bug_003_schema_failure_is_503 in quality/test_regression.test.ts
- Regression patch: quality/patches/BUG-003-regression-test.patch
- Fix patch: quality/patches/BUG-003-fix.patch
- Red receipt: quality/results/BUG-003.red.log
- Green receipt: quality/results/BUG-003.green.log
- Command: npx vitest run --config quality/vitest.config.ts quality/test_regression.test.ts -t BUG-003
- Current result: FIXED; green regression PASS on 2026-08-10.
