> Generated by Quality Playbook v1.5.6 — 2026-08-10

# BUG-005: Idempotency completion is best effort after business success

## Summary

The lib/integrations/writeIdempotency.ts:173-225 code path violates REQ-005: Completion failures are logged and may leave a committed write in processing after success was returned. In practice, Commit a mutation and make the idempotency completion update fail transiently.

## Spec reference

- Requirement: REQ-005
- Spec basis: ../../upload/Pasted markdown(20260810-093430).md:74; quality/REQUIREMENTS.md (REQ mapping)
- Behavioral contract quote: “Standardisera idempotency för alla externa writes”

## The code

At lib/integrations/writeIdempotency.ts:173-225, the current implementation does not enforce the required boundary. Completion failures are logged and may leave a committed write in processing after success was returned. The contract instead requires: Mutation, required audit and replayable completion must share an atomic or recoverable durable protocol before success.

## Observable consequence

Trigger: Commit a mutation and make the idempotency completion update fail transiently. The observable result is that completion failures are logged and may leave a committed write in processing after success was returned. The expected outcome is: Mutation, required audit and replayable completion must share an atomic or recoverable durable protocol before success.

## Depth judgment

The failure is masked in small or happy-path fixtures: it appears only with large histories, an injected persistence/schema failure, connected live capabilities, tenant-specific secret state, or conditional cache/auth paths. That explains why broad unit coverage can remain green while the external invariant is broken.

## The fix

The proposed change moves enforcement into the shared boundary named by the requirement so parallel routes cannot silently diverge.

```diff
diff --git a/lib/integrations/writeIdempotency.ts b/lib/integrations/writeIdempotency.ts
--- a/lib/integrations/writeIdempotency.ts
++ b/lib/integrations/writeIdempotency.ts
@@ completeIntegrationWriteIdempotency
-  // Completion is best effort after the business mutation.
-  if (error) console.error('[idempotency] completion failed', error)
  if (error) {
    throw new IntegrationWriteIdempotencyError({
      status: 503,
      code: 'idempotency_completion_failed',
      retryable: true,
    })
  }
```

## The test

- Regression test: regression_bug_005_completion_failure_blocks_success in quality/test_regression.test.ts
- Regression patch: quality/patches/BUG-005-regression-test.patch
- Fix patch: quality/patches/BUG-005-fix.patch
- Red receipt: quality/results/BUG-005.red.log
- Green receipt: quality/results/BUG-005.green.log
- Command: npx vitest run --config quality/vitest.config.ts quality/test_regression.test.ts -t BUG-005
- Current result: FIXED; green regression PASS on 2026-08-10.
