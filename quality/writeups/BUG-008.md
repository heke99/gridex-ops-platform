> Generated by Quality Playbook v1.5.6 — 2026-08-10

# BUG-008: Repository migration source is behind the official dev ledger

## Summary

The supabase/migrations/ (ends at 20260809143000) versus connected gridex-ops-dev ledger 20260809180628-20260809191135 code path violates REQ-011: Ten applied statements for indexes, constraints, keysets, auth, identity and fingerprints are absent from the archive. In practice, Compare connected dev ledger versions with files under supabase/migrations.

## Spec reference

- Requirement: REQ-011
- Spec basis: ../../upload/Pasted markdown(20260810-093430).md:127; quality/REQUIREMENTS.md (REQ mapping)
- Behavioral contract quote: “Verifiera clean migration replay.”

## The code

At supabase/migrations/ (ends at 20260809143000) versus connected gridex-ops-dev ledger 20260809180628-20260809191135, the current implementation does not enforce the required boundary. Ten applied statements for indexes, constraints, keysets, auth, identity and fingerprints are absent from the archive. The contract instead requires: Restore the exact official statements so clean replay and catalog parity reconstruct connected dev.

## Observable consequence

Trigger: Compare connected dev ledger versions with files under supabase/migrations. The observable result is that ten applied statements for indexes, constraints, keysets, auth, identity and fingerprints are absent from the archive. The expected outcome is: Restore the exact official statements so clean replay and catalog parity reconstruct connected dev.

## Depth judgment

The failure is masked in small or happy-path fixtures: it appears only with large histories, an injected persistence/schema failure, connected live capabilities, tenant-specific secret state, or conditional cache/auth paths. That explains why broad unit coverage can remain green while the external invariant is broken.

## The fix

The proposed change moves enforcement into the shared boundary named by the requirement so parallel routes cannot silently diverge.

```diff
diff --git a/supabase/migrations/20260809182215_authenticate_integration_request_v1.sql b/supabase/migrations/20260809182215_authenticate_integration_request_v1.sql
new file mode 100644
--- /dev/null
++ b/supabase/migrations/20260809182215_authenticate_integration_request_v1.sql
@@
-- Restore the exact statement array from the official connected-dev
-- supabase_migrations.schema_migrations ledger. Do not reconstruct by hand.
-- Verified ledger version: 20260809182215.
```

## The test

- Regression test: regression_bug_008_live_migration_tail_is_present in quality/test_regression.test.ts
- Regression patch: quality/patches/BUG-008-regression-test.patch
- Fix patch: quality/patches/BUG-008-fix.patch
- Red receipt: quality/results/BUG-008.red.log
- Green receipt: quality/results/BUG-008.green.log
- Command: npx vitest run --config quality/vitest.config.ts quality/test_regression.test.ts -t BUG-008
- Current result: FIXED; green regression PASS on 2026-08-10.
