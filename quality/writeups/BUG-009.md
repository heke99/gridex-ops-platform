> Generated by Quality Playbook v1.5.6 — 2026-08-10

# BUG-009: Webhook projection and signing are not tenant-specific closed contracts

## Summary

The lib/integrations/webhooks.ts:57-175; lib/integrations/tenantWebsiteProvisioning.ts:187-235 code path violates REQ-010: Payloads are blacklist-sanitized and a subscription without a secret reference may use a global fallback. In practice, Provision two subscriptions without secret references and add an unregistered payload property.

## Spec reference

- Requirement: REQ-010
- Spec basis: ../../upload/Pasted markdown(20260810-093430).md:102; quality/REQUIREMENTS.md (REQ mapping)
- Behavioral contract quote: “Skapa en canonical webhook registry.”

## The code

At lib/integrations/webhooks.ts:57-175; lib/integrations/tenantWebsiteProvisioning.ts:187-235, the current implementation does not enforce the required boundary. Payloads are blacklist-sanitized and a subscription without a secret reference may use a global fallback. The contract instead requires: Registered event/version allowlists and per-subscription active/previous secret versions, without weakening durable delivery.

## Observable consequence

Trigger: Provision two subscriptions without secret references and add an unregistered payload property. The observable result is that payloads are blacklist-sanitized and a subscription without a secret reference may use a global fallback. The expected outcome is: Registered event/version allowlists and per-subscription active/previous secret versions, without weakening durable delivery.

## Depth judgment

The failure is masked in small or happy-path fixtures: it appears only with large histories, an injected persistence/schema failure, connected live capabilities, tenant-specific secret state, or conditional cache/auth paths. That explains why broad unit coverage can remain green while the external invariant is broken.

## The fix

The proposed change moves enforcement into the shared boundary named by the requirement so parallel routes cannot silently diverge.

```diff
diff --git a/lib/integrations/webhooks.ts b/lib/integrations/webhooks.ts
--- a/lib/integrations/webhooks.ts
++ b/lib/integrations/webhooks.ts
@@
export const WEBHOOK_EVENT_REGISTRY = defineWebhookEvents({
  'contract.application_received': applicationReceivedEventV1,
})
@@ signingSecret
-  return subscription.signing_secret_ref ?? process.env.WEBHOOK_SIGNING_SECRET_FALLBACK
  if (!subscription.signing_secret_ref) throw new Error('webhook_signing_secret_missing')
  return resolveSubscriptionSecret(subscription.signing_secret_ref)
```

## The test

- Regression test: regression_bug_009_webhooks_fail_closed_per_tenant in quality/test_regression.test.ts
- Regression patch: quality/patches/BUG-009-regression-test.patch
- Fix patch: quality/patches/BUG-009-fix.patch
- Red receipt: quality/results/BUG-009.red.log
- Green receipt: quality/results/BUG-009.green.log
- Command: npx vitest run --config quality/vitest.config.ts quality/test_regression.test.ts -t BUG-009
- Current result: FIXED; green regression PASS on 2026-08-10.
