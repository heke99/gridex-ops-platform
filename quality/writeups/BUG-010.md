> Generated by Quality Playbook v1.5.6 — 2026-08-10

# BUG-010: Registry rate class and live atomic authentication are ignored by runtime

## Summary

The lib/api/publicRouteRegistry.ts:1-67; lib/integrations/apiAuth.ts:294-460 code path violates REQ-008: The limiter uses a flat client limit and authentication performs separate client/company/rate queries instead of the live authenticate_integration_request_v1 RPC. In practice, Call read, write and expensive routes while the atomic auth RPC is present live.

## Spec reference

- Requirement: REQ-008
- Spec basis: ../../upload/Pasted markdown(20260810-093430).md:92; quality/REQUIREMENTS.md (REQ mapping)
- Behavioral contract quote: “Gör rateLimitClass verklig.”

## The code

At lib/api/publicRouteRegistry.ts:1-67; lib/integrations/apiAuth.ts:294-460, the current implementation does not enforce the required boundary. The limiter uses a flat client limit and authentication performs separate client/company/rate queries instead of the live authenticate_integration_request_v1 RPC. The contract instead requires: Pass registry cost into one fail-closed atomic authentication/rate decision and preserve tenant isolation.

## Observable consequence

Trigger: Call read, write and expensive routes while the atomic auth RPC is present live. The observable result is that the limiter uses a flat client limit and authentication performs separate client/company/rate queries instead of the live authenticate_integration_request_v1 RPC. The expected outcome is: Pass registry cost into one fail-closed atomic authentication/rate decision and preserve tenant isolation.

## Depth judgment

The failure is masked in small or happy-path fixtures: it appears only with large histories, an injected persistence/schema failure, connected live capabilities, tenant-specific secret state, or conditional cache/auth paths. That explains why broad unit coverage can remain green while the external invariant is broken.

## The fix

The proposed change moves enforcement into the shared boundary named by the requirement so parallel routes cannot silently diverge.

```diff
diff --git a/lib/integrations/apiAuth.ts b/lib/integrations/apiAuth.ts
--- a/lib/integrations/apiAuth.ts
++ b/lib/integrations/apiAuth.ts
@@ resolveIntegrationApiAccess
-  const client = await loadClient(tokenHash)
-  const company = await loadCompany(client.company_id)
-  const rateLimit = await rateLimitDecision(client, route)
  const { data, error } = await supabaseService.rpc(
    'authenticate_integration_request_v1',
    atomicAuthenticationArgs(input, routeContract.rateLimitClass),
  )
  if (error || !data?.allowed) throw failClosedAuthenticationError(error, data)
```

## The test

- Regression test: regression_bug_010_auth_uses_route_cost_rpc in quality/test_regression.test.ts
- Regression patch: quality/patches/BUG-010-regression-test.patch
- Fix patch: quality/patches/BUG-010-fix.patch
- Red receipt: quality/results/BUG-010.red.log
- Green receipt: quality/results/BUG-010.green.log
- Command: npx vitest run --config quality/vitest.config.ts quality/test_regression.test.ts -t BUG-010
- Current result: FIXED; green regression PASS on 2026-08-10.
