> Generated by Quality Playbook v1.5.6 — 2026-08-10

# BUG-011: Final Customer Portal serializer accepts arbitrary public output

## Summary

The lib/customer-portal/externalApi.ts:31-108; lib/customer-portal/publicDto.ts:31-339 code path violates REQ-001: Most mappers allowlist fields, but customerPortalJson serializes unconstrained success objects with no output schema or recursive forbidden key/value check. In practice, Pass a success object containing data.nested.company_id to customerPortalJson.

## Spec reference

- Requirement: REQ-001
- Spec basis: ../../upload/Pasted markdown(20260810-093430).md:67; quality/REQUIREMENTS.md (REQ mapping)
- Behavioral contract quote: “Stäng all exponering av interna ID:n”

## The code

At lib/customer-portal/externalApi.ts:31-108; lib/customer-portal/publicDto.ts:31-339, the current implementation does not enforce the required boundary. Most mappers allowlist fields, but customerPortalJson serializes unconstrained success objects with no output schema or recursive forbidden key/value check. The contract instead requires: Validate the exact operation DTO and reject forbidden names/UUIDs at the final response boundary.

## Observable consequence

Trigger: Pass a success object containing data.nested.company_id to customerPortalJson. The observable result is that most mappers allowlist fields, but customerPortalJson serializes unconstrained success objects with no output schema or recursive forbidden key/value check. The expected outcome is: Validate the exact operation DTO and reject forbidden names/UUIDs at the final response boundary.

## The fix

The proposed change moves enforcement into the shared boundary named by the requirement so parallel routes cannot silently diverge.

```diff
diff --git a/lib/customer-portal/externalApi.ts b/lib/customer-portal/externalApi.ts
--- a/lib/customer-portal/externalApi.ts
++ b/lib/customer-portal/externalApi.ts
@@ customerPortalJson
  if (!rawError) {
    assertSafePublicPayload(record, {
      allowedUuidFields: ['request_id', 'correlation_id'],
    })
  }
   return NextResponse.json(versionedEnvelope, { ...init, headers })
```

## The test

- Regression test: regression_bug_011_serializer_rejects_internal_fields in quality/test_regression.test.ts
- Regression patch: quality/patches/BUG-011-regression-test.patch
- Fix patch: quality/patches/BUG-011-fix.patch
- Red receipt: quality/results/BUG-011.red.log
- Green receipt: quality/results/BUG-011.green.log
- Command: npx vitest run --config quality/vitest.config.ts quality/test_regression.test.ts -t BUG-011
- Current result: FIXED; green regression PASS on 2026-08-10.
