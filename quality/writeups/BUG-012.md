> Generated by Quality Playbook v1.5.6 — 2026-08-10

# BUG-012: Public-contract feed loads select-star before computing cache response

## Summary

The lib/website/publicContracts.ts:2325-2355,2588-2605; app/api/v1/website/public-contracts/route.ts:128-169 code path violates REQ-009: Hot reads pull every view column and the route builds the full feed before comparing the response ETag. In practice, Request public contracts with a matching If-None-Match and inspect DB calls.

## Spec reference

- Requirement: REQ-009
- Spec basis: ../../upload/Pasted markdown(20260810-093430).md:97; quality/REQUIREMENTS.md (REQ mapping)
- Behavioral contract quote: “Ta bort .select("*") från heta externa routes.”

## The code

At lib/website/publicContracts.ts:2325-2355,2588-2605; app/api/v1/website/public-contracts/route.ts:128-169, the current implementation does not enforce the required boundary. Hot reads pull every view column and the route builds the full feed before comparing the response ETag. The contract instead requires: Use explicit public projections and query a tenant publication fingerprint first for immediate 304.

## Observable consequence

Trigger: Request public contracts with a matching If-None-Match and inspect DB calls. The observable result is that hot reads pull every view column and the route builds the full feed before comparing the response ETag. The expected outcome is: Use explicit public projections and query a tenant publication fingerprint first for immediate 304.

## Depth judgment

The failure is masked in small or happy-path fixtures: it appears only with large histories, an injected persistence/schema failure, connected live capabilities, tenant-specific secret state, or conditional cache/auth paths. That explains why broad unit coverage can remain green while the external invariant is broken.

## The fix

The proposed change moves enforcement into the shared boundary named by the requirement so parallel routes cannot silently diverge.

```diff
diff --git a/lib/website/publicContracts.ts b/lib/website/publicContracts.ts
--- a/lib/website/publicContracts.ts
++ b/lib/website/publicContracts.ts
@@
-    .select('*')
    .select(PUBLIC_CONTRACT_OFFER_PROJECTION)
diff --git a/app/api/v1/website/public-contracts/route.ts b/app/api/v1/website/public-contracts/route.ts
--- a/app/api/v1/website/public-contracts/route.ts
++ b/app/api/v1/website/public-contracts/route.ts
@@ GET
  const fingerprint = await loadPublicContractFeedFingerprint(client.company_id)
  if (request.headers.get('if-none-match') === fingerprint.etag) return notModified(fingerprint.etag)
   const feed = await loadPublicContracts(client.company_id)
```

## The test

- Regression test: regression_bug_012_public_contracts_use_projection_and_early_fingerprint in quality/test_regression.test.ts
- Regression patch: quality/patches/BUG-012-regression-test.patch
- Fix patch: quality/patches/BUG-012-fix.patch
- Red receipt: quality/results/BUG-012.red.log
- Green receipt: quality/results/BUG-012.green.log
- Command: npx vitest run --config quality/vitest.config.ts quality/test_regression.test.ts -t BUG-012
- Current result: FIXED; green regression PASS on 2026-08-10.
