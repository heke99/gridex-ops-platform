> Generated by Quality Playbook v1.5.6 — 2026-08-10

# BUG-013: Portal identity resolver RPC exists live but runtime still executes divergent fallbacks

## Summary

The lib/customer-portal/customerResolver.ts:118-170,234-348; connected migration 20260809182554_resolve_portal_customer_identity_v1 code path violates REQ-008: Runtime performs several fallback queries with different ambiguity behavior and does not call the canonical live resolver. In practice, Provide duplicate or conflicting portal identity signals for one tenant.

## Spec reference

- Requirement: REQ-008
- Spec basis: ../../upload/Pasted markdown(20260810-093430).md:95; quality/REQUIREMENTS.md (REQ mapping)
- Behavioral contract quote: “Konsolidera Customer Portal identity resolution.”

## The code

At lib/customer-portal/customerResolver.ts:118-170,234-348; connected migration 20260809182554_resolve_portal_customer_identity_v1, the current implementation does not enforce the required boundary. Runtime performs several fallback queries with different ambiguity behavior and does not call the canonical live resolver. The contract instead requires: Use the tenant-bound resolver policy in one roundtrip and return explicit match/ambiguity state.

## Observable consequence

Trigger: Provide duplicate or conflicting portal identity signals for one tenant. The observable result is that runtime performs several fallback queries with different ambiguity behavior and does not call the canonical live resolver. The expected outcome is: Use the tenant-bound resolver policy in one roundtrip and return explicit match/ambiguity state.

## Depth judgment

The failure is masked in small or happy-path fixtures: it appears only with large histories, an injected persistence/schema failure, connected live capabilities, tenant-specific secret state, or conditional cache/auth paths. That explains why broad unit coverage can remain green while the external invariant is broken.

## The fix

The proposed change moves enforcement into the shared boundary named by the requirement so parallel routes cannot silently diverge.

```diff
diff --git a/lib/customer-portal/customerResolver.ts b/lib/customer-portal/customerResolver.ts
--- a/lib/customer-portal/customerResolver.ts
++ b/lib/customer-portal/customerResolver.ts
@@ resolvePortalCustomer
  const { data, error } = await supabaseService.rpc(
    'resolve_portal_customer_identity_v1',
    portalIdentityRpcArgs(companyId, identifiers),
  )
  if (error) throw portalIdentityInfrastructureError(error)
  return parsePortalIdentityResolution(data)
```

## The test

- Regression test: regression_bug_013_identity_uses_canonical_rpc in quality/test_regression.test.ts
- Regression patch: quality/patches/BUG-013-regression-test.patch
- Fix patch: quality/patches/BUG-013-fix.patch
- Red receipt: quality/results/BUG-013.red.log
- Green receipt: quality/results/BUG-013.green.log
- Command: npx vitest run --config quality/vitest.config.ts quality/test_regression.test.ts -t BUG-013
- Current result: FIXED; green regression PASS on 2026-08-10.
