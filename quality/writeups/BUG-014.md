> Generated by Quality Playbook v1.5.6 — 2026-08-10

# BUG-014: New tenant profile still provisions legacy portal scopes and no automatic smoke flow

## Summary

The supabase/migrations/20260724170000_single_api_key_tenant_website_integration.sql:34-57; lib/integrations/tenantWebsiteProvisioning.ts:276-466 code path violates REQ-007: tenant_website default scopes include customer_portal.read/write aliases and provisioning ends at readiness reconciliation without executing the requested context/public-contract/portal smoke flow. In practice, Provision a new tenant_website profile and inspect granted scopes and post-provision calls.

## Spec reference

- Requirement: REQ-007
- Spec basis: ../../upload/Pasted markdown(20260810-093430).md:88; quality/REQUIREMENTS.md (REQ mapping)
- Behavioral contract quote: “Använd granulära scopes för nya tenants.”

## The code

At supabase/migrations/20260724170000_single_api_key_tenant_website_integration.sql:34-57; lib/integrations/tenantWebsiteProvisioning.ts:276-466, the current implementation does not enforce the required boundary. tenant_website default scopes include customer_portal.read/write aliases and provisioning ends at readiness reconciliation without executing the requested context/public-contract/portal smoke flow. The contract instead requires: Provision granular scopes only, publish an explicit legacy sunset, and run a harmless real-client smoke check.

## Observable consequence

Trigger: Provision a new tenant_website profile and inspect granted scopes and post-provision calls. The observable result is that tenant_website default scopes include customer_portal.read/write aliases and provisioning ends at readiness reconciliation without executing the requested context/public-contract/portal smoke flow. The expected outcome is: Provision granular scopes only, publish an explicit legacy sunset, and run a harmless real-client smoke check.

## The fix

The proposed change moves enforcement into the shared boundary named by the requirement so parallel routes cannot silently diverge.

```diff
diff --git a/supabase/migrations/20260724170000_single_api_key_tenant_website_integration.sql b/supabase/migrations/20260810120000_granular_tenant_integration_profile.sql
new file mode 100644
--- /dev/null
++ b/supabase/migrations/20260810120000_granular_tenant_integration_profile.sql
@@
update public.integration_api_client_profiles
set default_scopes = array[
  'integration_context.read','customer_profile.read','customer_sites.read',
  'customer_contracts.read','customer_invoices.read','customer_metering.read',
  'customer_documents.read','customer_notifications.read','customer_sync.write',
  'customer_contact.write','customer_facility_data.write'
]
where key = 'tenant_website';
diff --git a/lib/integrations/tenantWebsiteProvisioning.ts b/lib/integrations/tenantWebsiteProvisioning.ts
--- a/lib/integrations/tenantWebsiteProvisioning.ts
++ b/lib/integrations/tenantWebsiteProvisioning.ts
@@ provisionTenantWebsiteIntegration
  const smoke = await runTenantIntegrationSmokeTest({ clientId, companyId })
  if (!smoke.ready) throw new TenantWebsiteProvisioningError('SMOKE_TEST_FAILED', smoke.message, receiptId)
```

## The test

- Regression test: regression_bug_014_new_tenant_uses_granular_scopes_and_smoke in quality/test_regression.test.ts
- Regression patch: quality/patches/BUG-014-regression-test.patch
- Fix patch: quality/patches/BUG-014-fix.patch
- Red receipt: quality/results/BUG-014.red.log
- Green receipt: quality/results/BUG-014.green.log
- Command: npx vitest run --config quality/vitest.config.ts quality/test_regression.test.ts -t BUG-014
- Current result: FIXED; green regression PASS on 2026-08-10.
